# 0819 수정 계획 1 — Meta 소재별 영상 지표 추이 화면 구현

## 0. 문서 목적과 인수인계 기준

이 문서는 `design-previews/meta-creative-video-trend-mockup.html`로 확정한 화면을 실제 Next.js/NestJS 애플리케이션에 구현하기 위한 작업 계획서다. 다음 작업 세션은 이 문서만 읽고도 추가 구조 조사 없이 구현을 시작할 수 있어야 한다.

- 기준 목업: `design-previews/meta-creative-video-trend-mockup.html`
- 신규 화면 이름: **Meta 소재 추이**
- 신규 Web 경로: **`/creative-trends`**
- 신규 API: **`GET /api/metrics/ads/creative-video-trends`**
- 기본 조회 범위: 기존 전역 기간 선택기가 정한 기간
- 기본 광고 상태: `active`
- 기본 선택 소재: 조회 결과의 앞 3개 소재
- 기본 선택 지표: `도달수`, `3초 재생률`
- 이번 작업은 조회·시각화 전용이다. Meta 광고 상태나 예산을 수정하지 않는다.

> 중요: 현재 작업 트리에는 `apps/api/src/domain/meta-video-metrics.ts`와 그 테스트 파일의 사용자 변경이 존재한다. 이 변경은 도달수 0인 행의 빈 영상 지표를 올바르게 처리하기 위한 선행 수정이므로 절대 되돌리거나 덮어쓰지 않는다.

### 0.1 현재 작업 트리 보호

계획서 작성 시점에 아래 기존 파일들이 이미 수정된 상태다. 일부는 소재 순이익 표시 작업과 영상 지표 보정 작업에 해당한다. 이번 기능 구현 세션은 이 변경을 사용자 소유 변경으로 취급하고, 파일 전체를 과거 버전으로 교체하거나 관련 없는 줄을 정리해서는 안 된다.

- `apps/api/src/domain/meta-video-metrics.spec.ts`
- `apps/api/src/domain/meta-video-metrics.ts`
- `apps/api/src/metrics/meta-ad-metric-aggregates.ts`
- `apps/api/src/metrics/meta-ad-metrics-read.service.spec.ts`
- `apps/web/src/app/ads/page.tsx`
- `apps/web/src/features/meta-daily-report/report-model.spec.ts`
- `apps/web/src/lib/meta-ads-sort.spec.ts`
- `apps/web/src/lib/meta-ads-sort.ts`
- `apps/web/src/lib/meta-ads-xlsx.spec.ts`
- `apps/web/src/lib/meta-ads-xlsx.ts`
- `apps/web/src/lib/meta-daily-xlsx.spec.ts`
- `apps/web/src/types/meta.ts`

위 목록은 작성 시점의 스냅샷일 뿐이다. 다음 세션 시작 시 `git status --short`와 각 대상 파일의 `git diff`를 다시 확인한 결과가 최종 기준이다. 특히 이번 계획도 `meta-ad-metrics-read.service.spec.ts`와 `types/meta.ts`에 내용을 추가하므로 기존 변경 뒤에 좁은 범위로 병합해야 한다.

---

## 1. 기능을 만드는 이유와 화면이 표시해야 하는 것

현재 `Meta Ads` 화면은 선택 기간 전체의 소재별 합계 성과를 표로 보여준다. 이 표는 기간 총합 비교에는 적합하지만 다음과 같은 시간 흐름은 보여주지 못한다.

- 같은 기간 합계가 비슷해도 특정 소재의 도달수가 최근 급감했는지
- 소재의 첫 3초 후킹 성능이 좋아지거나 나빠지는 중인지
- 25% → 50% → 75% → 100% 구간별 이탈이 어느 시점부터 커졌는지
- 한 제품에 연결된 여러 소재 중 어떤 소재가 안정적으로 성과를 유지하는지
- 단순 노출량 감소와 콘텐츠 재생 품질 하락을 구분할 수 있는지

신규 화면은 **한 제품을 선택한 뒤, 그 제품의 여러 광고 소재를 체크박스로 선택하고, 선택 기간의 일별 도달수와 영상 재생률을 선 그래프로 비교**하기 위한 화면이다.

지표의 해석은 다음과 같다.

| 지표 | 화면에서 확인하려는 의미 |
| --- | --- |
| 도달수 | 해당 소재가 날짜별로 확보한 광고 노출 규모 |
| 3초 재생률 | 영상 초반 후킹이 사용자의 시선을 붙잡았는지 |
| 25% 재생률 | 도입부 이후에도 내용을 계속 보게 했는지 |
| 50% 재생률 | 영상 중반까지 관심을 유지했는지 |
| 75% 재생률 | 핵심 메시지 후반까지 도달했는지 |
| 100% 재생률 | 영상을 끝까지 소비한 비율 |

이 화면의 목적은 운영자가 소재 피로도, 후킹 약화, 구간별 이탈 변화를 빠르게 발견하도록 돕는 것이다. 자동으로 광고를 끄거나 예산을 바꾸는 기능은 아니다.

---

## 2. 확정된 화면 구성과 동작

### 2.1 전체 레이아웃

기준 목업과 기존 앱 디자인을 결합해 다음 구조로 구현한다.

1. 기존 좌측 사이드바의 `Meta Ads` 바로 아래에 `Meta 소재 추이` 메뉴를 추가한다.
2. 기존 전역 상단 기간 선택기를 그대로 사용한다.
3. 페이지 제목은 `소재별 영상 지표 추이`로 표시한다.
4. 제목 오른쪽에는 현재 기간 요약 칩과 `선택 초기화` 버튼을 표시한다.
5. 본문은 왼쪽 조건 패널과 오른쪽 차트 패널의 2열 구조로 배치한다.
6. 좁은 화면에서는 조건 패널과 차트 패널을 1열로 쌓는다.

### 2.2 왼쪽 조건 패널

조건 선택 순서는 목업과 동일하게 유지한다.

#### 1. 제품 선택

- 기존 `GET /api/products`에서 활성 제품 목록을 가져온다.
- 표시명 우선순위는 `displayName → name → code`다.
- API 응답 순서가 이미 `sortOrder`, `createdAt` 기준이므로 Web에서 임의 재정렬하지 않는다.
- 최초 진입 시 첫 번째 활성 제품을 자동 선택한다.
- 활성 제품은 존재하지만 해당 기간에 활성 소재 데이터가 없으면 제품 선택은 유지하고 차트 영역에 빈 상태를 표시한다.

#### 2. 비교 소재

- 선택 제품의 추이 API 응답에 포함된 소재 목록을 체크박스로 표시한다.
- 기본값은 앞 3개 소재 선택이다. 소재가 3개 미만이면 전부 선택한다.
- 각 소재에는 고정 색상, 소재명, 원본 광고명, 활성 배지를 표시한다.
- 주 표시명은 `materialNo`가 있으면 `NN번 소재`, 없으면 `displayName`을 사용한다.
- 보조 문구는 가장 최근 원본 광고명 또는 `displayName`을 사용한다.
- `전체 선택` 버튼은 다음과 같이 동작한다.
  - 일부만 선택됐으면 전부 선택
  - 이미 전부 선택됐으면 전부 해제
- 소재가 0개 선택되면 차트를 숨기고 안내 문구를 표시한다.

#### 3. 표시 지표

- 다음 6개 지표를 각각 독립 체크박스로 제공한다.
  - 도달수
  - 3초 재생률
  - 25% 재생률
  - 50% 재생률
  - 75% 재생률
  - 100% 재생률
- 기본 선택은 `도달수`, `3초 재생률`이다.
- 체크된 지표 수를 `N개 선택`으로 표시한다.
- 모든 지표를 해제할 수 있으며, 이 경우 차트 영역에 지표 선택 안내를 표시한다.
- 조건 패널 하단에는 아래 계산 안내를 표시한다.
  - `재생률 = 해당 재생 구간 수 ÷ 도달수 × 100`
  - `단위가 다른 지표는 각각의 그래프로 표시됩니다.`

### 2.3 오른쪽 차트 패널

- 헤더: `{제품 표시명} · 소재 추이`
- 보조 문구: `선택한 N개 소재의 일별 성과`
- 메타 칩:
  - `일별 · N일 선택`
  - `데이터 존재 N일`
  - `KST 기준`
- 상단 범례는 선택된 소재의 색상과 이름을 표시한다.
- 선택한 지표마다 별도의 차트 카드를 만든다.
- 데스크톱은 2열, 좁은 화면은 1열로 표시한다.
- 하나의 차트 카드 안에서는 선택한 소재들이 각각 다른 색상의 선이 된다.
- 도달수와 재생률은 단위가 다르므로 한 차트의 이중 축으로 합치지 않는다.

각 카드에는 다음 내용을 표시한다.

- 지표명
- `일자별 도달 인원` 또는 `일자별 재생률`
- `마지막 데이터 평균` 칩
- 일별 선 그래프
- 마우스 오버 툴팁의 날짜, 소재명, 값

### 2.4 하단 요약 스트립

목업의 하단 요약은 새 API 없이 화면에 이미 내려온 데이터를 사용해 계산한다.

- 선택 소재별로 기간 내 첫 번째/마지막 **유효한 3초 재생률**을 구한다.
- 변화량은 `마지막 값 - 첫 값`이며 단위는 `%p`다.
- 비교 가능한 소재 중 상승 폭이 가장 큰 소재를 표시한다.
- 상승 소재가 없으면 절댓값이 가장 큰 변화 소재를 표시한다.
- 비교에 필요한 유효 값이 2개 미만이면 `비교 가능한 3초 재생률 데이터가 부족합니다.`를 표시한다.
- 3초 재생률 체크박스가 꺼져 있어도 요약 계산은 가능하다. 응답에는 항상 모든 재생률 필드가 포함되기 때문이다.

### 2.5 선택 초기화

페이지 제목 오른쪽에 `RotateCcw` 아이콘 버튼을 둔다. 클릭 시 다음만 초기화한다.

- 제품: 활성 제품 목록의 첫 제품
- 소재: 해당 제품의 앞 3개
- 지표: 도달수 + 3초 재생률

기간은 전역 상태이므로 초기화하지 않는다. 기간 변경은 기존 상단 기간 선택기만 담당한다.

---

## 3. 데이터 정의와 반드시 지켜야 할 계산 규칙

### 3.1 데이터 원본

이미 `MetaAdDailyMetric` 모델에 다음 필드가 존재한다.

- `reach`
- `videoPlay3sCount`
- `videoPlay25Count`
- `videoPlay50Count`
- `videoPlay75Count`
- `videoPlay100Count`

이 필드는 `apps/api/prisma/migrations/20260810170000_add_meta_video_play_counts/migration.sql`에서 추가됐다. 따라서 이번 기능을 위해 Prisma 스키마나 DB 마이그레이션을 추가하면 안 된다.

### 3.2 일별 도달수

한 소재가 같은 날짜에 여러 광고/광고세트 행을 가지면 일별 도달수는 해당 행들의 `reach` 합계다.

소스 데이터에 사용자 식별자가 없으므로 여러 광고 행 또는 여러 날짜 사이의 중복 사용자를 제거할 수 없다. 화면의 도달수는 Meta CSV에 들어온 행 단위 도달수의 합이며, 고유 사용자 수를 다시 계산한 값이 아니다.

### 3.3 일별 재생률

절대로 행별 재생률의 단순 평균을 사용하지 않는다. 날짜·소재별로 먼저 카운트를 합한 뒤 다음 수식을 사용한다.

```text
일별 N% 재생률 = sum(해당 날짜·소재의 N% 재생 카운트)
                   / sum(해당 날짜·소재의 도달수)
                   × 100
```

계산은 기존 `aggregateMetaVideoMetrics()`에만 맡긴다. Web에서 재생률을 재계산하지 않는다.

### 3.4 null과 0의 구분

- 실제 카운트 `0`은 유효한 0으로 표시한다.
- 재생 카운트가 수집되지 않은 `null`은 0으로 바꾸지 않는다.
- 양의 도달수가 있는 행 중 특정 재생 구간 값이 `null`이면 그 날짜·소재의 해당 구간 재생률은 `null`이다.
- 도달수 0인 행의 빈 영상 셀은 다른 정상 행의 완전성을 깨지 않는다. 현재 작업 트리의 `meta-video-metrics.ts` 수정이 이 규칙을 구현한다.
- 전체 도달수 합계가 0이면 재생률은 `null`이다.
- `null`은 그래프의 선을 끊고 툴팁에는 `-`로 표시한다.
- 0은 그래프의 0 값으로 표시한다.

### 3.5 값 보정 금지

기존 도메인 테스트가 명시하듯 API는 다음 보정을 하지 않는다.

- 재생률을 0~100으로 강제 제한하지 않는다.
- 25% ≥ 50% ≥ 75% ≥ 100% 순서를 강제로 만들지 않는다.
- 잘못돼 보이는 원본 값을 조용히 수정하지 않는다.

따라서 재생률 차트 Y축도 최대 100으로 고정하면 안 된다. 실제 최댓값을 기준으로 10 단위의 적절한 상한을 사용한다.

---

## 4. 기존 코드/API 재사용 목록

중복 함수와 중복 API를 만들지 않기 위해 다음 자원을 반드시 재사용한다.

| 기존 자원 | 경로 | 이번 기능에서의 사용 |
| --- | --- | --- |
| 활성 제품 목록 API | `GET /api/products` | 제품 선택 옵션. 별도 추이용 제품 목록 API를 만들지 않는다. |
| 전역 기간 선택기 | `apps/web/src/components/date-range-picker.tsx` | 새 날짜 선택 UI를 만들지 않는다. |
| 현재 기간 읽기 | `apps/web/src/lib/use-range.ts`의 `useRange()` | URL의 `from`, `to`를 읽고 기간 변경에 반응한다. |
| API GET 래퍼 | `apps/web/src/lib/api.ts`의 `apiGet()` | 직접 `fetch()`를 작성하지 않는다. |
| 기간 쿼리 생성 | `apps/web/src/lib/api.ts`의 `rangeQuery()` | `from`, `to`, `productId`, `deliveryStatus` 구성. |
| 날짜 범위 검증 | `apps/api/src/common/date-range.ts`의 `parseDateRange()` | 신규 API 날짜 검증과 Date 변환. |
| 기간 일수 계산 | 같은 파일의 `dateRangeDays()` | 응답의 `selectedDays`. |
| 날짜 문자열 변환 | `apps/api/src/domain/date-number.ts`의 `formatDateOnly()` | 일별 포인트의 `YYYY-MM-DD`. |
| 현재 광고 일별 행 조회 | `MetaAdMetricsReadService.currentAdMetrics()` | `isCurrent`, 기간, 제품, 단계, 배송 상태 필터를 중복 작성하지 않는다. |
| 광고 배송 상태 필터 | `parseDeliveryStatusFilter()`, `adDeliveryStatusWhere()` | `currentAdMetrics()`를 통해 그대로 적용한다. |
| 소재명 파싱 | `CreativeNameParser` | 현재 Ads 화면과 동일한 `creativeKey`, `displayName`, `materialNo`를 사용한다. |
| 영상 카운트/재생률 집계 | `aggregateMetaVideoMetrics()` | 날짜·소재별 도달수와 재생률 계산의 단일 진실 공급원. |
| 그룹 유틸 | `groupBy()` | 소재 및 날짜 그룹화. |
| 상태 요약 | `summarizeDeliveryStatus()` | 소재 배지 상태. |
| 문자열 중복 제거 | `uniqueNonEmpty()` | 원본 광고명 목록. |
| 재생률 라벨 | `META_VIDEO_RATE_COLUMNS` | 3초/25/50/75/100 라벨을 새로 중복 선언하지 않는다. |
| 퍼센트 표시 | `formatPercent()` | 툴팁·요약 퍼센트 표시. 필요 시 소수 자릿수 옵션만 별도 함수로 감싼다. |
| 숫자 표시 | `numberFmt()` | 도달수 표시. |
| 차트 라이브러리 | 이미 설치된 `recharts` | 새 차트 패키지를 설치하지 않는다. |
| 색·패널 디자인 토큰 | `apps/web/src/app/globals.css`의 CSS 변수 | 목업 전용으로 색상 시스템을 다시 만들지 않는다. |

### 기존 코드 중 직접 재사용하지 않는 것

- `apps/web/src/components/Charts.tsx`의 `TrendChart`는 광고비/구매/CPA/마진이 하드코딩돼 있어 이번 다중 소재·동적 지표 차트에 맞지 않는다. 기존 컴포넌트를 억지로 수정하지 말고 기능 전용 차트 컴포넌트를 추가한다.
- `GET /api/metrics/ads/creatives`는 기간 합계, 환율, 원가, 매출까지 계산하므로 일별 영상 추이용으로 호출하면 불필요한 계산과 큰 응답이 발생한다. 기존 API 계약은 유지하고 가벼운 일별 추이 API를 별도로 추가한다.
- `GET /api/dashboard/trends`는 광고세트 기반 재무 지표 추이이므로 소재 영상 지표 용도로 확장하지 않는다.

### 대소문자 중복 파일 주의

이 저장소에는 이름이 비슷한 컴포넌트가 중복으로 존재한다. 실제 `app/layout.tsx`가 사용하는 파일만 수정한다.

- **수정 대상**: `apps/web/src/components/app-shell.tsx`
- **수정 대상/재사용**: `apps/web/src/components/date-range-picker.tsx`
- **수정 금지**: `apps/web/src/components/AppShell.tsx`
- **수정 금지**: `apps/web/src/components/DateRangePicker.tsx`

`layout.tsx`는 소문자 `@/components/app-shell`을 import하고 있다.

---

## 5. 신규 API 설계

### 5.1 엔드포인트

```http
GET /api/metrics/ads/creative-video-trends
```

### 5.2 Query parameter

| 이름 | 필수 | 예시 | 규칙 |
| --- | --- | --- | --- |
| `from` | 필수 | `2026-08-01` | 기존 `parseDateRange()` 사용 |
| `to` | 필수 | `2026-08-14` | `from` 이상이어야 함 |
| `productId` | 필수 | UUID | 활성 제품 선택기에서 전달 |
| `deliveryStatus` | 선택 | `active` | 생략 시 기존 규칙에 따라 `active`; 현재 UI는 명시적으로 `active` 전달 |

새 화면에는 캠페인/광고세트/단계 필터가 없으므로 이번 API 공개 계약에는 이 파라미터를 넣지 않는다. 내부 `currentAdMetrics()` 호출에는 `from`, `to`, `productId`, `deliveryStatus`만 전달한다.

### 5.3 응답 계약

```ts
type MetaCreativeVideoTrendPoint = {
  date: string;
  reach: number;
  videoPlay3sRatePct: number | null;
  videoPlay25RatePct: number | null;
  videoPlay50RatePct: number | null;
  videoPlay75RatePct: number | null;
  videoPlay100RatePct: number | null;
};

type MetaCreativeVideoTrendSeries = {
  creativeKey: string;
  displayName: string;
  productName: string | null;
  materialNo: string | null;
  deliveryStatus: string | null;
  originalAdNames: string[];
  dataDays: number;
  points: MetaCreativeVideoTrendPoint[];
};

type MetaCreativeVideoTrendResponse = {
  productId: string;
  period: {
    from: string;
    to: string;
    selectedDays: number;
    dataDays: number;
  };
  creatives: MetaCreativeVideoTrendSeries[];
};
```

응답 예시:

```json
{
  "productId": "123e4567-e89b-42d3-a456-426614174000",
  "period": {
    "from": "2026-08-01",
    "to": "2026-08-14",
    "selectedDays": 14,
    "dataDays": 13
  },
  "creatives": [
    {
      "creativeKey": "두줄보호대_04",
      "displayName": "두줄보호대_04",
      "productName": "두줄보호대",
      "materialNo": "04",
      "deliveryStatus": "active",
      "originalAdNames": ["260801_두줄보호대_04_IG"],
      "dataDays": 13,
      "points": [
        {
          "date": "2026-08-01",
          "reach": 73049,
          "videoPlay3sRatePct": 57.676354228,
          "videoPlay25RatePct": 12.629878574,
          "videoPlay50RatePct": 6.799545511,
          "videoPlay75RatePct": 4.522991417,
          "videoPlay100RatePct": 2.427137948
        }
      ]
    }
  ]
}
```

API는 원본 정밀도의 숫자를 반환하고 반올림은 Web 표시 단계에서만 한다.

### 5.4 API 처리 순서

`MetaAdMetricsReadService.creativeVideoTrends(query)`를 다음 순서로 구현한다.

```ts
1. productId 누락 또는 UUID 형식 오류를 BadRequestException으로 거부
2. parseDateRange(query.from, query.to)
3. currentAdMetrics({ from, to, productId, deliveryStatus })로 현재 행 한 번 조회
4. 각 행의 adNameSnapshot을 기존 CreativeNameParser로 파싱
5. parsedName.creativeKey 기준으로 소재 그룹 생성
6. 각 소재 그룹을 metricDate 기준으로 다시 그룹화
7. 날짜 그룹의 원본 행들을 aggregateMetaVideoMetrics()로 집계
8. 응답 point에 date, reach, 5개 rate만 복사
9. 소재 metadata와 dataDays 구성
10. materialNo 자연 정렬 → displayName 정렬
11. 전체 고유 날짜 수로 period.dataDays 계산
```

현재 `creativeMetrics()`와 신규 메서드가 모두 아래 코드를 필요로 하므로 서비스 내부 private helper로 한 번만 만든다.

```ts
private parseCreativeRows(rows: AdDailyMetricRow[]) {
  return rows.map((row) => ({
    row,
    parsedName: this.creativeNameParser.parse(row.adNameSnapshot)
  }));
}
```

기존 `creativeMetrics()`의 직접 `metrics.map(...)`도 이 helper 호출로 바꿔 소재 파싱 로직이 두 곳에 생기지 않게 한다.

### 5.5 API 오류 계약

기존 Nest 예외 필터 형식을 따른다.

- `productId` 누락: `PRODUCT_ID_REQUIRED`
- UUID가 아닌 `productId`: `INVALID_PRODUCT_ID`
- 날짜 누락: 기존 `DATE_RANGE_REQUIRED`
- 날짜 오류: 기존 `INVALID_DATE_RANGE`
- 잘못된 상태: 기존 `INVALID_DELIVERY_STATUS`
- 존재하지만 데이터가 없는 제품 UUID: 오류가 아니라 `creatives: []`, `dataDays: 0`

### 5.6 성능 원칙

- 제품/기간이 바뀔 때 `MetaAdDailyMetric.findMany()` 1회만 실행한다.
- 신규 메서드에서 `creativeMetrics()`를 호출하지 않는다. 해당 메서드는 환율·원가·매출 문맥 조회가 포함돼 불필요하다.
- 모든 집계는 이미 기간과 제품으로 제한된 행에 대해서만 메모리에서 실행한다.
- DB 필드와 현재 인덱스 `@@index([productId, metricDate])`를 그대로 활용한다.
- 이번 기능을 위해 추가 인덱스나 마이그레이션을 만들지 않는다.

---

## 6. Web 상태와 데이터 흐름

### 6.1 React Query

페이지에서 두 개의 쿼리만 사용한다.

```ts
const productsQuery = useQuery({
  queryKey: ["products"],
  queryFn: () => apiGet<MetaProductListItem[]>("/products")
});

const trendsQuery = useQuery({
  queryKey: [
    "meta-creative-video-trends",
    range.from,
    range.to,
    selectedProductId
  ],
  queryFn: () => apiGet<MetaCreativeVideoTrendResponse>(
    `/metrics/ads/creative-video-trends?${rangeQuery(range, {
      productId: selectedProductId,
      deliveryStatus: "active"
    })}`
  ),
  enabled: Boolean(selectedProductId)
});
```

제품 목록 쿼리 키는 기존 화면과 동일한 `["products"]`를 사용해 React Query 캐시를 재사용한다.

### 6.2 페이지 상태

```ts
selectedProductId: string
selectedCreativeKeys: Set<string>
selectedMetricKeys: Set<MetaCreativeTrendMetricKey>
```

기본 지표 상수:

```ts
const DEFAULT_TREND_METRIC_KEYS = new Set([
  "reach",
  "videoPlay3sRatePct"
]);
```

### 6.3 제품/기간 변경 시 소재 선택 규칙

선택 상태가 불필요하게 매번 리셋되지 않도록 다음 규칙을 지킨다.

1. **제품이 바뀐 경우**: 새 응답이 오면 앞 3개 소재를 선택한다.
2. **같은 제품에서 기간만 바뀐 경우**: 기존 선택과 새 소재 목록의 교집합을 유지한다.
3. 교집합이 비어 있으면 새 목록의 앞 3개를 선택한다.
4. 응답이 비어 있으면 선택 소재도 빈 집합으로 만든다.
5. 지표 선택은 제품/기간 변경으로 리셋하지 않는다.

이 규칙은 `useRef`로 마지막 초기화 제품 ID를 추적하거나 순수 helper 함수로 작성한다. 렌더 중 `setState()`를 호출하지 않는다.

### 6.4 날짜 누락 처리

API는 실제 데이터가 있는 날짜만 반환한다. Web의 순수 변환 함수가 `from~to` 모든 날짜를 만들고 누락 날짜는 `null`로 채운다.

- 날짜 생성은 이미 설치된 `date-fns`의 `eachDayOfInterval`, `parseISO`, `format`을 사용한다.
- 누락 날짜에서 선을 연결하지 않는다: Recharts `connectNulls={false}`.
- 실제 0은 `null`로 바꾸지 않는다.

### 6.5 Recharts dataKey 안전성

`creativeKey`에는 점이나 특수문자가 들어갈 수 있으므로 이를 Recharts `dataKey`로 직접 사용하지 않는다. 차트 모델을 만들 때 `series_0`, `series_1` 같은 내부 키를 할당한다.

```ts
type CreativeChartLine = {
  dataKey: `series_${number}`;
  creativeKey: string;
  label: string;
  color: string;
};
```

차트 데이터 예시:

```ts
[
  { date: "2026-08-01", series_0: 73049, series_1: 3317 },
  { date: "2026-08-02", series_0: null,  series_1: 4021 }
]
```

### 6.6 색상 규칙

소재 색상은 전체 응답 소재의 정렬 인덱스를 기준으로 고정한다. 체크 순서가 바뀌거나 일부 소재를 해제해도 남은 소재 색상이 바뀌면 안 된다.

권장 팔레트:

```ts
[
  "#2764b7",
  "#dc7218",
  "#7b4ab6",
  "#137a45",
  "#b42318",
  "#147d9a",
  "#a15c00",
  "#6f7780"
]
```

8개를 넘으면 순환 사용한다. 범례와 체크박스의 색상 점, 차트 선은 동일한 색상 맵을 공유한다.

---

## 7. 파일별 구체적인 수정 계획

### 7.1 API 기존 파일

#### `apps/api/src/metrics/metric-types.ts`

추가:

```ts
export type CreativeVideoTrendQuery = {
  from?: string;
  to?: string;
  productId?: string;
  deliveryStatus?: string;
};
```

기존 `AdMetricQuery`를 그대로 공개 계약으로 사용하지 않는다. 신규 API가 지원하지 않는 `campaignId`, `adsetId`, `stage`가 섞이는 것을 방지하기 위해 전용 query type을 둔다.

#### `apps/api/src/metrics/meta-ad-metrics-read.service.ts`

수정 내용:

1. `CreativeVideoTrendQuery` import.
2. `aggregateMetaVideoMetrics` import.
3. `isUuid`는 이미 import 중이므로 그대로 사용.
4. 기존 `creativeMetrics()`의 파싱 map을 `parseCreativeRows()` helper로 교체.
5. `creativeVideoTrends(query)` 메서드 추가.
6. 하단 private helper `parseCreativeRows()` 추가.
7. 소재 정렬은 `localeCompare("ko-KR", { numeric: true, sensitivity: "base" })` 사용.

`creativeVideoTrends()`는 재무 문맥을 만들지 않아야 한다. 반환 point에는 count 필드를 노출하지 않고 `reach`와 5개 rate만 넣는다.

#### `apps/api/src/metrics/metrics.service.ts`

추가:

```ts
creativeVideoTrends(query: CreativeVideoTrendQuery) {
  return this.adMetricsReadService.creativeVideoTrends(query);
}
```

`metric-types.ts`에서 신규 type을 import한다. 서비스 계층은 위임만 하며 집계 코드를 넣지 않는다.

#### `apps/api/src/metrics/metrics.controller.ts`

`creativeAds()` 인접 위치에 다음 route를 추가한다.

```ts
@Get("ads/creative-video-trends")
creativeVideoTrends(
  @Query("from") from?: string,
  @Query("to") to?: string,
  @Query("productId") productId?: string,
  @Query("deliveryStatus") deliveryStatus?: string
) {
  return this.metricsService.creativeVideoTrends({
    from,
    to,
    productId,
    deliveryStatus
  });
}
```

route 문자열은 `/ads/creatives`와 혼동하지 않게 정확히 `ads/creative-video-trends`로 한다.

#### `apps/api/src/metrics/meta-ad-metrics-read.service.spec.ts`

기존 fixture를 확장해 신규 메서드의 조회·응답 계약을 검증한다. 최소 테스트 목록은 10절에 별도로 정리한다.

#### `apps/api/src/domain/meta-video-metrics.ts`

신규 로직을 추가하지 않는다. 현재 사용자 변경을 보존하고 신규 endpoint가 `aggregateMetaVideoMetrics()`를 호출하도록 한다.

#### `apps/api/src/domain/meta-video-metrics.spec.ts`

현재 사용자 변경을 보존한다. 신규 API 테스트에서 같은 도메인 규칙을 다시 구현하지 말고 서비스가 helper 결과를 전달하는지만 검증한다.

### 7.2 Web 신규 타입 및 순수 로직

#### `apps/web/src/types/meta.ts`

다음 타입을 추가한다.

```ts
export type MetaProductListItem = {
  id: string;
  code: string;
  name: string;
  displayName: string | null;
  isActive: boolean;
};

export type MetaCreativeVideoTrendPoint = { /* API 계약과 동일 */ };
export type MetaCreativeVideoTrendSeries = { /* API 계약과 동일 */ };
export type MetaCreativeVideoTrendResponse = { /* API 계약과 동일 */ };
export type MetaCreativeTrendMetricKey = "reach" | MetaVideoRateKey;
```

API 응답과 Web 타입의 필드명을 다르게 만들지 않는다.

#### `apps/web/src/lib/meta-video-display.ts`

기존 `META_VIDEO_RATE_COLUMNS`를 재사용해 추이 지표 설정을 추가한다.

```ts
export const META_CREATIVE_TREND_METRICS = [
  { key: "reach", label: "도달수", unit: "count", description: "일자별 도달 인원" },
  ...META_VIDEO_RATE_COLUMNS.map((column) => ({
    ...column,
    unit: "percent" as const,
    description: "일자별 재생률"
  }))
] as const;
```

3초/25/50/75/100 라벨을 다시 문자열로 작성하지 않는다.

#### 신규 `apps/web/src/lib/meta-creative-trends.ts`

React에 의존하지 않는 순수 함수와 상수를 둔다.

- `CREATIVE_TREND_COLORS`
- `DEFAULT_TREND_METRIC_KEYS`
- `productLabel(product)`
- `creativeLabel(series)`
- `selectDefaultCreativeKeys(creatives, limit = 3)`
- `reconcileCreativeSelection(previous, creatives, productChanged)`
- `buildCreativeMetricChartModel({ creatives, selectedKeys, metricKey, from, to })`
- `latestMetricAverage(chartModel)`
- `buildThreeSecondInsight(creatives, selectedKeys)`
- `formatTrendDateTick(date)`
- `formatTrendMetricValue(value, metricKey)`

여기에 차트 데이터 생성과 요약 계산을 모아 컴포넌트 안의 복잡한 map/reduce 중복을 방지한다.

#### 신규 `apps/web/src/lib/meta-creative-trends.spec.ts`

위 순수 함수를 Vitest로 검증한다. React DOM 테스트 패키지는 새로 설치하지 않는다.

### 7.3 Web 컴포넌트

#### 신규 `apps/web/src/features/meta-creative-trends/creative-trend-controls.tsx`

책임:

- 제품 select 렌더링
- 소재 체크박스 목록
- 전체 선택/해제
- 지표 체크박스 목록
- 선택 개수 표시
- 계산 안내

이 컴포넌트는 API를 직접 호출하거나 제품 변경 후 기본값을 결정하지 않는다. page에서 받은 값과 callback만 사용한다.

권장 props:

```ts
type CreativeTrendControlsProps = {
  products: MetaProductListItem[];
  productId: string;
  creatives: MetaCreativeVideoTrendSeries[];
  selectedCreativeKeys: ReadonlySet<string>;
  selectedMetricKeys: ReadonlySet<MetaCreativeTrendMetricKey>;
  colorByCreativeKey: ReadonlyMap<string, string>;
  onProductChange: (productId: string) => void;
  onCreativeToggle: (creativeKey: string) => void;
  onAllCreativesToggle: () => void;
  onMetricToggle: (metricKey: MetaCreativeTrendMetricKey) => void;
};
```

접근성:

- select에 `aria-label="제품 선택"`
- 체크박스 label에 소재/지표명이 포함되게 한다.
- 색상만으로 시리즈를 구분하지 않고 텍스트 범례를 함께 표시한다.

#### 신규 `apps/web/src/features/meta-creative-trends/creative-metric-chart.tsx`

책임:

- 지표 1개의 차트 카드 렌더링
- Recharts의 `ResponsiveContainer`, `LineChart`, `CartesianGrid`, `XAxis`, `YAxis`, `Tooltip`, `Line` 사용
- 차트 모델과 metric config를 props로 받음
- 도달수/재생률 포매터 분리

설정:

- `Line type="monotone"`
- `strokeWidth={2}`
- `dot={false}`
- `activeDot={{ r: 4 }}`
- `connectNulls={false}`
- X축 `dataKey="date"`
- X축 tick은 `MM.DD`
- 도달수 Y축은 0부터 자동 상한
- 재생률 Y축은 0부터 실제 최대값을 10 단위로 올림
- 재생률 100 초과도 표시 가능
- 애니메이션은 초기 렌더 흔들림을 줄이기 위해 `isAnimationActive={false}` 권장

#### 신규 `apps/web/src/app/creative-trends/page.tsx`

페이지의 책임:

1. `useRange()` 호출
2. 제품 목록 query
3. 선택 제품 state
4. 추이 query
5. 소재/지표 선택 state와 초기화 규칙
6. 색상 map 생성
7. page title, 기간 요약, reset 버튼
8. 조건 패널과 차트 패널 조립
9. loading/error/empty state 분기
10. 하단 3초 재생률 요약 표시

페이지 파일 안에 차트 계산 코드를 직접 작성하지 않는다. `meta-creative-trends.ts` 순수 helper를 사용한다.

### 7.4 Web 기존 파일

#### `apps/web/src/components/app-shell.tsx`

- `lucide-react`에서 `TrendingUp` 아이콘 import.
- 실제 `navGroups`의 Meta `Meta Ads` 바로 다음에 추가:

```ts
{ href: "/creative-trends", label: "Meta 소재 추이", icon: TrendingUp }
```

`/ads/trends`를 사용하지 않는다. 기존 active 판정이 `pathname.startsWith()`를 쓰므로 `/ads/trends`는 `Meta Ads`와 신규 메뉴가 동시에 활성화될 수 있다.

#### `apps/web/src/app/globals.css`

모든 신규 class는 `creative-trend-` prefix를 사용한다.

필수 class 그룹:

- `.creative-trend-page-title-actions`
- `.creative-trend-period-chip`
- `.creative-trend-layout`
- `.creative-trend-controls`
- `.creative-trend-control-section`
- `.creative-trend-creative-list`
- `.creative-trend-creative-option`
- `.creative-trend-series-dot`
- `.creative-trend-active-badge`
- `.creative-trend-metric-grid`
- `.creative-trend-metric-option`
- `.creative-trend-chart-panel`
- `.creative-trend-chart-header`
- `.creative-trend-meta-chips`
- `.creative-trend-legend`
- `.creative-trend-chart-grid`
- `.creative-trend-chart-card`
- `.creative-trend-chart-box`
- `.creative-trend-insight`
- `.creative-trend-empty`

스타일 원칙:

- 기존 `--bg`, `--panel`, `--line`, `--line-strong`, `--text`, `--muted`, `--brand`, `--brand-weak` 사용
- 데스크톱: `grid-template-columns: 286px minmax(0, 1fr)`
- 조건 패널: `position: sticky; top: 82px`
- 차트 grid: 2열
- 차트 높이: 약 250~290px
- 1120px 이하: 차트 1열
- 기존 앱 사이드바가 접히는 breakpoint에 맞춰 조건/차트 전체 1열
- 체크된 옵션은 `var(--brand-weak)` 배경과 `var(--brand)` 경계
- focus-visible 스타일을 반드시 제공

#### 수정하지 않는 파일

- `apps/web/src/components/Charts.tsx`
- `apps/web/src/components/AppShell.tsx`
- `apps/web/src/components/DateRangePicker.tsx`
- `apps/web/src/components/date-range-picker.tsx` 자체 로직
- `apps/web/next.config.mjs`
- Prisma schema 및 migration
- 업로드 파서/CSV 처리 코드

---

## 8. 로딩·오류·빈 상태 처리

### 제품 목록 로딩

- 조건 패널 select를 disabled 처리하고 `제품 목록을 불러오는 중입니다.` 표시.

### 제품 목록 오류

- 기존 `.warning-strip` 스타일로 `제품 목록을 불러오지 못했습니다. API 연결 또는 DB 설정을 확인해주세요.` 표시.
- 차트 query는 실행하지 않는다.

### 활성 제품 없음

- `등록된 활성 제품이 없습니다.` 표시.
- Product Settings로 이동하는 링크는 선택 사항이지만 새 mutation은 만들지 않는다.

### 추이 API 로딩

- 기존 차트 패널 크기를 유지하는 loading 영역을 표시해 레이아웃 이동을 줄인다.

### 추이 API 오류

- 조건 패널은 유지한다.
- 차트 패널에 `소재 추이 데이터를 불러오지 못했습니다.` 표시.
- React Query의 `refetch()`를 호출하는 `다시 시도` 버튼을 제공해도 된다.

### 기간 내 소재 없음

- 왼쪽에 `활성 소재 0개 · 선택 기간 데이터 없음` 표시.
- 오른쪽에 `선택한 기간에 활성 소재 데이터가 없습니다.` 표시.

### 소재 또는 지표 선택 없음

- 소재 없음: `비교할 소재를 1개 이상 선택해주세요.`
- 지표 없음: `표시할 지표를 1개 이상 선택해주세요.`
- 두 조건이 모두 없으면 하나의 결합 메시지를 사용해도 된다.

### 일부 지표만 null

- 해당 소재/날짜 선만 끊는다.
- 다른 지표와 다른 소재는 정상 표시한다.
- 카드 전체를 숨기지 않는다.

---

## 9. 구현 순서

### 1단계 — 작업 트리 보호와 기준 확인

1. `git status --short` 확인.
2. 기존 사용자 수정 파일을 기록하고 건드리지 않는다.
3. 기준 목업을 브라우저에서 열어 구조를 다시 확인한다.
4. 관련 현재 테스트를 먼저 실행해 baseline을 확인한다.

### 2단계 — API 단위 테스트를 먼저 작성

1. `CreativeVideoTrendQuery` type 추가.
2. 서비스 spec에 신규 API 계산/그룹 테스트 추가.
3. 테스트가 실패하는 상태를 확인.
4. `creativeVideoTrends()` 구현.
5. `MetricsService` 위임과 controller route 추가.
6. API test/typecheck 통과.

### 3단계 — Web 순수 모델 구현

1. API 응답 type 추가.
2. `META_CREATIVE_TREND_METRICS` 설정 추가.
3. `meta-creative-trends.ts`와 spec 추가.
4. 날짜 채우기, null/0, 최신 평균, 요약 계산 테스트 통과.

### 4단계 — Web UI 구현

1. 컨트롤 컴포넌트.
2. 차트 컴포넌트.
3. 페이지 조립.
4. CSS 추가.
5. 실제 소문자 app shell에 메뉴 추가.

### 5단계 — 통합·시각 검증

1. API/Web dev server 실행.
2. `/creative-trends?from=YYYY-MM-DD&to=YYYY-MM-DD` 접속.
3. 실제 DB 데이터로 제품·소재·지표·기간 전환 확인.
4. 브라우저 console과 network 오류 확인.
5. 기준 목업과 나란히 비교.
6. 1440px 데스크톱과 좁은 화면을 확인.

---

## 10. 자동 테스트 계획

### 10.1 API 테스트

`apps/api/src/metrics/meta-ad-metrics-read.service.spec.ts`에 다음 사례를 추가한다.

1. `productId`가 없으면 `PRODUCT_ID_REQUIRED`.
2. UUID가 아니면 `INVALID_PRODUCT_ID`.
3. Prisma where에 다음이 들어가는지 확인.
   - `isCurrent: true`
   - 선택 기간
   - `productId`
   - case-insensitive `active` 상태
4. 같은 `creativeKey`의 여러 광고 ID가 한 소재로 합쳐지는지.
5. 같은 날짜의 여러 행이 `sum(count) / sum(reach)`로 계산되는지.
6. 행별 비율 단순 평균이 아닌지.
7. 특정 재생 구간이 불완전하면 그 구간만 `null`인지.
8. 도달수 0 + 빈 영상 행이 정상 행을 무효화하지 않는지.
9. 날짜가 오름차순인지.
10. 소재가 materialNo 자연 순서인지 (`2`가 `10`보다 먼저).
11. 데이터 없음 응답의 `creatives: []`, `dataDays: 0`.
12. `originalAdNames`가 중복 제거되는지.

기존 `aggregateMetaVideoMetrics`의 세부 산술 테스트를 그대로 복사하지 않는다. 신규 서비스 테스트는 helper가 올바른 단위의 행 그룹에 호출된 결과만 확인한다.

### 10.2 Web 순수 함수 테스트

`apps/web/src/lib/meta-creative-trends.spec.ts`:

1. `from~to` 모든 날짜가 생성되는지.
2. API에 없는 날짜가 `null`인지.
3. 실제 0이 `null`로 바뀌지 않는지.
4. 퍼센트 API 값 48.81이 0.4881로 다시 나뉘지 않는지.
5. 특수문자 creativeKey 대신 `series_N` dataKey가 만들어지는지.
6. 선택 해제 후에도 소재별 색상 인덱스가 유지되는지.
7. 마지막 데이터 평균에서 null이 제외되는지.
8. 평균 대상이 하나도 없으면 null인지.
9. 3초 재생률 요약이 첫/마지막 유효 값으로 `%p`를 계산하는지.
10. 유효 값이 1개뿐이면 부족 메시지를 반환하는지.
11. 제품 변경 시 앞 3개 선택인지.
12. 기간 변경 시 기존 선택과 신규 목록의 교집합이 유지되는지.

### 10.3 실행 명령

구현 중 빠른 확인:

```powershell
npm.cmd --workspace @meta-ads-performance/api run test -- src/metrics/meta-ad-metrics-read.service.spec.ts
npm.cmd --workspace @meta-ads-performance/web run test -- src/lib/meta-creative-trends.spec.ts
```

최종 확인:

```powershell
npm.cmd run prisma:validate
npm.cmd run lint
npm.cmd run test
npm.cmd run build
```

스키마 변경은 없지만 기존 Prisma 타입과 모델 사용에 문제가 없는지 `prisma:validate`를 포함한다.

---

## 11. 수동 검증 시나리오

1. 화면 진입 시 첫 제품, 앞 3개 소재, 도달수/3초 재생률이 선택된다.
2. 제품 select를 바꾸면 소재 목록과 그래프가 새 제품 기준으로 바뀐다.
3. 소재를 하나 해제하면 범례와 모든 차트에서 같은 색상 선이 사라진다.
4. 소재를 다시 선택하면 이전과 같은 색상으로 돌아온다.
5. `전체 선택`과 전체 해제가 정상 동작한다.
6. 25%, 50%, 75%, 100%를 각각 선택하면 카드가 추가된다.
7. 지표를 해제하면 해당 카드만 사라진다.
8. 전역 기간을 바꾸면 URL, query key, API 요청, 기간 칩, X축이 함께 갱신된다.
9. 데이터가 없는 날짜는 선이 끊기며 0인 날짜는 Y=0에 그려진다.
10. 도달수 툴팁은 `N명`, 재생률은 `N.NN%` 형식이다.
11. 재생률이 100을 넘는 테스트 데이터도 잘리지 않는다.
12. 마지막 데이터 평균과 하단 3초 변화 요약이 선택 소재 기준으로 바뀐다.
13. 선택 초기화는 기간을 바꾸지 않고 제품/소재/지표만 초기화한다.
14. active 소재 데이터가 없는 제품에서 명확한 빈 상태가 표시된다.
15. API 오류 상태에서도 사이드바와 전역 기간 선택기는 정상 작동한다.
16. 데스크톱에서 조건 패널이 스크롤 중 상단에 고정된다.
17. 좁은 화면에서 가로 스크롤 없이 1열로 배치된다.
18. 키보드 Tab으로 제품, 소재, 지표, 초기화 버튼에 접근할 수 있다.
19. 브라우저 console error가 없다.
20. 기존 Dashboard, Ads, Daily Report 화면에 회귀가 없다.

---

## 12. 완료 조건

다음 항목이 모두 충족돼야 구현 완료로 본다.

- [ ] 좌측 Meta 메뉴에 `Meta 소재 추이`가 추가됨
- [ ] `/creative-trends` 직접 접근 가능
- [ ] 전역 기간 선택과 연동됨
- [ ] 활성 제품을 선택할 수 있음
- [ ] 선택 제품의 소재 목록이 표시됨
- [ ] 소재 다중 선택과 전체 선택/해제가 동작함
- [ ] 6개 지표를 독립적으로 선택할 수 있음
- [ ] 선택 지표마다 별도 선 그래프 카드가 표시됨
- [ ] 각 카드에서 소재별 선 색상이 일관됨
- [ ] 일별 집계가 `sum(count) / sum(reach)` 규칙을 따름
- [ ] null과 0이 구분됨
- [ ] 빈 날짜가 선으로 연결되지 않음
- [ ] 재생률 100 초과 값이 잘리지 않음
- [ ] 마지막 데이터 평균이 표시됨
- [ ] 3초 재생률 변화 요약이 표시됨
- [ ] loading/error/empty 상태가 구현됨
- [ ] API 및 Web 순수 함수 테스트가 추가됨
- [ ] 전체 lint/test/build 통과
- [ ] Prisma migration이 새로 생기지 않음
- [ ] 기존 사용자 수정이 보존됨
- [ ] 목업과 실제 화면의 주요 구조와 시각 계층이 일치함

---

## 13. 이번 작업 범위에서 제외할 것

다음은 별도 요청이 없으면 구현하지 않는다.

- Meta 광고 ON/OFF 또는 예산 변경
- 그래프 이미지/PDF/Excel 내보내기
- 소재별 목표선 또는 경고 임계값 설정
- 시간 단위/주 단위 집계 전환
- 소재 검색, 캠페인/광고세트/단계 추가 필터
- 비활성 소재 조회 토글
- 그래프 줌/브러시/드래그 확대
- 사용자별 화면 설정 서버 저장
- 신규 DB 컬럼, 테이블, 인덱스, 마이그레이션
- 기존 Ads 합계 API 응답 계약 변경
- 기존 Dashboard trend API 확장

이 항목들은 핵심 일별 추이 화면이 안정화된 뒤 별도 기능으로 검토한다.

---

## 14. 다음 세션 시작 체크리스트

1. 이 문서와 `design-previews/meta-creative-video-trend-mockup.html`을 읽는다.
2. `git status --short`로 사용자 변경을 확인한다.
3. 실제 AppShell이 소문자 파일임을 다시 확인한다.
4. `meta-video-metrics.ts`의 현재 zero-reach 처리 수정이 남아 있는지 확인한다.
5. 9절의 구현 순서대로 API 테스트부터 시작한다.
6. Prisma 변경 없이 기존 필드를 사용한다.
7. 전체 테스트와 브라우저 시각 검증이 끝나기 전 완료로 보고하지 않는다.
