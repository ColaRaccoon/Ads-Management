# 쿠팡 데일리 리포트 사용자 지정 카테고리·필터·출력 확장 작업계획서

> 문서 상태: **다음 구현 세션의 단일 기준 문서**
>
> 작성 기준일: 2026-07-24
>
> 대상 저장소: `Meta-Ads-Performance`
>
> 대상 화면: `/coupang/daily-report`
>
> 선행 구현 커밋: `22c9dc9 feat: 쿠팡 데일리 리포트 탭 구현`
>
> 이 문서는 기존 `쿠팡_데일리_리포트_개편_작업계획서.md`에 따라 완성된 현재 화면을 다시 만드는 계획이 아니다. 기존 문서는 1차 개편의 역사적 설계 자료이며, **사용자 지정 카테고리와 필터 결과 기준 집계·출력에 관한 다음 작업에서는 이 문서가 우선한다.**

---

## 0. 다음 세션에서 가장 먼저 알아야 할 결론

이번 작업의 목적은 단순히 검색창 옆에 그룹 선택 셀렉트를 하나 추가하는 것이 아니다. 사용자가 원하는 제품들을 직접 골라 재사용 가능한 **리포트 카테고리**로 저장하고, 선택한 카테고리에 속한 제품만을 기준으로 아래 값이 모두 다시 계산되어야 한다.

- 매출
- 판매수
- 가구매수
- 광고비
- 광고수익률(ROAS)
- 오가닉 매출
- 최종 순이익

필터 결과는 다음 다섯 곳에서 반드시 동일해야 한다.

1. 상단 합계
2. 제품그룹 부모 합계
3. 옵션/단일제품 상세 행
4. 인쇄 및 PDF 저장
5. CSV/XLSX 내보내기

이를 위해 다음 방향을 확정한다.

1. 기존 `CoupangProductGroup`은 그대로 유지한다.
   - 이 모델은 색상·사이즈 같은 옵션을 하나의 판매 제품으로 묶는 **기존 제품 계층**이다.
   - 새 리포트 카테고리와 의미가 다르므로 절대 재사용하거나 이름만 바꾸지 않는다.
2. 별도 `CoupangDailyReportCategory`와 제품 연결 테이블을 추가한다.
   - 한 제품은 여러 리포트 카테고리에 들어갈 수 있다.
   - 카테고리는 “저장된 제품 필터 묶음”으로 취급한다.
3. 카테고리와 검색 필터는 서버에서 같은 제품 ID 집합으로 해석한다.
   - 프런트엔드가 받은 숫자를 임의로 더해 합계를 만들지 않는다.
4. 여러 카테고리를 동시에 선택하면 소속 제품의 **합집합**을 사용한다.
   - 같은 제품이 여러 선택 카테고리에 들어 있어도 한 번만 집계한다.
5. 필터된 원본 제품 행으로 상단 합계와 기존 제품그룹 계층을 다시 만든다.
   - 그룹의 ROAS는 자식 ROAS 평균이 아니라 광고 전환매출 합계 ÷ 광고비 합계이다.
6. 검색도 서버 필터에 포함한다.
   - 현재처럼 화면 행만 숨기고 합계·출력은 전체 값을 유지하는 동작을 폐기한다.
7. 화면·인쇄·CSV·XLSX는 API가 반환한 동일한 필터 결과를 사용한다.
8. 초기 배포 시 카테고리가 하나도 없어도 기존 “전체 보기”는 그대로 동작한다.

---

## 1. 사용자 요구를 구현 언어로 번역한 완료 상태

### 1.1 사용자가 할 수 있어야 하는 일

- 데일리 리포트 화면에서 `리포트 카테고리 관리`를 연다.
- 새 카테고리를 만든다. 예:
  - 홈트레이닝
  - 매트류
  - 집중관리 제품
  - 신제품
- 기존 제품그룹과 그 안의 옵션, 그룹 없는 단일제품을 검색해 체크한다.
- 선택한 제품들을 카테고리에 저장한다.
- 한 제품을 둘 이상의 카테고리에 넣을 수 있다.
- 리포트 상단 필터에서 카테고리 하나 또는 여러 개를 선택한다.
- 필요하면 `미분류 제품`도 함께 선택한다.
- 카테고리 범위 안에서 제품명·옵션명·메모를 추가 검색한다.
- 선택 결과에 해당하는 7개 지표만 화면에서 본다.
- 현재 필터가 그대로 반영된 인쇄물, CSV, XLSX를 만든다.
- 카테고리 이름을 수정하거나 비활성화할 수 있다.

### 1.2 사용자가 보게 될 핵심 화면 상태

- 기본 진입: `전체 제품`
- 카테고리 하나 선택: `홈트레이닝`
- 여러 개 선택: `홈트레이닝 + 신제품`
- 미분류만 선택: `미분류 제품`
- 카테고리와 미분류 동시 선택: 선택 카테고리 제품과 미분류 제품의 합집합
- 검색 추가: 위 범위 안에서 검색에 맞는 제품만 표시·집계
- 결과 없음: 필터 조건은 유지하고 0 합계와 명확한 빈 상태를 표시

### 1.3 이번 작업의 비목표

- 기존 쿠팡 판매/광고/가구매/원가/수수료 계산식 변경
- 기존 `CoupangProductGroup` 옵션 묶음 구조 변경
- 카테고리별 원가 규칙 추가
- 날짜별 카테고리 소속 이력 관리
- 사용자별 권한 또는 사용자별 개인 카테고리
- 카테고리 드래그앤드롭 정렬
- 자동 분류 규칙 또는 AI 분류
- 기존 대시보드·수익분석·광고분석 화면에 새 카테고리 필터 확산

위 기능은 이번 요구를 완료한 뒤 별도 단계로 다룬다.

---

## 2. 현재 코드 기준선

### 2.1 현재 데일리 리포트 화면

주요 파일:

- `apps/web/src/app/coupang/daily-report/page.tsx`
- `apps/web/src/app/coupang/daily-report/rows.tsx`
- `apps/web/src/lib/coupang-daily-report.ts`
- `apps/web/src/types/coupang.ts`
- `apps/web/src/app/globals.css`

현재 구현:

- 날짜 한 개를 선택한다.
- API에서 현재일과 전일 데이터, 상단 합계, 제품 계층을 받는다.
- 제품그룹 부모 행 아래에 옵션 행을 표시한다.
- 그룹 접기/펼치기를 지원한다.
- 제품명·옵션명·메모 문자열 검색을 지원한다.
- 인쇄, CSV, XLSX를 지원한다.
- 화면에는 다음 8개 열이 있다.
  - 제품/옵션
  - 쿠팡 원본매출
  - 원본 판매수량
  - 가구매수량
  - 광고비
  - 광고수익률
  - 오가닉 매출
  - 최종 순이익

### 2.2 현재 서버 흐름

주요 파일:

- `apps/api/src/coupang/coupang.controller.ts`
- `apps/api/src/coupang/coupang.service.ts`
- `apps/api/src/coupang/coupang.service.spec.ts`

현재 엔드포인트:

```text
GET /coupang/daily-report?date=YYYY-MM-DD
```

현재 `dailyReport()`의 핵심 순서:

1. 날짜 검증
2. 선택일 `buildProductProfitRows()` 계산
3. 전일 `buildProductProfitRows()` 계산
4. 전체 쿠팡 제품과 기존 제품그룹 조회
5. 선택일 가구매 메모 조회
6. 필터되지 않은 제품 원본 행으로 현재/전일 전체 합계 계산
7. `buildCoupangDailyHierarchy()`로 제품그룹 부모와 옵션 계층 생성

재사용해야 하는 계산 함수:

- `buildProductProfitRows()`
- `summarizeCoupangProductProfitRows()`
- `aggregateCoupangProductProfitGroup()`
- `buildCoupangDailyHierarchy()`
- `hasDailyReportActivity()`
- `toDailyVisibleMetrics()`
- `toDailyPreviousMetrics()`

### 2.3 현재 기존 제품그룹의 의미

Prisma 모델:

- `CoupangProductGroup`
- `CoupangProduct.groupId`

현재 구조는 한 `CoupangProduct`가 최대 한 개의 기존 제품그룹에 속하는 일대다 관계다. 이 그룹은 웨이브 밸런스바의 블랙·베이지 옵션처럼, 리포트에서 부모 제품과 자식 옵션 계층을 만드는 용도다.

새 카테고리는 다음 이유로 기존 모델과 분리해야 한다.

- 기존 제품그룹은 화면 계층을 만든다.
- 새 카테고리는 사용자가 저장하는 필터 범위를 만든다.
- 하나의 제품은 `홈트레이닝`과 `신제품`에 동시에 들어갈 수 있다.
- 기존 제품그룹 하나에 속한 옵션 중 일부만 집중관리 카테고리에 넣을 수도 있어야 한다.
- 기존 그룹을 카테고리로 재사용하면 제품 계층을 바꾸는 순간 과거 리포트 표시도 예상치 못하게 바뀐다.

### 2.4 현재 발견된 필터·출력 불일치

현재 `page.tsx`와 `coupang-daily-report.ts`에는 아래 불일치가 있다.

1. `filterDailyReportRows()`는 브라우저에서 행만 거른다.
2. 옵션 하나가 검색되어도 부모 제품그룹의 숫자는 전체 옵션 합계를 유지한다.
3. 상단 `SummaryStrip`도 전체 API 합계를 유지한다.
4. `exportRows`는 `report.data.rows` 전체를 사용하므로 검색을 무시한다.
5. 인쇄 CSS는 `.coupang-daily-search-hidden`을 다시 보이게 만들어 검색을 무시한다.
6. `filterDailyReportRowsWithSales()`는 판매수량 0인 행을 화면에서만 제거한다.
7. 판매수량 0이지만 광고비·가구매·경고가 있는 제품은 API 합계와 출력에는 남고 화면에서는 사라질 수 있다.

이번 작업에서는 카테고리 기능만 얹지 말고 이 불일치를 함께 해소해야 한다.

### 2.5 기준 테스트 상태

2026-07-24 확인 결과:

- API `coupang.service.spec.ts`: 130개 통과
- Web `coupang-daily-report.spec.ts`: 22개 통과

실행 명령:

```powershell
npm.cmd --workspace @meta-ads-performance/api run test -- coupang.service.spec.ts
npm.cmd --workspace @meta-ads-performance/web run test -- coupang-daily-report.spec.ts
```

Windows PowerShell에서는 `npm` 대신 `npm.cmd`를 사용한다. 현재 실행 정책에서 `npm.ps1`은 차단된다.

---

## 3. 용어와 모델 경계

혼동을 막기 위해 구현과 UI에서 아래 용어를 구분한다.

| 용어 | 코드/DB | 의미 |
|---|---|---|
| 기존 제품그룹 | `CoupangProductGroup` | 옵션을 부모 제품 아래에 묶는 기존 계층 |
| 제품/옵션 | `CoupangProduct` | 모든 계산의 중복 없는 최소 집계 단위 |
| 리포트 카테고리 | `CoupangDailyReportCategory` | 사용자가 저장하는 제품 필터 묶음 |
| 카테고리 소속 | `CoupangDailyReportCategoryProduct` | 리포트 카테고리와 제품의 다대다 연결 |
| 미분류 제품 | 별도 DB 행 없음 | 활성 리포트 카테고리에 하나도 속하지 않은 제품 |
| 선택 범위 | API 필터 결과 제품 ID 집합 | 카테고리·미분류·검색을 적용한 최종 계산 범위 |

UI에서는 기존 `제품그룹`과 새 기능이 혼동되지 않게 다음 문구를 쓴다.

- 기존 계층: `옵션 묶음 제품`
- 새 기능: `리포트 카테고리`

DB와 기존 API의 `productGroup` 이름은 호환성을 위해 바꾸지 않는다.

---

## 4. 확정 데이터 모델

### 4.1 Prisma 모델

`apps/api/prisma/schema.prisma`에 아래 두 모델을 추가한다.

```prisma
model CoupangDailyReportCategory {
  id           String                              @id @default(uuid()) @db.Uuid
  standardName String                              @unique @map("standard_name")
  displayName  String                              @map("display_name")
  sortOrder    Int                                 @default(100) @map("sort_order")
  isActive     Boolean                             @default(true) @map("is_active")
  createdAt    DateTime                            @default(now()) @map("created_at")
  updatedAt    DateTime                            @updatedAt @map("updated_at")
  members      CoupangDailyReportCategoryProduct[]

  @@index([isActive, sortOrder])
  @@map("coupang_daily_report_categories")
}

model CoupangDailyReportCategoryProduct {
  categoryId      String                     @map("category_id") @db.Uuid
  coupangProductId String                    @map("coupang_product_id") @db.Uuid
  createdAt       DateTime                   @default(now()) @map("created_at")
  category        CoupangDailyReportCategory @relation(fields: [categoryId], references: [id], onDelete: Cascade)
  product         CoupangProduct             @relation(fields: [coupangProductId], references: [id], onDelete: Cascade)

  @@id([categoryId, coupangProductId])
  @@index([coupangProductId])
  @@map("coupang_daily_report_category_products")
}
```

`CoupangProduct`에는 역방향 관계만 추가한다.

```prisma
dailyReportCategoryMemberships CoupangDailyReportCategoryProduct[]
```

실제 필드명은 Prisma 포맷 후 읽기 쉬운 이름으로 다듬어도 되지만, DB 테이블·열 이름과 관계 의미는 위 계약을 유지한다.

### 4.2 관계 규칙

- 한 카테고리는 0개 이상의 제품을 가질 수 있다.
- 한 제품은 0개 이상의 카테고리에 들어갈 수 있다.
- 같은 카테고리와 제품 연결은 복합 기본키로 한 번만 허용한다.
- 카테고리 비활성화 시 소속 연결은 삭제하지 않는다.
  - 다시 활성화할 때 기존 구성을 복구할 수 있어야 한다.
- 카테고리를 실제 하드 삭제하면 연결은 `Cascade`로 정리된다.
  - 이번 API의 `DELETE`는 하드 삭제가 아니라 `isActive=false`이다.
- 제품은 현재도 일반적으로 소프트 비활성화한다.
  - 혹시 하드 삭제되면 고아 연결을 남기지 않도록 연결은 `Cascade`로 정리한다.

### 4.3 카테고리 소속의 시간 의미

카테고리 소속에는 적용 시작일/종료일을 두지 않는다.

확정 의미:

- 카테고리는 현재 저장된 보고용 분류다.
- 오늘 카테고리 구성을 바꾸면 과거 날짜 리포트도 새 분류 기준으로 필터된다.
- 매출·비용 원본을 수정하는 것이 아니라 조회 범위만 바꾸는 것이므로 계산 원천 데이터는 보존된다.

과거 시점의 카테고리 구성을 그대로 재현해야 한다는 요구가 생기면 별도의 기간 이력 모델을 추가한다. 이번 범위에서는 구현하지 않는다.

### 4.4 마이그레이션

새 마이그레이션 경로:

```text
apps/api/prisma/migrations/20260724170000_add_coupang_daily_report_categories/migration.sql
```

마이그레이션은 다음만 수행한다.

1. `coupang_daily_report_categories` 생성
2. `coupang_daily_report_category_products` 생성
3. 고유 인덱스와 조회 인덱스 생성
4. 외래키 생성

기존 데이터 백필은 하지 않는다.

- 배포 직후 카테고리 0개 상태가 정상이다.
- 카테고리 필터를 적용하지 않은 기존 전체 리포트가 그대로 동작해야 한다.
- 기존 `coupang_products.group_id`는 수정하지 않는다.

추가형 마이그레이션이므로 대규모 테이블 잠금이나 계산 데이터 재작성은 필요 없다.

---

## 5. 카테고리 선택과 집계의 정확한 규칙

### 5.1 제품 ID 집합으로 모든 필터를 통일한다

정의:

- `P_all`: 활성/비활성을 포함한 쿠팡 제품 카탈로그의 제품 ID 전체
- `P_category`: 선택한 모든 활성 카테고리의 제품 ID 합집합
- `P_uncategorized`: 활성 카테고리에 하나도 속하지 않은 제품 ID
- `P_scope`: 카테고리와 미분류 선택을 해석한 1차 범위
- `P_query`: 검색어에 맞는 제품 ID
- `P_final`: 실제 계산에 쓰는 최종 제품 ID

규칙:

```text
카테고리 선택 없음 + 미분류 미선택:
P_scope = P_all

카테고리 선택 있음:
P_scope = 선택 카테고리 제품 ID의 합집합

미분류 선택:
P_scope = P_scope ∪ P_uncategorized

검색어 없음:
P_final = P_scope

검색어 있음:
P_final = P_scope ∩ P_query
```

여러 카테고리에 같은 제품이 있어도 `Set<string>` 의미로 중복을 제거한다.

활성 카테고리의 저장 구성원이라면 제품 자체가 현재 비활성이어도 `P_category`에 포함한다. 비활성 제품에 과거 또는 선택일 실적이 있으면 그 실적을 잃지 않기 위해서다. 다만 실제 활동도 메모도 없는 제품은 아래 표시 대상 규칙에 따라 상세 빈 행으로 만들지 않는다.

### 5.2 미분류의 정의

미분류는 별도 카테고리 DB 행으로 만들지 않는다.

제품이 다음 조건을 만족하면 미분류다.

```text
활성 리포트 카테고리와의 연결 수 = 0
```

비활성 카테고리에만 연결된 제품은 미분류로 본다. 사용자가 선택할 수 없는 비활성 분류가 제품을 영구히 숨기면 안 된다.

### 5.3 검색 규칙

검색어는 앞뒤 공백을 제거하고 한국어 로케일 소문자로 정규화한다. 연속된 공백은 한 칸으로 정리한다.

검색 대상:

- 기존 제품그룹 표시명
- 제품/옵션 표시명
- 선택일 가구매 메모

검색 동작:

- 기존 제품그룹명이 일치하면 `P_scope` 안에 있는 해당 그룹의 제품 전체를 `P_query`에 포함한다.
- 옵션/제품명이 일치하면 해당 제품만 포함한다.
- 메모가 일치하면 해당 메모의 제품만 포함한다.
- 검색은 선택 카테고리 밖의 제품을 다시 끌어오지 않는다.
- 전일 데이터는 선택일에서 확정한 `P_final` 제품 ID로 매칭한다.
- 제품명 문자열로 현재일과 전일을 연결하지 않는다.

검색어 길이는 서버에서 최대 100자로 제한한다.

### 5.4 화면 표시 대상

현재의 브라우저 전용 `filterDailyReportRowsWithSales()`는 제거한다.

새 표시 기준:

- `P_final`에 속하면서 `hasDailyReportActivity(row) === true`인 선택일 제품
- 선택일에 유효 메모가 있는 제품
- 표시되는 기존 제품그룹 안에서 전일 데이터가 있거나 활성 상태인 필터 대상 옵션

따라서 판매수량이 0이어도 광고비, 가구매, 계산 경고 등 유효 활동이 있으면 화면에 보여야 한다. 사용자가 요청한 광고비와 순이익을 정확히 추적하려면 판매수량만으로 행을 제거하면 안 된다.

아무 활동도 없고 메모도 없는 카테고리 구성원은 상세 테이블에 빈 행으로 만들지 않는다. 카테고리 관리 화면의 구성원 수에는 포함한다.

### 5.5 상단 합계

상단 합계는 `P_final`에 속한 **원본 제품 행만** 사용한다.

금지:

- 기존 제품그룹 부모 합계와 자식 옵션을 동시에 더하기
- 프런트엔드에서 화면에 보이는 부모 행 숫자를 다시 합산하기
- 카테고리별 합계를 더해 다중 카테고리 전체 합계를 만들기

카테고리가 겹칠 수 있으므로 상단 합계는 항상 중복 제거된 원본 제품 ID 집합으로 계산해야 한다.

### 5.6 기존 제품그룹 부모 합계

카테고리 또는 검색 때문에 기존 제품그룹의 일부 옵션만 `P_final`에 남을 수 있다.

이 경우:

- 부모 그룹은 남은 옵션만 자식으로 가진다.
- `childProductCount`는 필터 후 자식 수다.
- 부모 그룹의 현재 합계는 필터 후 현재 제품 행만 집계한다.
- 부모 그룹의 전일 값은 같은 제품 ID의 전일 행만 집계한다.
- 부모 그룹 이름은 기존 `CoupangProductGroup.displayName`을 유지한다.
- 그룹 전체 옵션이 필터에서 빠지면 부모 행도 만들지 않는다.

현재 코드처럼 검색된 자식은 한 개뿐인데 부모 숫자는 전체 그룹 값인 상태를 허용하지 않는다.

### 5.7 지표 원천과 계산 규칙

| 화면 지표 | 서버 원천 | 필터 후 규칙 |
|---|---|---|
| 매출 | `reportedSalesKrw` | `P_final` 제품 행 합 |
| 판매수 | `reportedSalesQuantity` | `P_final` 제품 행 합 |
| 가구매수 | `manualPurchaseQuantity` | `P_final` 제품 행 합 |
| 광고비 | `adSpendKrw` | `P_final` 제품 행 합 |
| 광고수익률 | `adConversionSalesKrw / adSpendKrw` | 합산 원천으로 가중 재계산 |
| 오가닉 매출 | `actualOrganicSalesKrw` | 기존 nullable 엄격 합계 유지 |
| 최종 순이익 | `marginKrw` | 기존 계산 완전성 규칙 유지 |

ROAS 금지 사항:

- 제품 ROAS의 산술 평균
- 카테고리 ROAS의 산술 평균
- 표시 문자열 `%`를 다시 숫자로 파싱

광고비가 0이면 ROAS는 기존 `safeDivide()` 의미에 따라 `null`이며 화면에는 `-`로 표시한다.

### 5.8 불완전 계산

필터 후 범위에 계산 불완전 제품이 하나라도 있으면:

- 해당 기존 제품그룹의 `marginKrw`는 `null`
- 필터 상단 `summary.current.isComplete`는 `false`
- 상단에는 `knownMarginKrw`를 부분 합계로 보여 주고 경고를 유지
- `incompleteProductCount`, `excludedNetSalesKrw`, `excludedSalesQuantity`도 필터 범위만 기준으로 계산
- 필터 밖의 불완전 제품은 선택 결과 경고에 포함하지 않음

전일 계산 완전성도 같은 `P_final` 제품 ID 범위를 사용한다.

### 5.9 빈 결과

`P_final`이 비거나 선택일 활동 행이 없을 때:

- HTTP 200
- `rows: []`
- 현재 합계 금액/수량은 0
- 현재 `isComplete: true`
- 전일 원천 행도 없으면 전일 `marginKrw: null`
- 적용 필터 정보는 그대로 반환
- 화면에는 `선택한 카테고리와 검색 조건에 해당하는 실적이 없습니다.` 표시
- CSV/XLSX는 합계 1행과 필터 메타데이터를 출력할 수 있으므로 버튼을 비활성화하지 않음

---

## 6. API 설계

### 6.1 카테고리 목록

```text
GET /coupang/daily-report/categories?includeInactive=false
```

응답:

```ts
export type CoupangDailyReportCategorySummary = {
  id: string;
  displayName: string;
  sortOrder: number;
  isActive: boolean;
  memberCount: number;
  activeMemberCount: number;
  updatedAt: string;
};
```

규칙:

- 기본은 활성 카테고리만 반환
- 정렬은 `sortOrder ASC`, `displayName ASC`
- `memberCount`는 비활성 제품도 포함한 저장 구성원 수
- `activeMemberCount`는 현재 활성 제품 수
- 필터 바에서는 이 경량 엔드포인트만 사용

### 6.2 카테고리 관리 카탈로그

관리 창을 열 때만 지연 조회한다.

```text
GET /coupang/daily-report/category-catalog?includeInactive=true
```

응답:

```ts
export type CoupangDailyReportCategory = {
  id: string;
  displayName: string;
  sortOrder: number;
  isActive: boolean;
  productIds: string[];
  updatedAt: string;
};

export type CoupangDailyReportCategoryProductOption = {
  id: string;
  displayName: string;
  sortOrder: number;
  isActive: boolean;
  productGroup: {
    id: string;
    displayName: string;
    sortOrder: number;
  } | null;
  categoryIds: string[];
};

export type CoupangDailyReportCategoryCatalogResponse = {
  categories: CoupangDailyReportCategory[];
  products: CoupangDailyReportCategoryProductOption[];
};
```

이 응답은 원가 이력과 매칭 규칙을 포함하지 않는다. 기존 `/coupang/product-settings` 응답을 재사용하면 관리 창에 불필요한 대용량 필드가 따라오므로 별도 경량 계약을 사용한다.

### 6.3 카테고리 생성

```text
POST /coupang/daily-report/categories
```

요청:

```json
{
  "displayName": "홈트레이닝",
  "sortOrder": 100,
  "productIds": [
    "11111111-1111-1111-1111-111111111111",
    "22222222-2222-2222-2222-222222222222"
  ]
}
```

규칙:

- `displayName` 필수, trim 후 1~80자
- `standardName`은 서버가 공백 정리와 소문자 정규화로 생성
- 같은 정규화 이름은 409
- `productIds`는 선택 입력
- 중복 제품 ID는 제거
- 존재하지 않는 제품 ID가 하나라도 있으면 전체 요청 실패
- 카테고리와 초기 소속 저장은 한 트랜잭션
- 빈 카테고리 생성은 허용

### 6.4 카테고리 메타데이터 수정

```text
PATCH /coupang/daily-report/categories/:id
```

허용 필드:

```json
{
  "displayName": "홈트레이닝 핵심",
  "sortOrder": 50,
  "isActive": true
}
```

제품 소속은 이 엔드포인트에서 수정하지 않는다.

### 6.5 카테고리 제품 전체 교체

```text
PUT /coupang/daily-report/categories/:id/products
```

요청:

```json
{
  "productIds": [
    "11111111-1111-1111-1111-111111111111",
    "33333333-3333-3333-3333-333333333333"
  ],
  "expectedUpdatedAt": "2026-07-24T07:20:00.000Z"
}
```

의미:

- 전달한 `productIds`가 저장 후 정확한 전체 구성이다.
- 기존에 있고 요청에 없는 연결은 삭제한다.
- 요청에 새로 있는 연결은 생성한다.
- 다른 카테고리 소속은 건드리지 않는다.
- 모든 검증과 연결 교체는 한 트랜잭션에서 수행한다.
- `expectedUpdatedAt`이 현재 카테고리 `updatedAt`과 다르면 409 `COUPANG_DAILY_CATEGORY_CHANGED`
- 성공 시 카테고리의 `updatedAt`을 갱신하고 최신 `productIds`를 반환한다.

동시 편집 충돌을 무시하고 마지막 저장이 덮어쓰게 만들지 않는다.

### 6.6 카테고리 비활성화

```text
DELETE /coupang/daily-report/categories/:id
```

실제 동작:

```text
isActive = false
```

연결 행은 유지한다. UI에서는 확인 문구를 표시한다.

비활성 카테고리가 현재 선택 필터에 포함되어 있으면:

- 카테고리 목록을 다시 조회
- 해당 ID를 선택 상태에서 제거
- 남은 선택이 없으면 전체 보기로 복귀
- 리포트를 다시 조회

### 6.7 확장된 데일리 리포트 조회

```text
GET /coupang/daily-report
  ?date=2026-07-24
  &categoryIds=<uuid>,<uuid>
  &includeUncategorized=true
  &q=웨이브
```

쿼리 계약:

| 파라미터 | 필수 | 의미 |
|---|---:|---|
| `date` | 예 | `YYYY-MM-DD` |
| `categoryIds` | 아니요 | 쉼표 구분 활성 카테고리 UUID, 최대 50개 |
| `includeUncategorized` | 아니요 | 엄격한 `true`/`false`, 기본 `false` |
| `q` | 아니요 | 제품그룹명·제품명·옵션명·선택일 메모 검색, 최대 100자 |

카테고리 선택이 없고 `includeUncategorized=false`이면 전체 제품이다. `categoryIds=` 빈 문자열도 전체 제품으로 정규화한다.

### 6.8 데일리 리포트 응답 확장

```ts
export type CoupangDailyReportCategoryRef = {
  id: string;
  displayName: string;
};

export type CoupangDailyAppliedFilter = {
  mode: "ALL" | "FILTERED";
  categories: CoupangDailyReportCategoryRef[];
  includeUncategorized: boolean;
  query: string | null;
  matchedCatalogProductCount: number;
  activityProductCount: number;
  label: string;
};

export type CoupangDailyProductRow = CoupangDailyVisibleMetrics & {
  rowType: "PRODUCT";
  productId: string;
  productName: string;
  groupId: string | null;
  groupName: string | null;
  reportCategories: CoupangDailyReportCategoryRef[];
  memo: string | null;
  previous: CoupangDailyPreviousMetrics;
  calculationStatus: "COMPLETE" | "INCOMPLETE";
  warnings: string[];
};

export type CoupangDailyGroupRow = CoupangDailyVisibleMetrics & {
  rowType: "GROUP";
  groupId: string;
  groupName: string;
  productName: string;
  reportCategories: CoupangDailyReportCategoryRef[];
  childProductCount: number;
  children: CoupangDailyProductRow[];
  previous: CoupangDailyPreviousMetrics;
  calculationStatus: "COMPLETE" | "INCOMPLETE";
  warnings: string[];
};

export type CoupangDailyReportResponse = {
  date: string;
  previousDate: string;
  appliedFilter: CoupangDailyAppliedFilter;
  summary: {
    current: CoupangDailySummary;
    previous: CoupangDailySummary;
  };
  rows: CoupangDailyReportRow[];
};
```

`reportCategories` 규칙:

- 제품 행: 해당 제품의 활성 카테고리 전체
- 기존 제품그룹 부모: 필터 후 자식들이 가진 활성 카테고리의 중복 없는 합집합
- 정렬: 카테고리 `sortOrder`, `displayName`

`appliedFilter.mode`는 카테고리 선택, 미분류 선택, 정규화 후 남은 검색어가 모두 없을 때만 `ALL`이다. 셋 중 하나라도 적용되면 `FILTERED`다.

`appliedFilter.label` 예:

- `전체 제품`
- `홈트레이닝`
- `홈트레이닝 외 2개`
- `미분류 제품`
- `홈트레이닝 + 미분류`
- 검색까지 있으면 프런트가 별도 검색 칩을 표시하므로 서버 label에는 검색어를 붙이지 않아도 됨

카운트 정의:

- `matchedCatalogProductCount`: `P_final`에 들어 있는 카탈로그 제품 ID 수
- `activityProductCount`: 최종 `rows`의 자식 옵션과 단일제품을 제품 ID로 중복 제거한 수. 선택일 활동 또는 유효 메모 때문에 실제 보고서에 나타난 제품 수

### 6.9 오류 계약

기존 날짜 오류를 유지한다.

- `DATE_REQUIRED`
- `INVALID_DATE`

새 오류:

| HTTP | 코드 | 조건 |
|---:|---|---|
| 400 | `INVALID_DAILY_CATEGORY_IDS` | UUID 형식 오류 또는 50개 초과 |
| 400 | `INVALID_INCLUDE_UNCATEGORIZED` | true/false 외 값 |
| 400 | `DAILY_QUERY_TOO_LONG` | 검색어 100자 초과 |
| 400 | `INVALID_DAILY_CATEGORY_PRODUCT_IDS` | 제품 ID 배열 형식 오류 또는 과도한 개수 |
| 404 | `COUPANG_DAILY_CATEGORY_NOT_FOUND` | 카테고리 없음 |
| 404 | `COUPANG_PRODUCT_NOT_FOUND` | 소속 저장 중 제품 없음 |
| 409 | `COUPANG_DAILY_CATEGORY_NAME_CONFLICT` | 정규화 이름 중복 |
| 409 | `COUPANG_DAILY_CATEGORY_CHANGED` | 동시 편집 충돌 |
| 409 | `COUPANG_DAILY_CATEGORY_INACTIVE` | 비활성 카테고리를 조회 필터로 직접 요청 |

Prisma 고유 제약 오류 문자열을 그대로 사용자에게 노출하지 않는다.

---

## 7. 서버 구현 설계

### 7.1 컨트롤러

대상:

```text
apps/api/src/coupang/coupang.controller.ts
```

추가 메서드:

- `listDailyReportCategories()`
- `dailyReportCategoryCatalog()`
- `createDailyReportCategory()`
- `updateDailyReportCategory()`
- `replaceDailyReportCategoryProducts()`
- `deleteDailyReportCategory()`

기존 `dailyReport()`에는 다음 쿼리를 전달한다.

```ts
{
  date,
  categoryIds,
  includeUncategorized,
  q
}
```

### 7.2 서비스 조회 순서

대상:

```text
apps/api/src/coupang/coupang.service.ts
```

새 `dailyReport()` 순서:

1. 날짜와 필터 파라미터 검증/정규화
2. 선택 카테고리 조회 및 활성 상태 검증
3. 다음 데이터를 가능하면 `Promise.all()`로 조회
   - 선택일 제품 손익 원본 행
   - 전일 제품 손익 원본 행
   - 쿠팡 제품 카탈로그
   - 기존 `CoupangProductGroup`
   - 활성 리포트 카테고리 소속
   - 선택일 가구매 메모
4. 카테고리와 미분류 조건으로 `P_scope` 생성
5. 검색어가 있으면 `P_query` 생성
6. `P_final` 생성
7. 선택일/전일 원본 제품 행을 `P_final`로 필터
8. 필터된 원본 제품 행으로 현재/전일 `summary` 계산
9. 필터된 카탈로그와 행으로 기존 제품그룹 계층 생성
10. 각 행에 `reportCategories` 투영
11. `appliedFilter`와 함께 반환

### 7.3 순수 필터 로직 분리

`coupang.service.ts`가 이미 5천 줄 이상이므로 필터 집합 로직을 새 파일로 분리한다.

신규 파일:

```text
apps/api/src/coupang/coupang-daily-report-filter.ts
apps/api/src/coupang/coupang-daily-report-filter.spec.ts
```

순수 함수:

```ts
export function resolveDailyCategoryScope(...): Set<string>;
export function resolveDailySearchProductIds(...): Set<string>;
export function intersectProductIds(...): Set<string>;
export function filterProfitRowsByProductIds(...): ProductProfitRow[];
export function collectDailyRowCategories(...): CoupangDailyReportCategoryRef[];
export function normalizeDailySearchQuery(...): string | null;
```

DB 조회와 예외 발생은 서비스에 두고, 집합 연산과 검색 매칭은 순수 함수로 테스트한다.

기존 계층 함수를 무리하게 전면 이동하지 않는다. 이번 작업과 직접 관련된 최소 범위만 추출한다.

### 7.4 `buildCoupangDailyHierarchy()` 변경

입력에 최종 제품 범위를 추가한다.

```ts
type BuildCoupangDailyHierarchyInput = {
  currentProductRows: ProductProfitRow[];
  previousProductRows: ProductProfitRow[];
  products: CoupangDailyCatalogProduct[];
  manualPurchases: CoupangDailyMemo[];
  includedProductIds: ReadonlySet<string>;
};
```

필수 변경:

- `groupProducts`를 만들 때 `includedProductIds`와 교집합
- 검색/카테고리에서 빠진 활성 옵션을 0행으로 다시 추가하지 않음
- 부모 현재 합계는 필터 후 `currentGroupRows`
- 부모 전일 합계는 같은 필터 제품의 `previousGroupRows`
- 부모 `reportCategories`는 필터 후 자식 기준
- 단일제품도 `includedProductIds` 밖이면 생성하지 않음

### 7.5 카테고리 CRUD 트랜잭션

제품 전체 교체는 다음 절차를 한 트랜잭션에서 수행한다.

1. 카테고리 조회
2. `expectedUpdatedAt` 비교
3. 제품 ID 전체 존재 확인
4. 요청에 없는 기존 연결 `deleteMany`
5. 새 연결 `createMany({ skipDuplicates: true })`
6. 카테고리 `updatedAt` 갱신
7. 최신 카테고리와 연결 반환

제품 ID는 최대 5,000개까지 허용한다. 상한 초과 시 400으로 거절한다.

### 7.6 성능

- 연결 테이블의 `category_id`는 복합 PK의 첫 열이라 카테고리별 조회에 사용 가능
- `coupang_product_id` 보조 인덱스로 미분류 및 역조회 지원
- 카테고리 목록은 원가/매칭 규칙을 포함하지 않는 경량 쿼리
- 검색 입력은 프런트에서 250~300ms debounce
- React Query 키에는 정렬된 카테고리 ID와 정규화 검색어를 사용
- 카테고리 ID 순서만 바뀌었을 때 중복 요청하지 않음

이번 구현에서는 현재 `buildProductProfitRows()` 전체 계산 후 제품 ID를 필터한다. 정확성을 먼저 확보하고, 실제 제품 수 증가로 병목이 확인될 때 DB 단계 필터링을 별도 최적화한다.

---

## 8. 프런트엔드 UX 설계

### 8.1 파일 분리

현재 `page.tsx`가 이미 큰 편이므로 새 UI를 한 파일에 모두 넣지 않는다.

파일:

```text
apps/web/src/app/coupang/daily-report/page.tsx
apps/web/src/app/coupang/daily-report/rows.tsx
apps/web/src/app/coupang/daily-report/category-filter.tsx
apps/web/src/app/coupang/daily-report/category-manager.tsx
apps/web/src/lib/coupang-daily-report.ts
apps/web/src/lib/coupang-daily-category.ts
apps/web/src/types/coupang.ts
apps/web/src/app/globals.css
```

`coupang-daily-category.ts`에는 다음 순수 로직을 둔다.

- 선택 카테고리 ID 정렬/정규화
- API URL 생성
- 필터 label 생성
- 제품그룹별 카테고리 편집 트리 구성
- 부모 체크박스의 checked/indeterminate 계산
- 검색된 제품 일괄 선택
- 파일명용 안전한 필터 slug 생성

### 8.2 상단 필터 바

날짜 아래 또는 상세 패널 상단에 별도 `리포트 범위` 영역을 둔다.

구성:

1. `전체 제품` 버튼
2. `카테고리 선택` 다중선택 팝오버
3. 선택 카테고리 칩
4. `미분류 제품` 체크박스
5. 기존 검색 입력
6. `필터 초기화`
7. `카테고리 관리`

카테고리 팝오버:

- 카테고리명
- 활성 제품 수
- 체크박스
- 내부 카테고리명 검색
- 전체 선택/전체 해제
- 선택 완료

여러 카테고리 선택은 OR/합집합이다. UI에 `선택한 카테고리의 제품을 합쳐서 표시하며 중복 제품은 한 번만 계산합니다.` 문구를 한 번 표시한다.

### 8.3 적용 필터 표시

리포트 응답을 받은 뒤 화면에 서버가 확정한 필터를 표시한다.

예:

```text
적용 범위  홈트레이닝  신제품  미분류
검색      웨이브
대상      카탈로그 제품 12개 · 활동 제품 8개
```

클라이언트의 임시 선택 상태가 아니라 `report.data.appliedFilter`를 출력한다. 서버가 실제로 계산한 범위를 사용자에게 보여 주기 위해서다.

### 8.4 카테고리 관리 창

레이아웃:

- 왼쪽: 카테고리 목록
- 오른쪽 상단: 선택 카테고리 이름/상태/정렬값
- 오른쪽 본문: 제품 선택 트리
- 하단: 취소, 저장

카테고리 목록 기능:

- 새 카테고리
- 활성/비활성 함께 보기
- 이름 수정
- 비활성화
- 다시 활성화
- 구성원 수 표시

제품 선택 트리:

- 기존 `CoupangProductGroup`을 부모 노드로 표시
- 그 아래 `CoupangProduct` 옵션 표시
- 기존 제품그룹이 없는 제품은 `단일 제품` 섹션에 표시
- 부모 체크 시 현재 보이는 자식 전체 선택
- 일부만 선택되면 indeterminate
- 제품명/옵션명 검색
- `검색 결과 전체 선택`
- 각 제품 옆에 다른 리포트 카테고리 소속 배지 표시
- 비활성 제품은 별도 상태 배지
- `비활성 제품 포함` 토글

카테고리 중복 소속은 허용한다. 다른 카테고리에 이미 들어 있다는 이유로 체크를 막지 않는다.

### 8.5 저장 중 상태와 충돌

- 저장 중 버튼 비활성화
- 저장 성공 후 카테고리 목록, 카탈로그, 데일리 리포트 쿼리 무효화
- 409 동시 변경 응답이면 현재 편집 내용을 자동 덮어쓰지 않음
- `다른 화면에서 이 카테고리가 변경되었습니다. 최신 구성을 불러온 뒤 다시 저장하세요.` 표시
- 사용자가 새로고침을 선택할 때만 서버 상태로 편집 내용을 교체

### 8.6 검색

기존 즉시 클라이언트 필터를 서버 검색으로 바꾼다.

- `searchInput`: 사용자가 입력 중인 값
- `debouncedQuery`: 250~300ms 뒤 API에 전달할 값
- React Query 키에 `debouncedQuery` 포함
- 검색 중 이전 응답을 잠깐 유지하더라도 `isFetching` 표시
- 새 응답 전 숫자를 검색 결과인 것처럼 표시하지 않도록 작은 로딩 상태 표시

기존 `visibleRows`, `visibleRowsByKey`, `filterDailyReportRows()` 기반 렌더링은 제거한다.

### 8.7 요약 스트립

기존 7개 지표 구조를 유지하되 제목을 응답에 따라 바꾼다.

- 전체 모드: `전체 합계`
- 필터 모드: `선택 범위 합계`

하단 보조 문구:

- 적용 카테고리 수
- 필터 대상 카탈로그 제품 수
- 실제 활동 제품 수
- 기존 제품그룹/단일제품/옵션 행 수

합계 숫자는 무조건 API `summary`를 사용한다.

### 8.8 상세 테이블

기존 8개 열과 고밀도 디자인은 유지한다.

변경:

- 서버 응답 `rows`를 그대로 렌더링
- 클라이언트 검색으로 행을 숨기지 않음
- 카테고리 때문에 기존 제품그룹의 일부 옵션만 남으면 그 옵션만 표시
- 제품명 셀에 카테고리 칩을 반복하지 않음
  - 선택 범위는 상단 필터 바에서 표시
  - 제품명 셀의 `title`에 행의 `reportCategories` 이름을 표시
- 접기/펼치기는 화면 표현 상태일 뿐 계산 필터가 아님

검색어가 있으면 검색 결과 그룹은 임시로 펼쳐 보이게 한다. 사용자가 접은 상태를 강제로 영구 변경하지 않는다.

### 8.9 로딩·오류·빈 상태

별도 상태:

- 카테고리 목록 로딩 실패
- 리포트 로딩 실패
- 카테고리 관리 카탈로그 로딩 실패
- 카테고리 저장 실패
- 필터 결과 0건
- 카테고리 0개

카테고리 목록이 실패해도 전체 리포트 조회까지 막지 않는다. 전체 보기와 기존 날짜 조회는 계속 가능해야 한다.

카테고리 0개일 때:

- `아직 만든 리포트 카테고리가 없습니다.`
- `첫 카테고리 만들기` 버튼
- 전체 리포트는 정상 표시

### 8.10 접근성

- 관리 창: `role="dialog"`, `aria-modal="true"`, 제목 연결
- 열릴 때 첫 입력 또는 현재 카테고리에 포커스
- 닫을 때 `카테고리 관리` 버튼으로 포커스 복귀
- Escape 닫기
- 배경 포커스 이동 방지
- 카테고리 팝오버와 체크박스 모두 키보드 조작 가능
- indeterminate 부모 체크박스는 시각 상태뿐 아니라 적절한 `aria-checked="mixed"` 제공
- 저장/오류 알림은 `aria-live`
- 색상만으로 활성/비활성 또는 선택 상태를 구분하지 않음

---

## 9. 인쇄·CSV·XLSX 설계

### 9.1 단일 원칙

출력은 다음 응답만 사용한다.

```text
현재 화면의 날짜 + 카테고리 + 미분류 + 검색어로 서버가 반환한
appliedFilter + summary + rows
```

별도의 “전체 데이터” 배열을 내보내기에 사용하지 않는다.

### 9.2 인쇄

인쇄물 상단 메타데이터:

```text
조회일 2026-07-24
범위 홈트레이닝 + 신제품
검색 웨이브
```

규칙:

- 필터 바와 관리 버튼은 인쇄하지 않음
- 필터 메타데이터는 인쇄
- 화면에서 접은 그룹의 **필터 후 자식 옵션**은 모두 인쇄
- 필터 밖의 제품은 CSS로 다시 살리지 않음
- 기존 `.coupang-daily-search-hidden` 인쇄 강제 표시 규칙 제거
- 접힘 복원용 `.coupang-daily-collapsed` 인쇄 규칙은 유지
- A4 가로, 6mm 여백 유지

### 9.3 CSV

CSV는 현재 필터를 재현할 수 있도록 앞 열을 추가한다.

열 순서:

1. 조회일
2. 선택 필터
3. 검색어
4. 소속 리포트 카테고리
5. 행구분
6. 제품/옵션
7. 쿠팡 원본매출
8. 원본 판매수량
9. 전일 원본 판매수량
10. 가구매수량
11. 광고비
12. 전일 광고비
13. 광고수익률
14. 전일 광고수익률
15. 오가닉 매출
16. 최종 순이익
17. 전일 최종 순이익

`선택 필터`는 서버 `appliedFilter.label`을 사용한다. `소속 리포트 카테고리`는 행별 `reportCategories` 이름을 ` · `로 연결한다.

### 9.4 XLSX

CSV와 같은 데이터 열을 사용한다.

- 숫자는 계속 숫자 셀로 저장
- ROAS는 비율 숫자와 기존 Ratio 스타일 유지
- 조회일/필터/검색어는 Text 스타일
- 전체/선택 합계 행은 Total 스타일
- 헤더 고정과 자동 필터 유지
- 빈 결과도 합계 1행은 내보내기

### 9.5 내보내기 행 구분

기존 행 구분을 유지하되 합계 이름만 필터 상태에 맞춘다.

- `전체합계`
- `선택합계`
- `그룹합계`
- `옵션`
- `단일제품`
- `기타사항`

전체 모드이면 `전체합계`, 필터 모드이면 `선택합계`다.

### 9.6 파일명

파일명 형식:

```text
2026-07-24_쿠팡_데일리리포트_전체.xlsx
2026-07-24_쿠팡_데일리리포트_홈트레이닝.xlsx
2026-07-24_쿠팡_데일리리포트_카테고리3개.xlsx
2026-07-24_쿠팡_데일리리포트_미분류.csv
```

파일명 조각:

- Windows 금지 문자 제거
- 공백은 `_`
- 최대 길이 제한
- 검색어는 민감하거나 지나치게 길 수 있으므로 기본 파일명에는 넣지 않고 파일 내부 메타데이터에만 기록

### 9.7 `flattenDailyReportExportRows()` 변경

기존:

```ts
flattenDailyReportExportRows(summary, rows)
```

변경:

```ts
flattenDailyReportExportRows(response)
```

응답 전체를 받게 해 날짜, 적용 필터, 검색어, 행별 카테고리를 누락하지 않게 한다.

---

## 10. 프런트엔드 쿼리와 상태

### 10.1 상태

```ts
const [date, setDate] = useState(todayInputValue());
const [selectedCategoryIds, setSelectedCategoryIds] = useState<Set<string>>(new Set());
const [includeUncategorized, setIncludeUncategorized] = useState(false);
const [searchInput, setSearchInput] = useState("");
const debouncedQuery = useDebouncedValue(searchInput, 300);
const [collapsedGroupIds, setCollapsedGroupIds] = useState<Set<string>>(new Set());
const [categoryManagerOpen, setCategoryManagerOpen] = useState(false);
```

필터 선택은 날짜를 바꿔도 유지한다. 페이지를 완전히 새로 열면 기본 전체 보기로 시작한다. 이번 범위에서는 localStorage 또는 URL 영속화를 필수로 하지 않는다.

### 10.2 안정적인 Query Key

```ts
const canonicalCategoryIds = [...selectedCategoryIds].sort();

[
  "coupang-daily-report",
  date,
  canonicalCategoryIds.join(","),
  includeUncategorized,
  debouncedQuery.trim()
]
```

URL도 같은 정렬 순서를 사용한다.

### 10.3 쿼리 무효화

카테고리 생성/수정/소속저장/비활성화 성공 시:

- `["coupang-daily-report-categories"]`
- `["coupang-daily-report-category-catalog"]`
- `["coupang-daily-report"]`

제품 설정에서 제품명, 활성 상태 또는 기존 `groupId`가 바뀔 때:

- 카테고리 카탈로그
- 데일리 리포트

도 무효화해야 한다.

현재 `apps/web/src/app/coupang/products/page.tsx`는 비용 변경 여부에 따라 데일리 리포트 무효화가 달라질 수 있다. 제품명/기존 제품그룹 변경도 리포트 계층과 카테고리 관리 트리에 영향을 주므로 별도로 보완한다.

업로드 및 가구매 저장 후 기존 `["coupang-daily-report"]` prefix 무효화는 유지한다.

---

## 11. 파일별 변경 계획

### `apps/api/prisma/schema.prisma`

- `CoupangDailyReportCategory` 추가
- `CoupangDailyReportCategoryProduct` 추가
- `CoupangProduct` 역관계 추가
- 기존 `CoupangProductGroup` 의미와 관계 유지

### `apps/api/prisma/migrations/20260724170000_add_coupang_daily_report_categories/migration.sql`

- 두 테이블, 인덱스, 외래키 생성
- 기존 데이터 변경/백필 없음

### `apps/api/src/coupang/coupang-daily-report-category-migration.spec.ts` 신규

- 테이블명
- 복합 PK/고유성
- 제품 역조회 인덱스
- 두 외래키와 삭제 정책
- 기존 `coupang_products.group_id`를 변경하지 않는지 검사

### `apps/api/src/coupang/coupang.controller.ts`

- 카테고리 목록/카탈로그/CRUD/제품교체 라우트 추가
- 데일리 리포트 필터 쿼리 전달

### `apps/api/src/coupang/coupang.service.ts`

- 카테고리 CRUD
- 경량 카탈로그 조회
- 입력 검증과 오류 매핑
- `dailyReport()`에 제품 범위 해석 추가
- 필터 후 summary 계산
- 필터 후 계층 생성
- `appliedFilter`, `reportCategories` 응답
- 기존 계산 공식 재사용

### `apps/api/src/coupang/coupang-daily-report-filter.ts` 신규

- 카테고리 합집합
- 미분류 계산
- 검색 교집합
- 제품 행 필터
- 행별 카테고리 중복 제거/정렬

### `apps/api/src/coupang/coupang-daily-report-filter.spec.ts` 신규

- 순수 집합/검색/중복 제거 테스트

### `apps/api/src/coupang/coupang.service.spec.ts`

- API 오케스트레이션
- 필터 summary
- 기존 제품그룹 부분 집계
- CRUD/동시성/오류
- 무필터 호환성 테스트

### `apps/web/src/types/coupang.ts`

- 카테고리 목록/카탈로그 타입
- `appliedFilter`
- 행별 `reportCategories`
- 확장 응답 타입

### `apps/web/src/app/coupang/daily-report/page.tsx`

- 카테고리 목록 쿼리
- 필터 상태와 debounce
- 서버 필터 URL
- 필터 결과 summary/rows 사용
- 인쇄 메타데이터
- 필터 결과 내보내기
- 관리 창 연결
- 기존 클라이언트 검색 투영 제거

### `apps/web/src/app/coupang/daily-report/category-filter.tsx` 신규

- 카테고리 다중선택
- 미분류
- 필터 칩
- 초기화
- 카테고리 관리 진입

### `apps/web/src/app/coupang/daily-report/category-manager.tsx` 신규

- 카테고리 CRUD
- 제품 트리
- 검색/일괄선택
- 저장/충돌/오류

### `apps/web/src/app/coupang/daily-report/rows.tsx`

- `visibleRow`, `searchHidden` 기반 숨김 제거
- API의 필터 완료 행만 렌더링
- 접기/펼치기와 메모 표시 유지
- 행 소속 카테고리를 제품명 셀의 `title`에 제공

### `apps/web/src/lib/coupang-daily-report.ts`

- `filterDailyReportRowsWithSales()` 제거
- 서버 검색 전환 후 `filterDailyReportRows()` 제거 또는 더 이상 production에서 사용하지 않음
- `flattenDailyReportExportRows(response)`로 변경
- 출력 메타데이터와 `선택합계` 지원

### `apps/web/src/lib/coupang-daily-category.ts` 신규

- 쿼리 URL/키 정규화
- 필터 label
- 카테고리 편집 트리
- 체크 상태
- 파일명 slug

### `apps/web/src/lib/coupang-daily-report.spec.ts`

- 새 출력 계약
- 서버 필터 완료 행 렌더링
- 접힘 인쇄 DOM 유지
- 기존 숫자 형식과 메모 회귀

### `apps/web/src/lib/coupang-daily-category.spec.ts` 신규

- 다중선택 정렬
- 합집합 label
- 부모 mixed 상태
- 그룹 전체 선택
- 비활성 제품 보존
- 파일명 정리

### `apps/web/src/app/coupang/products/page.tsx`

- 제품명/기존 제품그룹 변경 후 데일리 리포트와 카테고리 카탈로그 무효화
- UI의 기존 `제품그룹` 설명을 `옵션을 하나의 제품으로 묶는 그룹`이라고 명확히 표시

### `apps/web/src/app/globals.css`

- `coupang-daily-category-*` 접두사 스타일
- 필터 바/칩/팝오버/관리 창/제품 트리
- 반응형
- 인쇄 시 컨트롤 숨김, 필터 메타 표시
- 검색 숨김 행 강제 복원 CSS 제거
- 접힌 필터 후 옵션 인쇄 복원 유지

---

## 12. 테스트 계획

### 12.1 마이그레이션

1. 두 새 테이블 생성
2. 카테고리 이름 고유 인덱스
3. 카테고리-제품 복합 PK
4. 제품 ID 역조회 인덱스
5. 카테고리 삭제 Cascade
6. 제품 삭제 Cascade
7. 기존 제품그룹 테이블/열 미변경
8. 백필 SQL 없음

### 12.2 카테고리 CRUD API

1. 빈 카테고리 생성
2. 초기 제품과 함께 생성
3. 이름 trim/정규화
4. 정규화 이름 충돌 409
5. 이름 수정
6. 정렬값 수정
7. 비활성화 후 목록 제외
8. includeInactive 목록 포함
9. 재활성화 시 기존 제품 소속 보존
10. 제품 전체 교체
11. 중복 제품 ID 제거
12. 존재하지 않는 제품 때문에 전체 트랜잭션 롤백
13. 한 제품을 두 카테고리에 저장 가능
14. 다른 카테고리 소속은 전체 교체 시 유지
15. `expectedUpdatedAt` 충돌 409
16. 비활성 제품도 기존 소속으로 보존 가능

### 12.3 필터 집합

1. 아무 필터 없음 → 전체 제품
2. 카테고리 하나 → 해당 제품만
3. 카테고리 여러 개 → 합집합
4. 겹친 제품 → 한 번만 포함
5. 미분류만
6. 카테고리 + 미분류
7. 비활성 카테고리 소속만 있는 제품 → 미분류
8. 비활성 카테고리 직접 요청 → 409
9. 빈 카테고리 → 빈 결과
10. UUID 형식 오류
11. 카테고리 50개 초과

### 12.4 검색과 필터 교집합

1. 검색어 없음 → 카테고리 범위 유지
2. 기존 제품그룹명 검색 → 범위 안의 그룹 옵션 전체
3. 옵션명 검색 → 해당 옵션만
4. 단일제품명 검색 → 해당 제품만
5. 선택일 메모 검색 → 해당 제품만
6. 카테고리 밖 이름이 일치해도 포함하지 않음
7. 공백/대소문자 정규화
8. 100자 초과 거절
9. 검색 결과 0건

### 12.5 집계 정확성

1. 상단 매출이 필터 제품 합과 일치
2. 판매수가 필터 제품 합과 일치
3. 가구매수가 필터 제품 합과 일치
4. 광고비가 필터 제품 합과 일치
5. ROAS가 광고 전환매출 합 ÷ 광고비 합
6. 자식 ROAS 단순 평균이 아님
7. 오가닉 매출 nullable 엄격 합계
8. 최종 순이익 nullable 엄격 합계
9. 불완전 제품이 필터 밖이면 선택 summary는 완전
10. 불완전 제품이 필터 안이면 선택 summary는 불완전
11. known margin과 제외 금액/수량이 필터 범위만 반영
12. 전일도 같은 제품 ID 집합
13. 현재/전일을 제품명으로 연결하지 않음
14. 겹친 카테고리 제품 중복 합산 없음

### 12.6 기존 제품그룹 계층

1. 그룹의 모든 옵션이 필터에 있으면 기존과 동일
2. 일부 옵션만 필터에 있으면 부모와 해당 옵션만
3. 부모 숫자가 필터 옵션 합과 일치
4. 부모 전일 값도 같은 옵션 집합
5. 필터 옵션이 0개면 부모 제거
6. 그룹 없는 제품은 단일 행
7. 판매 0·광고비 존재 제품 표시
8. 메모만 있는 제품 표시
9. 완전 무활동 제품은 상세 행 미생성
10. 필터 후 메모 행은 남은 옵션 메모만 포함

### 12.7 프런트엔드 카테고리 관리

1. 카테고리 0개 안내
2. 새 카테고리 생성
3. 기존 제품그룹 부모 체크 → 자식 전체
4. 일부 자식 체크 → mixed
5. 제품 검색 후 결과 전체 선택
6. 다른 카테고리 소속 배지
7. 중복 소속 허용
8. 비활성 제품 숨김/포함
9. 숨긴 비활성 기존 구성원 저장 시 유실되지 않음
10. 저장 중 중복 클릭 방지
11. 충돌 시 로컬 편집 자동 덮어쓰기 금지
12. 비활성화한 선택 카테고리 필터에서 제거

### 12.8 화면·인쇄·내보내기 일치

동일한 필터 조합에 대해 다음을 비교한다.

1. 상단 합계
2. 테이블 부모 합계
3. 테이블 자식 목록
4. 인쇄 미리보기
5. CSV 행
6. XLSX 행

필수 사례:

- 전체
- 카테고리 하나
- 겹친 카테고리 둘
- 미분류
- 카테고리 + 검색
- 옵션 하나 검색
- 결과 없음
- 불완전 순이익

CSV/XLSX 추가 검증:

- 필터 메타데이터 존재
- 행별 소속 카테고리 존재
- 금액/수량/ROAS 숫자 타입
- 메모 행 숫자 셀은 빈 값
- 필터 결과 0건이어도 선택합계 행 존재
- 파일명 안전 문자

### 12.9 회귀 테스트

- 날짜 오류 유지
- 기존 전체 모드 응답 숫자 유지
- 기존 제품그룹 접기/펼치기
- 기존 메모 공백 정규화
- 기존 A4 가로 인쇄
- 기존 CSV BOM
- 기존 XLSX 숫자 스타일
- 쿠팡 업로드 후 리포트 갱신
- 가구매 저장 후 리포트 갱신
- 제품 비용 변경 후 리포트 갱신
- 다른 쿠팡 탭과 Meta 탭 영향 없음

---

## 13. 수동 검증 시나리오

테스트용으로 다음 구성을 만든다.

```text
기존 옵션 묶음 제품 A
  - 옵션 A-1: 홈트레이닝, 신제품
  - 옵션 A-2: 홈트레이닝
  - 옵션 A-3: 카테고리 없음

기존 옵션 묶음 제품 B
  - 옵션 B-1: 매트류
  - 옵션 B-2: 집중관리, 신제품

단일 제품 C
  - 홈트레이닝

단일 제품 D
  - 카테고리 없음

비활성 제품 E
  - 홈트레이닝
```

확인:

1. 전체 보기 숫자가 변경 전 전체 리포트와 동일한지
2. 홈트레이닝 선택 시 A-1, A-2, C만 포함되는지
3. 홈트레이닝 + 신제품 선택 시 A-1이 한 번만 계산되는지
4. 미분류 선택 시 A-3, D가 포함되는지
5. 옵션 A-1 이름 검색 시 A 부모 합계가 A-1 값만 가지는지
6. 제품 A 그룹명 검색 시 선택 카테고리 범위 안의 A 옵션 전체가 포함되는지
7. 메모 검색 시 해당 옵션만 포함되는지
8. 판매수 0이고 광고비만 있는 제품이 보이는지
9. 화면 합계와 CSV/XLSX 합계가 같은지
10. 화면에서 그룹을 접어도 인쇄에는 필터 후 옵션이 모두 나오는지
11. 필터 밖 옵션이 인쇄에서 다시 나타나지 않는지
12. 카테고리 이름과 검색어가 인쇄/파일 내부에 기록되는지
13. 과거 날짜 조회에도 현재 카테고리 구성이 적용되는지
14. 카테고리 수정 직후 현재 보고서가 다시 계산되는지

---

## 14. 구현 순서

### 1단계: 기준선 보호

1. 이 문서를 끝까지 읽는다.
2. `git status --short`로 사용자 변경을 확인한다.
3. 현재 API/Web 대상 테스트를 실행한다.
4. 현재 전체 모드 샘플 응답 또는 테스트 fixture를 회귀 기준으로 확보한다.

### 2단계: 스키마와 마이그레이션

1. Prisma 모델 추가
2. 마이그레이션 SQL 작성
3. 마이그레이션 계약 테스트 작성
4. `prisma validate`
5. `prisma generate`

### 3단계: 카테고리 CRUD

1. 서버 타입과 검증 함수
2. 목록/카탈로그
3. 생성/수정/비활성화
4. 제품 전체 교체 트랜잭션
5. 오류 코드
6. 서비스 테스트

### 4단계: 필터 집합과 서버 검색

1. 순수 필터 파일과 단위 테스트
2. 카테고리 합집합
3. 미분류
4. 검색 교집합
5. 중복 제거
6. 행별 카테고리 투영

### 5단계: 데일리 리포트 집계 연결

1. `dailyReport()` 쿼리 계약 확장
2. 필터 후 현재/전일 원본 행 생성
3. 필터 summary
4. 필터 계층
5. 부분 옵션 그룹 재집계
6. `appliedFilter`
7. 무필터 호환성 테스트

### 6단계: 프런트 타입과 순수 헬퍼

1. 응답 타입
2. 쿼리 URL/키
3. 카테고리 label
4. 관리 트리
5. 파일명/출력 변환
6. 단위 테스트

### 7단계: 필터 UI

1. 카테고리 목록
2. 다중선택/미분류
3. debounce 검색
4. 서버 응답 기반 summary/rows
5. 로딩/오류/빈 상태
6. 접기 상태 유지

### 8단계: 카테고리 관리 UI

1. 관리 창
2. 카테고리 CRUD
3. 제품 트리/검색/일괄선택
4. 비활성 제품
5. 저장 충돌
6. 쿼리 무효화

### 9단계: 출력

1. 인쇄 메타데이터
2. 필터 밖 행 인쇄 복원 방지
3. CSV 필터 메타
4. XLSX 필터 메타
5. 빈 결과 출력
6. 파일명

### 10단계: 전체 검증

1. API/Web 대상 테스트
2. 전체 테스트
3. lint
4. build
5. 실제 브라우저 화면
6. Chromium 인쇄 미리보기
7. CSV/XLSX 직접 열기
8. 기존 다른 탭 회귀 확인

---

## 15. 검증 명령

프로젝트 루트:

```powershell
npm.cmd run prisma:validate
npm.cmd run prisma:generate
npm.cmd --workspace @meta-ads-performance/api run test -- coupang-daily-report-filter.spec.ts coupang.service.spec.ts
npm.cmd --workspace @meta-ads-performance/web run test -- coupang-daily-category.spec.ts coupang-daily-report.spec.ts
npm.cmd run test
npm.cmd run lint
npm.cmd run build
```

개발 서버:

```powershell
npm.cmd run dev
```

확인 URL:

```text
http://localhost:3100/coupang/daily-report
```

실제 DB 마이그레이션은 올바른 `DATABASE_URL`이 설정된 환경에서만 실행한다.

---

## 16. 배포와 롤백

### 16.1 배포 순서

1. DB 마이그레이션 적용
2. API 배포
3. Web 배포
4. 카테고리 0개 상태 전체 리포트 확인
5. 테스트 카테고리 생성
6. 필터/출력 확인

새 테이블만 추가하므로 API/Web이 아직 구버전이어도 기존 기능에는 영향이 없어야 한다.

### 16.2 롤백

문제 발생 시:

1. Web을 이전 버전으로 되돌림
2. API를 이전 버전으로 되돌림
3. 새 카테고리 테이블은 즉시 삭제하지 않고 유지 가능

새 테이블이 기존 계산 경로에서 참조되지 않으므로 코드 롤백만으로 기존 전체 리포트로 복귀할 수 있다. 저장된 카테고리 구성을 보존하기 위해 DB 테이블 삭제는 마지막 수단으로 둔다.

---

## 17. 완료 조건

아래 항목이 모두 충족되어야 작업 완료다.

- [ ] 기존 제품그룹과 새 리포트 카테고리가 별도 모델이다.
- [ ] 사용자가 리포트 화면에서 카테고리를 생성할 수 있다.
- [ ] 사용자가 제품/옵션을 직접 검색하고 카테고리에 저장할 수 있다.
- [ ] 한 제품을 여러 카테고리에 넣을 수 있다.
- [ ] 카테고리 이름 수정, 비활성화, 재활성화가 가능하다.
- [ ] 카테고리 하나 또는 여러 개를 필터로 선택할 수 있다.
- [ ] 여러 카테고리 선택은 합집합이며 중복 제품을 한 번만 집계한다.
- [ ] 미분류 제품만 또는 선택 카테고리와 함께 볼 수 있다.
- [ ] 검색은 카테고리 범위와 교집합으로 동작한다.
- [ ] 검색된 옵션만 남으면 기존 제품그룹 부모 합계도 그 옵션 값으로 다시 계산된다.
- [ ] 상단 합계가 필터 후 원본 제품 행만 사용한다.
- [ ] 매출, 판매수, 가구매수, 광고비가 필터 범위와 일치한다.
- [ ] ROAS가 필터 후 광고 전환매출 합 ÷ 광고비 합이다.
- [ ] 오가닉 매출의 기존 nullable 규칙이 유지된다.
- [ ] 최종 순이익과 불완전 계산 경고가 필터 범위만 반영한다.
- [ ] 판매수량 0이어도 광고비/가구매/경고 활동이 있으면 표시된다.
- [ ] 완전 무활동 제품은 상세 빈 행으로 만들지 않는다.
- [ ] 화면, 상단 합계, 부모 합계, 인쇄, CSV, XLSX가 같은 필터 결과를 사용한다.
- [ ] 화면 접힘 상태는 출력 범위를 바꾸지 않는다.
- [ ] 필터 밖 제품이 인쇄 CSS로 다시 나타나지 않는다.
- [ ] CSV/XLSX에 조회일, 필터, 검색어, 소속 카테고리가 기록된다.
- [ ] 빈 필터 결과도 합계가 포함된 파일로 출력할 수 있다.
- [ ] 카테고리 0개 상태에서 기존 전체 리포트가 정상이다.
- [ ] 무필터 전체 모드의 기존 숫자와 계층이 회귀하지 않는다.
- [ ] 카테고리 저장 후 관련 React Query 캐시가 갱신된다.
- [ ] API/Web 테스트, lint, build가 모두 통과한다.
- [ ] 실제 브라우저와 A4 가로 인쇄 미리보기를 확인했다.

---

## 18. 구현 중 금지 사항

- 기존 `CoupangProductGroup`을 새 카테고리로 재사용하지 않는다.
- 카테고리명을 `groupId`에 저장하지 않는다.
- 제품명 문자열을 카테고리 소속 키로 사용하지 않는다.
- 카테고리 제품 목록을 JSON 한 칸에 저장하지 않는다.
- 다중 카테고리 합계를 카테고리별 합계의 단순 합으로 만들지 않는다.
- 필터된 부모 그룹 합계를 프런트엔드에서 화면 숫자로 다시 계산하지 않는다.
- ROAS를 자식 ROAS 평균으로 계산하지 않는다.
- 검색을 DOM `display:none`만으로 처리하지 않는다.
- 화면과 export에 서로 다른 행 배열을 사용하지 않는다.
- 접힌 행과 필터 밖 행을 같은 CSS 상태로 취급하지 않는다.
- 카테고리 비활성화 시 소속을 삭제하지 않는다.
- 소속 전체 교체를 트랜잭션 없이 delete/create로 나누지 않는다.
- 존재하지 않는 제품을 일부만 무시하고 저장하지 않는다.
- 동시 편집 충돌에서 조용히 마지막 저장으로 덮어쓰지 않는다.
- 새 요구를 이유로 쿠팡 손익 계산 공식을 변경하지 않는다.
- 새 카테고리 때문에 기존 업로드 데이터를 재매칭하지 않는다.
- 이번 범위에서 다른 쿠팡 분석 탭까지 카테고리 필터를 확산하지 않는다.

---

## 19. 다음 세션 시작 체크리스트

1. 이 파일을 처음부터 끝까지 읽는다.
2. 이전 `쿠팡_데일리_리포트_개편_작업계획서.md`는 현재 구현의 배경 자료로만 사용한다.
3. `git status --short`로 기존 변경을 보호한다.
4. `apps/api/prisma/schema.prisma`의 기존 제품그룹 관계를 다시 확인한다.
5. `dailyReport()`, `buildCoupangDailyHierarchy()`, `summarizeCoupangProductProfitRows()`를 다시 읽는다.
6. 현재 검색·출력 불일치가 그대로인지 확인한다.
7. 기준 테스트를 먼저 통과시킨다.
8. 스키마/API/테스트를 먼저 만든 뒤 UI를 연결한다.
9. 카테고리와 검색을 모두 제품 ID 집합으로 처리한다.
10. 마지막에는 반드시 겹친 카테고리, 일부 옵션 검색, 미분류, 빈 결과, 불완전 순이익을 직접 검증한다.

이 문서의 핵심은 “카테고리 UI 추가”가 아니라 **사용자가 저장한 제품 범위를 단일 진실 원천으로 삼아 모든 숫자와 모든 출력이 같은 범위를 보게 만드는 것**이다.
