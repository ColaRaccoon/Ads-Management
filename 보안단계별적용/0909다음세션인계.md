# 2026-09-09 클라우드 실배포 다음 세션 인계

작성 기준: 2026-09-09 10:24:01 KST read-only 재확인  
인계 작성 직전 branch: `codex/security-rbac`  
인계 작성 직전 execution-bundle 기준 HEAD: `cc90a4e39e948f04c77ee84cfebe4fbc8ed8815a`  
현재 전체 판정: **R2A G-DB-00 phase 1 PASS / phase 2 LOCAL PREPARED·PROVIDER NOT RUN / operationalReady=false**

이 문서는 다음 세션의 탐색 시간을 줄이기 위한 현재 상태 snapshot이다. 작업 순서, 외부 변경
승인 gate, 병렬 wave, 파일 소유권, 검증, rollback과 완료 조건의 single source of truth는 계속
`0901클라우드실배포잔여작업계획.md`다. 이 문서와 SSOT가 충돌하면 SSOT와 실제 provider
receipt·현재 repository 상태를 우선하고, 충돌을 숨기지 말고 기록한다.

## 1. 다음 세션이 가장 먼저 할 일

다음 문서를 **처음부터 끝까지** 순서대로 읽는다.

1. `AGENTS.md`
2. `00_전체요구사항과실행순서.md`
3. `0901클라우드실배포잔여작업계획.md`
4. 이 문서 `0909다음세션인계.md`
5. `0901클라우드실배포프리플라이트.md`
6. `0908staging별도DB역할설계.md`
7. `진행상태.md`의 최신 R1/R2A 절
8. `증거/0908-staging-db-roles/manifest.json`

그 뒤 어떤 파일도 바꾸거나 provider에 접속하기 전에 다음을 다시 확인한다.

- `git rev-parse HEAD`, branch, worktree 위치와 `git status --short`
- staged/unstaged diff 전체와 untracked 파일 목록·내용
- 사용자 변경과 이 인계 이후 새 변경의 존재 여부
- execution-bundle 기준 `cc90a4e39e948f04c77ee84cfebe4fbc8ed8815a` 이후 차이가
  인계 문서에만 국한되는지
- Docker Desktop/engine, 공식 CA, pgpass, DPAPI store, execution-state/temp 경로의 현재 상태
- 아래 G-REL-01 review/expiry 조건이 여전히 유효한지

사용자 변경은 보존한다. `reset`, `checkout`, `clean`, `stash`로 변경을 제거하지 않는다.
과거 LAN/Windows/local-native 계획이나 과거 새 세션 prompt를 production 기준으로 재사용하지
않는다. main agent만 commit하며 push/main merge/tag는 별도 승인 전 실행하지 않는다.

## 2. 시간 민감한 release 경계

- release source: `f8a94fcf43aa4705befcaafbcb91045e154ae54b`
- 승인 범위: exact 6-image와 공통 13 Critical/High, exact 두 SARIF hash
- 승인 당시 판정: `P1-REL-001=ACCEPTED_TIME_BOUND`
- 승인 당시 R1 판정: image/base/CVE 범위만 `CONDITIONAL_PASS`
- owner: `ColaRaccoon production owner`
- expiry: **2026-09-10 23:59:59 KST**
- 첫 owner review: `2026-09-08T01:12:03.4308677Z`
- 당시 문서가 정한 다음 정기 review 시한: **2026-09-09 10:12:03 KST**, 이후 최대 24시간
  간격 및 publish/deploy/maintenance 직전 추가 review

인계 시점 `2026-09-09 10:24:01 KST`에는 repository 문서에 새 owner review가 기록되지 않은 채
위 `10:12:03 KST` 시한이 이미 지났다. 따라서 이 인계에서는 기존 위험 수용을 **과거 승인 이력,
현재 review-miss로 무효화된 상태**로 취급한다. 다음 세션은 현재 KST와 새 기록 유무를 다시 확인하고,
SSOT의 재검토 및 필요한 별도 승인 전에는 `P1-REL-001`과 R1 image/base/CVE 조건부 PASS를 현재
release/deploy/maintenance 근거로 사용하지 않는다. 자동 연장하거나 원래 expiry만 보고 유효하다고
판정하지 않는다.

review 누락 외에도 expiry, source/image/CVE/fix/KEV/도달 가능성/hardening/actual 검증 변화는 위험
수용을 조기 무효화한다. phase 2 DB gate는 G-REL-01과 별도지만, SSOT 순서상 현재 release review
상태가 phase 2 진행을 허용하는지는 먼저 판정해야 한다. 이 위험 수용은 provider DB/Auth/Storage/
Koyeb/registry/push 승인이 아니다.

## 3. 완료와 NOT RUN 경계

| 범위 | 현재 상태 |
|---|---|
| R0/R1/R1A local 준비·검증 | 문서에 기록된 범위만 완료 |
| exact f8a94fc 6-image online advisory/Scout | 실행·집계 완료; 공통 13 C/H 위험 수용은 review 시한 누락으로 현재 무효, 재검토 필요 |
| staging extended read-only inventory | 지정 범위 PASS; 실제 Auth flow/effective bucket authorization 등은 NOT RUN |
| G-DB-POOLER-RO-01 | staging base `postgres` session pooler/TLS read-only PASS |
| G-DB-00 phase 1 | exact staging NOLOGIN role 3 + 별도 DB bootstrap PASS |
| G-DB-00 phase 2 local artifact/rehearsal | PASS_LOCAL; provider equivalence는 NOT_CLAIMED |
| 실제 phase 2 credential 준비 | **NOT RUN** |
| 실제 role SCRAM verifier+LOGIN mutation | **NOT RUN** |
| 실제 세 role provider login/routing | **NOT RUN** |
| G-DB-02 staging migration | **NOT RUN** |
| phase 3 post-migration exact grants | **NOT RUN** |
| provider backup/R2 upload/restore | **NOT RUN** |
| Auth/Storage/data/application lifecycle | **NOT RUN** |
| registry/Koyeb staging·production | **NOT RUN** |
| production DB/migration/grant/restore | **NOT RUN** |
| full production CMD/actual adapter route | **NOT RUN** |
| operationalReady | **false** |

실제 승인 target에서 실행하지 않은 항목을 local/mock/source 결과로 PASS 처리하지 않는다.

## 4. 고정 staging target과 제외 대상

- organization ID: `synohgzwspodxmfemoks`
- project/ref: `Meta Ads Performance Security Dev` / `ehnfrrmbkvlsbpvqcvkr`
- plan/region: Free / `ap-northeast-2`
- session pooler: `aws-0-ap-northeast-2.pooler.supabase.com:5432`
- external admin user: `postgres.ehnfrrmbkvlsbpvqcvkr`
- base admin DB: `postgres`
- application DB: `meta_ads_staging`
- application schema: `public`
- roles:
  - `meta_ads_stg_runtime`
  - `meta_ads_stg_migration`
  - `meta_ads_stg_backup`

제외 대상은 production ref `iygjmosbelbosfxidqxv`, transaction pooler port 6543, 현재 host에서
route가 없는 direct IPv6 fallback, `patima_app`, 네이버 마진 프로그램 DB와 그 데이터다.
다른 project/DB/schema/role에 범위를 확대하지 않는다.

## 5. G-DB-00 phase 1 provider PASS receipt

- provider 실행: 2026-09-08 14:42:26–14:42:29 KST
- 실행 횟수/retry/fallback: 1 / 0 / 0
- DB `meta_ads_staging` OID: `25404`
- runtime role OID: `25397`
- migration role OID: `25399`
- backup role OID: `25401`
- 세 role 상태: `NOLOGIN`, restricted, connection limit 2
- creator management edges: 3
- base `postgres` ACL MD5 전후: `94af03f1e723fcb05d1fd0a1590bbf99`
- 종료 후 target session count: 0
- receipt: `증거/0908-staging-db-roles/provider-phase1-receipt.json`
- receipt SHA-256: `d596ccd6bc43d479705d43cdd720221cdc71dec3c242f52ace1d70cdaba04059`

raw stdout는 보존하지 않았으므로 provider 실행값은 `MAIN_REPORTED` structured receipt다. 이 PASS는
NOLOGIN bootstrap과 admin custom-DB routing만 증명한다. LOGIN/SCRAM, migration, grants, backup,
Auth/Storage/data/app/Koyeb는 증명하지 않는다.

## 6. G-DB-00 phase 2 local 준비 상태

현재 30-file manifest:

- 경로: `증거/0908-staging-db-roles/manifest.json`
- SHA-256: `0ab107b4cb9a3fe3a5733b07f9f145f4212537e0bc66eee184558236730743d1`
- status: `PROVIDER_PHASE1_PASS_PHASE2_LOCAL_PREPARED`
- `operationalReady=false`

고정 artifact:

| artifact | bytes | SHA-256 |
|---|---:|---|
| `phase2-prepare-credentials.ps1` | 3,701 | `9f739f75dcbe379391aaacb57d30a1dbedb073409c77bb6f7d389f4484a887dd` |
| `phase2-provider-run.ps1` | 38,142 | `15eb19263d06ba3babd4786692abf7a7b8ed907d89934237336becc650f51002` |
| `phase2-secret-lib.ps1` | 9,672 | `48ddce9d009f8c2e4aa46f18060608ed818807ceba546ea401666a20950fec2c` |
| `phase2-provider-activation.sql` | 8,884 | `8b8527e25caff9fa9edab37420bd4808fbdf953fd75182a17e8e8c0c080671a7` |
| `phase2-role-verify.sql` | 7,142 | `846762928ddc6012202fe989336536fd07c34dd37cd126591e81fe3da8043efe` |
| `phase2-final-admin-verify.sql` | 4,837 | `3c13be90502d9947cc13f7bf42eea1e796001a12cdb7ef1755b6b600ef26f184` |
| `phase2-container-entrypoint.sh` | 462 | `3116be65ed91b60e9e3db6f191440eedab020a671d1c438c965b32fe4d9e2c41` |
| `phase2-local-forced-failure.sql` | 612 | `2302e83e9ace0aceec35596f1f8b059d02f89795db62029b8c0439f066a3ce09` |
| `phase2-local-rehearsal.ps1` | 15,618 | `0a14b68d6c7466487e4bdf3b2380b85dcf7ad8a383e79edbad38f6e464c0851f` |
| `phase2-local-rehearsal-summary.json` | 1,503 | `9e956deb85e71b6f14bdfae94005e51b806b6d26ac80d766cdae254b53ff3320` |

최종 local rehearsal은 `2026-09-08T07:41:04.1475847Z`–`07:41:16.1750049Z`에 PASS했다.
exact saved activation/role/final SQL, provider-shaped non-superuser PG17, client-side SCRAM verifier,
forced pre-COMMIT rollback, positive SCRAM role login 3개, synthetic CA TLS `verify-full`, exact
OID/ACL/default ACL, named PREPARE→EXECUTE→DEALLOCATE, final session 0, matching Docker
container/network 0을 검증했다. provider TLS/pooler 결과는 `NOT_CLAIMED`, provider mutation은 0이다.

DPAPI atomic create/idempotent no-rotation/atomic marker test도 redirected APPDATA에서 PASS했고 test
store는 삭제했다. 최초 non-TTY `\password` 방식 실패는 숨기지 않았고 client-side SCRAM verifier
설계로 교체했다. 최종 A/B/C 독립 정적·모의 평가는 P0/P1/P2/P3 0, GO였다. 실제 provider 성공
평가가 아니다.

## 7. 실제 local secret/state 경계

인계 작성 직전 확인된 상태:

- 실제 credential store `%APPDATA%\MetaAdsSecurity\staging-db-roles`: 없음
- 실제 execution temp `%LOCALAPPDATA%\MetaAdsSecurity\phase2-temp`: 없음
- 실제 phase 2 credential 생성: 0
- 실제 provider phase 2 attempt: 0
- activation marker: 없음

다음 세션은 이를 그대로 가정하지 말고 값 노출 없이 exact path, reparse point, owner/ACL, unexpected
item, marker, stale temp, execution-state를 다시 확인한다. credential 값이나 hash를 출력·채팅·Git·SQL·
argv·environment·evidence에 저장하지 않는다. pgpass/DPAPI 파일 내용을 출력하지 않는다.

## 8. 현재 대기 중인 외부 변경 gate

사용자에게 이미 설명했지만 **아직 승인받지 않은** exact 문구는 다음이다.

```text
G-DB-00 phase 2 승인 — commit cc90a4e39e948f04c77ee84cfebe4fbc8ed8815a, manifest 0ab107b4cb9a3fe3a5733b07f9f145f4212537e0bc66eee184558236730743d1, prepare 9f739f75dcbe379391aaacb57d30a1dbedb073409c77bb6f7d389f4484a887dd, runner 15eb19263d06ba3babd4786692abf7a7b8ed907d89934237336becc650f51002, staging Execute 1회
```

여기서 `cc90a4e…`는 phase 2 execution artifact bundle을 고정한 기준 commit이다. 이 인계 문서 추가
commit이 생겨도 실행 bundle과 manifest가 바뀌지 않았음을 diff와 raw-byte hash로 별도 증명해야
한다. 인계 외 파일 또는 artifact가 달라졌다면 위 문구를 재사용하지 말고 새 exact gate를 구성한다.

사용자가 위 exact phrase를 새로 보내지 않은 상태에서는 실행하지 않는다. 과거 포괄적인 "승인할게",
phase 1 승인, G-REL-01 또는 이 인계 요청을 phase 2 승인으로 재사용하지 않는다. 승인 요청 직전에는
다시 다음을 한 번에 제시한다.

- 대상: 위 exact staging org/ref/session pooler/DB/role OID만
- 실행: actual DPAPI credential 3개 준비 후 `Mode Execute` provider 1회
- 영향: 세 role에 서로 다른 SCRAM verifier와 LOGIN 설정; 짧은 connection/auth/DDL metadata log
- 알려지지 않은 영향: provider-side verifier 저장·표시·마스킹과 Supavisor 전파 상한은 UNKNOWN
- 비용/시간: 예상 추가 비용 0, 약 1–3분
- 성공: committed activation, 세 positive role TLS/SCRAM session, exact principal/OID/속성/privilege/
  default ACL/named prepare, base ACL 불변, final target session 0, cleanup·marker PASS
- rollback/실패 분기: 아래 9절

승인 뒤에도 prepare와 Execute 직전에 HEAD/diff/manifest/artifact/receipt/CA/PG17 image/target/
pgpass/Docker/secret-state 불변식을 다시 확인한다. 하나라도 다르면 token을 사용하지 않는다.

## 9. phase 2 실행과 실패 분기

승인 후에만 실제 credential 준비를 수행하고, exact runner의 `Mode Execute`를 **최대 1회** 수행한다.
script 내부 approval token은 사용자 문구와 별개의 fail-closed 실행 guard이며 사용자 승인을 대체하지
않는다. wrong-password/cross-secret probe, transaction pooler, direct IPv6 fallback, automatic retry는
금지한다.

- mutation 전 실패: provider mutation 0을 증명하고 중단한다.
- pre-COMMIT exit 3: transaction rollback 예상 상태로 기록하고 자동 재실행하지 않는다.
- COMMIT 결과 불명확 또는 commit 후 positive verify 실패: activation을 다시 실행하지 않는다.
- unknown 상태: 별도 승인된 admin read-only catalog/Pooler Logs 분류부터 수행한다.
- remote credential이 이미 active일 개연성이 확인된 경우에만 새 별도 승인으로 `Mode VerifyOnly`를
  정확히 1회 고려한다. VerifyOnly는 activation/verifier 생성 0이어야 한다.
- 다른 경우 containment가 필요하면 새 별도 승인으로 exact `90-rollback-containment.sql`을 사용한다.
  이 스크립트는 `ALLOW_CONNECTIONS=false`와 세 role `NOLOGIN`만 수행하고 강제 session 종료나
  drop은 하지 않는다.
- destructive drop은 이번 gate 범위 밖이며 별도 exact 승인 없이는 실행하지 않는다.

stale temp, cleanup failure, marker/state mismatch, unexpected item, 두 번째 attempt는 fail closed한다.
matching owner label+cidfile+full container ID가 확인된 자기 container만 정리하고 이름 충돌 container는
보존한다. cleanup과 temp 삭제를 확인한 뒤에만 atomic `active_verified` marker를 publish한다.

## 10. phase 2 성공 뒤 수행할 일

성공을 terminal 출력만으로 끝내지 않는다.

1. secret을 제외한 provider receipt/result와 attempt/timestamp/target/hash/cleanup state를 기록한다.
2. manifest, `진행상태.md`, preflight, R2A 설계를 실제 결과에 맞게 갱신한다.
3. 실제 provider 범위와 MAIN_REPORTED 경계를 분리한다.
4. SSOT가 지정한 병렬 wave와 파일 소유권으로 독립 평가를 수행한다.
5. main agent만 변경을 검토·commit한다.
6. worktree/status/diff와 release source/`apps/**`/`deploy/**`/lockfile 불변을 재확인한다.
7. `operationalReady=false`를 유지한다.

phase 2 PASS가 다음 외부 변경을 승인하지 않는다. 그 다음 후보는 별도 **G-DB-02 staging
migration**이다. 이후 phase 3 post-migration grants, Auth/Storage/data gate, R2B, R3, R4 Koyeb
staging, R5, R6, R7, R8을 SSOT 순서와 각 별도 승인 gate로 진행한다.

## 11. 이후 provider 관련 알려진 정보

Supabase production:

- project URL: `https://supabase.com/dashboard/project/iygjmosbelbosfxidqxv`
- project name: `ColaRaccoon's Project`
- ref: `iygjmosbelbosfxidqxv`
- region: AWS `ap-northeast-2`
- plan: Pro
- organization URL: `https://supabase.com/dashboard/organizations`
- production exact organization name/ID는 현재 기록에서 확정값을 추측하지 말고 inventory 문서와
  Dashboard에서 다시 확인한다.
- 기존 dev Auth 사용자 10명은 시험용이었다. 별도 production read-only inventory에서는 Auth user
  1명이 보고됐지만 실제 flow/이관 완료를 뜻하지 않는다.

Backup/Koyeb:

- backup target 선택: Cloudflare R2
- 한국 region 고정 불필요
- client-side key는 사용자가 보관
- Koyeb account는 아직 가입/초기 설정이 안 된 것으로 보고됨
- Koyeb organization 후보: `colaraccoon`
- Koyeb는 small/1GB 후보
- custom domain은 필요 없고 Koyeb 자동 `koyeb.app` hostname을 사용할 계획

이 정보는 Koyeb/R2 생성 승인이 아니다. R4와 해당 gate 전에는 계정·조직·app/service·registry·
domain을 생성하거나 비용을 발생시키지 않는다.

## 12. 절대 유지할 판정

- `operationalReady=false`
- provider phase 2는 승인·실행 전 `NOT RUN`
- 실제 target에서 하지 않은 것은 `PASS`가 아니다.
- local rehearsal은 provider equivalence가 아니다.
- 과거 G-REL-01은 exact release risk만 허용했으며, 현재는 review 시한 누락으로 재검토 전 무효다.
- push/main merge/tag는 별도 승인 전 0건이다.
- 외부 변경은 매 gate 실행 직전에 정확한 대상, 실행 내용, 영향, 비용, 성공 판정, rollback을
  다시 제시하고 별도 승인을 받는다.

복사 가능한 다음 세션 첫 prompt는 `0909다음세션시작프롬프트.txt`에 있다.

## 13. 2026-09-09 후속 세션 read-only 재검토 — 현재 실행 NO-GO

세션 시작은 **2026-09-09 10:29:08 KST**, 시작 HEAD는
`e39b498c515f52ce568cdbc959b07af1b9b9eef5`다. branch/worktree가 요청과 일치했고
staged/unstaged/untracked는 모두 0이었다. `cc90a4e39e948f04c77ee84cfebe4fbc8ed8815a` 이후
commit은 인계 MD와 시작 prompt TXT 두 파일 추가뿐이다. 전체 diff를 읽었다.

### 직접 재확인한 로컬 범위

- main과 B가 독립적으로 manifest 30/30 raw bytes/hash 및 manifest 자체의 기존 hash 일치를 확인했다.
  main의 JSON Schema 검사도 PASS다. phase 2 prepare/runner/SQL/lib/phase-1 receipt는 불변이다.
- `apps/**`, `deploy/**`, `package-lock.json`은 release source `f8a94fc` 이후 불변이다.
  lockfile은 기존 exact hash와 일치한다. 현재 full source test/build는 재실행하지 않았다.
- 공식 CA 파일의 1,367 bytes와 고정 hash가 일치한다. CA 재다운로드나 provider TLS 접속은 하지 않았다.
- B의 실제 local store/temp/state/marker/lock 존재 검사는 모두 ABSENT이며, 관련 ancestor의
  reparse point는 없다. credential 생성 0, 이 세션 provider attempt 0이다. 과거 phase 1 실행은
  MAIN_REPORTED receipt의 보존·내부 일관성만 독립 확인했고 현재 provider catalog는 조회하지 않았다.
- **Docker 환경 BLOCKED:** 제한된 sandbox에서 config 접근 실패 뒤 실제 사용자 권한의 read-only
  검사로 `desktop-linux`와 `npipe:////./pipe/dockerDesktopLinuxEngine`를 확인했다. 그러나 engine pipe가
  없고 Docker Desktop/backend process도 관찰되지 않았다. PG17 및 6-image 현재 inspect는 NOT RUN이다.
  process Docker host/context/config/TLS override 변수는 모두 없었다. Docker를 시작하지 않았다.
- **P1-R2A-LOCAL-PGPASS-ACL OPEN:** 실제 `seong` 권한의 read-only 재확인에서도
  `%APPDATA%\postgresql\pgpass.conf`는 owner seong이지만 inheritance protected=false이고,
  seong/SYSTEM 외 Administrators 및 CodexSandboxUsers의 inherited Allow가 있다. exact runner의
  protected current-user+SYSTEM-only precondition에 불일치한다. sandbox identity 차이만의 문제가 아니다.
  내용/shape는 읽지 않았고 secret 값·길이·hash를 출력하지 않았으며 ACL도 수정하지 않았다.
  실제 비밀 유출을 확인한 것은 아니다. ACL을 정확히 복구·독립 재검증하기 전 credential prepare와
  provider token 사용은 금지한다. ACL 변경 범위는 이 exact 파일과 필요한 parent 계약만 따로 검토하며
  광역 ACL 변경이나 계정·그룹 제거로 우회하지 않는다.

### release 재검토와 phase 2 순서 판정

main+A/B/C의 SSOT Wave 1은 모두 read-only였고 각 담당 범위만 조사했다. A는 release/공식 자료,
B는 DB bundle와 local secret-state 경계, C는 업무 NOT RUN 및 gate 순서를 맡았다. 파일 편집은 main만
이 인계 문서에 수행한다. 이 wave는 새 DB 승인 후 필요한 pre-execution wave를 대체하지 않는다.

새 owner review 기록은 없다. **G-REL-01=VOID_REVIEW_MISS, P1-REL-001 OPEN, R1 BLOCKED**다.
마스터 §8의 단계 건너뛰기 금지, SSOT §6 R1 성공 조건과 §9의 미수용 C/H 중단 조건에 따라,
별도 DB gate라는 이유로 R1을 우회할 수 없다. **G-DB-00 phase 2 provider 실행도 BLOCKED**다.
기존 riskAcceptance JSON과 진행상태/preflight의 승인 당시 CONDITIONAL_PASS는 역사적 기록이다.

A는 2026-09-09 10:33:20 KST까지 공개 공식 자료를 다시 확인했다.

- exact 13개 CVE의 Debian tracker에서 installed Bookworm package가 vulnerable이며 Bookworm fixed row가
  없었다. [Perl](https://security-tracker.debian.org/tracker/CVE-2026-13221),
  [OpenSSL](https://security-tracker.debian.org/tracker/CVE-2026-75803),
  [util-linux](https://security-tracker.debian.org/tracker/CVE-2026-78410),
  [zlib](https://security-tracker.debian.org/tracker/CVE-2026-85091).
  일부 설명/버전표 모순을 finding 제거나 suppression 근거로 사용하지 않는다.
- [Docker Hub 공식 metadata](https://hub.docker.com/v2/namespaces/library/repositories/node/tags/22-bookworm-slim)의
  index `83f487e0a63425e5b4d146fb5e5be574bcbe1b7b843d3ebafdd95eaf7767a7e5`, amd64
  `4d676821dff059fd00d277ee4261ef34ea712317fed0737c03941481b5760c96`는 기존 base와 같다.
- [CISA 공식 mirror](https://raw.githubusercontent.com/cisagov/kev-data/develop/known_exploited_vulnerabilities.json)의
  catalog `2026.09.08`, released `2026-09-08T18:00:21.1079Z`, 1,699건에서 exact 13 CVE match는 0이었다.
  이전 1,695건 snapshot과 구별한다. 이는 조회 시점 미등재만 증명하며 불변 archive를 새로 저장하거나
  악용 불가능을 증명한 것이 아니다.

현재 image tuple/hardening 및 fresh npm advisory/Scout는 NOT RUN이다. 위 공개 자료 조회는 owner의
새 위험 수용이 아니며, 과거 SARIF를 오늘 scan으로 승격하지 않는다. release/deploy/maintenance 차단을
유지한다. 기존 provider phase 1 PASS, phase 2 LOCAL PREPARED·PROVIDER NOT RUN과
`operationalReady=false`는 유지한다.

### 다음 승인·실행 순서

1. **다음 로컬 준비 후보(아직 미승인): Docker Desktop 시작 + exact local image read-only 재검증.**
   대상은 이 Windows host의 Docker Desktop과 `desktop-linux` engine 하나다. 존재를 확인한
   `C:\Program Files\Docker\Docker\Docker Desktop.exe`를 `Start-Process -WindowStyle Hidden`으로 시작하고,
   context/endpoint/engine과 고정 PG17 및 아래 scan summary의 release source에 연결된
   여섯 immutable image의 allowlisted identity/size/revision/port만 읽는다. image pull/build/run,
   Scout/npm audit, registry login/publish, DB/provider 접속, 기존 container stop/delete/prune는 포함하지 않는다.
   시작 시 host CPU/memory를 사용하고 기존 Docker restart policy가 있는 container가 자동 시작될 수 있다.
   예상 추가 provider 비용 0, 약 1–3분이다. engine 또는 tuple 불일치·예상 밖 상태이면 중단하고
   provider/scan으로 진행하지 않는다. 기존 container를 강제 중지하는 rollback은 하지 않고 상태만 보고한다.
2. Docker가 가용해진 뒤 exact source `f8a94fcf43aa4705befcaafbcb91045e154ae54b`의 public/auth/
   migration/storage/backup/legacy **6-index는 `증거/0908-r1-f8a94fc-online-scan-summary.json`과 대조**하고, 도구·공간 guard를
   확인한 다음 **새 R1 online advisory + 6-image Scout metadata 전송 gate**를 별도로 제시한다.
   공식 npm registry audit 1회와 각 immutable image Scout 1회, 새 no-clobber evidence, lockfile 불변,
   사전 `1GiB + 2 × image.Size + 64MiB`/사후 1GiB 공간 guard를 포함한다. package/PURL/image/layer
   metadata는 외부 전송 뒤 회수 불가, 예상 추가 비용 0·3–8분, 유료 요구/오류/tuple drift 시 중단,
   automatic retry와 강제 archive cleanup은 금지한다. 이 문서만으로 전송을 승인하지 않는다.
3. A/D의 fresh 결과 검토 뒤 조건에 맞을 때만 **새 별도 G-REL-01**을 제시한다. owner, exact
   source/6-image/CVE/SARIF, expiry와 24시간 이내·실행 직전 review를 다시 결속한다. 과거 예외는 부활시키지 않는다.
4. pgpass ACL exact 복구와 비밀 비노출 shape 검증도 완료해야 한다. R1이 회복되고 모든 로컬
   blocker가 해소된 뒤에만 §8의 **별도 G-DB-00 phase 2** 승인안을 실행 직전에 다시 제시한다.
   이 문서 수정은 handoff-only이며 manifest/artifact는 바꾸지 않았다. 그 사이 handoff 외 변경이
   생기면 기존 exact phrase를 재사용하지 않고 새 gate를 고정한다.
5. 새 DB 승인 뒤 main+sub-agent pre-execution wave를 다시 수행한다. 모든 precondition 일치 시에만
   실제 credential 3개와 Execute 최대 1회로 진행하고, 이후 성공/unknown/VerifyOnly/containment는
   §9–10의 별도 gate 및 기록 경계를 그대로 따른다.

이 세션에서 provider 접속·mutation, credential 준비, scan metadata 전송, push/main merge/tag는 0건이다.

Wave 4 D는 위 인계 diff와 gate·runner ACL 계약을 독립 read-only 검토하여 **새 actionable
P0/P1/P2/P3 0, 문서·승인안 제시만 GO**로 판정했다. 기존 P1 두 건과 Docker 환경 BLOCKED는
열려 있으며 provider 실행은 NO-GO다. main의 추가 내용 표적 secret-marker 검사 0건 및
`git diff --check`는 PASS다. 문서만 변경했으므로 source/runtime/provider 검증을 새로 수행한
PASS로 기록하지 않는다.

## 14. 2026-09-09 Docker 사용자 시작 후 승인된 read-only 재검증

사용자는 Docker Desktop을 직접 시작했다고 보고하고 §13의 요청을 승인했다. **USER_REPORTED 시작 /
local image read-only 재검증 승인**으로 한정하며 scan 전송·ACL 변경·새 G-REL-01·G-DB-00 승인이 아니다.
재검증 시작 시각은 **2026-09-09 10:48:04 KST**, HEAD는
`39dd878a7bcb165da1d22d348db743f58cf9f843`, branch/worktree 일치, staged/unstaged/untracked 0이다.

- main은 context `desktop-linux`, literal `npipe:////./pipe/dockerDesktopLinuxEngine`, Docker
  client/server `29.3.1`, Linux amd64/cgroup v2를 확인했다. Docker engine 부재 blocker는 해소됐다.
- 고정 PG17 index `f3bd19c606e442c3d7bdfa8002e03fe260a1023351e0ea4598032022b68dd6e3`가 존재하고
  linux/amd64다. container 실행·실제 psql version/DB 연결은 수행하지 않았다.
- main/A 각각 exact 6 image의 index/size/platform/user/revision/port를 대조해 6/6 일치를 확인했다.
  main은 `image inspect --platform linux/amd64` descriptor도 기존 6개 manifest digest와 대조해 6/6 일치했다.
  모두 revision `f8a94fcf43aa4705befcaafbcb91045e154ae54b`, user `node`, public은 8000/tcp 하나,
  maintenance 5개는 exposed port가 없다. running container hardening/actual CMD PASS가 아니다.
- 최초 maintenance inspect는 ExposedPorts 속성 부재를 허용하지 않은 출력 template 오류였다.
  null-safe accessor로 read-only 검사를 완료했으며 image 불일치나 scan/provider 재시도가 아니다.
- Scout version `1.20.3`, npm `11.9.0`을 확인했다. 과거 npm `10.9.4`로 수행한 audit와 구별한다.
  local `npm config get registry`는 공식 `https://registry.npmjs.org/`였다. audit는 하지 않았다.
- main 측정 C: free는 `12,487,057,408 B`였다. manifest raw-byte 30/30 및 manifest 자체 hash는 불변이다.
- config/SPDX/provenance raw blob bytes는 이번 allowlisted inspect로 재검증하지 않았다. fresh online
  audit/Scout도 NOT RUN이다. pgpass ACL blocker와 G-REL-01 review-miss는 계속 OPEN이다.

### 다음 별도 승인안: R1 재검토용 online advisory + exact 6-image Scout 전송

**상태: 미승인 / 전송 0.** 다음 정확한 source와 `linux/amd64` local immutable index만 대상으로 한다.

source: `f8a94fcf43aa4705befcaafbcb91045e154ae54b`

| 순서/purpose | local repository | index SHA-256 |
|---|---|---|
| 1 public | meta-ads-cloud | aca7055edfaa6848a93f4caabe4669f12ceca4b906597cfc38ae89287651ae7d |
| 2 auth | meta-ads-maintenance-auth | bc54264137bfdfb4d2d4c1aeaa21960b4332ba9089ab040b0206059895ecdd97 |
| 3 migration | meta-ads-maintenance-migration | f8d53394a68c7257eb4fc5c6f4d226d6fb96cbd410664992f454cadc649c34fd |
| 4 storage | meta-ads-maintenance-storage | f66caaf6d6ce289c2d6a8d8c3fb649cef03f714eddd3a7584e86f4d38ee1d672 |
| 5 backup | meta-ads-maintenance-backup | b6054c05dc2b314dc4047e1ec1b0cbc05baece1475e51e3575f75b7a84c4dd8f |
| 6 legacy | meta-ads-maintenance-legacy | b4527854d1c2e655401aee38af745a32e2a470b76e8ebd63ec60cb76ddb3ce74 |

- **실행:** 전송 전에 HEAD/diff, lock SHA-256
  `42eb0902b3ab57c58659368fcc593edec61d54f216dac5584a42fc4307df20f3`, local tuple와
  config/SPDX/provenance 결속을 검증한다. 필요한 OCI evidence는 exact local image에서만 추출하며
  image pull/build/container run은 하지 않는다. raw blob 검증 실패나 누락을 과거 PASS로 대체하지 않고
  전송 전에 중단한다. npm effective registry와 override를 비밀 비노출로 검사하고 수신자 변경 시 중단한다.
  npm 11.9.0에서 `npm audit --omit=dev --json --registry=https://registry.npmjs.org`를 1회 실행한다.
  과거 경고가 난 `--audit-registry` 옵션은 사용하지 않는다. audit fix/install/lockfile 수정은 없다.
  valid report와 lock 불변을 확인한 뒤 Scout 1.20.3의
  `docker scout cves --platform linux/amd64 --format sarif --exit-code local://<repository>@sha256:<index>`를
  표 순서로 각 1회 수행한다. 자동 retry는 없다.
- **증거·공간:** 새 no-clobber 경로
  `C:\Users\seong\AppData\Local\Temp\meta-ads-scan-f8a94fc-20260909T0148Z`는 검사 당시 없었다.
  각 전송 전 `1GiB + 2 × image.Size + 64MiB`, 직후 1GiB free를 강제한다. local OCI 추출에도
  공간을 확인하고 부족하면 시작하지 않는다. 이전 evidence/archive는 보존하며 새 report/ledger는
  source/index/platform/config/SPDX와 UTC 시각·도구·exit·attempt·집계를 비밀 없이 결속한다.
- **외부 영향:** 공식 npm registry와 Docker Scout 분석 서비스에 dependency/package/PURL/version,
  image/layer hash와 내부 식별자를 포함할 수 있는 분석 metadata가 전송되며 회수할 수 없다.
  image publish와 별개다. env/DB·API credential/업무 row/object는 입력하지 않는다.
- **비용/시간:** 새 유료 resource/plan 없이 예상 추가 비용 0, 약 3–8분(로컬 OCI 추출 시간 별도).
  결제·구독·upgrade 요구 시 중단한다.
- **성공:** valid npm JSON의 집계를 직접 읽고 exit 0/1 및 error 유무를 분리한다. Scout 6개는 valid
  SARIF와 exit 0 또는 2, exact tuple 및 attempt 1회 증거가 있어야 한다. exit 2는 CVE 발견이며
  release PASS가 아니다. A 분석+D 독립 평가 뒤에만 수집 완료를 판정한다.
- **실패/rollback:** tool/network/report/tuple/space 오류면 남은 작업을 중단하고 NOT RUN으로 둔다.
  유효 SARIF·exact tuple·다른 오류 없음이 모두 성립할 때에만 same-index 임시 archive file-in-use
  cleanup 경고를 보존하고 다음 image 전 공간 guard를 다시 통과하는 제한 예외를 함께 제안한다.
  다른 cleanup 오류, 강제 unlock/delete/prune와 재scan은 금지한다. 전송 취소는 불가능하므로 기존
  image/evidence를 보존하고 추가 전송·승격을 중단하는 것이 rollback이다.
- 이 gate는 새 G-REL-01 위험 수용, pgpass ACL 변경, G-DB-00 phase 2, provider/Auth/Storage/Koyeb/R2,
  push/main merge/tag를 승인하지 않는다. fresh 결과 검토 뒤에만 별도 G-REL-01을 제시한다.

승인 요청 문구:
`R1 0909 재검토 scan 승인 — f8a94fc exact 6-image, npm audit 1회·Scout 각 1회, 위 metadata 영향과 제한적 cleanup 예외 포함`

provider phase 2 attempt는 0, actual credential store 준비는 NOT RUN이며 `operationalReady=false`다.

D의 §14 인계·scan gate 독립 평가는 새 actionable P0/P1/P2/P3 0, 승인안 제시만 GO다.
main/A actual Docker inspect와 D 문서 검토를 구분하며, scan/provider 실행은 미승인 NO-GO다.

## 15. 2026-09-09 scan 승인 후 새 dependency blocker — 로컬 수정 소유권

사용자는 §14의 scan 제안 직후 승인했고, 이후 이미 승인한 범위의 자잘한 확인을 반복하지 않고
계속 진행하라고 요청했다. main은 이를 해당 exact scan 실행 및 통상적 local 조사·수정의 승인으로
적용한다. 새 DB phase 2 exact 승인, destructive 변경, release 위험 수용을 대체하지 않는다.

새 audit 1회와 exact 6-image Scout 각 1회가 완료됐다. npm은 valid report exit 1이며
High 4/Moderate 1, Scout는 6개 모두 valid SARIF/exit 2다. 재시도와 cleanup 경고는 0이었다.
scan 집계·독립 평가는 진행 중이다. npm High 4는 독립 CVE 4개가 아니라 Nest의 multer 경유
meta finding을 포함한 취약 dependency 항목 수다.

source 수정 전에 SSOT의 파일 소유권을 다음처럼 고정한다.

- main: root/API package manifest, package-lock, 이 인계·진행상태/preflight와 비밀 없는 scan summary,
  `apps/api/src/file-security/upload-profiles.ts`의 공통 multipart 구조 제한,
  local install·전체 source 검증 및 commit 통합.
- A: dependency chain/공식 수정 버전의 read-only 검토. 이미 원본 OCI 36/36 독립 hash 검증 완료.
- B: `apps/api/src/file-security/dependency-security-regression.spec.ts` 한 파일의 로컬 회귀 test.
  다른 파일은 main 요청 없이 변경하지 않는다. commit·provider·secret 접근 금지.
- C: raw SARIF/audit/ledger의 read-only 집계와 과거 결과 비교.
- D: slot이 비면 source diff·test/scan evidence·gate의 독립 평가만 수행.

최소 수정 후보는 multer 2.3.0(API 직접 및 Nest 하위), csv-parse 7.0.2, sharp 0.35.4다.
Nest 7.5.5로의 audit 자동 downgrade는 하지 않는다. 고정 npm 10.9.4로 lock을 갱신하고 provider
없이 upload/CSV 및 전체 회귀를 검증한다. source/lock 변경은 기존 release와 구별하며 새 source/image
검증 전 PASS로 승격하지 않는다. handoff 외 변경이 생기므로 과거 cc90a4e phase 2 승인 문구도
재사용하지 않는다. R1/G-DB phase 2 BLOCKED와 `operationalReady=false`를 유지한다.

### 15.1 local credential 파일 ACL 복구와 검증 경계

사용자의 반복 확인 없는 local 준비 진행 요청에 따라 main이 기존
`%APPDATA%\postgresql\pgpass.conf`의 ACL만 복구했다. 실제 seong 권한에서 owner를 보존하고
상속을 차단하며 current user와 SYSTEM만 FullControl로 제한했다. 비밀 내용은 변경하지 않았다.
이전 ACL의 SDDL만 scan 임시 evidence의 `pgpass-acl-before.json`에 보존했다.

B가 실제 seong 권한으로 owner/상속/허용 주체와 reparse 부재를 독립 확인했다. 내부 shape 검사에서
pooler postgres 및 staging exact 항목을 확인했으며 credential 값·길이·hash는 기록하지 않았다.
기존 direct host wildcard DB 항목은 phase 2 runner가 사용하지 않으며 fallback으로 허용한 것이 아니다.
DPAPI store·execution-state·stale temp·marker·lock은 여전히 없고 provider attempt는 0이다.
이 검증은 phase 2 실행 직전 pre-execution wave를 대체하지 않는다.

### 15.2 source 검증 진행 기록

고정 npm 10.9.4로 의존성을 설치한 Node 22 격리 환경에서 Prisma validate/generate가 통과했다.
첫 lint의 새 회귀 시험 옵션 타입 오류를 수정했고 corrected lint가 통과했다. 첫 전체 시험에서
웹 279개가 통과했으나 API startup-order 시험 1개가 기본 5초 제한으로 실패했다
(API 1177 통과, 23 skip). 단독 실행도 같은 timeout이므로 전체 시험 PASS로 판정하지 않았다.
실패 로그를 보존하고 동일 시험 조건으로 Linux 컨테이너 내부 파일시스템에서 재검증 중이다.
D의 최신 source/adapter 독립 검토는 새 수정 필요 결함 0이며 최종 검증 판정은 대기다.
B의 추가 read-only bundle 검토도 manifest raw bytes/hash 30/30 및 자체 hash 불변을 확인했다.
새 application image 이후 manifest release SHA·migration/backup index, 실제 image에서 재측정한
migration release ID·chain hash와 관련 증거를 다시 결속해야 한다. phase2 runner가 직접 사용하는 것은
별도 pinned PG17 client·CA·phase-1 receipt·target/OID·execution artifacts이므로 application dependency
변경만을 이유로 runner/lib를 임의 수정하지 않는다. 새 committed bundle의 exact gate는 별도 준비한다.
### 2026-09-09 11:34 KST source 재검증 완료

동일 소스의 Linux 내부 파일시스템 검증에서 기본 timeout을 변경하지 않고 startup-order 3/3,
전체 API 1178 통과/23 skip, 웹 279 통과, cloud tests 52/52, launcher 14/14를 확인했다.
Prisma validate/generate, corrected lint, 전체 build, maintenance compile도 통과했다.
Windows 연결 경로의 최초 전체·단독 timeout은 보존한다. 내부 경로에서 해당 시험은 333ms로
통과했으며 파일 접근 지연 가설의 근거지만 원인을 확정한 것은 아니다.

정확한 변경 소스 5개와 시험 snapshot의 raw bytes/hash를 대조했다. 상세 로그 결속은
`증거/0909-r1-source-remediation-validation.json`이다. offline audit 0은 fresh online advisory나
이미지 scan을 대체하지 않는다. 23개 skip은 NOT RUN, 4GiB source 검증은 production 1GiB 측정이
아니다. 새 source의 6-image build/scan/resource 검증과 새 G-REL-01은 아직 미완료다.
phase 2 provider attempt 0, actual credential store 없음, operationalReady=false를 유지한다.
## 16. 2026-09-09 53e87ba 새 이미지 로컬 검증과 외부 scan pending gate

소스 수정 및 앞선 scan/source 검증 증거를 `53e87ba3ebf9c062463072ce5e4c8db77d8e9076`로
main만 commit했다. clean worktree에서 고정 Node base로 public 및 maintenance 5개를 각각 1회
build했다. 최초 Compose 사전 검사에서 비밀 아닌 NODE_IMAGE 변수가 없어 build 시작 전에
중단했고, exact NODE_IMAGE/RELEASE_GIT_SHA를 해당 process에만 지정한 뒤 검사를 통과했다.
이것은 provider 또는 image build retry가 아니다.

main/A 원본 OCI metadata hash 36/36 및 source/base/subject 결속이 일치했다. closure 6/6,
새 synthetic fixture 생성, 1GiB probe 6/6 통과이며 모든 해당 own container의 종료·삭제를 확인했다.
최대 RSS 722.73MiB, cgroup peak 692.81MiB, OOM delta 0이다. 기본 상태 한도 700MiB,
나머지 850MiB를 통과했고 동시 요청 승인 1/거부 1/503을 확인했다. 네트워크 없음,
읽기 전용 root, UID/GID 1000, cap 제거·NNP, PID 256, tmpfs 192MiB의 CMD override 검증이다.
actual production CMD·provider·실제 workload PASS로 확대하지 않는다.

증거: `증거/0909-r1-53e87ba-local-verification-summary.json`.
원본: `%LOCALAPPDATA%\Temp\meta-ads-release-53e87ba-20260909T0236Z`.
D의 검증 스크립트 P2 2건(정리 완료 전 성공 기록, 실패한 fixture proof 허용)을 실행 전에 수정하고
독립 재검토를 통과했다. 최종 저장 결과 독립 평가는 아래 후속 기록을 따른다.

### 새 payload의 외부 scan — 실행 전 별도 승인 필요

사용자의 계속 진행 요청에 따라 같은 종류의 새 source scan을 제안했으나 자동 승인 검토가
명령 시작 전 거부했다. 이유는 새 private source/image의 dependency·image 분석 metadata를
외부 destination에 전송할 명시적 승인이 부족하다는 것이다. 우회·간접 실행은 하지 않았다.
**새 source npm audit attempt 0, 새 6-image Scout attempt 각각 0 / NOT RUN**이다.
과거 f8a94fc 결과나 offline audit 0으로 이 공백을 대체하지 않는다.

아래 exact source와 local linux/amd64 index만 승인 대상이다.
source: `53e87ba3ebf9c062463072ce5e4c8db77d8e9076`

| 목적 | local repository | exact index SHA-256 |
|---|---|---|
| public | meta-ads-cloud | dc5d3adcb545f525ac6c912bc326872e651bc8f3448c010084911543d131133e |
| auth | meta-ads-maintenance-auth | 0d5009f713cdb41cbcf3f37fbb58807ab903bee9b8ad8cf380d97c71046add98 |
| migration | meta-ads-maintenance-migration | bb886aecaadaa75d9995ce9fdadd33552528a34d58b9885e3cc34cdf4e12a2a4 |
| storage | meta-ads-maintenance-storage | 8ed1547bfb4c4d280a2d7d6ca4b0e8ff1e66e1bf1d7df247a1d47c064cf9fbc8 |
| backup | meta-ads-maintenance-backup | 64ee17e94ff83587995161ae59beb8878e995ec988f49a98c8eddf163d2ba863 |
| legacy | meta-ads-maintenance-legacy | c9ad9f17601cdd720107a430fcd3ea9154d076098f95852cc8f08b4ef3c83f9c |

- 실행 전: 현재 HEAD/branch/worktree·전체 diff·clean 상태, 53e87ba 이후 문서/evidence-only 변경,
  exact lockfile·6-image index/platform/config/SPDX/provenance 36 raw bytes, 실제 Docker context/endpoint,
  npm 10.9.4 및 Scout 1.20.3과 공식 registry를 재확인한다. source/image 변경 시 새 승인안으로 교체한다.
- 실행: npm `audit --omit=dev --json --registry=https://registry.npmjs.org` 1회 후 valid report일 때
  `docker scout cves --platform linux/amd64 --format sarif --exit-code local://<repository>@sha256:<index>`를
  표 순서대로 각각 1회. audit fix/install, image publish, provider 변경, 자동 retry는 없다.
- 수신자·영향: 공식 npm registry `https://registry.npmjs.org`와 Docker Scout 분석 서비스에
  dependency/package/PURL/version, image/layer digest 및 내부 식별자가 포함될 수 있는 분석 metadata를
  전송한다. private source/image에서 유래한 정보이며 전송 회수는 불가능하다. env·credential·실제
  DB row/object는 입력하지 않는다. Docker Scout가 요구하는 새 로그인·결제·구독 변경은 포함하지 않는다.
- 비용/시간: 새 유료 resource나 plan 없이 예상 추가 비용 0, 약 3–8분. 유료 요구 또는 수신자·대상
  변경 시 중단한다. 각 scan 전 `1GiB + 2 × image.Size + 64MiB`, 직후 1GiB free를 확인한다.
- 성공: npm valid JSON/error 없음 및 exit 0/1을 집계와 분리, Scout valid SARIF/exit 0 또는 2,
  exact tuple·횟수·도구·시간·raw report hash를 기록하고 독립 평가한다. CVE 발견 exit 2는 release PASS가 아니다.
- 실패/rollback: 네트워크·도구·report·space·tuple 불일치 또는 cleanup 경고 발생 시 이후 전송 중단,
  원본 증거 보존, 자동 retry/강제 unlock/delete/prune 금지. 전송 자체의 rollback은 불가능하다.
- 승인 범위: 위 새 source와 표의 6-index 외부 분석만이다. G-REL-01 위험 수용, G-DB-00 phase 2,
  migration·Auth·Storage·Koyeb·production·push/main merge/tag를 승인하지 않는다.

요청 문구:
`R1 53e87ba 새 이미지 검사 승인 — npmjs.org audit 1회, Docker Scout exact 6-image 각 1회 분석 metadata 전송`

원래 DB bundle 30/30 raw bytes 및 manifest 자체는 현재까지 불변이다. 새 release로의 DB manifest
재결속은 pending이다. 이전 cc90a4e exact phase2 승인 문구는 source 변경 때문에 사용할 수 없다.
현재 R1 BLOCKED, 새 G-REL-01 미승인, phase2 PROVIDER NOT RUN/attempt 0, actual credential store 없음,
operationalReady=false다. 새 scan 승인과 fresh 결과 평가 후에만 다음 release 위험 수용안을 준비한다.
D 최종 저장 증거 독립 평가: runtime result/stdout 각각 13/13 hash 및 내용, build ledger 6/6 일치,
새 수정 필요 결함 0. 실행/횟수/정리/승인 거부는 MAIN_REPORTED, D는 저장 증거 내용과 무결성을
직접 확인했다. 로컬 기록 확정과 새 exact scan 승인안 제시만 GO이며 release/provider 승격은 NO-GO다.
## 17. 2026-09-09 새 scan 승인 후 public 정리 경고 / 나머지 5개 중단

사용자는 §16의 새 payload·수신처·횟수 승인안에 “승인할게”라고 답했고 저장 공간 부족으로
불필요한 Docker image·종속성을 지양하라고 요청했다. 이번 continuation에서는 image build 0,
OCI archive 추가 export 0, image 삭제/prune 0이다. source `53e87ba`, execution HEAD `51de1f6`의
clean 상태와 문서/evidence-only 차이, B 독립 원본 OCI 36/36 재검증을 확인했다.

- npm 10.9.4 공식 registry audit 1회: `2026-09-09T03:04:43.467Z`–`03:04:51.021Z`, valid JSON,
  exit 0, 모든 severity 0, lock 불변.
- public exact index `dc5d3adcb545f525ac6c912bc326872e651bc8f3448c010084911543d131133e` Scout 1회:
  `03:05:52.641Z`–`03:06:28.292Z`, valid SARIF 2.1.0/exit 2, 56 results,
  **3 Critical/11 High/7 Medium/31 Low/4 Unspecified**. Multer·Sharp·csv-parse finding은 없다.
- stderr는 동일 public index 경로의 임시 image archive 삭제 실패를 보고했다. OS 메시지는 file-in-use이며
  다른 분석 실패 메시지는 없다. 실제 점유 process와 근본 원인은 UNKNOWN이다.
  원본 ledger의 `cleanupWarning=true`, `suspiciousError=true`, `stopRequired=true`를 보존한다.
- §16의 cleanup 경고 중단 조건대로 auth/migration/storage/backup/legacy는 **각 attempt 0 / NOT RUN**이다.
  public 재검사·자동 retry·강제 unlock/delete·우회 실행은 없다.
- D가 audit/public stdout·stderr bytes/hash, 56 rules/results와 집계를 독립 확인했다.
  분석 결과 유효성과 정리 실패는 별도다. 실행·횟수·정리 상태는 MAIN_REPORTED다.

증거: `증거/0909-r1-53e87ba-partial-online-scan.json`.
public SARIF hash `85ea074fbab43c58ca7b4c9015df8935b187540671342ce6a43d3f3ec07da98d`,
audit hash `f95ccb3b9aea1bf99d267a032daef24004cbce4b964796d69bc6a8ca147caf27`.

A는 12:07:12 KST에 official Node base 동일, OS 14개 Bookworm fixed row 없음,
CISA 2026.09.08/1699건 exact14 match0을 재확인했다. KEV 미등재는 악용·도달 불가 증명이 아니다.
[Node](https://hub.docker.com/v2/namespaces/library/repositories/node/tags/22-bookworm-slim),
[Perl](https://security-tracker.debian.org/tracker/source-package/perl),
[OpenSSL](https://security-tracker.debian.org/tracker/source-package/openssl),
[util-linux](https://security-tracker.debian.org/tracker/source-package/util-linux),
[zlib](https://security-tracker.debian.org/tracker/source-package/zlib),
[CISA](https://raw.githubusercontent.com/cisagov/kev-data/develop/known_exploited_vulnerabilities.json).

### 공간 보존 방침

조사 시 C 여유 약7.06GB(이후 관찰 약5.76GB), public Scout stale archive는 290,256,896B였다.
Docker image 표시 총21.61GB와 cache13.85GB는 공유 layer가 있어 합산하지 않는다.
cache reclaimable 약6.00GB나 image reclaimable17.21GB는 삭제 가능 승인이나 실제 host 회수량이 아니다.
기존 source 검증용 stopped container들의 writable layer 합계는 약479KB라 직접 회수 효과도 작다.

새53e87ba6개·이전f8a94fc6개·고정PG17·필요한Node base는 현재 검증/복구 경계를 보존한다.
64fa25f/eb4d4675 과거 image와 미참조 fixture volume 등은 추가 선별 후보일 뿐 삭제하지 않았다.
목적별 maintenance image는 최소 권한/도구 분리용이며 공통 layer를 공유한다. 단순 용량 절약을 위해
목적 분리를 없애거나 검증되지 않은 bundle로 합치지 않는다. 새 build/OCI export 없이 기존 image를 재사용한다.

공식 Scout의 `sbom://` 입력도 조사했지만 BuildKit Syft 목록 기반 분석과 image 직접 분석의
탐지 범위는 같다고 보장되지 않아 현재 승인·완료 기준을 자동 대체하지 않는다.
[Scout](https://docs.docker.com/reference/cli/docker/scout/cves/),
[BuildKit SBOM](https://docs.docker.com/build/metadata/attestations/sbom/).

### 다음 별도 승인안: 남은 exact 5-image scan의 제한적 정리 경고 예외

상태: **미승인 / 남은 5개 전송 0**. source와 index는 §16 표의 auth, migration, storage, backup,
legacy 5개 그대로이며 순서도 같다. 수신자는 Docker Scout 분석 서비스, 전송 자료는 해당 private image의
dependency/package/PURL/version·image/layer digest 및 내부 식별자가 포함될 수 있는 분석 metadata다.
전송 회수는 불가능하다. npm 및 public 재검사, build/export/publish/provider 접속은 포함하지 않는다.

실행 전에 HEAD/diff/clean 및 source 이후 문서·evidence-only, exact tuple/raw36, lock, 실제 context,
도구 버전과 기존 public report/hash를 다시 확인한다. Scout 1.20.3의 §16 local:// 명령을 남은 목적별
각 1회만 실행한다. 각 scan 전 `1GiB + 2 × image.Size + 64MiB`, 직후 1GiB free를 강제한다.
기존 남은 5개 OCI 보관 파일 합계 약778MB는 임시 공간 참고값이며 새 임시 파일 크기 보장은 아니다.
새 유료 resource 없이 예상 추가 비용0, 약3–8분이다. 공간 부족·새 유료 요구면 중단한다.

기존 public의 정리 실패와 향후 동일 종류의 경고에 대한 예외는 아래 전부 만족할 때만 허용한다.

1. 해당 exact index·platform, valid SARIF/exit 2, error field 없음, raw report/stderr hash와 lock 불변.
2. 경고가 해당 index의 `docker-scout/sha256/<index>/<UUID>` 동일 경로를 반복한 file-in-use 임시 archive
   삭제 실패 하나뿐이고 다른 tool/network/analysis/error/fatal/cleanup 오류는 없음.
3. scan 후 1GiB free와 다음 scan 전 공간 guard가 통과함.
4. 원본 ledger의 stop/error/cleanup 값을 변경하지 않고 별도 continuation-review에 제한 예외와 판정을 기록함.

이 경우만 다음 아직 실행하지 않은 image로 진행한다. invalid report, index/bytes/space/lock 불일치,
다른 오류는 이후 scan을 중단한다. 자동 retry, public 재검사, 강제 unlock/delete/prune는 금지다.
전송 rollback은 불가능하므로 추가 전송을 멈추고 기존 image/evidence를 보존한다.
정리 실패는 성공으로 숨기지 않으며 정리 자체는 이 승인의 대상이 아니다.

성공 판정은 남은5 valid report·각1회·exact tuple·독립 집계 검증 완료이며, CVE 발견은 release PASS가 아니다.
새 G-REL-01 위험 수용·G-DB-00 phase2·다른 provider/Git gate는 계속 별도다.

요청 문구:
`R1 53e87ba 남은 5-image 검사 승인 — 각 1회, 동일 index 유효 SARIF의 file-in-use 정리 경고만 제한 예외`

현재 R1 BLOCKED, 새 위험 수용 미승인, DB phase2 provider attempt0/actual credential store 없음,
operationalReady=false다. 원래 DB manifest 재결속은 새 release 검토 이후 pending이다.

후속 runner 준비본은 임시 evidence의 `run-remaining-maintenance-scan.cjs`이며 SHA-256 `27d623414e4aa5be6a7d0fb75cf815bdf7834439af32b583b21f6e694905cfaf`다.
실행 없이 helper만 검증했다. 최초 SARIF 구조/알 수 없는 정리 오류 P2 및 오류 필드/플랫폼 결속 P2를
보완했고 B의 기존 14개+추가 15개 메모리 입력 검증이 통과했다. 실제 raw report/ledger는 변경하지 않았다.
준비본의 execution HEAD는 현재 검증 기준 51de1f6이며, 후속 문서 commit 뒤 실행 전 문서/evidence-only
차이 확인과 현재 HEAD 재결속이 필요하다. source/index/lock/정리 예외 정책 변경은 허용하지 않는다.

D 최종 helper 독립 15case도 원본만 허용하고 변조/추가오류/invalid/공간부족을 모두 거부했다.
준비 도구의 발견사항은 모두 해소, 새 결함0이다. 문서·부분기록 확정과 제한 예외 승인안 제시만 GO이며
새 승인 전 후속5 scan은 NO-GO다.

## 18. 2026-09-09 잔여 scan 완료 / 새 53e87ba G-REL-01 승인안

사용자는 §17의 남은5 각1회·정리 경고 제한 예외에 명시적으로 승인했다. execution HEAD
`159a20051ce694b46d7442180552f57ce4b93cf1`, source53e87ba 이후 문서·evidence-only/clean,
runner 준비본 hash와 HEAD-only 재결속, B raw OCI36/36 및 actual Docker endpoint/platform6/6을 확인했다.
남은5를 `2026-09-09T03:30:05.507Z`–`03:31:47.611Z`에 각1회 실행했다.

| 목적 | C | H | M | L | U | 결과/정리 |
|---|---:|---:|---:|---:|---:|---|
| public | 3 | 11 | 7 | 31 | 4 | 앞선1회 valid SARIF, 정리 경고 보존 |
| auth | 3 | 11 | 7 | 31 | 4 | valid SARIF, 정리 경고 없음 |
| migration | 3 | 11 | 7 | 31 | 4 | valid SARIF, 승인된 same-index file-in-use 예외 |
| storage | 3 | 11 | 7 | 31 | 4 | valid SARIF, 정리 경고 없음 |
| backup | 3 | 11 | 7 | 40 | 4 | valid SARIF, 승인된 same-index file-in-use 예외 |
| legacy | 3 | 11 | 7 | 31 | 4 | valid SARIF, 정리 경고 없음 |

6개 모두 exact 동일한 OS C/H14이며 Multer/Sharp/CSV 취약점 항목은 없다. f8a94fc 당일 결과보다
public은6개, 다른 image는5개 finding이 제거됐고 추가 finding은0이다. npm online audit는 앞선1회
exit0/취약점0 결과를 유지하며 재실행하지 않았다. 이번 승인은 모두 소진됐고 추가 scan retry를 허용하지 않는다.

D가 모든 SARIF/stderr raw bytes/hash·엄격한 구조·count/C/H집합·정리 예외 일치를 독립 확인했다.
원본 `stopRequired=true` 및 `suspiciousError=true`는 public/migration/backup에서 보존했다.
정리 실패를 성공으로 바꾸지 않았고 유지보수5의 별도 continuation-review를 기록했다.
실행/시간/횟수/공간은 MAIN_REPORTED, D는 저장 증거 무결성과 내용을 직접 확인했다.

새 source의 R1A baked maintenance ENTRYPOINT 경계를 03:37:12Z–03:37:37Z에 추가 확인했다.
5목적×(noenv/비밀 아닌 다른 목적 표시값/다른 명령/closure) **20/20 PASS**이며 실제 credential이나
provider를 사용하지 않았다. networknone/read-only/UID1000/capdrop/NNP/PID256/1GiB와
`/tmp:rw,noexec,nosuid,nodev,size=192m,mode=1777`을 확인했고 생성cidfile/fullCID/owner/image를
대조한 own container20개를 정리했다. 과거 image의20PASS를 재사용한 것이 아니다.
준비 도구의 label/CID/no-clobber P2 세건은 실행 전에 B가 수정하고 A가 재평가했다.

완료 증거:
- `증거/0909-r1-53e87ba-completed-online-scan.json`, SHA-256
  `1f9d19c396348200dc4a55b7673c4075b7cd399303c6a93e2bbcf3c65c962be6`.
- `증거/0909-r1-53e87ba-local-verification-summary.json`, SHA-256
  `7c610f675a210fe1d2859ae53e904d7f7b67ee9bc0f53e8dcc94ff219b0335ee`.
- `증거/0909-r1a-53e87ba-maintenance-boundary.json` — 새 실제 로컬20case 결과.

용량: 새 image build/추가 OCI export0, 기존 image재사용. 마지막 C 여유 약4.74GB.
경고의 임시파일3개는 public290,256,896B/migration257,738,752B/backup143,713,280B가 남아 있음을
읽기 전용으로 확인했다. 합계691,708,928B이며 강제삭제·prune는 하지 않았다. 이것은 새 Docker image
3개가 생겼다는 뜻이 아니다. 추가 clone/export·검증 없는 의존성 결합은 하지 않는다.

### 새 G-REL-01 — exact 53e87ba 6-image 공통 14 C/H 한시 위험 수용

**상태: 미승인 / P1-REL-001 OPEN / R1 BLOCKED / operationalReady=false.**
기존 f8a94fc/13 C/H 승인은 review-miss로 무효이며 이 새14개/새source에 재사용하지 않는다.
scan·정리 예외 승인은 위험 수용 승인이 아니다.

source: `53e87ba3ebf9c062463072ce5e4c8db77d8e9076`.
platform/config/SPDX/provenance 및 source/base는 위 local summary raw hash와 완료 scan summary에 결속한다.

| exact local repository | index SHA-256 |
|---|---|
| meta-ads-cloud | dc5d3adcb545f525ac6c912bc326872e651bc8f3448c010084911543d131133e |
| meta-ads-maintenance-auth | 0d5009f713cdb41cbcf3f37fbb58807ab903bee9b8ad8cf380d97c71046add98 |
| meta-ads-maintenance-migration | bb886aecaadaa75d9995ce9fdadd33552528a34d58b9885e3cc34cdf4e12a2a4 |
| meta-ads-maintenance-storage | 8ed1547bfb4c4d280a2d7d6ca4b0e8ff1e66e1bf1d7df247a1d47c064cf9fbc8 |
| meta-ads-maintenance-backup | 64ee17e94ff83587995161ae59beb8878e995ec988f49a98c8eddf163d2ba863 |
| meta-ads-maintenance-legacy | c9ad9f17601cdd720107a430fcd3ea9154d076098f95852cc8f08b4ef3c83f9c |

SARIF SHA-256은 public/auth/migration/storage/legacy 공통
`85ea074fbab43c58ca7b4c9015df8935b187540671342ce6a43d3f3ec07da98d`, backup은
`c5e97bc61010e8dae5a5cb098bb917ba0bc4f38fa7dc63a80eb9390a61d54e7e`다.

| source package/설치 버전 | Critical | High |
|---|---|---|
| Perl 5.36.0-7+deb12u3 | CVE-2026-12087, CVE-2026-13221 | CVE-2026-48959, CVE-2026-48962, CVE-2026-57432 |
| OpenSSL 3.0.20-1~deb12u2 | CVE-2026-75803 | CVE-2026-54874, CVE-2026-63072, CVE-2026-63076 |
| util-linux 2.38.1-5+deb12u3 | 0 | CVE-2026-76642, CVE-2026-78408, CVE-2026-78409, CVE-2026-78410 |
| zlib 1:1.2.13.dfsg-1 | 0 | CVE-2026-85091 |

공식 Node/Bookworm fix 및 CISA 재확인 근거는 §17의 12:07:12 KST 기록과 직접 공식 링크를 따른다.
고정 Node base83f487e…와 amd64 4d676821…가 동일하고 Bookworm fixed row는 관찰되지 않았다.
KEV exact14 match0은 악용·도달 불가 증명이 아니다.

- **실행·영향:** 위 exact source/6-index/14-CVE/SARIF 범위를 후속 release 준비에서만 한시 예외로
  문서화한다. 취약 코드가 실제로 도달 가능하거나 완화가 깨지면 목적별 process/data/credential의
  노출·변조 및 서비스 중단 위험이 남는다. Medium/Low/미분류 결과도 기록하며 해결됐다고 처리하지 않는다.
- **완화와 한계:** 새 실제 local image의 non-root·closure·public8000-only/maintenance무포트 선언,
  합성 read-only/capdrop/NNP/PID/1GiB, resource6case 및 maintenance20case만 근거다.
  기본 상태RSS89.74MiB, 최대RSS722.73MiB/cgroup692.81MiB/OOM0을 확인했다.
  실제 full production CMD·provider DB/Auth/Storage/R2 egress·최소 credential·실제 입력 경로는 NOT RUN이다.
  networknone 합성 검증을 실제 통신 환경으로 확대하지 않는다. 포트 미선언도 방화벽 증명이 아니다.
  native/transitive 코드의 완전한 비도달성은 미증명이다. 목적별 credential 분리와 동일 UID G-ISO-01은 별도다.
- **owner/expiry 제안:** `ColaRaccoon production owner`, **2026-09-10 23:59:59 KST**.
  같은 이름/날짜의 과거 승인을 재사용하지 않는다. 이 exact 범위의 새 명시 승인 수신 시각을 첫 owner
  review로 기록하고, 이후 **24시간을 넘기지 않는 간격**과 publish/deploy/실제 maintenance 직전에
  owner가 추가 검토해야 한다. 자동 검토나 예약 실행은 만들지 않았으며 이행을 보장하지 않는다.
- **조기 무효화:** source/index/SARIF/C/H집합 변경, fixed Bookworm/base 제공, KEV 등재·실제 악용 또는
  도달 가능성 변화, hardening 불일치, actual 검증 실패, owner review 누락, expiry 중 하나라도 발생하면
  즉시 VOID하고 release/deploy/maintenance를 중단한다. 다른 source/digest/CVE로 자동 확대·연장하지 않는다.
- **비용·시간:** 문서 기록 자체 예상 추가 provider 비용0, 약1–3분. image build/scan/publish나 유료
  resource 생성은 포함하지 않는다.
- **성공:** 사용자가 위 exact범위·owner/expiry·영향·재검토/무효화 조건을 새로 승인하고 D가 결속을
  확인할 때만 P1-REL-001=ACCEPTED_TIME_BOUND, R1의 image/base/CVE만 CONDITIONAL_PASS로 기록한다.
  그 이전에는 R1 BLOCKED이고 DB phase2 진행을 허용하지 않는다. 승인 뒤에도 SSOT 순서와 별도 DB exact gate를 지킨다.
- **실패/rollback:** 미승인·만료·무효화 시 예외를 철회·보류하고 publish/deploy/maintenance를 차단한다.
  아직 provider 배포가 없어 되돌릴 서비스/DB 변경은 없다. 수정된 secure digest를 준비·재검증하기 전
  취약한 과거 f8a94fc/eb4d/64fa image를 안전한 공개 fallback으로 사용하지 않는다. 이후 배포 상태라면
  별도 운영 승인으로 traffic 차단 또는 검증된 secure digest 복구를 처리한다. 자동 destructive rollback은 없다.
- **제외:** provider/registry/Koyeb/DB/Auth/Storage/R2 변경, migration/dump/restore, DPAPI credential
  준비·G-DB-00 phase2, 다른 gate, push/main merge/tag는 포함하지 않는다. operationalReady=false를 유지한다.

요청 문구:
`새 G-REL-01 승인 — 위 53e87ba exact 6-image/공통 14 C/H/SARIF 범위, owner: ColaRaccoon production owner, expiry: 2026-09-10 23:59:59 KST, 24시간 이내·실행 직전 재검토 및 조기 무효화 조건 포함`

원래 DB30-file manifest와 phase2 artifact bytes는 불변이나 release 재결속은 pending이며
cc90a4e phase2 문구는 source 변경으로 재사용할 수 없다. 실제 credential store 없음/provider phase2 attempt0이다.
D의 최종20case·완료scan종합 독립 검토는 새 결함0, raw20/20bytes/hash/content와 예상code/hardening일치다.
실제실행·정리수행은 MAIN_REPORTED이며 로컬합성거부경계와구성검증만 증명한다.

D의 §18 최종위험수용승인안 검토도 source/6tuple/14CVE/두SARIF·요약hash 결속 및 owner/expiry/review/rollback범위일치,
새결함0이다. 승인안확정·제시만GO이며 위험수용·DB/provider실행승인은아니다.

## 19. 2026-09-09 13:57 KST 새 G-REL-01 승인 수신 / R2A 로컬 재결속

사용자가 §18 승인안에 “승인할게”라고 응답했다. main 확인 시각은 2026-09-09T04:57:20Z다.
D가 exact source53e87ba/6-image/공통14C/H/두SARIF·요약hash·owner/expiry 결속을 확인했고,
A의 13:58:19 KST 공식 Node/Bookworm/KEV 조회에서도 무효화 조건 변화를 발견하지 않았다.

새 P1-REL-001=ACCEPTED_TIME_BOUND, R1은 image/base/CVE 범위만 CONDITIONAL_PASS다.
첫 owner review=2026-09-09 13:57:20 KST, 다음 정기 review 시한=2026-09-10 13:57:20 KST,
expiry=2026-09-10 23:59:59 KST. 이후 최대24시간 간격 및 publish/deploy/maintenance 직전
추가 검토가 필요하며, 누락·기한·조건 변화 시 즉시 VOID한다. 자동 검토/예약은 만들지 않았다.
과거f8a94fc 예외를 되살린 것이 아니다. 승인 기록은 `증거/0909-g-rel-01-53e87ba-acceptance.json`이다.

R2A 로컬 재결속은 진행 가능하다. main은 manifest/설계/문서 및 release metadata만 소유하고,
B는 기존30-file/재결속범위, A는 공식release조건, D는 결속·gate의 독립 읽기 검토를 담당한다.
실제 DB phase2 exact승인·DPAPI credential준비·provider 실행은 포함하지 않는다.
operationalReady=false, phase2 provider attempt0/actualcredentialstore없음을 유지한다.

### 19.1 Docker 엔진 장애로 재결속 보류 — 2026-09-09 14:12 KST

새 G-REL-01 승인 기록은 완료했고 B의 독립 검토는 승인 경계 결함0, 기존 bundle30/30 raw bytes/hash 불변이다.
새 이미지 내부 release 재결속은 NOT RUN이다. 첫 로컬 image-read는 Docker pipe 부재로 CREATE_FAILED였고
컨테이너 생성·provider 접속 이전에 중단됐다. Docker Desktop 실행 후에도 엔진 조회가 15초 제한에서
응답하지 않았다. WSL 조회에서 docker-desktop Stopped를 확인했고 Docker 공식 start는 already running을
반환했다. 정상 restart 요청도 응답하지 않아 해당 대기 클라이언트만 중단했다. Docker 전용 WSL의
/bin/true 시작은 exit0이었으나 이후 엔진 API도 시간 초과였다. Docker/WSL 강제 종료·초기화·prune,
이미지 추가 다운로드/재빌드/삭제는 하지 않았다. 로컬 대기 명령 중단은 provider 실행 시도가 아니다.

C: 여유는 이번 확인에서 약32GB로 달라졌다. 원인은 확인하지 않았으며 이미지 잔존 여부를 추정하지 않는다.
엔진 정상화 후 exact migration/backup/PG17 이미지 잔존과 digest를 먼저 확인하고, 필요한 이미지가 없으면
보존된 exact OCI archive의 무결성을 검증한 뒤 로컬 복구 범위를 판단한다. 새 빌드나 scan을 자동 반복하지 않는다.

manifest/migration-release/phase2 runner·SQL·prepare·phase1 receipt는 변경하지 않았다. 기존 manifest는
과거 f8a94fc 결속 상태이므로 새 source53 실행 승인으로 사용하지 않는다. 새 release 재결속과 독립 평가,
새 commit/manifest/prepare/runner에 결속된 DB exact 승인안을 완성하기 전 actual credential 준비 및
phase2 Execute는 금지한다. provider phase2 attempt0, operationalReady=false를 유지한다.

D의 최종4파일 독립 읽기 검토는 새 수정 필요 결함0이며 기록 확정 가능이다. Docker 진단과 실제 상태는 MAIN_REPORTED다.

## 20. 2026-09-09 14:19 KST Docker 복구 / 정상 재시작 재발 미해결

사용자가 다른 세션의 이미지 정리 영향 확인과 Docker 복구를 요청했다. 해당 작업
`사용하지 않는 Docker 컨테이너 삭제`의 실제 명령 기록에서 기존 이미지16개와 캐시 정리,
최신53e87ba 이미지6개·볼륨4개 보존을 확인했다. 삭제목록에는 phase2에 필요한 pinned PG17
f3bd19c606e442c3d7bdfa8002e03fe260a1023351e0ea4598032022b68dd6e3도 포함됐다.
PG17은 현재 부재이며 복구하지 않았다. 다음 DB precondition 전에 exact digest 복구·검증이 필요하다.

직접 시작 실패는 Docker/run/dockerInference 소켓의 Windows error1920이다. 실제 로그 및
fsutil 조회로 확인했다. 이미지 정리가 이 오류를 유발했다는 인과관계나 디스크 손상은 입증하지 않았다.
내용이 0바이트 소켓뿐임을 확인한 런타임 폴더만 같은 부모 아래 보존 이름으로 변경하고,
오류 상태 Docker 프로세스를 종료한 뒤 Windows 셸로 시작하여 엔진29.3.1을 복구했다.
이미지·볼륨·설정·실제 credential store 삭제/초기화는 수행하지 않았다.

복구 후 기존 exact migration 이미지의 network-none/pull-never/nonroot/readonly Node smoke는
v22.23.2 PASS였으나, 공식 normal stop 성공 후 재시작은 동일 소켓 오류로 실패했다.
따라서 단순 시작 경로 변경으로 영구 해결됐다고 판단하지 않는다. 새로 남은 run 소켓2개와
별도 docker-secrets-engine의 engine.sock만 있는 디렉터리를 각각 보존 이름으로 변경해 다시 복구했다.
두 번째 Node smoke와 engine info PASS, 종료 후 컨테이너0개, exact 최신 이미지6개와 볼륨4개 확인.
현재 Docker 엔진은 실행 중이다. Windows 재부팅은 NOT RUN이며 재부팅의 영구 해결 효과도 미검증이다.
재시작 재발 문제는 남아 있으므로 엔진 복구와 영구 수정 완료를 구분한다.

이번 신규 image pull/build/delete 및 volume delete는 모두0. C: 여유29.51GiB 관찰.
안전한 요약은 `증거/0909-docker-recovery.json`이다. 실행 결과는 MAIN_REPORTED 로컬 검증 범위이며
새 DB 승인·credential 준비·provider phase2 실행이 아니다. phase2attempt0/operationalReady=false 유지.
앞선 §19.1의 엔진 무응답 현재 상태는 본 절로 대체하며 release 재결속은 여전히 pending이다.

## 21. 2026-09-09 14:43 KST source53 R2A 재결속 완료 / 새 phase 2 exact gate

현재 execution-bundle 기준 commit은 `c0fafda3522b352e699a8cfa80d3b43074723c97`, manifest SHA-256은
`312428954f1e6b4a355b199f37ab30e9a9d384ecfc306cd9444bf519a8832216`이다. source
`53e87ba3ebf9c062463072ce5e4c8db77d8e9076`, migration image index
`bb886aecaadaa75d9995ce9fdadd33552528a34d58b9885e3cc34cdf4e12a2a4`, backup image index
`64ee17e94ff83587995161ae59beb8878e995ec988f49a98c8eddf163d2ba863`, release ID
`9bfd01c2097d18836d76b56d3def91410d928572aab530f3a5b75b188ed3014f`, migration chain
`3620f082569de51cebbd5218c8fa1bf38005e60c1dee7b45d5d8eac20d930f6b`에 결속됐다.

network none/read-only/nonroot/`--pull never`로 image 내부 Node/Prisma/schema와 37 migration raw bytes를
읽어 generate/verify PASS했고, migration 37개 총122467 bytes, managed relation53개, 기존 chain 불변이다.
manifest schema와 entries 30/30 bytes/hash PASS, phase1 receipt와 phase2 artifact 10개 bytes/hash 불변이다.
A의 14:32:29 KST 공식 재검토는 Node base 불변, exact14 Bookworm fix0, KEV0으로 EARLY_VOID=false다.
B/D 독립 검토는 canonical release ID와 결속에서 새 P0/P1/P2 0이다. 세부 receipt는
`증거/0909-r2a-53e87ba-rebinding.json`이다. 최초 Windows PowerShell bind 실패 원인은 MAIN_REPORTED이고,
성공 전 owner container 생성0과 성공 container2개의 exit0/destroy는 독립 확인됐다.

다른 정리에서 빠진 pinned PG17 image
`postgres:17@sha256:f3bd19c606e442c3d7bdfa8002e03fe260a1023351e0ea4598032022b68dd6e3` 하나를 다시 받아
linux/amd64 exact digest를 확인했다. 새 build/export/delete/prune는0이며 이 복구 pull만1회다.
official CA는1367 bytes/SHA-256 `700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7`,
Docker context `desktop-linux`와 engine pipe는 현재 응답한다. pgpass는 비밀을 출력하지 않고
5-field3개, exact pooler admin postgres/staging 각1개, 보호 ACL current user+SYSTEM을 확인했다.
actual credential store, prepare lock, phase2 temp/execution-state/marker는 없다.

새 외부 변경 후보는 exact staging 조직 `synohgzwspodxmfemoks`, project `ehnfrrmbkvlsbpvqcvkr`
(`Meta Ads Performance Security Dev`, Free, ap-northeast-2), session pooler
`aws-0-ap-northeast-2.pooler.supabase.com:5432`, DB `meta_ads_staging`의 G-DB-00 phase 2다.
서로 다른 credential3개를 로컬 DPAPI CurrentUser store에 만들고 exact OID의 세 role
`25397/25399/25401`에 SCRAM verifier와 LOGIN을 한 admin transaction으로 설정한 뒤, role별
TLS verify-full/SCRAM/current identity/속성/최소권한/default ACL/named prepared statement와 final
admin postcondition을 검증한다. migration, grant, app data 변경은 포함하지 않는다.

provider 비용은 Free project 기준 예상 추가0이고 예상 시간은1–3분이다. provider 로그의 verifier
마스킹 여부와 새 pooler user 전파 상한은 UNKNOWN이다. 성공은 Execute exit0, 세 role positive verify,
base ACL fingerprint `94af03f1e723fcb05d1fd0a1590bbf99` 불변, final target session0,
temp/container cleanup 및 `active_verified` marker를 모두 만족할 때만 인정한다.

실행 직전 SSOT의 main+sub-agent pre-execution wave로 HEAD/diff, manifest30/30, artifacts, phase1 receipt,
CA, PG17, target/OID, pgpass ACL/shape, Docker endpoint, DPAPI/state/temp/marker를 다시 확인한다.
하나라도 다르면 token을 사용하지 않는다. Execute는 최대1회이며 wrong-password/cross-secret,
transaction pooler, direct IPv6 fallback, 자동 재시도는 금지한다. COMMIT 불명확/verify 실패 시 재활성화하지
않고 별도 승인된 read-only 분류 후 새 VerifyOnly1회 또는 containment gate로 분기한다. destructive
rollback은 별도 승인 전 금지한다. phase2 성공도 G-DB-02 승인으로 재사용하지 않는다.

아래 exact 문구를 사용자가 새로 보내기 전에는 actual credential 준비와 Execute를 실행하지 않는다.

`G-DB-00 phase 2 승인 — commit c0fafda3522b352e699a8cfa80d3b43074723c97, manifest 312428954f1e6b4a355b199f37ab30e9a9d384ecfc306cd9444bf519a8832216, prepare 9f739f75dcbe379391aaacb57d30a1dbedb073409c77bb6f7d389f4484a887dd, runner 15eb19263d06ba3babd4786692abf7a7b8ed907d89934237336becc650f51002, staging Execute 1회`

현재 phase2 provider attempt0, actual credential 없음, operationalReady=false다. push/main merge/tag는 하지 않았다.
