# 2026-09-01 클라우드 실배포 R0 preflight

상태: **R0 부분 완료, dev candidate read-only fingerprint 완료,**
**production read-only fingerprint 완료, Koyeb target 미생성으로 BLOCKED,**
**staging/production 실제 runtime PASS 0건, `operationalReady=false`**

조사 시각: `2026-09-01T15:41:52+09:00`  
dev Supabase fingerprint 시각: `2026-09-01T15:58:41+09:00`  
사용자 target 제공 시각: `2026-09-01T16:10:09+09:00`  
production Supabase fingerprint 시각: `2026-09-01T16:24:29+09:00`  
조사 source: `codex/security-rbac@0c360b25dc58550fdbbf11a5b013bf893162e45e`

이 문서는 `0901클라우드실배포잔여작업계획.md` R0의 읽기 전용 기준선과
PASS/NOT RUN 경계를 기록한다. provider target mutation이나 production 준비 완료 증거가
아니다. secret, password, token, 이메일, 개인정보, credential URL은 기록하지 않았다.

## 1. Git과 파일 보존

- repository/worktree는
  `C:\Users\seong\Desktop\workspace\Meta-Ads-Performance-security-rbac`, branch는
  `codex/security-rbac`, 시작 HEAD는 위 full SHA다.
- 시작 시 staged/unstaged/untracked 변경은 모두 0건이었다.
- 계획서 작성 기준 `5a41079` 뒤에는 `4cad01c`, `4c835d2`, `0c360b2`의 문서 commit만
  있다. 실행 기준은 현재 worktree의 세 필수 문서다.
- `reset`, `checkout`, `clean`, `stash`, push, main merge, tag, remote 변경은 수행하지
  않았다.

## 2. Wave 1 읽기 전용 조사

계획서의 최대 4개 슬롯을 사용해 메인과 다음 역할을 병렬 배치했다.

| 역할 | 읽기 전용 결과 | 파일 변경/외부 mutation |
|---|---|---|
| A 플랫폼·컨테이너 | Docker/image/tooling과 공식 Node/Koyeb/registry 계약 조사 | 0 / 0 |
| B Supabase·DB | 정적 target binding, 37 migrations, env key 존재, maintenance gap 조사 | 0 / 0 |
| C 앱·업무검증 | Auth/RBAC/Storage/KPI/resource 기존 test와 actual-target gap 조사 | 0 / 0 |

세 역할 모두 실제 실행한 local inventory만 PASS로 분류하고 실제 staging/production
항목은 NOT RUN 또는 BLOCKED로 유지했다.

## 3. 로컬 platform·artifact 기준선

| 항목 | 결과 |
|---|---|
| Docker | PASS: `desktop-linux`, client/server `29.3.1`, Linux amd64, cgroup v2 |
| Docker host | PASS: 12 CPU, 8,182,243,328 B memory, overlayfs |
| build tools | PASS: buildx `0.32.1-desktop.1`, BuildKit `0.28.1`, Compose `5.1.1`, Scout `1.20.3` |
| local image | PASS: `meta-ads-cloud:64fa25f` 존재 |
| image ID | `sha256:846e7f84999e79dedb283116fbdf41fb00305f7ddc2f9918c574a04f02f5d93a` |
| linux/amd64 manifest | `sha256:030db1386e9004a7367ed572bbd607fe941705813b148388c752e5b5995110d0` |
| image boundary | PASS: amd64, user `node`, `8000/tcp` 하나, source revision `64fa25f9f8e71f14fb0bbd4d6a3bfd9c2bcb23eb` |
| compose static parse | PASS: service `app` 하나, `config --quiet --no-env-resolution` exit 0 |
| provider CLIs | `supabase`, `koyeb`, `gh`, `psql`, `pg_dump`, `pg_restore` 모두 ABSENT |

local image와 이전 1GiB harness PASS는 보존 기준선일 뿐 수정하지 않은 full production
CMD나 actual adapter/application route의 PASS가 아니다.

### 2026-09-02 latest R1 candidate

| 항목 | 결과 |
|---|---|
| release source | `ab95b4ecb815b9f422d7c94460fdb88c1857a813`, clean worktree |
| pinned Node base | index `sha256:83f487e0a63425e5b4d146fb5e5be574bcbe1b7b843d3ebafdd95eaf7767a7e5`; linux/amd64 `sha256:4d676821dff059fd00d277ee4261ef34ea712317fed0737c03941481b5760c96` |
| image | index `sha256:f03822be4daf058b9a53cee16d70e1aa6fa1b3c1613d5a9b56f1f5d0563e7595`; linux/amd64 `sha256:a6e831731db76ee6e445e0f8c2786dcfb5d66d91ee9802863c6c4d2a3eb242af`; config `sha256:e6660456a4cc389eed5ed328bbc5a51f32a6437f30a2632bb32213675904b719` |
| attestation/provenance | manifest `sha256:444cc4db1d375fc5ee31fb8c1fc0e3e3c44644a27553ee56b1e630c5a1c4ed3d`; attachment `sha256:0c8dd2561d4d8f74506a3764d9b2e98770fbafa84953e5412817b7170b8a5241` |
| SBOM/SARIF | SPDX `869deb28c5a0320392d628f6474b0da7de0407c02f8b7795f032d386ea2078d4`; SARIF `9d5495216df8137c0b9fbbec4aa423246d84c9778c238035482a0e9273a820cd`; 반드시 composite tuple로 사용 |
| image boundary | PASS: linux/amd64, `node` UID/GID 1000, read-only, cap 0, NNP, pids 256, 1GiB/swap 0, `8000/tcp` 하나, exact revision/CMD/closure |
| dependency | online npm audit full/prod 0; Scout 3 Critical/5 High 모두 no-fix이므로 `P1-REL-001` OPEN |
| 1GiB probe | 6/6 PASS, OOM delta 0, max cgroup peak `739,958,784 B`; CMD override synthetic fixture |
| production CMD | cold/live/protected-ready/signal/restart **NOT RUN** |

이 candidate는 source/image/provenance/closure/1GiB probe 범위에서 재현됐지만
`G-REL-01` 전에는 R1 PASS가 아니다. registry publish나 provider 배포는 수행하지 않았다.

### 2026-09-07 승인 유효성 재확인 (후속 maintenance 보정 전 이력)

승인 당시 상태: Docker Desktop 실행과 기존 exact image의 일회성 Scout metadata 전송 승인을
받아 09:22–09:23 KST 재검증했다. **새 High 6건**이 발견돼 기존 8건에 한정한
`G-REL-01`은 **VOID_NEW_HIGH**다. 이는 기존 8건 예외의 역사적 무효화이며 새 승인과 구분한다.
수정 가능한 PCRE2/qs와 합성 fixture 시각 보정을 반영한 최종 candidate
source는 `685a3f48ca1cdbd0377456c8e167d76eb12a6bbf`다. clean Linux 전체 source 회귀와 build가
PASS이며, 후속 일회성 전송 승인으로 10:31–10:32 KST 새 image scan도 실행했다.
PCRE2/qs 3건은 미검출로 바뀌었지만 **3 Critical / 10 High**가 남았다. 10:41:06 KST에
직전 exact 13-CVE 제안의 신규 승인을 수신했고 A의 local tuple 불변 확인과 D 독립 평가를
결합해 **P1-REL-001 ACCEPTED_TIME_BOUND / R1 image·base·CVE만 CONDITIONAL_PASS**다.
취약점 수정이나 actual provider 검증 완료가 아니며 **operationalReady=false**를 유지한다.
기존 8건 승인은 재사용하지 않는다.
상세 tuple, 소스·이미지 검증과 승인 범위는 `진행상태.md`의 후속 9월 7일 절을 따른다.
아래는 정정 전 최초 만료 승인에 대한 역사적 기록이다.

- `G-REL-01` owner `ColaRaccoon production owner` / expiry `2026-09-05 23:59:59 KST`를
  9월 7일 수신했으므로 **RECEIVED_BUT_EXPIRED**다. 자동 연장·소급 적용 없이
  `P1-REL-001 OPEN`, **R1 BLOCKED**, **`operationalReady=false`**를 유지한다.
- official Node index/amd64 digest는 위 값과 동일하고 Debian Bookworm의 기존 8개 CVE는
  여전히 vulnerable이다. CISA KEV catalog `2026.09.04`에서는 해당 8건이 미등재이나
  악용 부재를 증명하지 않는다. 전체 근거와 확인 시각은 `진행상태.md`의 9월 7일 절에 있다.
- Docker engine pipe 부재로 현재 image inspect/full rescan은 **NOT RUN**이다. SBOM 기반
  대체 scan도 외부 metadata 전송 우려로 실행환경에서 거부됐으며 우회하지 않았다.
  기존 SPDX/SARIF hash 일치는 역사적 artifact의 보존만 증명한다.
- 미래 expiry 정정과 exact image fresh scan/tuple 재검증 전에는 위험 예외를 유효 처리하지
  않는다. 9월 2일 source/runtime 검증을 오늘 새로 수행한 PASS로 바꾸지 않는다.

### 2026-09-07 승인 시점 local candidate (미배포, 후속 maintenance 보정 전)

| 항목 | 현재 결과 |
|---|---|
| release source | `685a3f48ca1cdbd0377456c8e167d76eb12a6bbf`, build 직전 clean |
| image index | `sha256:763982ea6423147d2a2f18c4b38d4cd9c5c9788311f1ea9a9957d3b36f17109c` |
| linux/amd64 manifest | `sha256:c251f1a1468b15c5bd149953b6f5eb690ef5ec44cb32861beba7e9e964601e5f` |
| config | `sha256:a09055d98c7b6839e24f31ec540f868259933e88f231aa52058eea3b0103461e` |
| source 검증 | clean Linux Prisma/lint/root test/build PASS; API 1,169 PASS/23 SKIPPED, Web 279 PASS, E2E offline TS PASS |
| image 검증 | non-root/8000-only/fixed CMD/33,670-file closure, PCRE2 `10.42-1+deb12u1` / qs `6.16.0` 설치 PASS |
| npm advisory | current online full/prod 및 offline prod 0; OS CVE 0의 증거 아님 |
| local SBOM | SPDX 2.3 / 617 packages, exact amd64 subject; full tuple와 artifact hash는 `진행상태.md` local release manifest 참조 |
| 1GiB synthetic probe | 6/6 PASS, OOM delta 0, max cgroup peak 764,837,888 B; CMD override |
| 새 Scout scan | 승인 후 실행: 3 Critical / 10 High / 7 Medium / 31 Low / 5 Unspecified; 13 C/H 모두 Bookworm no-fix, PCRE2/qs 3건 제거·추가 0 |
| G-REL-01 / R1 | 새 exact 13건 **ACCEPTED_TIME_BOUND**, image/base/CVE 범위 **CONDITIONAL_PASS**; 기존 8건 승인은 **VOID_NEW_HIGH** |
| R1A maintenance actual artifact | exact 685a3f48 전용 compile/5 purpose build/20개 no-network closure·fail-closed 검사 PASS; D 독립 확인 신규 P0–P3 0. maintenance CVE scan·provider 검증은 NOT RUN |
| actual production CMD / provider | **NOT RUN**, `operationalReady=false`, registry publish와 provider mutation 0 |

새 scan은 Scout 1.20.3 / 643 indexed packages / 15 vulnerable packages / 56 results다.
SARIF SHA-256 `c3f9fdfcc6c0ce96e8877e126b63849d9d0555bd89e5bddb3fdc691cfb3dd988`과 exact
source/image tuple을 묶는다. 새 G-REL-01의 13개 CVE, owner/expiry/재검토/조기 무효화 조건은
`진행상태.md`의 후속 승인 기록을 따른다. owner `ColaRaccoon production owner`, expiry
**2026-09-10 23:59:59 KST**, 첫 owner 재검토 **2026-09-08 10:00 KST**, 이후 24시간 이내와
publish/deploy 직전 검토가 조건이다. source/image/CVE/완화 전제 변경, fix·KEV·악용/도달 가능성
변화, R2B 실패, 재검토 미이행 또는 만료 시 보류·재평가한다. 자동 점검은 생성하지 않았다.
이번 승인 후 새 scan/metadata 전송은 0건이며 maintenance 이미지에는 이 예외가 적용되지 않는다.

### 2026-09-07 후속 maintenance 보정으로 새 release 재검증 필요

위 5개 maintenance diagnostic image의 actual dpkg/SPDX에서 PCRE2 `10.42-1`을 확인해
**P1-MAINT-001**(known fix-available packaging gap)을 기록했다. 메인이 maintenance Dockerfile의
두 base 단계에 `10.42-1+deb12u1` exact pin/설치 검사를 보정하고 계약 test를 추가했다.
수정 전 해당 test FAIL, 수정 후 artifact/entrypoint 38/38 PASS다. 새 실제 image 검증은 후속 기록을 따른다.
SSOT R1A에 따라 전체 source·public image·maintenance 5개·SBOM/CVE·1GiB를 새 global SHA로
재결속해야 한다. 위 **685a3f48/763982의 승인과 CONDITIONAL_PASS는 승인 당시 exact tuple의
역사적 기록**이며 새 release에 승계하지 않는다. **현재 release R1 BLOCKED / 새 검증 대기 /
operationalReady=false**다. 새 Scout metadata 전송도 별도 exact 승인을 받는다.

### 2026-09-07 현재 eb4d4675 local release (6개 scan·신규 기한부 예외, 미배포)

| 항목 | 현재 판정 |
|---|---|
| source | `eb4d4675201e2605f7d5b6b5c5dd5c37c7c1719d`; pin 보정 후 archive CRLF test도 보정 |
| public image index | `sha256:9483d265c57b4f581b745d6897f391cabd9fa5b1275d3bb15c187a4608fd6791` |
| public amd64 / config | `sha256:c36383466f60f16ea44ee39823333fb585d0da8ef084ab80be88bf29f76853dc` / `sha256:65c36829d5cf06195f129536cc64ef1d92b31a95b1c1e05a4762820377f5ecb6` |
| clean source | D가 Git/ZIP 719파일 일치 확인; 실제 dotenv·금지 generated payload 0. 전 내용의 비밀 부재 증명은 아님 |
| 전체 Linux source | Prisma/lint/maintenance compile/cloud 48+launcher 14/API 1,169/Web 279/API+Web build PASS; API 23 SKIPPED 별도 유지 |
| npm advisory | exact source의 02:18 UTC fresh full/prod 0; OS CVE 0 의미 아님 |
| maintenance | 5개 actual image build/closure와 B 20-case PASS; D 독립 exact image·guard·hardening 확인 PASS |
| public 1GiB synthetic | A 6/6 PASS, max 763,637,760 B < 850MiB; CMD override이며 actual provider readiness 아님 |
| P1-MAINT-001 | source pin/guard/test와 actual fixed version 확인, 새 5개 scan에서 PCRE2 finding 미검출; actual provider 검증은 미완료 |
| 새 CVE / G-REL-01 | public + maintenance 5개 실제 scan 완료: 각 **3 Critical/10 High/7 Medium/5 Unspecified**, Low는 backup 40/나머지 31; 새 exact 6-image 공통 13 C/H만 **ACCEPTED_TIME_BOUND** |
| maintenance scan | 중단 뒤 별도 재개 승인으로 **5/5 각 1회 완료**, 모두 Scout exit 2; migration만 승인된 동일 file-in-use cleanup 예외, 재scan/강제 cleanup 0 |
| 현재 release | **R1 image/base/CVE만 CONDITIONAL_PASS / operationalReady=false**, actual CMD/OPS/ISO/provider NOT RUN |

각 purpose의 exact index와 source/SBOM/provenance/actual 검사 기록 및 metadata 전송 승인·실행
기록은 `진행상태.md`의 마지막 후속 절에 있다. 04:11:11 UTC 사용자 승인은 해당 6개 각 1회
전송에만 한정됐고 public scan은 04:17:11.3518413–04:17:49.9451421 UTC에 수행했다.
SARIF 363,959 B / SHA256 `c3f9fdfcc6c0ce96e8877e126b63849d9d0555bd89e5bddb3fdc691cfb3dd988`는
옛 685 결과와 같지만 새 command/time/log/tuple과 결속해야 하며 옛 CVE 예외를 승계하지 않는다.
이때 289,858,560 B 임시 archive를 보존하고 남은 전송을 중단했다. 이후 **04:31:09 UTC**
별도 재개 승인으로 auth→migration→storage→backup→legacy를 **04:33:23.5658730–04:37:08.1268887 UTC**
각 1회 수행했다. migration만 승인된 제한적 cleanup 예외였으며 257,757,696 B 파일을 보존했다.
모든 사전/사후 disk guard를 충족했고 마지막 free는 1,642,381,312 B다. backup SARIF는
438,601 B / SHA256 `60ca6398fc5904882cb641c5d6058f9314451c0b6813d2884412e927411badd2`,
나머지 4개는 public과 같은 hash/size이나 각각 새 invocation/tuple로 결속했다.
A/D 독립 actual evidence 검증 PASS, 공통 13 C/H의 Bookworm 공식 no-fix 및 KEV 미등재를 새로
확인했다. **04:57:03 UTC** 사용자 명시적 새 G-REL-01 승인을 수신했고 A의 04:58–04:59 UTC
actual image/manifest/blob/SARIF/log/ledger 불변 확인과 D 승인 유효성 평가를 결합했다.
owner **ColaRaccoon production owner**, expiry **2026-09-10 23:59:59 KST**, 첫 검토
**2026-09-08 10:00 KST**, 이후 24시간 이내 및 publish/deploy/실제 maintenance 직전 재검토다.
변경·수정본/KEV·악용/완화 전제·실제 검증 실패·재검토 미이행·만료 시 보류 조건을 유지하며
옛 예외는 승계하지 않는다. 새로운 scan/전송·registry publish·Koyeb·DB·Auth·Storage 변경은 없다.
실제 cloud PostgreSQL server major도 미확인으로 pg_dump 15.19 호환성/실제 restore는 NOT RUN이다.

**05:04:02 UTC G-SUP-STG-01 승인**을 수신하고 05:06:11 UTC Overview 확인에서
org `synohgzwspodxmfemoks` / project `ehnfrrmbkvlsbpvqcvkr` / Free / Healthy / Seoul / NANO가
일치해 **격리 staging 대상으로 지정·기록했다(G-SUP-STG-01 designation PASS)**.
최근 60분 PostgreSQL 113 requests/112 errors의 원인은 미조사이며 provider/DB PASS가 아니다.
기존 시험용 Auth 사용자와 모든 DB/Storage 데이터를 보존했고 설정·데이터 변경은 0이다.
다음 R2A 상세 read-only fingerprint는 범위를 제시해 별도 승인받으며 실제 운영은 NOT RUN이다.

## 4. 공식 계약 재확인

아래 계약 목록은 2026-09-02 조사 기록이다. 9월 7일 재확인한 범위와 NOT RUN은 위 절을 따른다.

- Docker Hub의 현재 official `node:22-bookworm-slim`은 이전 release와 같은 index
  `sha256:83f487e0a63425e5b4d146fb5e5be574bcbe1b7b843d3ebafdd95eaf7767a7e5`,
  linux/amd64 manifest
  `sha256:4d676821dff059fd00d277ee4261ef34ea712317fed0737c03941481b5760c96`다.
  R0에서는 `docker buildx imagetools inspect --raw`로 registry metadata만 조회했고, 이후
  latest R1 candidate에서 같은 pinned base를 pull/build했다. 공식 tag:
  <https://hub.docker.com/_/node/tags?name=22>
- Debian bookworm의 현재 `perl 5.36.0-7+deb12u3`와
  `openssl 3.0.20-1~deb12u2`는 latest candidate의 3 Critical/5 High CVE에 대한 fix가 아직 없다.
  예: <https://security-tracker.debian.org/tracker/CVE-2026-13221>,
  <https://security-tracker.debian.org/tracker/CVE-2026-63076>.
  source `ab95b4e...` image를 rebuild/rescan해 같은 package version과 3 Critical/5 High를
  확인했으며 `G-REL-01` 전까지 `P1-REL-001`은 열려 있다.
- Koyeb standard `small`은 1 vCPU/1GB/10GB SSD, `$0.0144/hour`, `$10.71/month`다.
  `eco-small`은 0.5 vCPU/1GB/8GB SSD, `$0.0072/hour`, `$5.36/month`이며
  Washington/Frankfurt/Singapore에서만 제공된다. 세금과 실제 조직 비용은 생성 직전
  공식 화면에서 다시 확인해야 한다: <https://www.koyeb.com/docs/reference/instances>.
- Koyeb HTTP health check는 header를 지원하고 모든 공개 port의 check가 통과해야 새
  deployment가 traffic을 받는다. 실패하면 이전 healthy deployment가 계속 traffic을
  받고 첫 deployment라면 traffic은 0이다:
  <https://www.koyeb.com/docs/run-and-scale/health-checks>,
  <https://www.koyeb.com/docs/reference/deployments>.
- Koyeb secret은 service 환경변수와 private registry credential에 연결할 수 있다.
  그러나 health header가 secret-manager reference를 안전하게 소비한다는 공식 증거와
  Koyeb image 입력이 `repo@sha256`을 수용한다는 명시적 계약은 확인하지 못했다.
- Supabase는 persistent container에 direct connection을 권장하고 IPv4 제약이면 shared
  pooler session mode 5432를 대안으로 둔다. migration/backup은 direct가 기준이다:
  <https://supabase.com/docs/guides/database/connecting-to-postgres>.
- Supabase Pro daily DB backup 보존은 현재 7일이며 custom role password와 Storage object
  body는 DB backup에 포함되지 않는다:
  <https://supabase.com/docs/guides/platform/backups>.
- Supabase private Storage는 `storage.objects` RLS를 사용한다. service key는 RLS를
  완전히 우회하므로 계획대로 scoped runtime token과 별도 Auth admin key를 사용해야 한다:
  <https://supabase.com/docs/guides/storage/security/access-control>.

## 5. source·runner inventory

- cloud startup은 Auth/DB/Storage의 같은 20자 Supabase project ref, exact DB host/name/
  schema/runtime role, TLS `verify-full`, connection limit을 정적으로 강제한다.
- migration directory는 37개이고 head는
  `20260826020000_add_local_edge_request_nonces`다. 실제 staging/production history,
  checksum, pending, drift는 조회하지 않았다.
- `deploy/cloud/.env.local`은 없고 ignored `apps/api/.env`는 cloud 필수 target-binding,
  scoped Storage token, readiness, environment fingerprint key가 불완전하다. 값·길이·hash는
  출력하지 않았다.
- 기존 Auth/RBAC/CSRF/Origin, Supabase adapter, tombstone/compensation, canonical KPI,
  heavy-operation test는 재사용할 수 있다. 다만 현재 smoke/CLI는 local provider,
  LocalFileStorage, direct service 또는 synthetic harness에 묶여 있다.
- release-neutral actual-target E2E runner, cloud SUPER_ADMIN/user disposition runner,
  scoped Storage token rotation artifact, immutable `prisma migrate deploy` artifact,
  cross-system DB+object backup/restore 도구는 없다.

## 6. exact target와 gate inventory

### Staging

**2026-09-07 G-SUP-STG-01 승인 후 지정된 staging**은 기존 개발 project
`ehnfrrmbkvlsbpvqcvkr`, org `synohgzwspodxmfemoks`, Seoul, Free다.
05:06:11 UTC Healthy/NANO/t4g.nano fingerprint를 재확인했다. 이 지정은 실제 direct
PostgreSQL/Auth/object lifecycle PASS가 아니며 provider 설정·데이터를 변경하지 않았다.

사용자는 기존 Auth 사용자 estimated 10명이 모두 staging 시험용이라고 확인했다. 이를
근거로 exact staging target은 기존 dev project로 지정하되, project 설정·role·migration·Auth·
Storage·fixture mutation은 아직 승인하지 않은 것으로 분리한다. G-SUP-STG-01은 지정 자체만
승인됐으며 후속 데이터 작업은 영향과 cleanup manifest를 제시하고 각각 승인받아야 한다.

사용자가 `G-SUP-STG-03`을 승인한 뒤 위 exact dev project만 Dashboard/API에서 읽기
전용 조회했다. resource/key/user/data/설정 변경은 0건이고 비밀·이메일·object key/row
body는 출력하지 않았다.

아래 상세 표는 **이전 G-SUP-STG-03 조회 당시 기준선**이다. 이번 Overview refresh에서 Auth/
Storage/DB 설정·사용자·role·schema를 새로 검사한 결과로 재사용하지 않는다.

| 기존 dev 상세 항목 | 이전 읽기 전용 결과 |
|---|---|
| 조직 | `Meta Ads Performance Security Dev` (`synohgzwspodxmfemoks`), Free |
| project | `Meta Ads Performance Security Dev` (`ehnfrrmbkvlsbpvqcvkr`) |
| region/compute | Northeast Asia (Seoul) `ap-northeast-2`, Nano |
| health/API | Dashboard Healthy, Auth settings와 Storage bucket metadata read PASS |
| Auth | public signup OFF, Email provider만 ON, email confirmation ON, custom SMTP OFF |
| Auth URL | Site URL `http://localhost:3200`, redirect 1건 `http://localhost:3200/invite/accept` |
| 사용자 inventory | estimated total 10명; 이메일/identity/row body는 조회 결과에 기록하지 않음 |
| Storage | bucket `meta-ads-security-step7-dev` 존재, private, 50MiB, MIME 제한 없음 |
| Storage RLS | bucket/object policy 0건 |
| DB/dashboard migration | `public` schema app table 0건, Supabase CLI migration 0건 |
| DB connection | ignored env의 dev/test/shadow DB URL은 모두 loopback `127.0.0.1:55432`; Supabase direct PostgreSQL credential/role은 없음 |
| DB SSL/backup | incoming SSL enforcement OFF, pool size 15/Nano client 한도 200, Free plan scheduled backup 없음 |

Dashboard의 migration 화면은 Supabase CLI migration만 나타내므로 이것만으로 Prisma
`_prisma_migrations`를 판정하지 않는다. 다만 `public` schema에 app table이 없고 direct
PostgreSQL 연결도 없으므로 이 exact project에서 repository의 37 migration history/checksum,
pending/drift는 **NOT RUN/BLOCKED**다. private bucket metadata PASS도 실제 object body
write/download/delete나 앱 Storage lifecycle PASS가 아니다.

- 기존 project 재사용 지정: `G-SUP-STG-01` 승인·기록 완료(2026-09-07); 설정·데이터 변경 제외
- 새 project/plan 생성: `G-SUP-STG-02`
- 어느 staging이든 read-only fingerprint: `G-SUP-STG-03`

필요한 비밀 아닌 값은 조직/project/ref/region/plan, direct 또는 session pooler 선택,
DB host/port/name/schema와 runtime/migration/backup role 이름, Auth Site URL/redirect/SMTP
시험 방식, private bucket/prefix/RLS/token subject·issuer·rotation owner, fixture cleanup,
DB/object backup·isolated restore target이다.

### Production

사용자 제공 exact project는 `ColaRaccoon's Project`, ref
`iygjmosbelbosfxidqxv`, AWS Seoul `ap-northeast-2`, Pro다. 제공된 organization URL
`https://supabase.com/dashboard/organizations`는 account 공통 목록 주소라 exact 조직명/ID를
식별하지 못한다. exact project Dashboard의 소유 조직 link를 read-only로 확인해야 한다.

사용자가 `G-SUP-PRD-01`을 별도로 승인한 뒤 이 exact project만 Dashboard와 공개 health
endpoint로 읽기 전용 조회했다. project/ref/name/region이 예상과 일치했고 소유 조직은
`ColaRaccoon` (`oqfvvptouenqglsibqsg`), Pro임을 독립 확인했다. resource/key/user/data/설정
변경, SQL 실행, 이메일 발송은 0건이고 API key/secret/user identity/row body/object key·body는
조회 결과에 포함하지 않았다.

| production 항목 | 읽기 전용 before-state |
|---|---|
| 조직 | `ColaRaccoon` (`oqfvvptouenqglsibqsg`), Pro |
| project | `ColaRaccoon's Project` (`iygjmosbelbosfxidqxv`) |
| region/compute | Northeast Asia (Seoul) `ap-northeast-2`, `t4g.nano`, 최대 0.5GB memory |
| disk | 2GB gp3, spend cap에서 최대 8GB까지 auto-expand 표시 |
| project URL | `https://iygjmosbelbosfxidqxv.supabase.co` |
| direct DB endpoint | `db.iygjmosbelbosfxidqxv.supabase.co:5432`, IPv6 기본 |
| session pooler | `aws-1-ap-northeast-2.pooler.supabase.com:5432`, IPv4 대안 |
| DB network/SSL/pool | 모든 IP 허용, incoming SSL enforcement OFF, pool size 15, client connection 상한 200 |
| Auth provider | signup ON, confirm email ON, Email만 ON, manual linking/anonymous/OAuth·custom provider OFF |
| Auth URL/SMTP | Site URL `http://localhost:3000`, redirect 0건, custom SMTP OFF |
| Auth user inventory | 2026-09-02 재조회에서 `Total: 10 users (estimated)` 표시; exact row/identity/email은 조회하지 않음 |
| public DB | Data API OFF, 21 table 모두 RLS OFF/API OFF/policy 0건 |
| dashboard migration | Supabase CLI migration 0건; Prisma `_prisma_migrations`는 SQL 미실행이라 NOT RUN |
| DB role | 2026-09-02 재조회에서 managed role 외 `meta_ads_app`, `patima_app`, `postgres`, `supabase_privileged_role` 표시; 두 app role의 소유자/login/grant/workload는 NOT RUN |
| Storage | bucket 0건, `storage.objects`/`storage.buckets` policy 0건, public status endpoint 200; unauthenticated Auth health는 401이라 authenticated health NOT RUN |
| DB backup | scheduled restore point 7건, PITR add-on OFF; Storage object body는 DB backup에 포함되지 않음 |
| Security Advisor | errors 0, warnings 0, info suggestions 0 |

Dashboard policy inventory의 public table 21개는
`ad_campaign_daily_costs`, `ad_campaign_signatures`, `ad_excel_uploads`,
`ad_upload_preview_rows`, `audit_logs`, `campaign_sales_unit_mappings`,
`canonical_sales_units`, `commerce_api_credentials`, `daily_fake_purchases`,
`daily_sales_unit_profits`, `daily_store_summaries`, `operations`, `order_items`,
`order_source_signatures`, `orders`, `products`, `sales_unit_cost_settings`,
`sales_unit_cost_snapshot_entries`, `sales_unit_cost_snapshots`, `storage_metadata`, `stores`다.
repository Prisma model table 52개와 이름을 비교하면 `products`만 겹치고 legacy-only 20개,
expected missing 51개다. 이는 Dashboard metadata 비교이며 row body·row count 또는 실제
`_prisma_migrations` checksum/drift 판정이 아니다.

따라서 exact production control-plane fingerprint는 PASS지만 실제 direct PostgreSQL login,
DB version, `_prisma_migrations`, role grant/default ACL, table row/size, backup restore는
**NOT RUN/BLOCKED**다. 현재 production DB에 기존 table/backup이 있으므로 pristine target처럼
취급하거나 destructive migration을 실행하면 안 된다. Free dev project와도 완전히 구별한다.
`meta_ads_app`과 `patima_app`은 후속 승인된 read-only SQL과 사용자 용도 확인 전 기존 workload
role로 보존하며, 새 cloud runtime/migration/backup role로 재사용·변경하지 않는다.

### Koyeb/registry/domain/data

사용자는 Koyeb에 가입하거나 Dashboard를 사용한 적이 없어 현재 조직/app/service/domain은
아직 존재하지 않는 것으로 보고했다. 1GB 방향은 standard `small`로 고정하며 `eco-small`로
자동 대체하지 않는다. Koyeb 조직명 후보는 사용자 선택 `colaraccoon`이며 가입/생성 화면에서
고유성 확인 전까지 proposed 상태다. 별도 domain 구매나 custom domain/DNS 전환은 하지 않고
Koyeb가 App에 발급하는 `https://<app>-<organization>-<hash>.koyeb.app` provider domain을
staging과 production endpoint로 사용한다. 그러므로 현재 결정에서 `G-DNS-01`은 N/A이며,
향후 custom domain 요청이 생길 때만 새 gate로 되살린다.

Koyeb app/service/region, fixed replica 1, 월 비용 상한,
registry 종류/조직/repository/visibility, publish identity와 deploy read identity, provider
domain의 실제 발급값, source DB 관계, local file inventory 범위, 대표 KPI 기간과 expected
값은 미확정이다. 현재 제안은 staging App `meta-ads-performance-staging`, production App
`meta-ads-performance`, 각 Service `web`, region `TYO`, private GHCR
`ghcr.io/colaraccoon/ads-management`지만 생성·확정하지 않았다.

향후 mutation 순서는 `G-REG-01` → `G-REG-02` → `G-KOY-01` → `G-KOY-02` →
`G-KOY-03`이며 어느 것도 이번 R0에서 실행하지 않았다.

## 7. 현재 판정

| 범위 | 판정 |
|---|---|
| Git/local Docker/image/source inventory | PASS |
| 공식 가격/health/connection/backup 계약 | PASS(2026-09-02 재조회) |
| dev Supabase candidate read-only fingerprint | PASS (`G-SUP-STG-03` exact target만) |
| exact staging target | G-SUP-STG-01 designation PASS(2026-09-07); 설정·데이터 변경/direct DB/app는 NOT RUN |
| staging 확장 inventory(2026-09-07) | G-SUP-STG-03 승인; Phase 1, Phase 2a/2b scoped catalog와 Auth configuration read-only inventory PASS. Auth1, private50MiB bucket/object0/MIME제한0/다른bucket0; role30/membership24/ACLgroup309. 앱 전용 role 미관찰, DB PUBLIC TEMPORARY 발견. 실제 Auth flow·pristine/Prisma history·effective 접근은 NOT RUN. 상세 `0907staging확장조회기록.md` |
| staging backup client/server 호환성 | Dashboard PG17.6.1.155와 SQL PG17.6 / 기존 pg_dump15.19 gap. backup/restore NOT RUN, 새 client/사전 version guard 필요 |
| exact production control-plane fingerprint | PASS (`G-SUP-PRD-01`, project/org/Pro/Auth/DB/Storage/backup metadata) |
| production direct DB/migration/grant/row·size/restore | BLOCKED / NOT RUN |
| R1A local maintenance/source contract | PASS; D 독립 최종 평가 OPEN P0/P1/P2/P3 0 |
| E2E strict typecheck/Playwright list | PASS(local evidence); actual browser/target은 NOT RUN |
| backup/legacy direct provider source composition | PASS(local mock/source); actual R2/Supabase/PostgreSQL과 backup/restore는 NOT RUN |
| latest source image rebuild/SBOM/CVE scan | eb4d4675의 6개 local build/tuple/SBOM/scan 완료; 공통 3 Critical/10 High만 신규 G-REL-01 ACCEPTED_TIME_BOUND, R1 image/base/CVE만 CONDITIONAL_PASS |
| full production CMD + actual adapter/app route | resource probe 6/6만 PASS; unmodified production CMD와 actual route는 NOT RUN |
| Koyeb/registry/DNS/staging·production 실제 배포 | NOT RUN |
| 실제 Auth/RBAC/Storage/KPI/backup/restore/rollback | NOT RUN |

`P1-REL-001`은 위 새 exact 6-image/13-CVE에 한정한 ACCEPTED_TIME_BOUND다.
나머지 열린 blocker는 계획서의 `P1-REL-002`, `P1-OPS-001`~`005`,
`P1-ISO-001`, `P2-OPS-001`을 유지한다. Koyeb exact digest 입력 계약이 확정되지 않은
`P1-PLAT-001`도 staging deploy 전 해소해야 한다. production에는 legacy table과 backup이
있고 cloud target schema/Auth/Storage가 준비되지 않은 새 gap이 확인됐다. R1A source finding
`P1-D-R1A-014`, `P1-D-R1A-015`, `P1-D-R1A-017`~`025`는 local 구현과 독립 재평가로
CLOSED다. production role 용도 미확인 `P1-SUP-PRD-ROLE-DRIFT`와 현재 71-file 기준선의
별도 operational finding `P1-LEGACY-SOURCE-DRIFT`는 OPEN이다. 다음 단계에서도
지정만 승인된 G-SUP-STG-01과 후속 상세 조회 G-SUP-STG-03·각 DB/Auth/Storage gate, production DB/backup 조회·복구
gate를 서로 분리해 승인받는다. 완료 조건 전에는 **`operationalReady=false`**다.

## 2026-09-08 PG17 보정 후 superseding local release refresh

이 절은 위 `eb4d4675` image/CVE 상태를 새 source에 자동 승계하지 않고, 이번에 실제 수행한
local 범위만 대체한다. source는 `f8a94fcf43aa4705befcaafbcb91045e154ae54b`다.

| 항목 | 현재 판정 |
|---|---|
| PG17 backup artifact | PASS(local): `pg_dump 17.11`, client/libpq `17.11-1.pgdg12+2`, common `293.pgdg12+1` exact 설치·실행 |
| dump 전 client/server major guard | PASS(local tests): server 17 fixture/client 17 PASS, client 15/18 및 malformed/process failure fail-closed; actual DB는 NOT RUN |
| full source regression | PASS on clean retry; 최초 한 기존 test 5s timeout과 isolated retry PASS를 함께 기록 |
| 새 6-image build/tuple | PASS: pinned base, linux/amd64, full revision, non-root, 1 public port, purpose별 closure |
| 새 SPDX/SLSA | PASS: 6개 모두 payload hash/subject→amd64 manifest 결속 |
| 새 1GiB synthetic | 6/6 PASS; max bundle 746,172,416 B < 850MiB, OOM/oom_kill 0 |
| 새 online advisory / 6-image CVE scan | offline prod audit 0만 PASS; **fresh online/Scout는 NOT RUN — 별도 metadata 전송 승인 전 실행 금지** |
| actual full CMD/staging adapter/dump·restore | **NOT RUN** |

비밀 없는 full tuple과 측정값은
`증거/0908-r1a-f8a94fc-local-verification-summary.json`에 있으며 SHA-256은
`e92e26ac9d022ac298ccc804bf27927522703b9bcf273b9786e589342a4af27e`다. 과거 G-REL-01은 이 source/image에
적용하지 않으며 새 scan 결과가 나온 뒤에만 별도 위험 수용 여부를 판단한다.
`operationalReady=false`를 유지한다.

## 2026-09-08 f8a94fc online advisory / 6-image Scout superseding 결과

사용자의 exact metadata 전송 승인 뒤 source
`f8a94fcf43aa4705befcaafbcb91045e154ae54b`에 결속된 online production npm audit와
6개 `linux/amd64` local immutable index scan을 실행했다. 이는 위 NOT RUN 행을 아래처럼
대체하며 과거 `eb4d4675` 위험 수용을 승계하지 않는다.

| 항목 | 현재 판정 |
|---|---|
| online production npm audit | PASS: valid JSON, 모든 severity 0, lock SHA-256 전후 불변; `--audit-registry` unknown-config 경고는 별도 적용 주장 금지 |
| 6-image Scout evidence | COMPLETE 6/6: 모두 Scout 1.20.3 exit 2, valid SARIF 2.1.0; exact source/index/platform 결속 PASS |
| 공통 image CVE | **3 Critical / 10 High / 7 Medium / 31 Low / 5 Unspecified**; backup만 Low 40, 나머지 동일 |
| fixed/current status | Scout의 공통 13 C/H fixed version 모두 `not fixed`; 2026-09-08 Debian tracker의 installed Bookworm versions 모두 vulnerable이며 Bookworm fixed-version 행 없음 |
| latest official Node 22 base | `node:22-bookworm-slim` = Node 22.23.2, index `83f487e0…a7e5`, amd64 `4d676821…0c96`; 2026-09-08T01:02:41Z 조회와 build provenance 일치 |
| CISA KEV snapshot | CISA-operated mirror immutable `ac9d37166471fbed6a03dff6c84f05e9c6b1d2c2`, catalog `2026.09.04`/1,695건에서 exact 13건 match 0; canonical fetch 403은 내용 증거로 미사용 |
| independent review | A와 D 모두 신규 P0/P1/P2/P3 0; execution 시각·exit·attempt/recipient는 MAIN_REPORTED 경계 |
| G-REL-01 / R1 | **새 exact 위험 수용 미승인; P1-REL-001 OPEN; R1 BLOCKED** |
| actual full CMD/staging adapter/dump·restore/provider | **NOT RUN** |

비밀 없는 집계 파일은 `증거/0908-r1-f8a94fc-online-scan-summary.json`이다. commit `592fea0`의
승인 전 snapshot SHA-256은 `7f7823d2a990f6e036c22bfa8e511fe0cecbe268f384685aaaa07af21d68752f`다. 공통 5개 SARIF
SHA-256은 `3f80cc3e3aec7d966ffae51acbb1cc10f9bc70b77a57219c75799cc8e60cf869`, backup은
`1c9d9980047d0d1b984e207f7bd6b859b0de10e4682c37bbcf2e3103b0f1bce4`다. public/migration의
same-index file-in-use cleanup 경고는 승인된 제한 예외로 archive를 보존했고, 다른 오류·재시도·강제
삭제는 없었다.

다음 gate는 owner `ColaRaccoon production owner`, expiry **2026-09-10 23:59:59 KST**로
제안한 새 `G-REL-01`이다. 대상은 이 source, 6-index, 공통 13 C/H, 위 두 SARIF hash만이며
승인 시점을 첫 review로 기록하고 이후 24시간을 넘기지 않는 간격의 반복 review와
publish/deploy/maintenance 직전 추가 review 및 source/image/CVE/fix/KEV/도달 가능성/
hardening/actual 검증 변화·재검토 누락·expiry의 조기 무효화 조건을 포함한다. 승인 전에는
**`operationalReady=false`**를 유지한다.

## 2026-09-08 f8a94fc 새 G-REL-01 승인 반영

사용자는 `2026-09-08T01:12:03.4308677Z`에 source
`f8a94fcf43aa4705befcaafbcb91045e154ae54b`, 위 exact 6-index, 공통 13 C/H와 두 SARIF hash,
owner `ColaRaccoon production owner`, expiry **2026-09-10 23:59:59 KST** 범위의 새
`G-REL-01`을 명시적으로 승인했다. 이 시점이 첫 owner review이며 다음 정기 review는 늦어도
**2026-09-09 10:12:03 KST**, 이후 최대 24시간 간격과 publish/deploy/maintenance 직전에
추가로 수행한다.

승인 receipt가 포함된 같은 summary의 현재 SHA-256은
`9df16bebcc57f009a6de0384f2bdcc23fc88b0de4df5e6489d2fbf7a313f3fc2`다. summary의
`preApprovalAnalysisAgent`/`preApprovalReleaseEvaluator`와 현재 `riskAcceptance`를 구분한다.

| 항목 | 승인 후 판정 |
|---|---|
| P1-REL-001 | **ACCEPTED_TIME_BOUND** — exact source/6-index/13 C/H/SARIF 범위만 |
| R1 | **image/base/CVE 범위만 CONDITIONAL_PASS** |
| registry publish / Koyeb / Supabase / DB / Auth / Storage / R2 | **승인 아님 / NOT RUN 유지** |
| actual full CMD / dump·restore / maintenance provider path | **NOT RUN** |
| operationalReady | **false** |

source/image/SARIF/Critical·High 집합, Bookworm/base fix, KEV·실제 악용·도달 가능성, hardening,
actual 검증 결과 변화, review 누락 또는 expiry는 예외를 즉시 무효화한다. rollback은 위험 수용
철회와 publish/deploy/maintenance 차단 후 patched image 재빌드·전체 재검증이다. push/main
merge/tag도 별도 승인 전 실행하지 않는다.

A의 expiry/scope 검토와 D의 post-approval exact artifact 평가는 모두 신규 P0/P1/P2/P3 0이다.
D는 위 범위의 `P1-REL-001 = ACCEPTED_TIME_BOUND`, R1 image/base/CVE만
`CONDITIONAL_PASS` 판정 가능하다고 확인했다. actual CMD/provider/backup은 계속 NOT RUN이다.

## 2026-09-08 R2A DB role/별도 database local rehearsal

| 항목 | 판정 |
|---|---|
| release source/image boundary | `f8a94fc`와 exact 6-image 불변; `deploy/**`, `apps/**`, lockfile 변경 0 |
| staging target | org `synohgzwspodxmfemoks`, ref `ehnfrrmbkvlsbpvqcvkr`; phase 1 NOLOGIN roles 3 + `meta_ads_staging` PASS |
| exact migration bytes | image 내부 37 SQL/122,467 B, Node/Prisma/schema SHA 전부 manifest 일치 |
| local PG17 migration | PASS: PG 17.6 disposable/internal/no-host-port에서 37/37 |
| local catalog | PASS: table 54, enum 20, function 1, user trigger 2 |
| local grants | PASS: runtime any-DML 48, direct-DML deny 6, backup SELECT 54; DDL/TEMP/TRUNCATE/function 및 protected table expected deny |
| local backup probe | PASS: exact backup image `pg_dump 17.11`, custom dump non-empty와 `pg_restore --list`; restore/R2/provider runner NOT RUN |
| local containment/cleanup | PASS: 3 role NOLOGIN, synthetic DB/role rollback, matching container/network 0 |
| base DB residual | OPEN: 기존 `postgres` PUBLIC CONNECT+TEMP를 새 cluster-global role이 상속; full isolation 주장 금지 |
| provider credential/TLS/pooler/runner | admin TLS/session-pooler/custom-DB route PASS; role credential/runner **NOT RUN** |
| Auth/Storage/data/app readiness | **NOT RUN** |
| operationalReady | **false** |

설계는 `0908staging별도DB역할설계.md`, file/hash manifest는
`증거/0908-staging-db-roles/manifest.json`에 있다. local summary SHA-256은
`3e511bf60af1cacf9c009f637d4b6860138107241fbbd016543f57e270e1a922`, phase 1 완료 당시 20-file manifest
SHA-256은 `f212d5cee8df622dba506509b96a38c699d2b81c335978147b1664108e83367b`다. R2A 파일만 LF로
고정한 `.gitattributes`를 포함해 새 checkout의 raw-byte 해시 변동을 막았다. 앞선 실패·보정 14건은
summary에 남겼고 열다섯 번째 final exact saved runner만 현재 PASS evidence다. 독립 평가가 발견한
function/type global default privilege, unexpected non-table relation, PUBLIC/default ACL drift,
권한 거부 오류 분류 gap은 보정하고 각 negative rehearsal로 거부를 확인했다. bounded DDL timeout과
`pg_catalog` search path는 정적 보정 뒤 정상 full rehearsal에서 적용을 확인했다.

당시 다음 외부 mutation 후보는 진행상태 문서의 **G-DB-00 phase 1 — staging NOLOGIN role·별도 DB
bootstrap**이다. direct admin password를 채팅/Git 밖의 user-only client path에 준비하고 exact
endpoint read-only preflight를 통과한 뒤에만 별도 승인으로 실행한다. password reset, pooler fallback,
credential LOGIN, migration, grant, Auth/Storage/data는 포함하지 않는다. 승인 전 **NOT RUN**이다.

## 2026-09-08 staging Database Password availability

값을 출력하지 않은 local check에서 process DB credential/pgpass는 없었고, ignored
`apps/api/.env`의 `DATABASE_URL`은 local `127.0.0.1:55432/meta_ads_security_dev` 전용이었다.
Supabase API secret은 direct PostgreSQL Database Password가 아니다. exact staging
`ehnfrrmbkvlsbpvqcvkr` Dashboard는 password가 생성 뒤 보이지 않으며 reset이 기존 연결을 끊는다고
표시했다. UI reset과 direct login은 실행하지 않았다.

따라서 G-DB-00 phase 1은 password 회수 또는 진행상태 문서의 별도
**G-DB-PASSWORD-RESET-01** 승인·사용자 수행 전까지 **BLOCKED/NOT RUN**이다. reset은 staging
Database Password 하나만 대상으로 하며 production/API key/Auth/Storage/schema/data는 제외한다.
기존 direct/pooler client 중단 가능성과 이전 unknown password로의 즉시 rollback 불가를 수용해야
한다. reset 승인은 G-DB-00 mutation 승인으로 재사용하지 않는다. `operationalReady=false`다.

## 2026-09-08 user-performed reset / direct IPv6 결과

사용자는 staging Database Password reset과 client-side pgpass 저장을 완료했다고 보고했다.
비밀 비노출 검사에서 exact direct host/user entry 1개, owner/ACL 제한을 확인했다. 완료 시각과 reset
receipt는 **USER_REPORTED**다. API secret을 DB password로 사용하지 않았다.

공식 CA는 1,367 B / SHA-256
`700723581420dd1ac98fd7e9ac529f0ef210eadcaf87fc868a3ad7d114c2f3b7`였다. direct read-only
preflight는 auth 전 DNS에서 중단됐다. endpoint는 A 0/AAAA
`2406:da12:5ca:b700:35d7:cc97:c7a5:fc61`이고 local IPv6 default route 0/TCP 5432 false다.
따라서 password/TLS/SQL 결과는 **NOT RUN**이며 이 direct preflight 시도에서 main이 실행한
provider mutation은 0건이다. 사용자 reset은 위 **USER_REPORTED** 외부 변경 기록과 별개다.

Dashboard의 exact IPv4 대안은 session pooler
`aws-0-ap-northeast-2.pooler.supabase.com:5432`, DB `postgres`, user
`postgres.ehnfrrmbkvlsbpvqcvkr`다. 이 기록 시점의 pooler test는 별도
**G-DB-POOLER-RO-01** 승인 전 NOT RUN이었고, 후속 결과는 아래 PASS 절이 supersede한다. 해당
read-only gate는 G-DB-00 phase 1 mutation 승인과 분리된다. `operationalReady=false`다.

## 2026-09-08 G-DB-POOLER-RO-01 PASS와 pooler-safe bundle

승인된 exact staging session pooler base DB read-only preflight는 공식 CA/TLS `verify-full`, PG17
client로 exit 0이었다. `current_database/current_user/session_user=postgres`, server `170006`,
read-only on, target role/DB 0, base PUBLIC CONNECT+TEMP true를 확인하고 `ROLLBACK`했다. agent가
실행한 provider mutation은 0이며 G-DB-00은 실행하지 않았다. receipt SHA-256은
`9a4c19b6ea9a19738639dd021bfcf9ea29a4914194cd9447656aa225834bd25b`다.

pooler login suffix를 보존하도록 `20/30/40/90/99` reconnect를 DB-only reuse로 바꿨다. containment는
exact DB/role OID가 없으면 거부하고, valid input이면 새 DB를 `ALLOW_CONNECTIONS=false`로 닫고 세
role을 NOLOGIN으로 만든다. attempt 15 local PG17 full rehearsal이 exit 0이었다. summary SHA-256은
`3e511bf60af1cacf9c009f637d4b6860138107241fbbd016543f57e270e1a922`, phase-1 실행 전 15-file manifest SHA-256은
`0bec9e088f321a3d5308f52ae3efaa0e60f3b50975ee6adcf4327a48a232565d`다.

session pooler의 custom database routing은 공식 문서상 지원되지만 exact hosted project의 pooler 경유
CREATE DATABASE 허용과 생성 직후 전파 상한은 **UNKNOWN**이다. 다음 mutation gate는 진행상태의
superseding `G-DB-00 phase 1 — staging session-pooler NOLOGIN role·별도 DB bootstrap`이다.
새 DB용 exact pgpass entry와 별도 승인이 필요하며 transaction pooler/direct fallback/자동 DDL retry는
포함하지 않는다. provider CREATE/custom route는 **NOT RUN**, `operationalReady=false`다.

## 2026-09-08 G-DB-00 phase 1 provider PASS

사용자가 exact `완료 — G-DB-00 phase 1 승인`을 명시했고, main은 실행 직전 clean HEAD
`24e39d4561368a664c7d3452f0e6d2d4c647a6be`, 00/10/20 및 당시 manifest hash, 두 exact pgpass
entry의 nonempty/equal shape와 user-only ACL, 공식 CA와 PG17 image를 다시 확인했다. secret은
출력·hash·Git 기록하지 않았다. 독립 pre-execution 평가는 P0–P3 0건/GO였다.

provider와 무관한 일회용 local 검증은 첫 built-in superuser shape 거부, 둘째 init 임시 socket race를
PASS로 숨기지 않았고, 셋째 provider-shaped non-superuser admin exact wrapper에서 PASS했다. 이후
동일 wrapper로 staging session pooler/TLS `verify-full`에서 `00 → base ACL fingerprint → 10 → 20 →
read-only postcondition`을 한 번 실행했다. provider 실행은 2026-09-08 14:42:26–14:42:29 KST,
exit 0 / 3.0402403s, DDL/script retry 0, fallback 0이었다.

결과는 DB `meta_ads_staging` OID `25404`, owner `meta_ads_stg_migration`; runtime/migration/backup role
OID `25397/25399/25401`, 모두 NOLOGIN/restricted/connection limit 2였다. creator 관리 edge 3,
temporary self SET edge 0, DB/schema PUBLIC 권한 0, runtime/backup CREATE·TEMP false, global function/type
default ACL 2, public relation/enum/routine/user trigger 0이었다. base `postgres` ACL fingerprint는 전후
`94af03f1e723fcb05d1fd0a1590bbf99`로 같았다. admin custom DB routing과 persistence postcondition이
PASS했고, 검증 중 pool backend 1개는 client 종료 후 별도 read-only 집계에서 total 0이 됐다.

receipt SHA-256은 `d596ccd6bc43d479705d43cdd720221cdc71dec3c242f52ace1d70cdaba04059`,
phase 1 완료 당시 20-file manifest SHA-256은
`f212d5cee8df622dba506509b96a38c699d2b81c335978147b1664108e83367b`다. provider catalog mutation은
승인된 role 3/DB 1 범위뿐이며 app table/data/Auth/Storage/production은 바뀌지 않았다. containment와
destructive rollback도 실행하지 않았다. role LOGIN/SCRAM, role별 routing, migration/grant,
backup/restore/R2, Auth/Storage/app/Koyeb/production은 계속 **NOT RUN**이고
`operationalReady=false`다. raw stdout 파일은 보존하지 않아 실행값은 MAIN_REPORTED structured
receipt이며 독립 평가는 보존 파일/hash/assertion의 내부 일관성 범위다.

## 2026-09-08 G-DB-00 phase 2 local 준비

phase 1 뒤 provider 접속·mutation 없이 세 role credential 활성화 경로를 준비했다. actual
credential store는 아직 없고, staging role은 계속 NOLOGIN이다. 승인 후보는 DPAPI CurrentUser와
현재 user SID+SYSTEM ACL의 Git 외부 credential store, client-side PostgreSQL SCRAM verifier,
한 admin transaction의 password+LOGIN, exact session pooler에서 세 positive role login 및 final
admin read-only verify로 구성한다. wrong-password/cross-secret probe, transaction pooler, direct IPv6
fallback과 automatic retry는 금지한다.

최종 local PG17/self-signed TLS rehearsal은 exact saved activation/role/final SQL, forced pre-COMMIT
rollback, 세 `require_auth=scram-sha-256` login, TLS `verify-full`, exact role/OID/ACL/default ACL,
named PREPARE와 final session 0을 PASS했다. summary SHA-256은
`9e956deb85e71b6f14bdfae94005e51b806b6d26ac80d766cdae254b53ff3320`, provider runner SHA-256은
`15eb19263d06ba3babd4786692abf7a7b8ed907d89934237336becc650f51002`다. A/B/C 독립 정적·모의
평가는 신규 P0/P1/P2/P3 0건/GO다.

현재 30-file manifest SHA-256은
`0ab107b4cb9a3fe3a5733b07f9f145f4212537e0bc66eee184558236730743d1`이며 30개 path/bytes/hash와
schema가 일치한다. client/container stdout·stderr의 plaintext/verifier 0은 local 검증됐지만
Supavisor/PostgreSQL provider log의 verifier 저장·표시·마스킹 보장은 **UNKNOWN**이다. 새
user+database+session-mode propagation 상한도 UNKNOWN이므로 실패 시 재시도하지 않고 별도 승인된
read-only 분류·VerifyOnly 1회 또는 containment로 진행한다.

이 절은 새 G-DB-00 phase 2 승인을 대체하지 않는다. actual credential prepare, role LOGIN/SCRAM,
role별 provider routing, migration/grant, backup/restore/R2, Auth/Storage/app/Koyeb/production은 모두
**NOT RUN**이고 `operationalReady=false`다.

## 2026-09-09 superseding release review와 local remediation 경계

기존 f8a94fc G-REL-01은 09-09 10:12:03 KST owner review 누락으로 무효다. 새 승인된 exact
rescan에서도 Perl `CVE-2026-57432`의 High 승격과 수정 가능한 Multer/Sharp/CSV finding이 추가돼
과거 13 C/H 예외를 재사용하지 않는다. **P1-REL-001 OPEN / R1 BLOCKED / DB phase 2 BLOCKED**다.
public 3C/15H, maintenance 5개 각각 3C/14H이며 상세 severity·delta·hash는
`증거/0909-r1-f8a94fc-rescan-summary.json`과 진행상태의 09-09 절을 따른다.

Docker 사용자 시작 뒤 engine·고정 PG17/6-image metadata, OCI raw blob 36/36을 로컬 확인했다.
provider 연결·runtime hardening 재검증은 아니다. 이후 main이 pgpass ACL만 protected
current-user+SYSTEM-only로 복구하고 B가 실제 seong 권한에서 ACL·shape를 독립 확인했다.
비밀 내용은 변경하지 않았고 DPAPI store·execution-state·marker와 provider attempt는 여전히 0이다.

수정 가능한 dependency의 로컬 최소 보정과 source 회귀·build가 통과했다. 최초 timeout과
재검증 및 NOT RUN 경계는 `증거/0909-r1-source-remediation-validation.json`에 보존했다. 새 source/image의 전체 검증과
위험 수용 전에는 기존 release를 승격하지 않는다. source/lock 변경으로 기존 cc90a4e phase 2
승인 문구는 사용할 수 없고 새 exact bundle gate가 필요하다. 기존 phase 1 MAIN_REPORTED PASS와
phase 2 provider NOT RUN, actual credential 준비 0, operationalReady=false를 유지한다.

### 2026-09-09 새 53e87ba image 검증 완료 / 외부 scan 승인 검토 중단

main commit `53e87ba3ebf9c062463072ce5e4c8db77d8e9076`의 clean source로 6개 image build,
원본 OCI metadata 36/36 main/A 독립 검증, closure 6/6 및 1GiB synthetic probe 6/6이 통과했다.
최대 RSS 722.73MiB/cgroup 692.81MiB, OOM 0, own container 정리 완료. actual CMD/provider는 NOT RUN이다.
상세 exact tuple와 한계는 `증거/0909-r1-53e87ba-local-verification-summary.json`을 따른다.

새 npm audit/Scout는 명령 시작 전 자동 승인 검토가 새 private payload의 외부 destination 전송에
명시 승인이 없다는 이유로 거부했다. 실제 시도는 모두 0이고 우회하지 않았다. 새 exact gate는
`0909다음세션인계.md` §16이다. 과거 결과나 offline 0을 새 release scan PASS로 재사용하지 않는다.
R1 및 DB phase2 BLOCKED, 새 위험 수용 미승인, 기존 phase1 MAIN_REPORTED PASS만 유지하며
actual credential store 없음, phase2 provider attempt 0, operationalReady=false다.
### 2026-09-09 12시 새 scan 승인·부분 실행·정리 경고 중단

사용자가 새53e87ba exact scan을 승인했고 불필요한 Docker 용량 증가 방지를 요청했다.
이번 continuation의 build/export/delete/prune는 모두0이며 기존 image를 재사용했다.
npm online audit1회 valid JSON/exit0/취약점0. public Scout1회 valid SARIF/exit2,
3C/11H/7M/31L/4U이나 same-index 임시 archive file-in-use 정리 경고가 발생했다.
승인된 §16 중단 조건대로 나머지5 scan은 각0회/NOT RUN이다. public 재검사·자동retry·강제삭제0.

D 독립 report hash·집계 검증 완료. public의 Multer/Sharp/CSV finding 부재를 유지보수5개로 확대하지 않는다.
분석 결과 유효성과 정리 실패를 구분하며 원본 stopRequired/suspiciousError를 보존했다.
상세 증거는 `증거/0909-r1-53e87ba-partial-online-scan.json`, 공간 방침과 새 제한 예외 승인안은
`0909다음세션인계.md` §17이다. 새 release 위험 수용 미승인, R1/DBphase2 BLOCKED,
actual credential store 없음/provider attempt0/operationalReady=false를 유지한다.
### 2026-09-09 새53e87ba scan6/6·R1A20/20 완료 / 새 G-REL-01 미승인

남은5의 제한 예외를 사용자가 승인했고 각각1회 실행해 전체6-image scan 수집을 완료했다.
6개 공통3C/11H/7M/31L/4U(backup Low40), npm online0이며 Multer/Sharp/CSV finding은 모두 없다.
public/migration/backup의same-index file-in-use 정리 실패와 원본stop표시는 보존했다.
D가 raw SARIF/stderr·집계·동일C/H14·승인된예외를 독립 확인했고 실제실행정보는 MAIN_REPORTED다.
상세: `증거/0909-r1-53e87ba-completed-online-scan.json`.

새 maintenance baked ENTRYPOINT 5목적×4case(noenv/다른목적의비밀아닌표시값/다른명령/closure)
20/20을 no-network/read-only/nonroot/capdrop/NNP/PID256/1GiB/tmpfsnodev192MiB로 확인했다.
own CID/owner/image 대조 후 생성container20개 정리. 실제provider/credential검증은 NOT RUN이다.
상세: `증거/0909-r1a-53e87ba-maintenance-boundary.json`.

현재 P1-REL-001 OPEN/R1 BLOCKED다. 새source6-index/14C/H/두SARIF·owner·expiry·review·rollback에
한정한 **새 미승인 G-REL-01**을 `0909다음세션인계.md` §18에 준비했다.
과거 f8a94fc 예외는 review-miss로 무효이며 scan승인과새위험수용을 혼동하지 않는다.
DB bundle 재결속pending, phase2attempt0/actualcredentialstore없음/operationalReady=false다.
image build/추가OCIexport0이며 디스크여유약4.74GB, staleScout파일3개약692MB는 삭제하지 않았다.

## 19. 2026-09-09 13:57 KST 새 G-REL-01 승인 수신 / R2A 로컬 재결속

사용자가 §18 승인안에 “승인할게”라고 응답했다. main 확인 시각은 2026-09-09T04:57:20Z다.
D가 exact source53e87ba/6-image/공통14C/H/두SARIF·요약hash·owner/expiry 결속을 확인했고,
A의 13:58:19 KST 공식 Node/Bookworm/KEV 조회에서도 무효화 조건 변화를 발견하지 않았다.

새 P1-REL-001=ACCEPTED_TIME_BOUND, R1은 image/base/CVE 범위만 CONDITIONAL_PASS다.
첫 owner review=2026-09-09 13:57:20 KST, 다음 정기 review 시한=2026-09-10 13:57:20 KST,
expiry=2026-09-10 23:59:59 KST. 이후 최대24시간 간격 및 publish/deploy/maintenance 직전
추가 검토가 필요하며, 누락·기한·조건 변화 시 즉시 VOID한다. 자동 검토/예약은 만들지 않았다.
과거f8a94fc 예외를 되살린 것이 아니다. 승인 기록은 `증거/0909-g-rel-01-53e87ba-acceptance.json`이다.

R2A 로컬 재결속은 진행 가능하다. main은 manifest/설계/문서 및 release metadata만 소유하고,
B는 기존30-file/재결속범위, A는 공식release조건, D는 결속·gate의 독립 읽기 검토를 담당한다.
실제 DB phase2 exact승인·DPAPI credential준비·provider 실행은 포함하지 않는다.
operationalReady=false, phase2 provider attempt0/actualcredentialstore없음을 유지한다.

### 19.1 Docker 엔진 장애로 재결속 보류 — 2026-09-09 14:12 KST

새 G-REL-01 승인 기록은 완료했고 B의 독립 검토는 승인 경계 결함0, 기존 bundle30/30 raw bytes/hash 불변이다.
새 이미지 내부 release 재결속은 NOT RUN이다. 첫 로컬 image-read는 Docker pipe 부재로 CREATE_FAILED였고
컨테이너 생성·provider 접속 이전에 중단됐다. Docker Desktop 실행 후에도 엔진 조회가 15초 제한에서
응답하지 않았다. WSL 조회에서 docker-desktop Stopped를 확인했고 Docker 공식 start는 already running을
반환했다. 정상 restart 요청도 응답하지 않아 해당 대기 클라이언트만 중단했다. Docker 전용 WSL의
/bin/true 시작은 exit0이었으나 이후 엔진 API도 시간 초과였다. Docker/WSL 강제 종료·초기화·prune,
이미지 추가 다운로드/재빌드/삭제는 하지 않았다. 로컬 대기 명령 중단은 provider 실행 시도가 아니다.

C: 여유는 이번 확인에서 약32GB로 달라졌다. 원인은 확인하지 않았으며 이미지 잔존 여부를 추정하지 않는다.
엔진 정상화 후 exact migration/backup/PG17 이미지 잔존과 digest를 먼저 확인하고, 필요한 이미지가 없으면
보존된 exact OCI archive의 무결성을 검증한 뒤 로컬 복구 범위를 판단한다. 새 빌드나 scan을 자동 반복하지 않는다.

manifest/migration-release/phase2 runner·SQL·prepare·phase1 receipt는 변경하지 않았다. 기존 manifest는
과거 f8a94fc 결속 상태이므로 새 source53 실행 승인으로 사용하지 않는다. 새 release 재결속과 독립 평가,
새 commit/manifest/prepare/runner에 결속된 DB exact 승인안을 완성하기 전 actual credential 준비 및
phase2 Execute는 금지한다. provider phase2 attempt0, operationalReady=false를 유지한다.

D의 최종4파일 독립 읽기 검토는 새 수정 필요 결함0이며 기록 확정 가능이다. Docker 진단과 실제 상태는 MAIN_REPORTED다.


### Docker 상태 보정 — 2026-09-09 14:19 KST
엔진은 소켓 전용 런타임 폴더 보존 후 복구했고 Node 로컬 실행·정리 PASS다. 정상 재시작에서
같은 error1920이 재발하여 두 번째 복구했으며 영구 해결은 미확인이다. 현재 이미지6개·볼륨4개 보존,
container0. 다른 정리 세션이 pinned PG17도 삭제했으므로 해당 이미지 복구·검증은 pending이다.
상세는 인계 §20 및 증거/0909-docker-recovery.json. release 재결속 pending, phase2attempt0, operationalReady=false.

## 21. 2026-09-09 14:43 KST source53 R2A 재결속 완료 / 새 phase 2 exact gate

execution-bundle commit `c0fafda3522b352e699a8cfa80d3b43074723c97`, manifest
`312428954f1e6b4a355b199f37ab30e9a9d384ecfc306cd9444bf519a8832216`으로 source53/new migration·backup
image/canonical release ID를 재결속했다. local image raw-byte generate/verify, manifest schema, entries30/30,
phase1 receipt와 phase2 artifact 불변 검증이 PASS했다. official release 재검토 EARLY_VOID=false다.
pinned PG17은 exact digest pull1회로 복구했다. 새 build/export/delete/prune는0이다.

외부 변경 전 최종 pre-execution wave와 새 exact 승인 문구는 `0909다음세션인계.md` §21을 따른다.
actual credential 준비·provider phase2·migration은 NOT RUN이고 provider attempt0, operationalReady=false다.

## 22. 2026-09-09 15:19 KST G-DB-00 phase 2 Execute 1회 결과

exact 사전 wave PASS 뒤 actual credential3개 준비와 Execute1회를 수행했다. activation COMMIT ack 뒤
첫 role positive login psql exit2로 중단되어 `COMMITTED_VERIFY_FAILED`, verify0/3, retry0,
VerifyOnly0, marker 없음이다. temp/container cleanup은 PASS다. activation 재실행은 금지한다.

실패 기록 commit `b2af0238cc47c2937d8a8838963483035db7c9d7`, manifest
`59a57b4cdc8b71b1fe46ec4d81113cc9b4ea71879977b8861481381fb81d4dcf`다. 다음 별도 승인 대상은
admin read-only catalog1회와 15:18:30–15:21:00 KST Pooler Logs 조회이며 exact gate와 분기는
`0909다음세션인계.md` §22를 따른다. provider attempt1, credential3개 보존,
migration/containment/destructive rollback NOT RUN, operationalReady=false다.

## 23. 2026-09-09 15:50 KST phase 2 read-only 분류

승인된 exact admin catalog는 15:46:18–15:46:19 KST에 1회 실행되어 role/DB/OID/ACL/membership과
target session0을 모두 PASS했다. 승인된 Pooler Logs 범위 15:18:30–15:21:00 KST에는 6건이 있었고,
15:19:33 KST의 첫 runtime positive verification에 대응하는 password authentication failure와 one-off
auth query 경로가 확인됐다. 원문 로그와 비밀은 보존하지 않았다.

판정은 `CREDENTIAL_OR_VERIFIER_MISMATCH`이며 `REMOTE_ACTIVE_LIKELY`가 아니다. VerifyOnly, activation
재실행, 자동 재시도, wrong-password/cross-secret test는 금지한다. actual DPAPI credential3개와
`COMMITTED_VERIFY_FAILED` state는 변경 없이 보호 보존한다. 새 credential-recovery artifact의 로컬
검증과 별도 exact 승인 또는 별도 containment 승인 전에는 provider mutation을 추가하지 않는다.
Execute attempt1, VerifyOnly0, migration/containment/destructive rollback NOT RUN,
`operationalReady=false`다.
