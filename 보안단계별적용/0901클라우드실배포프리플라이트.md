# 2026-09-01 클라우드 실배포 R0 preflight

상태: **R0 부분 완료, exact staging/production target 미확정으로 BLOCKED,**
**staging/production PASS 0건, `operationalReady=false`**

조사 시각: `2026-09-01T15:41:52+09:00`  
조사 source: `codex/security-rbac@0c360b25dc58550fdbbf11a5b013bf893162e45e`

이 문서는 `0901클라우드실배포잔여작업계획.md` R0의 읽기 전용 기준선과
PASS/NOT RUN 경계를 기록한다. provider target mutation이나 production 준비 완료 증거가
아니다. secret, password, token, 이메일, 개인정보, credential URL은 기록하지 않았다.

## 1. Git과 파일 보존

- repository/worktree는
  `C:\Users\seong\Desktop\workspace\Meta-Ads-Performance-security-rbac`, branch는
  `codex/security-rbac`, 시작 HEAD는 위 full SHA다.
- 시작 시 staged/unstaged/untracked 변경은 모두 0건이었다.
- 계획서 작성 기준 `5a41079` 뒤에는 `4cad01c`, `4c835d2`, `0c360b2`의 문서 commit만
  있다. 실행 기준은 현재 worktree의 세 필수 문서다.
- `reset`, `checkout`, `clean`, `stash`, push, main merge, tag, remote 변경은 수행하지
  않았다.

## 2. Wave 1 읽기 전용 조사

계획서의 최대 4개 슬롯을 사용해 메인과 다음 역할을 병렬 배치했다.

| 역할 | 읽기 전용 결과 | 파일 변경/외부 mutation |
|---|---|---|
| A 플랫폼·컨테이너 | Docker/image/tooling과 공식 Node/Koyeb/registry 계약 조사 | 0 / 0 |
| B Supabase·DB | 정적 target binding, 37 migrations, env key 존재, maintenance gap 조사 | 0 / 0 |
| C 앱·업무검증 | Auth/RBAC/Storage/KPI/resource 기존 test와 actual-target gap 조사 | 0 / 0 |

세 역할 모두 실제 실행한 local inventory만 PASS로 분류하고 실제 staging/production
항목은 NOT RUN 또는 BLOCKED로 유지했다.

## 3. 로컬 platform·artifact 기준선

| 항목 | 결과 |
|---|---|
| Docker | PASS: `desktop-linux`, client/server `29.3.1`, Linux amd64, cgroup v2 |
| Docker host | PASS: 12 CPU, 8,182,243,328 B memory, overlayfs |
| build tools | PASS: buildx `0.32.1-desktop.1`, BuildKit `0.28.1`, Compose `5.1.1`, Scout `1.20.3` |
| local image | PASS: `meta-ads-cloud:64fa25f` 존재 |
| image ID | `sha256:846e7f84999e79dedb283116fbdf41fb00305f7ddc2f9918c574a04f02f5d93a` |
| linux/amd64 manifest | `sha256:030db1386e9004a7367ed572bbd607fe941705813b148388c752e5b5995110d0` |
| image boundary | PASS: amd64, user `node`, `8000/tcp` 하나, source revision `64fa25f9f8e71f14fb0bbd4d6a3bfd9c2bcb23eb` |
| compose static parse | PASS: service `app` 하나, `config --quiet --no-env-resolution` exit 0 |
| provider CLIs | `supabase`, `koyeb`, `gh`, `psql`, `pg_dump`, `pg_restore` 모두 ABSENT |

local image와 이전 1GiB harness PASS는 보존 기준선일 뿐 수정하지 않은 full production
CMD나 actual adapter/application route의 PASS가 아니다.

## 4. 공식 계약 재확인

확인일은 2026-09-01이다.

- Docker Hub의 현재 official `node:22-bookworm-slim`은 이전 release와 같은 index
  `sha256:83f487e0a63425e5b4d146fb5e5be574bcbe1b7b843d3ebafdd95eaf7767a7e5`,
  linux/amd64 manifest
  `sha256:4d676821dff059fd00d277ee4261ef34ea712317fed0737c03941481b5760c96`다.
  `docker buildx imagetools inspect --raw`로 registry metadata만 조회했고 image pull/build는
  하지 않았다. 공식 tag: <https://hub.docker.com/_/node/tags?name=22>
- Debian bookworm의 현재 `perl 5.36.0-7+deb12u3`와
  `openssl 3.0.20-1~deb12u2`는 계획서의 2 Critical/5 High CVE에 대한 fix가 아직 없다.
  예: <https://security-tracker.debian.org/tracker/CVE-2026-13221>,
  <https://security-tracker.debian.org/tracker/CVE-2026-63076>.
  새 release image를 rebuild/rescan하지 않았으므로 현재 CVE 개수는 NOT RUN이며
  `P1-REL-001`은 열려 있다.
- Koyeb standard `small`은 1 vCPU/1GB/10GB SSD, `$0.0144/hour`, `$10.71/month`다.
  `eco-small`은 0.5 vCPU/1GB/8GB SSD, `$0.0072/hour`, `$5.36/month`이며
  Washington/Frankfurt/Singapore에서만 제공된다. 세금과 실제 조직 비용은 생성 직전
  공식 화면에서 다시 확인해야 한다: <https://www.koyeb.com/docs/reference/instances>.
- Koyeb HTTP health check는 header를 지원하고 모든 공개 port의 check가 통과해야 새
  deployment가 traffic을 받는다. 실패하면 이전 healthy deployment가 계속 traffic을
  받고 첫 deployment라면 traffic은 0이다:
  <https://www.koyeb.com/docs/run-and-scale/health-checks>,
  <https://www.koyeb.com/docs/reference/deployments>.
- Koyeb secret은 service 환경변수와 private registry credential에 연결할 수 있다.
  그러나 health header가 secret-manager reference를 안전하게 소비한다는 공식 증거와
  Koyeb image 입력이 `repo@sha256`을 수용한다는 명시적 계약은 확인하지 못했다.
- Supabase는 persistent container에 direct connection을 권장하고 IPv4 제약이면 shared
  pooler session mode 5432를 대안으로 둔다. migration/backup은 direct가 기준이다:
  <https://supabase.com/docs/guides/database/connecting-to-postgres>.
- Supabase Pro daily DB backup 보존은 현재 7일이며 custom role password와 Storage object
  body는 DB backup에 포함되지 않는다:
  <https://supabase.com/docs/guides/platform/backups>.
- Supabase private Storage는 `storage.objects` RLS를 사용한다. service key는 RLS를
  완전히 우회하므로 계획대로 scoped runtime token과 별도 Auth admin key를 사용해야 한다:
  <https://supabase.com/docs/guides/storage/security/access-control>.

## 5. source·runner inventory

- cloud startup은 Auth/DB/Storage의 같은 20자 Supabase project ref, exact DB host/name/
  schema/runtime role, TLS `verify-full`, connection limit을 정적으로 강제한다.
- migration directory는 37개이고 head는
  `20260826020000_add_local_edge_request_nonces`다. 실제 staging/production history,
  checksum, pending, drift는 조회하지 않았다.
- `deploy/cloud/.env.local`은 없고 ignored `apps/api/.env`는 cloud 필수 target-binding,
  scoped Storage token, readiness, environment fingerprint key가 불완전하다. 값·길이·hash는
  출력하지 않았다.
- 기존 Auth/RBAC/CSRF/Origin, Supabase adapter, tombstone/compensation, canonical KPI,
  heavy-operation test는 재사용할 수 있다. 다만 현재 smoke/CLI는 local provider,
  LocalFileStorage, direct service 또는 synthetic harness에 묶여 있다.
- release-neutral actual-target E2E runner, cloud SUPER_ADMIN/user disposition runner,
  scoped Storage token rotation artifact, immutable `prisma migrate deploy` artifact,
  cross-system DB+object backup/restore 도구는 없다.

## 6. exact target와 gate inventory

### Staging

known candidate는 기존 개발 project `ehnfrrmbkvlsbpvqcvkr`, Seoul, Free `$0/month`다.
이를 staging으로 재사용할지 결정되지 않았고 실제 direct PostgreSQL/Auth/object lifecycle
PASS로 승격할 수 없다.

- 기존 project 재사용 결정/영향: `G-SUP-STG-01`
- 새 project/plan 생성: `G-SUP-STG-02`
- 어느 staging이든 read-only fingerprint: `G-SUP-STG-03`

필요한 비밀 아닌 값은 조직/project/ref/region/plan, direct 또는 session pooler 선택,
DB host/port/name/schema와 runtime/migration/backup role 이름, Auth Site URL/redirect/SMTP
시험 방식, private bucket/prefix/RLS/token subject·issuer·rotation owner, fixture cleanup,
DB/object backup·isolated restore target이다.

### Production

조직/project/ref/region/Pro plan, Auth/DB/Storage endpoint와 before-state, DB role/pool,
bucket/prefix/RLS/token rotation, backup/restore, RPO/RTO가 저장소에 확정돼 있지 않다.
첫 조회도 `G-SUP-PRD-01` 승인 전에는 실행하지 않는다.

### Koyeb/registry/domain/data

Koyeb 조직/app/service/region, `small` 또는 `eco-small`, fixed replica 1, 월 비용 상한,
registry 종류/조직/repository/visibility, publish identity와 deploy read identity, provider
domain/custom domain/DNS/TTL, source DB 관계, local file inventory 범위, 대표 KPI 기간과
expected 값이 미확정이다.

향후 mutation 순서는 `G-REG-01` → `G-REG-02` → `G-KOY-01` → `G-KOY-02` →
`G-KOY-03`이며 어느 것도 이번 R0에서 실행하지 않았다.

## 7. 현재 판정

| 범위 | 판정 |
|---|---|
| Git/local Docker/image/source inventory | PASS |
| 공식 가격/health/connection/backup 계약 | PASS(2026-09-01 조회) |
| exact staging target fingerprint | BLOCKED / NOT RUN |
| exact production target fingerprint | BLOCKED / NOT RUN |
| latest source image rebuild/SBOM/CVE scan | NOT RUN |
| full production CMD + actual adapter/app route | NOT RUN |
| Koyeb/registry/DNS/staging/production | NOT RUN |
| 실제 Auth/RBAC/Storage/KPI/backup/restore/rollback | NOT RUN |

열린 blocker는 계획서의 `P1-REL-001`, `P1-REL-002`, `P1-OPS-001`~`005`,
`P1-ISO-001`, `P2-OPS-001`을 유지한다. Koyeb exact digest 입력 계약이 확정되지 않은
`P1-PLAT-001`도 staging deploy 전 해소해야 한다. 다음 실행은 exact staging target
결정과 `G-SUP-STG-03`, exact production target 제공과 `G-SUP-PRD-01`을 서로 분리해
승인받는 것이다.
