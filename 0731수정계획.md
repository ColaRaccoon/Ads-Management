# 0731 수정 계획 - Cafe24 쿠폰 설정 및 실매출 순마진 반영

## 1. 문서 목적

이 문서는 다음 개발 세션이 별도의 대화 내용이나 추가 설명 없이 이 파일만 보고 기능을 구현할 수 있도록 작성한 상세 구현 계획서다.

이번 기능의 목표는 Cafe24 주문 CSV에 기록된 `결제수단=쿠폰` 신호와 프로그램에 미리 등록한 쿠폰 금액을 이용하여, 실제 사용된 것으로 추정되는 쿠폰 하나를 주문별로 선택하고 그 금액을 Cafe24 실매출 기반 순마진에서 차감하는 것이다.

이 문서는 구현 계획만 다룬다. 이 단계에서는 Prisma 스키마, API, 화면, 마진 계산 코드를 아직 수정하지 않는다.

---

## 2. 확정 요구사항

### 2.1 사용자 요구사항

1. 상품별로 쿠폰을 등록할 수 있어야 한다.
2. 하나의 상품에는 서로 다른 금액의 쿠폰이 여러 개 등록될 수 있어야 한다.
3. 전체 상품에 공통으로 적용되는 쿠폰도 등록할 수 있어야 한다.
   - 예: 전체 상품 1,000원 쿠폰
4. 쿠폰 금액은 날짜에 따라 바뀔 수 있으므로 적용 시작일과 종료일을 관리해야 한다.
5. Cafe24 주문 CSV의 `결제수단`에 정확한 `쿠폰` 토큰이 있을 때만 쿠폰 계산을 시도한다.
6. 주문금액과 실결제금액의 차이를 구하고, 해당 주문에 적용 가능한 쿠폰 후보 중 차이에 가장 맞는 쿠폰 하나를 선택한다.
7. 쿠폰 중복 사용은 없다는 운영 전제를 따른다.
   - 한 주문에 상품 쿠폰과 전체 상품 쿠폰을 동시에 합산하지 않는다.
   - 최종 선택되는 쿠폰 규칙은 항상 하나다.
8. 적립금, 네이버페이 포인트, 선불금 등 쿠폰 외 할인·결제 요소는 순마진에서 별도로 차감하지 않는다.
9. 실결제 차이 전체를 비용으로 잡지 않고, 최종 선택된 설정 쿠폰 금액만 순마진에서 차감한다.
10. 쿠폰 추정이 불완전한 주문은 계산 결과와 함께 추정/경고 상태를 확인할 수 있어야 한다.

### 2.2 이번 설계에서 고정하는 회계 처리

쿠폰은 기존 매출과 VAT를 다시 계산하지 않고 순마진 계산의 별도 차감 항목으로 처리한다.

```text
기존 총비용 = 상품 관련 비용 + 광고비
쿠폰 반영 총비용 = 기존 총비용 + 적용 쿠폰 금액

쿠폰 적용 전 순마진 = 매출 - 기존 총비용
최종 순마진 = 쿠폰 적용 전 순마진 - 적용 쿠폰 금액
```

따라서 다음 항목은 그대로 유지한다.

- `revenueKrw`: 현재 설정 판매가 기반 매출
- `grossCostKrw`: 현재 상품 원가·배송비·VAT·기타 비용
- `roas`: 현재 매출과 광고비 기준
- `cpaKrw`: 현재 광고비와 판매수량 기준
- VAT 계산 방식

다음 항목만 변경한다.

- `couponDeductionKrw`: 새로 추가
- `totalCostKrw`: 기존 총비용에 쿠폰 차감액을 포함
- `marginKrw`: 쿠폰 금액만큼 감소
- `marginRate`: 변경된 `marginKrw / revenueKrw`

쿠폰 때문에 VAT를 다시 계산하는 문제는 이번 기능의 범위에 포함하지 않는다. 사용자가 요청한 것은 쿠폰 금액 전액을 순마진에서 차감하는 방식이다.

---

## 3. 실제 데이터에서 확인된 사실

검증에 사용한 파일:

```text
C:\Users\seong\Downloads\didadi0729_20260730_261_9c0d\didadi0729_20260730_261_9c0d.csv
```

확인 결과:

- CSV 데이터 행: 214개
- 고유 주문: 209개
- 다품목 주문: 5개
- 에어스텝퍼 주문: 17개
- 에어스텝퍼 중 `결제수단`에 `쿠폰`이 포함된 주문: 9개
  - 주문금액과 실결제금액 차이 5,000원: 8개
  - 차이 5,500원: 1개
  - 5,500원 주문의 결제수단: `적립금,쿠폰,신용카드`

에어스텝퍼의 쿠폰 설정을 5,000원으로 등록한다면 최종 쿠폰 차감은 다음과 같아야 한다.

```text
5,000원 × 9건 = 45,000원
```

5,500원 차이 주문에서도 5,000원 쿠폰만 적용하고 나머지 500원은 쿠폰 외 잔여 차이로 무시한다.

### 3.1 반드시 `총 주문금액`을 새로 저장해야 하는 이유

현재 프로그램은 CSV의 `판매가`와 `총 결제금액`은 저장하지만 `총 주문금액`은 저장하지 않는다.

실제 에어스텝퍼 데이터에는 다음 사례가 있다.

```text
판매가:        86,500원
총 주문금액:  90,000원
총 결제금액:  85,000원
결제수단:      쿠폰,신용카드
```

`판매가 - 총 결제금액`으로 계산하면 차이는 1,500원이 되어 5,000원 쿠폰을 찾지 못한다.

`총 주문금액 - 총 결제금액`으로 계산하면 차이는 5,000원이 되어 실제 쿠폰과 일치한다.

따라서 쿠폰 후보를 찾기 위한 기준 금액은 반드시 다음과 같이 정의한다.

```text
관측 결제 차이 = CSV 총 주문금액 - CSV 총 결제금액
```

상품 설정의 판매가 또는 CSV 행의 `판매가`는 쿠폰 금액 추정의 기준으로 사용하지 않는다.

---

## 4. 현재 코드 구조와 문제점

### 4.1 Cafe24 CSV 파서

대상 파일:

- `apps/api/src/domain/cafe24-csv.ts`
- `apps/api/src/domain/cafe24-csv.spec.ts`

현재 파서는 다음 값을 읽는다.

- 주문번호
- 품목별 주문번호
- 총 결제금액
- 상품번호
- 상품명/옵션명
- 수량
- 판매가
- 결제수단
- 발주일

현재 문제:

- `총 주문금액` 컬럼을 읽지 않는다.
- `ParsedCafe24OrderRow`에 주문 총액이 없다.
- `sanitizedRawRow`에도 주문 총액이 남지 않는다.
- 스키마 버전은 `CAFE24_ORDER_SCHEMA_VERSION = 1`이다.

### 4.2 Cafe24 주문 저장

대상 파일:

- `apps/api/prisma/schema.prisma`
- `apps/api/src/sales/cafe24-uploads.service.ts`

현재 `Cafe24OrderLine`에는 다음 필드가 있다.

- `salePriceKrw`
- `totalPaidKrw`
- `paymentMethod`

현재 문제:

- `totalOrderKrw`가 없다.
- 기존 `rawRow`는 PII 제거 정책에 따라 파서가 선택한 컬럼만 저장하므로, 과거 업로드 데이터의 `rawRow`에서도 `총 주문금액`을 복구할 수 없다.
- 기존 업로드를 그대로 두고 DB 마이그레이션만 실행해서는 정확한 쿠폰 계산이 불가능하다.

### 4.3 Cafe24 실매출 마진 집계

대상 파일:

- `apps/api/src/domain/cafe24-sales-calculator.ts`
- `apps/api/src/sales/sales-metrics.service.ts`
- `apps/api/src/sales/sales-metrics.service.spec.ts`

현재 `SalesMetricsService.aggregateSales()`는 주문 행을 바로 상품별로 누적한다.

현재 문제:

- 쿠폰은 주문 단위인데 계산은 행 단위다.
- 다품목 주문에서 주문 총액과 총 결제금액이 각 행에 반복되면 행마다 차이를 계산할 위험이 있다.
- `paymentMethod`를 저장하지만 마진 계산에는 사용하지 않는다.
- 쿠폰 설정 모델과 쿠폰 선택 로직이 없다.

### 4.4 설정 화면

대상 파일:

- `apps/web/src/app/settings/products/page.tsx`
- `apps/api/src/products/product-rules.controller.ts`
- `apps/api/src/products/products.service.ts`

현재 설정 가능한 값:

- 상품
- 원가 규칙
- CPA 규칙
- Cafe24 상품 매칭 규칙

현재 문제:

- 상품별/전체 쿠폰 규칙을 생성·조회·수정·비활성화할 수 없다.
- 쿠폰 기간 이력을 관리할 화면이 없다.

### 4.5 결과 화면

대상 파일:

- `apps/web/src/app/sales/page.tsx`
- `apps/web/src/app/daily-report/page.tsx`

현재 문제:

- 쿠폰 차감액이 별도 컬럼으로 표시되지 않는다.
- 정확 일치와 추정 일치를 구분할 수 없다.
- 쿠폰 토큰이 있지만 규칙을 찾지 못한 주문을 확인할 수 없다.
- 판매 성과 엑셀에도 쿠폰 차감 컬럼이 없다.

---

## 5. 최종 도메인 설계

## 5.1 쿠폰 규칙 범위

Prisma enum을 추가한다.

```prisma
enum Cafe24CouponScope {
  GLOBAL
  PRODUCT

  @@map("cafe24_coupon_scope")
}
```

의미:

- `GLOBAL`: 모든 활성 상품에 적용 가능한 전체 상품 쿠폰
- `PRODUCT`: 특정 상품에만 적용 가능한 상품 쿠폰

쿠폰 규칙은 Cafe24 실제 주문에만 적용하므로 `Cafe24CouponRule`이라는 이름을 사용한다. 쿠팡 프로모션이나 Meta 광고 구매수 기반 추정 마진에는 사용하지 않는다.

## 5.2 쿠폰 규칙 모델

`apps/api/prisma/schema.prisma`에 다음 모델을 추가한다.

```prisma
model Cafe24CouponRule {
  id           String              @id @default(uuid()) @db.Uuid
  name         String
  scope        Cafe24CouponScope
  productId    String?             @map("product_id") @db.Uuid
  discountKrw  Decimal             @map("discount_krw") @db.Decimal(14, 2)
  priority     Int                 @default(100)
  validFrom    DateTime            @map("valid_from") @db.Date
  validTo      DateTime?           @map("valid_to") @db.Date
  isActive     Boolean             @default(true) @map("is_active")
  note         String?
  createdAt    DateTime            @default(now()) @map("created_at")
  updatedAt    DateTime            @updatedAt @map("updated_at")
  product      Product?            @relation(fields: [productId], references: [id], onDelete: Cascade)

  @@index([isActive, validFrom, validTo])
  @@index([productId, isActive, validFrom, validTo])
  @@index([discountKrw])
  @@map("cafe24_coupon_rules")
}
```

`Product` 모델에 관계를 추가한다.

```prisma
cafe24CouponRules Cafe24CouponRule[]
```

### 5.2.1 DB 체크 제약

마이그레이션 SQL에 Prisma만으로 표현하기 어려운 다음 체크 제약을 직접 추가한다.

```sql
CHECK ("discount_krw" > 0)

CHECK ("valid_to" IS NULL OR "valid_to" >= "valid_from")

CHECK (
  ("scope" = 'GLOBAL' AND "product_id" IS NULL)
  OR
  ("scope" = 'PRODUCT' AND "product_id" IS NOT NULL)
)
```

### 5.2.2 쿠폰 적용 단위

이번 버전의 쿠폰 금액은 항상 `주문당 1회`다.

- 주문 수량이 2개여도 쿠폰 금액을 2배로 곱하지 않는다.
- 같은 주문번호의 여러 행에 `쿠폰`이 반복돼도 한 번만 적용한다.
- 한 주문에서 후보 쿠폰을 여러 개 더하지 않는다.

수량당 쿠폰 또는 쿠폰 중복 사용은 이번 범위에 포함하지 않는다.

## 5.3 Cafe24 주문 총액 필드

`Cafe24OrderLine`에 nullable 필드를 추가한다.

```prisma
totalOrderKrw Decimal? @map("total_order_krw") @db.Decimal(14, 2)
```

nullable인 이유:

- 기존 DB 행은 값을 가지고 있지 않다.
- SQL에서 `판매가 × 수량`으로 임의 백필하면 실제 데이터의 배송비·옵션가·주문 총액 차이 때문에 잘못된 쿠폰이 선택될 수 있다.
- 과거 데이터는 재업로드 전까지 쿠폰 계산 불가 상태로 명확하게 남겨야 한다.

기존 행에 `0` 또는 추정값을 일괄 입력하지 않는다.

---

## 6. Cafe24 CSV 파서 변경

대상 파일:

- `apps/api/src/domain/cafe24-csv.ts`
- `apps/api/src/domain/cafe24-csv.spec.ts`
- `apps/api/src/sales/cafe24-uploads.service.ts`

### 6.1 컬럼 별칭

다음 별칭을 추가한다.

```ts
totalOrderKrw: ["총 주문금액", "총주문금액", "총 주문 금액"]
```

`ParsedCafe24OrderRow`에 추가:

```ts
totalOrderKrw: number | null;
```

### 6.2 필수 컬럼 처리

기존 업로드 호환성을 위해 `총 주문금액`은 CSV 전체 업로드를 막는 필수 컬럼으로 만들지 않는다.

권장 구조:

```ts
CAFE24_ORDER_COLUMN_ALIASES
CAFE24_ORDER_REQUIRED_KEYS
CAFE24_ORDER_REQUIRED_COLUMNS
CAFE24_ORDER_OPTIONAL_COLUMNS
```

`Cafe24CsvHeaderValidator.validate()`는 기존 필수 컬럼만 검증한다.

`총 주문금액`이 없으면:

- 파일 업로드 자체는 허용
- `totalOrderKrw = null`
- 미리보기의 `couponReady = false`
- `couponMissingColumns = ["총 주문금액"]`
- 쿠폰 토큰 주문은 계산 시 `MISSING_TOTAL_ORDER` 경고
- 해당 주문에는 쿠폰을 임의 적용하지 않음

### 6.3 숫자 파싱

- 컬럼이 존재하고 값이 비어 있으면 `null`
- 컬럼이 존재하고 숫자가 아니면 기존 `ParseIssue` 구조로 `INVALID_NUMBER`
- 음수 주문금액은 오류
- 소수점 입력은 DB에는 저장할 수 있지만 쿠폰 규칙 입력은 원 단위 정수만 허용

### 6.4 원본 정리 데이터

`sanitizedRawRow()` 결과에 `총 주문금액`을 포함한다.

이 값은 PII가 아니며 향후 계산 감사를 위해 필요하다.

### 6.5 스키마 버전

```ts
CAFE24_ORDER_SCHEMA_VERSION = 2
```

업로드 배치의 `columnSchema.previewSummary`에 다음을 추가한다.

```ts
couponReady: boolean;
couponMissingColumns: string[];
totalOrderKrw: number | null;
```

여기서 미리보기 전체 `totalOrderKrw` 합계는 다품목 주문에서 주문 총액이 반복될 수 있으므로 단순 행 합계라는 점을 표시하거나, 필요하지 않으면 합계를 노출하지 않는다. 쿠폰 준비 여부만 필수다.

### 6.6 저장

`Cafe24UploadsService.saveCafe24OrderLine()`에서 다음을 저장한다.

```ts
totalOrderKrw:
  input.parsedRow?.totalOrderKrw === null ||
  input.parsedRow?.totalOrderKrw === undefined
    ? null
    : new Prisma.Decimal(input.parsedRow.totalOrderKrw)
```

---

## 7. 쿠폰 후보 선택 알고리즘

새 순수 도메인 파일을 만든다.

- `apps/api/src/domain/cafe24-coupon-matcher.ts`
- `apps/api/src/domain/cafe24-coupon-matcher.spec.ts`

DB 호출은 넣지 않는다. 주문 행과 활성 쿠폰 규칙을 입력받아 결과만 반환하게 한다.

## 7.1 주문 단위 전처리

`SalesMetricsService`가 쿠폰 계산을 행 단위로 실행하면 안 된다.

먼저 조회된 `Cafe24OrderLine[]`을 `orderNo`로 그룹화한다.

각 주문 그룹에서:

1. 결제수단 토큰을 모든 행에서 모은다.
2. 주문일이 일치하는지 확인한다.
3. `totalOrderKrw` 값이 행마다 같은지 확인한다.
4. `totalPaidKrw` 값이 행마다 같은지 확인한다.
5. 상품 매칭이 완료된 상품 ID를 모은다.
6. 주문 전체에 대한 쿠폰 후보를 한 번만 선택한다.

### 7.1.1 결제수단 토큰화

```ts
function paymentTokens(paymentMethod: string | null): string[] {
  return String(paymentMethod ?? "")
    .normalize("NFKC")
    .split(",")
    .map((value) => value.trim())
    .filter(Boolean);
}
```

쿠폰 사용 신호:

```ts
tokens.includes("쿠폰")
```

다음은 쿠폰으로 보지 않는다.

- `쿠폰할인`
- `쿠폰사용`
- 단순 부분 문자열 포함
- `적립금`
- `선불금`
- `신용카드`

실제 Cafe24 값이 향후 달라지면 별칭을 명시적으로 추가해야 하며 부분 문자열 판정으로 넓히지 않는다.

## 7.2 관측 결제 차이

주문별로 반복되는 총액은 한 번만 사용한다.

```ts
observedGapKrw = totalOrderKrw - totalPaidKrw
```

규칙:

- 1원 이내 차이는 숫자 반올림 오차 허용 범위로 본다.
- `totalOrderKrw`가 없으면 계산하지 않는다.
- 주문 행 사이의 `totalOrderKrw`가 1원보다 크게 다르면 계산하지 않는다.
- 주문 행 사이의 `totalPaidKrw`가 1원보다 크게 다르면 계산하지 않는다.
- `observedGapKrw <= 0`이면 쿠폰 토큰이 있어도 규칙을 적용하지 않고 경고한다.
- 실결제액이 0원이어도 쿠폰 토큰과 쿠폰 후보가 있으면 후보 선택은 가능하다. 다만 잔여 차이가 매우 큰 `ESTIMATED` 결과가 된다.

## 7.3 날짜별 활성 후보

주문일을 기준으로 다음 조건을 모두 만족하는 규칙만 후보가 된다.

```text
isActive = true
validFrom <= orderDate
validTo가 없거나 validTo >= orderDate
```

범위별 후보:

- `GLOBAL`: 모든 상품 주문의 후보
- `PRODUCT`: 해당 주문에 포함된 매칭 완료 상품의 규칙만 후보

상품이 하나도 매칭되지 않은 주문은 쿠폰을 상품 마진에 배분할 수 없으므로 적용하지 않는다.

다품목 주문에서 일부 행만 상품 매칭이 실패했다면 주문 총액 전체를 어떤 상품에 배분해야 하는지 확정할 수 없으므로 쿠폰을 자동 적용하지 않고 `PARTIAL_PRODUCT_MAPPING` 경고를 반환한다.

## 7.4 단일 쿠폰 선택 규칙

쿠폰 중복 사용 없음이 확정 전제이므로 후보 금액을 합산하지 않는다.

선택 순서는 다음과 같이 고정한다.

### 1단계: 정확 일치

```text
abs(observedGapKrw - discountKrw) <= 1원
```

정확히 일치하는 금액이 있으면 그 금액의 규칙만 선택한다.

### 2단계: 잔여 차이가 있는 추정 일치

정확 일치가 없으면 다음 후보만 남긴다.

```text
discountKrw <= observedGapKrw + 1원
```

남은 후보 중 `discountKrw`가 가장 큰 쿠폰 하나를 선택한다.

이 정책을 사용하는 이유:

- 쿠폰 외 네이버페이 포인트·적립금이 실결제액을 더 낮추면 관측 차이는 실제 쿠폰보다 커질 수 있다.
- 에어스텝퍼 5,500원 차이에서 5,000원 쿠폰을 선택하고 500원은 무시할 수 있다.
- 여러 후보를 합산하지 않는다.
- 가능한 쿠폰 중 가장 큰 값을 선택하므로 순마진을 과도하게 높게 잡지 않는 보수적 계산이 된다.

### 3단계: 같은 금액 규칙의 우선순위

동일 금액의 쿠폰 규칙이 여러 개면 다음 순서로 한 규칙만 선택한다.

1. `PRODUCT` 규칙 우선
2. `priority` 숫자가 작은 규칙 우선
3. `validFrom`이 최근인 규칙 우선
4. `createdAt`이 최근인 규칙 우선
5. 최종적으로 `id` 오름차순

같은 금액이면 전체 순마진 차감액은 동일하다. 이 순위는 상품별 귀속과 화면 표시를 결정하기 위한 것이다.

### 4단계: 적용 금액과 잔여 차이

```ts
couponDeductionKrw = selectedRule.discountKrw
ignoredResidualKrw = Math.max(0, observedGapKrw - couponDeductionKrw)
```

`ignoredResidualKrw`는 네이버페이 포인트, 적립금, 선불금, 기타 가격 차이일 수 있으므로 순마진에서 차감하지 않는다.

## 7.5 결과 신뢰도

DB에 매칭 결과를 영구 저장하지 않고 계산 결과에 다음 상태를 반환한다.

```ts
type CouponMatchStatus =
  | "NO_COUPON"
  | "APPLIED"
  | "UNMATCHED";

type CouponMatchConfidence =
  | "NONE"
  | "EXACT"
  | "ESTIMATED";
```

상태 판정:

- 쿠폰 토큰 없음: `NO_COUPON / NONE`
- 쿠폰 금액과 차이가 1원 이내: `APPLIED / EXACT`
- 가장 큰 하위 후보를 선택했고 잔여 차이가 있음: `APPLIED / ESTIMATED`
- 계산 조건 미충족: `UNMATCHED / NONE`

경고 코드는 문자열 union으로 관리한다.

```ts
type CouponWarningCode =
  | "MISSING_TOTAL_ORDER"
  | "INCONSISTENT_TOTAL_ORDER"
  | "INCONSISTENT_TOTAL_PAID"
  | "NON_POSITIVE_GAP"
  | "NO_ACTIVE_COUPON_RULE"
  | "NO_PLAUSIBLE_COUPON_AMOUNT"
  | "UNMATCHED_PRODUCT"
  | "PARTIAL_PRODUCT_MAPPING"
  | "NON_POSITIVE_ALLOCATION_BASE"
  | "RULE_TIE_RESOLVED_BY_PRIORITY"
  | "IGNORED_RESIDUAL_EXISTS";
```

추천 반환 타입:

```ts
type Cafe24CouponResolution = {
  orderNo: string;
  orderDate: Date | null;
  couponDetected: boolean;
  status: CouponMatchStatus;
  confidence: CouponMatchConfidence;
  totalOrderKrw: number | null;
  totalPaidKrw: number | null;
  observedGapKrw: number | null;
  selectedRuleId: string | null;
  selectedRuleName: string | null;
  selectedScope: "GLOBAL" | "PRODUCT" | null;
  couponDeductionKrw: number;
  ignoredResidualKrw: number;
  allocationsByProductId: Map<string, number>;
  warningCodes: CouponWarningCode[];
  candidateRuleIds: string[];
};
```

## 7.6 다품목 주문의 상품별 귀속

선택된 규칙이 `PRODUCT`이면:

- 쿠폰 전액을 해당 `productId`에 귀속한다.

선택된 규칙이 `GLOBAL`이면:

- 단일 상품 주문: 해당 상품에 전액 귀속
- 다품목 주문: 상품별 CSV 행 금액 `salePriceKrw × quantity` 비중으로 안분
- 소수점 반올림 후 마지막 상품에 나머지를 배정하여 전체 합계가 정확히 `couponDeductionKrw`와 일치하도록 한다.
- 상품 정렬은 `productId` 오름차순으로 고정해 결과가 매번 같게 한다.
- 안분 기준 금액 합계가 0 이하이면 임의 배분하지 않고 `NON_POSITIVE_ALLOCATION_BASE`로 미적용 처리한다.

다품목 주문에 미매칭 행이 하나라도 있으면 자동 안분하지 않는다.

## 7.7 중복 적용 방지 불변식

다음 테스트 가능한 불변식을 지켜야 한다.

```text
주문 하나의 selectedRuleId는 최대 1개
주문 하나의 couponDeductionKrw는 최대 쿠폰 하나의 금액
상품별 allocations 합계 = 주문 couponDeductionKrw
전체 상품 couponDeductionKrw 합계 = 적용 주문 couponDeductionKrw 합계
쿠폰 토큰이 없는 주문의 couponDeductionKrw = 0
```

---

## 8. 쿠폰 규칙 API

새 파일:

- `apps/api/src/sales/cafe24-coupon-rules.controller.ts`
- `apps/api/src/sales/cafe24-coupon-rules.service.ts`
- `apps/api/src/sales/cafe24-coupon-rules.service.spec.ts`

`apps/api/src/sales/sales.module.ts`에 controller/provider를 등록한다.

### 8.1 조회

```http
GET /sales/cafe24/coupon-rules
GET /sales/cafe24/coupon-rules?productId=<uuid>
GET /sales/cafe24/coupon-rules?scope=GLOBAL
GET /sales/cafe24/coupon-rules?includeInactive=true
```

정렬:

1. 활성 규칙 우선
2. `GLOBAL` 후 `PRODUCT` 또는 화면 요구에 맞춘 명시 정렬
3. 상품 `sortOrder`
4. `priority` 오름차순
5. `validFrom` 내림차순
6. `createdAt` 내림차순

응답에는 `product` 기본 정보도 포함한다.

### 8.2 생성

```http
POST /sales/cafe24/coupon-rules
```

요청 예시:

```json
{
  "name": "에어스텝퍼 5천원 쿠폰",
  "scope": "PRODUCT",
  "productId": "product-uuid",
  "discountKrw": 5000,
  "priority": 100,
  "validFrom": "2026-07-01",
  "validTo": null,
  "isActive": true,
  "note": "자사몰 상품 쿠폰"
}
```

전체 상품 쿠폰:

```json
{
  "name": "전체 상품 1천원 쿠폰",
  "scope": "GLOBAL",
  "productId": null,
  "discountKrw": 1000,
  "priority": 100,
  "validFrom": "2026-07-01",
  "validTo": null,
  "isActive": true
}
```

### 8.3 수정/비활성화

```http
PATCH /sales/cafe24/coupon-rules/:id
```

수정 가능 필드:

- `name`
- `scope`
- `productId`
- `discountKrw`
- `priority`
- `validFrom`
- `validTo`
- `isActive`
- `note`

쿠폰 금액 변경 권장 흐름:

1. 기존 규칙의 `validTo`를 변경 전날로 설정
2. 새 금액 규칙을 새 `validFrom`으로 생성

과거 계산의 재현성을 위해 기존 규칙의 금액 자체를 수정하는 것보다 기간 이력을 새로 만드는 방식을 UI 안내문으로 권장한다.

물리 삭제 API는 이번 버전에 만들지 않는다.

- 삭제 대신 `isActive = false`
- 과거 기간 재계산을 위해 규칙 이력을 보존

### 8.4 API 검증

서비스에서 DB 체크 제약보다 먼저 친절한 오류를 반환한다.

- `name` 필수
- `scope`는 `GLOBAL | PRODUCT`
- `GLOBAL`이면 `productId`는 반드시 null
- `PRODUCT`이면 `productId` 필수
- 상품은 존재하고 활성 상태여야 함
- `discountKrw`는 1 이상의 원 단위 정수
- `priority`는 정수
- `validFrom` 필수
- `validTo >= validFrom`

오류 코드 예시:

- `COUPON_RULE_NOT_FOUND`
- `INVALID_COUPON_SCOPE`
- `COUPON_PRODUCT_REQUIRED`
- `GLOBAL_COUPON_PRODUCT_FORBIDDEN`
- `INVALID_COUPON_AMOUNT`
- `INVALID_COUPON_DATE_RANGE`
- `PRODUCT_NOT_FOUND`
- `PRODUCT_INACTIVE`

---

## 9. 상품 삭제/비활성화 연동

대상 파일:

- `apps/api/src/products/products.service.ts`
- `apps/api/src/products/products.service.spec.ts`

현재 상품 삭제 로직은 운영 데이터가 있으면 상품을 비활성화하고, 운영 데이터가 없으면 관련 설정을 지운 뒤 상품을 물리 삭제한다.

쿠폰 규칙도 같은 정책을 따른다.

운영 데이터가 있어 상품이 비활성화될 때:

```ts
tx.cafe24CouponRule.updateMany({
  where: { productId: id },
  data: { isActive: false }
});
```

운영 데이터가 없어 상품이 물리 삭제될 때:

```ts
tx.cafe24CouponRule.deleteMany({
  where: { productId: id }
});
```

쿠폰 규칙 개수만으로 상품을 운영 데이터 보유 상품으로 판정하지 않는다.

- 기존 원가 규칙과 CPA 규칙도 설정 데이터일 뿐 운영 데이터 카운트에 포함되지 않는다.
- 쿠폰 규칙도 같은 취급을 한다.

글로벌 쿠폰은 `productId = null`이므로 상품 삭제의 영향을 받지 않는다.

---

## 10. SalesMetricsService 통합

대상 파일:

- `apps/api/src/sales/sales-metrics.service.ts`
- `apps/api/src/sales/sales-metrics.service.spec.ts`

### 10.1 데이터 조회

기존 병렬 조회에 쿠폰 규칙 조회를 추가한다.

조회 범위:

```text
isActive = true
validFrom <= 조회 종료일
validTo가 없거나 validTo >= 조회 시작일
scope = GLOBAL 또는 조회 상품 ID에 해당하는 PRODUCT
```

성능을 위해 모든 과거 쿠폰을 무조건 가져오지 않는다.

### 10.2 처리 순서

기존:

```text
판매 행 조회
→ 상품별 aggregateSales
→ 광고비 결합
→ 마진 계산
```

변경:

```text
판매 행 조회
→ 주문번호별 그룹 생성
→ 주문별 쿠폰 하나 선택
→ 쿠폰 금액을 상품별로 귀속
→ 기존 상품별 판매/비용 집계
→ 상품별 쿠폰 귀속액 추가
→ 광고비 결합
→ 최종 순마진 계산
```

추천 메서드 분리:

```ts
resolveCouponOrders(
  salesLines,
  couponRules
): {
  resolutions: Cafe24CouponResolution[];
  deductionsByProductId: Map<string, CouponProductAccumulator>;
}
```

### 10.3 SalesAccumulator 확장

```ts
type SalesAccumulator = {
  // 기존 필드
  product: ProductSnapshot;
  quantity: number;
  revenueKrw: number;
  totalPaidKrw: number;
  grossCostKrw: number;
  lineCount: number;
  priceMismatchCount: number;
  missingCostRule: boolean;

  // 신규
  couponDeductionKrw: number;
  couponOrderCount: number;
  couponExactOrderCount: number;
  couponEstimatedOrderCount: number;
  couponUnmatchedOrderCount: number;
  couponIgnoredResidualKrw: number;
};
```

`totalPaidKrw`의 기존 행 합산 동작은 이번 기능에서 쿠폰 기준으로 사용하지 않는다. 쿠폰 판정은 반드시 주문 그룹의 중복 제거된 총액을 사용한다.

### 10.4 결과 계산

```ts
const baseTotalCostKrw =
  adSpendKrw === null || grossCostKrw === null
    ? null
    : grossCostKrw + adSpendKrw;

const marginBeforeCouponKrw =
  baseTotalCostKrw === null
    ? null
    : sales.revenueKrw - baseTotalCostKrw;

const totalCostKrw =
  baseTotalCostKrw === null
    ? null
    : baseTotalCostKrw + sales.couponDeductionKrw;

const marginKrw =
  marginBeforeCouponKrw === null
    ? null
    : marginBeforeCouponKrw - sales.couponDeductionKrw;
```

행 응답에 추가:

```ts
marginBeforeCouponKrw: number | null;
couponDeductionKrw: number;
couponOrderCount: number;
couponExactOrderCount: number;
couponEstimatedOrderCount: number;
couponUnmatchedOrderCount: number;
couponIgnoredResidualKrw: number;
```

### 10.5 응답 summary 확장

```ts
couponDetectedOrderCount: number;
couponAppliedOrderCount: number;
couponExactOrderCount: number;
couponEstimatedOrderCount: number;
couponUnmatchedOrderCount: number;
couponDeductionKrw: number;
couponIgnoredResidualKrw: number;
couponMissingTotalOrderCount: number;
```

### 10.6 RuleStatus 처리

현재 `RuleStatus`는 원가/환율/가격 불일치를 표시한다.

쿠폰 경고가 기존 원가 규칙 상태를 덮어쓰지 않도록 둘을 합치지 않는다.

권장:

- 기존 `ruleStatus` 유지
- 별도 `couponStatus`와 카운트 제공

```ts
type CouponAggregateStatus =
  | "NO_COUPON"
  | "EXACT"
  | "ESTIMATED"
  | "UNMATCHED"
  | "MIXED";
```

상품 기간 내 결과가 여러 상태이면 `MIXED`.

### 10.7 적용 범위 주의

이 기능은 `/sales/product-performance`의 Cafe24 실매출 기반 마진에만 적용한다.

다음에는 적용하지 않는다.

- `apps/api/src/domain/margin-calculator.ts`의 Meta 구매수 기반 추정 마진
- Meta 광고 관리자에서 가져온 구매수만으로 계산하는 대시보드
- 쿠팡 마진 계산

이유:

- 쿠폰 사용 신호는 Cafe24 주문 CSV에만 존재한다.
- Meta 구매수에는 어떤 주문이 쿠폰을 사용했는지 연결할 수 없다.

`/daily-report`에서 표시하는 “Cafe24 실매출 기반 마진”은 `/sales/product-performance`를 재사용하므로 자동으로 반영된다.

---

## 11. 쿠폰 계산 점검 API

불완전한 추정 방식을 숨기지 않기 위해 점검 엔드포인트를 추가한다.

대상 파일:

- `apps/api/src/sales/sales-metrics.controller.ts`
- `apps/api/src/sales/sales-metrics.service.ts`

```http
GET /sales/cafe24/coupon-matches?from=2026-07-30&to=2026-07-30
GET /sales/cafe24/coupon-matches?from=...&to=...&status=ESTIMATED
GET /sales/cafe24/coupon-matches?from=...&to=...&status=UNMATCHED
```

응답에 포함:

- 주문번호
- 주문일
- 포함 상품 이름/ID
- 결제수단
- 총 주문금액
- 총 결제금액
- 관측 차이
- 선택 쿠폰 이름/범위/금액
- 무시한 잔여 차이
- 정확/추정/미적용 상태
- 경고 코드

수령인 이름, 전화번호, 주소 등 PII는 절대 반환하지 않는다.

기본적으로 `NO_COUPON` 주문은 반환하지 않고, 쿠폰 토큰이 있는 주문만 반환한다.

---

## 12. 설정 UI

대상 파일:

- `apps/web/src/app/settings/products/page.tsx`
- 필요 시 `apps/web/src/lib/cafe24-coupon.ts`
- `apps/web/src/lib/api.ts`의 기존 `apiGet/apiPost/apiPatch` 재사용

현재 Product Settings 페이지에 “Cafe24 쿠폰 설정” 패널을 추가한다.

### 12.1 조회

```ts
apiGet<Cafe24CouponRule[]>("/sales/cafe24/coupon-rules?includeInactive=true")
```

React Query key:

```ts
["cafe24-coupon-rules"]
```

### 12.2 생성 폼

필드:

- 쿠폰명
- 범위: `상품별` / `전체 상품`
- 상품 선택
  - 상품별일 때 필수
  - 전체 상품일 때 비활성화 및 null 전송
- 쿠폰 금액
- 우선순위
- 적용 시작일
- 적용 종료일
- 메모
- 활성 여부

안내문:

```text
쿠폰은 주문당 하나만 적용됩니다.
같은 상품에 여러 쿠폰을 등록할 수 있으며,
총 주문금액과 총 결제금액 차이에 가장 맞는 활성 쿠폰 하나만 선택합니다.
쿠폰 금액이 바뀌면 기존 규칙의 종료일을 지정하고 새 규칙을 추가하세요.
```

### 12.3 규칙 목록

컬럼:

- 활성 상태
- 범위
- 상품
- 쿠폰명
- 금액
- 적용 기간
- 우선순위
- 메모
- 편집
- 비활성화

겹치는 기간의 쿠폰을 금지하지 않는다.

- 같은 상품에 여러 쿠폰이 있을 수 있다는 요구사항 때문
- 금액 차이와 우선순위로 하나를 선택

### 12.4 수정

`PATCH /sales/cafe24/coupon-rules/:id` 사용.

사용자가 과거 금액을 직접 수정하려 할 때 확인 문구:

```text
기존 규칙을 수정하면 과거 기간의 마진도 다시 계산될 수 있습니다.
금액 변경 이력을 유지하려면 기존 규칙의 종료일을 설정하고 새 규칙을 추가하세요.
```

### 12.5 캐시 무효화

생성/수정 성공 후:

```ts
invalidateQueries({ queryKey: ["cafe24-coupon-rules"] })
invalidateQueries({ queryKey: ["products"] })
invalidateQueries({ queryKey: ["sales-product-performance"] })
invalidateQueries({ queryKey: ["daily-report-sales-product-performance"] })
```

화면별 query key가 날짜를 포함하므로 prefix 무효화를 사용한다.

---

## 13. 판매 성과 화면과 엑셀

대상 파일:

- `apps/web/src/app/sales/page.tsx`

### 13.1 타입 확장

`SalesProductRow`와 summary에 API 신규 필드를 추가한다.

### 13.2 화면 컬럼

제품별 성과 테이블 순서:

1. 제품
2. 수량
3. 매출
4. 쿠폰 차감
5. 광고비
6. 총비용
7. 쿠폰 적용 전 마진
8. 최종 순마진
9. 광고수익률
10. 전환당 비용
11. 쿠폰 상태

쿠폰 상태 예:

```text
정확 8 / 추정 1 / 미적용 0
```

### 13.3 상단 요약

다음 지표를 경고/요약 영역에 추가한다.

- 쿠폰 감지 주문
- 쿠폰 적용 주문
- 정확 일치
- 추정 일치
- 미적용
- 총 쿠폰 차감
- 무시 잔여 차이

`미적용 > 0` 또는 `추정 > 0`이면 경고 색상으로 표시한다.

### 13.4 점검 테이블

`/sales/cafe24/coupon-matches`를 조회하여 같은 페이지 하단에 “쿠폰 계산 점검” 표를 추가한다.

기본 표시:

- `ESTIMATED`
- `UNMATCHED`

정확 일치는 펼치기/필터로 볼 수 있게 한다.

### 13.5 판매 성과 엑셀

`downloadSalesExcel()`에 컬럼을 추가한다.

- 쿠폰 차감
- 쿠폰 적용 전 마진
- 최종 순마진
- 정확 주문 수
- 추정 주문 수
- 미적용 주문 수

합계 행에서도 쿠폰 금액을 합산한다.

열 개수 변경에 맞춰 다음을 함께 수정한다.

- 헤더
- 데이터 행
- 합계 행
- 열 너비
- 자동 필터 범위

---

## 14. 데일리 리포트 UI

대상 파일:

- `apps/web/src/app/daily-report/page.tsx`

`SalesProductRow` 타입에 신규 필드를 추가한다.

“Cafe24 실매출 기반 마진” 표에 다음 컬럼을 추가한다.

- 쿠폰 차감
- 쿠폰 적용 전 마진
- 최종 순마진

기존 `실제 마진`은 `최종 순마진`으로 명확하게 표시한다.

상품별 보조 문구:

```text
쿠폰 적용 9건 · 정확 8건 · 추정 1건 · 무시 잔여 500원
```

미적용 주문이 있으면:

```text
쿠폰 기록이 있으나 금액을 찾지 못한 주문 N건
```

데일리 리포트의 Meta 광고 지표 자체는 변경하지 않는다.

---

## 15. 업로드 화면

대상 파일:

- `apps/web/src/app/uploads/page.tsx`

Cafe24 업로드 결과에 다음을 표시한다.

```text
쿠폰 계산 준비: 가능 / 불가능
누락 컬럼: 총 주문금액
```

`총 주문금액`이 없더라도 업로드는 성공할 수 있지만, 다음 경고를 명확히 표시한다.

```text
총 주문금액 컬럼이 없어 쿠폰 금액 자동 선택은 적용되지 않습니다.
```

---

## 16. 마이그레이션 및 기존 데이터 처리

새 마이그레이션 디렉터리 예시:

```text
apps/api/prisma/migrations/20260731000000_add_cafe24_coupon_rules/
```

`migration.sql` 작업:

1. `cafe24_coupon_scope` enum 생성
2. `cafe24_coupon_rules` 테이블 생성
3. 체크 제약 생성
4. 인덱스 생성
5. 상품 FK 생성
6. `cafe24_order_lines.total_order_krw` nullable 컬럼 추가

### 16.1 기존 주문행 백필 금지

다음과 같은 SQL 백필은 하지 않는다.

```sql
UPDATE cafe24_order_lines
SET total_order_krw = sale_price_krw * quantity;
```

이 방식은 실제 파일의 주문 총액과 다른 사례가 있어 잘못된 쿠폰을 선택한다.

### 16.2 기존 파일 재업로드

배포 후 쿠폰을 과거 주문에도 적용하려면 Cafe24 CSV를 다시 업로드해야 한다.

동일 파일을 `SKIP`으로 올리면 기존 완전 업로드 배치를 재사용하여 새 `totalOrderKrw`가 저장되지 않는다.

반드시 다음 중 하나를 사용한다.

```text
OVERWRITE 권장
NEW_VERSION 가능
```

권장 절차:

1. DB 마이그레이션
2. Prisma Client 재생성
3. 애플리케이션 배포
4. 쿠폰 규칙 등록
5. 기존 Cafe24 CSV를 `OVERWRITE`로 재업로드
6. 업로드 결과에서 `couponReady = true` 확인
7. 판매 성과에서 쿠폰 차감 검증

기존 업로드 배치를 삭제하지 않는다.

---

## 17. 테스트 계획

## 17.1 CSV 파서 테스트

파일:

- `apps/api/src/domain/cafe24-csv.spec.ts`

필수 사례:

1. `총 주문금액`을 정상 파싱
2. 쉼표 포함 숫자 파싱
3. CP949 파일 파싱
4. `총 주문금액`이 없어도 기존 필수 컬럼 검증 통과
5. 누락 시 `totalOrderKrw = null`
6. 잘못된 주문금액은 `INVALID_NUMBER`
7. `sanitizedRawRow`에 총 주문금액 포함
8. preview에 `couponReady/couponMissingColumns` 포함
9. 스키마 버전 2 확인

## 17.2 쿠폰 matcher 순수 단위 테스트

파일:

- `apps/api/src/domain/cafe24-coupon-matcher.spec.ts`

필수 사례:

1. 쿠폰 토큰이 없으면 0원
2. `쿠폰,신용카드` 토큰 인식
3. 부분 문자열은 인식하지 않음
4. 상품 쿠폰 5,000원과 차이 5,000원 정확 일치
5. 전체 쿠폰 1,000원과 차이 1,000원 정확 일치
6. 상품 5,000원 + 전체 1,000원 후보에서 차이 5,000원이면 상품 쿠폰 선택
7. 같은 후보에서 차이 1,000원이면 전체 쿠폰 선택
8. 차이 5,500원이면 5,000원 선택, 잔여 500원, `ESTIMATED`
9. 후보 1,000/3,000/5,000원에서 차이 4,500원이면 3,000원 선택
10. 어떤 후보도 차이 이하가 아니면 미적용
11. 날짜 범위 밖 규칙 제외
12. 비활성 규칙 제외
13. 같은 금액이면 상품 범위 우선
14. 같은 금액/범위이면 낮은 priority 우선
15. 규칙이 여러 개여도 합산하지 않음
16. 수량 2개여도 쿠폰을 한 번만 적용
17. 주문번호가 같은 여러 행에서도 한 번만 적용
18. 총 주문금액 누락 시 미적용
19. 주문 행별 총 주문금액 불일치 시 미적용
20. 주문 행별 총 결제금액 불일치 시 미적용
21. 음수/0 차이 미적용
22. 다품목 글로벌 쿠폰 비례 안분
23. 안분 합계가 쿠폰 금액과 정확히 일치
24. 미매칭 상품이 섞인 다품목 주문은 미적용
25. 글로벌 쿠폰 안분 기준 금액 합계가 0이면 미적용
26. 실결제 0원 + 쿠폰 후보는 추정 적용하고 큰 잔여 차이 반환

## 17.3 쿠폰 규칙 서비스 테스트

파일:

- `apps/api/src/sales/cafe24-coupon-rules.service.spec.ts`

필수 사례:

1. 상품별 쿠폰 생성
2. 글로벌 쿠폰 생성
3. 상품별인데 productId 누락 거부
4. 글로벌인데 productId 전달 거부
5. 0원/음수/소수 쿠폰 거부
6. 종료일이 시작일보다 이른 경우 거부
7. 비활성 상품 거부
8. 목록 필터
9. 규칙 수정
10. 비활성화
11. 없는 규칙 수정 404

## 17.4 SalesMetricsService 테스트

파일:

- `apps/api/src/sales/sales-metrics.service.spec.ts`

기존 fake Prisma에 추가:

- `cafe24CouponRule.findMany`
- Cafe24 line의 `totalOrderKrw`
- `Product.isActive`

필수 사례:

1. 쿠폰 토큰 없는 기존 테스트 결과 불변
2. 5,000원 정확 쿠폰이 `totalCostKrw`에 추가
3. `marginKrw`가 정확히 5,000원 감소
4. `marginBeforeCouponKrw`는 기존 마진과 일치
5. 5,500원 차이에서 5,000원만 차감
6. 잔여 500원은 비용에 포함하지 않음
7. 글로벌 1,000원 쿠폰
8. 동일 주문 다중 행 중복 차감 방지
9. 상품별 쿠폰 귀속
10. 글로벌 쿠폰 다품목 안분
11. 규칙 누락 시 마진 차감 없음 + unmatched count
12. legacy `totalOrderKrw = null` 차감 없음
13. summary 총액과 상품별 합계 일치
14. 광고비 null/환율 누락 시 기존 null 정책 유지
15. 쿠폰이 있어도 ROAS/CPA는 변경되지 않음

## 17.5 상품 삭제 테스트

파일:

- `apps/api/src/products/products.service.spec.ts`

필수 사례:

1. 운영 데이터 상품 비활성화 시 상품 쿠폰 규칙도 비활성화
2. 운영 데이터 없는 상품 삭제 시 상품 쿠폰 규칙 삭제
3. 글로벌 쿠폰은 영향 없음

## 17.6 웹 검증

자동:

- TypeScript 타입 검사
- Next build
- 기존 Vitest

수동:

1. 상품 쿠폰 생성
2. 글로벌 쿠폰 생성
3. 규칙 기간 수정
4. 규칙 비활성화
5. 판매 성과 쿠폰 컬럼
6. 합계 행
7. 데일리 리포트 쿠폰 컬럼
8. 쿠폰 점검 표
9. 엑셀 다운로드 컬럼/합계/서식
10. 모바일/좁은 화면에서 표 가로 스크롤

---

## 18. 실제 데이터 수용 테스트

설정:

```text
상품: 에어스텝퍼
상품 쿠폰: 5,000원
기간: 2026-07-30 포함

글로벌 쿠폰: 1,000원
기간: 2026-07-30 포함
```

파일을 `OVERWRITE`로 재업로드한다.

에어스텝퍼 기대값:

```text
쿠폰 감지 주문: 9
정확 일치: 8
추정 일치: 1
미적용: 0
쿠폰 차감 합계: 45,000원
무시 잔여 차이: 500원
```

검증할 핵심:

- 5,500원 차이 주문에서 5,000원만 순마진 차감
- 적립금으로 추정되는 500원은 무시
- 쿠폰 없는 7,000원 차이 주문은 차감 0원
- `선불금`으로 실결제 0원인 쿠폰 없는 주문은 차감 0원
- 쿠폰 차감 후 에어스텝퍼 최종 순마진이 기존보다 정확히 45,000원 감소

글로벌 1,000원 쿠폰 검증용 주문은 실제 데이터 또는 테스트 fixture에서 별도 생성한다.

---

## 19. 구현 순서

다음 순서를 바꾸지 않는 것을 권장한다.

### 1단계: DB/Prisma

1. `Cafe24CouponScope` 추가
2. `Cafe24CouponRule` 추가
3. `Product` relation 추가
4. `Cafe24OrderLine.totalOrderKrw` 추가
5. migration.sql 작성
6. Prisma validate/generate

### 2단계: CSV 수집

1. 총 주문금액 별칭 추가
2. parser/type/preview/schema version 수정
3. 업로드 저장 수정
4. 파서/업로드 테스트

### 3단계: 쿠폰 설정 API

1. coupon rule service
2. controller
3. SalesModule 등록
4. API 테스트
5. 상품 삭제 연동

### 4단계: 순수 matcher

1. 주문 그룹 입력 타입
2. 토큰 파서
3. 후보 필터
4. 단일 쿠폰 선택
5. 다품목 귀속
6. 경고/상태
7. 단위 테스트 완성

### 5단계: 마진 통합

1. 쿠폰 규칙 조회
2. 주문별 resolution 생성
3. 상품별 쿠폰 누적
4. totalCost/margin 변경
5. API row/summary 확장
6. 점검 endpoint
7. 서비스 테스트

### 6단계: UI

1. 설정 폼/목록/수정/비활성화
2. 판매 성과 컬럼/요약/점검표
3. 판매 엑셀
4. 데일리 리포트
5. 업로드 준비 상태

### 7단계: 회귀 검증

1. 전체 테스트
2. lint
3. build
4. 실제 CSV OVERWRITE 재업로드
5. 에어스텝퍼 45,000원 수용 테스트

---

## 20. 예상 변경 파일 목록

### 신규

- `apps/api/prisma/migrations/20260731000000_add_cafe24_coupon_rules/migration.sql`
- `apps/api/src/domain/cafe24-coupon-matcher.ts`
- `apps/api/src/domain/cafe24-coupon-matcher.spec.ts`
- `apps/api/src/sales/cafe24-coupon-rules.controller.ts`
- `apps/api/src/sales/cafe24-coupon-rules.service.ts`
- `apps/api/src/sales/cafe24-coupon-rules.service.spec.ts`

### 수정

- `apps/api/prisma/schema.prisma`
- `apps/api/src/domain/cafe24-csv.ts`
- `apps/api/src/domain/cafe24-csv.spec.ts`
- `apps/api/src/sales/cafe24-uploads.service.ts`
- `apps/api/src/sales/cafe24-uploads.service.spec.ts`
- `apps/api/src/sales/sales.module.ts`
- `apps/api/src/sales/sales-metrics.controller.ts`
- `apps/api/src/sales/sales-metrics.service.ts`
- `apps/api/src/sales/sales-metrics.service.spec.ts`
- `apps/api/src/products/products.service.ts`
- `apps/api/src/products/products.service.spec.ts`
- `apps/web/src/app/settings/products/page.tsx`
- `apps/web/src/app/sales/page.tsx`
- `apps/web/src/app/daily-report/page.tsx`
- `apps/web/src/app/uploads/page.tsx`

필요 시 타입/헬퍼 분리를 위해 추가 가능:

- `apps/web/src/lib/cafe24-coupon.ts`
- `apps/web/src/lib/cafe24-coupon.spec.ts`

---

## 21. 검증 명령

워크스페이스 루트에서 실행:

```powershell
npm run prisma:validate
npm run prisma:generate
npm run lint
npm run test
npm run build
```

빠른 API 집중 테스트:

```powershell
npm --workspace @meta-ads-performance/api test -- cafe24-csv.spec.ts
npm --workspace @meta-ads-performance/api test -- cafe24-coupon-matcher.spec.ts
npm --workspace @meta-ads-performance/api test -- cafe24-coupon-rules.service.spec.ts
npm --workspace @meta-ads-performance/api test -- sales-metrics.service.spec.ts
npm --workspace @meta-ads-performance/api test -- products.service.spec.ts
```

현재 Vitest CLI 전달 방식과 맞지 않으면 전체 API 테스트를 실행한다.

```powershell
npm --workspace @meta-ads-performance/api test
```

---

## 22. 완료 기준

다음 조건을 모두 만족해야 완료다.

### 데이터/설정

- 상품별 쿠폰 여러 개 등록 가능
- 글로벌 쿠폰 등록 가능
- 날짜별 쿠폰 이력 가능
- 규칙 비활성화 가능
- 쿠폰 금액은 주문당 한 번만 적용

### 수집

- CSV `총 주문금액` 저장
- CSV `총 결제금액` 저장
- 결제수단의 정확한 `쿠폰` 토큰 판정
- 구형 업로드는 명확히 계산 불가 표시

### 계산

- 정확 금액 쿠폰 선택
- 잔여 차이가 있을 때 가장 큰 하위 쿠폰 하나 선택
- 쿠폰 후보 합산 금지
- 포인트로 추정되는 잔여 차이 무시
- 다품목 주문 중복 차감 금지
- 상품별 쿠폰 귀속
- 글로벌 쿠폰 안분
- `totalCostKrw`와 `marginKrw` 불변식 유지

### 표시

- 상품별 쿠폰 차감 표시
- 정확/추정/미적용 표시
- 점검 주문 확인 가능
- 데일리 리포트 반영
- 판매 엑셀 반영

### 실제 데이터

- 2026-07-30 에어스텝퍼 쿠폰 9건
- 총 쿠폰 차감 45,000원
- 무시 잔여 500원
- 쿠폰 없는 결제 차이는 차감하지 않음

### 품질

- Prisma validate 성공
- TypeScript lint 성공
- 전체 테스트 성공
- API/Web build 성공

---

## 23. 알려진 한계와 보완 방향

이번 방식은 현재 Cafe24 CSV로 가능한 최선의 추정이지만 완전한 실정산 계산은 아니다.

### 23.1 네이버페이 포인트가 섞인 경우

관측 차이가 7,000원이고 후보가 다음과 같다고 가정한다.

```text
상품 쿠폰 5,000원
글로벌 쿠폰 1,000원
```

알고리즘은 가장 큰 하위 후보인 5,000원을 선택한다.

하지만 실제로는 글로벌 1,000원 쿠폰 + 네이버페이 포인트 6,000원일 가능성도 있다. 현재 CSV에는 이를 분리할 금액 컬럼이 없으므로 완전한 판정은 불가능하다.

보완:

- 결과를 `ESTIMATED`로 표시
- 관측 차이와 무시 잔여를 노출
- 점검 화면에서 사용자가 확인
- 순마진에 관측 차이 전체가 아니라 설정 쿠폰 금액 하나만 반영

### 23.2 같은 금액 쿠폰

상품 쿠폰과 글로벌 쿠폰이 둘 다 1,000원이라면 어떤 쿠폰인지 확정할 수 없다.

전체 순마진은 동일하게 1,000원 감소하므로 금액 정확도에는 영향이 없다.

상품별 귀속은 다음 우선순위로 결정한다.

```text
PRODUCT 우선 → priority → 최근 validFrom → 최근 createdAt
```

### 23.3 미등록 상품 쿠폰

실제로 5,000원 상품 쿠폰이 있지만 프로그램에는 글로벌 1,000원 쿠폰만 등록된 경우, 프로그램은 1,000원 후보를 추정 적용할 수 있다.

운영 보완:

- 실제 운영 중인 상품별 쿠폰을 빠짐없이 등록
- `ESTIMATED`와 큰 잔여 차이 주문을 정기 확인
- 쿠폰 설정 누락을 점검 화면에서 발견

### 23.4 비정액 쿠폰

퍼센트 쿠폰, 최대 할인 한도가 있는 쿠폰, 수량별 쿠폰은 이번 범위에 포함하지 않는다.

향후 필요하면 규칙 모델에 다음을 추가하는 별도 기능으로 구현한다.

- 할인 유형: 정액/정률
- 할인율
- 최대 할인액
- 최소 주문금액
- 적용 수량

### 23.5 수동 주문별 확정 기능

이번 버전은 자동 추정과 경고까지만 구현한다.

주문별로 사용자가 쿠폰을 직접 확정하는 override 테이블은 포함하지 않는다. 추후 추정 오차가 실제 운영에 큰 영향을 주는 것으로 확인되면 다음 모델을 추가할 수 있다.

```text
Cafe24CouponManualOverride
orderNo
couponRuleId
couponDeductionKrw
note
```

---

## 24. 최종 결정 요약

구현을 막는 미결사항은 없다. 이번 계획에서는 다음 정책을 최종 결정으로 사용한다.

1. 쿠폰은 Cafe24 주문의 정확한 `쿠폰` 결제수단 토큰으로 감지한다.
2. 실제 차이는 `총 주문금액 - 총 결제금액`으로 계산한다.
3. 쿠폰 금액은 프로그램에 상품별/글로벌/기간별로 미리 등록한다.
4. 정확한 금액 후보를 우선 선택한다.
5. 정확 후보가 없으면 관측 차이 이하에서 가장 큰 쿠폰 하나를 선택한다.
6. 쿠폰은 주문당 하나만 적용하며 후보를 합산하지 않는다.
7. 선택 쿠폰 금액만 순마진에서 차감한다.
8. 나머지 차이는 포인트 등으로 보고 순마진에서 무시한다.
9. 추정 결과와 잔여 차이를 반드시 화면에 공개한다.
10. 기존 DB 행은 임의 백필하지 않고 원본 CSV를 `OVERWRITE`로 재업로드한다.
