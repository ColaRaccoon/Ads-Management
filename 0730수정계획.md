# 0730 쿠팡 데일리 리포트 XLSX 상품그룹 색상·판매수 증감 강조 수정 계획

> 작성일: 2026-07-30  
> 대상 저장소: `Meta-Ads-Performance`  
> 작성 시 기준 브랜치: `main`  
> 작성 시 기준 커밋: `1269135 feat: 모든 날짜 한국시간 기준 어제로 기본값 변경`  
> 목적: 다음 구현 세션이 이 파일 하나만 읽고도 사용자 의도, 현재 구조, 변경 범위, 구현 순서, 테스트와 완료 기준을 모두 파악할 수 있게 한다.

---

## 0. 다음 세션에서 가장 먼저 알아야 할 결론

이번 작업은 **쿠팡 데일리 리포트 화면의 XLSX 출력 양식만** 목업과 같은 제품 중심 리포트로 개편하는 작업이다.

반드시 완성해야 하는 결과는 다음과 같다.

1. XLSX에는 아래 10개 열만 표시한다.
   1. 제품명
   2. 매출
   3. 전일자 매출
   4. 판매수
   5. 전일자 판매수
   6. 가구매 수
   7. 광고수익률
   8. 오가닉매출
   9. 최종순이익
   10. 전일자 최종 순이익
2. 한 상품그룹의 `그룹합계`, 소속 `옵션`, 그 그룹의 `기타사항` 행은 **모두 정확히 같은 배경색**을 사용한다.
3. 다음 상품그룹 또는 단일제품 블록은 다른 파스텔 배경색을 사용해 제품 경계를 쉽게 알아볼 수 있게 한다.
4. 그룹합계는 배경색을 더 진하게 바꾸지 않는다. 옵션과 같은 배경색을 유지하고, 굵은 글씨·상단 경계선·제품명 표현으로만 합계임을 구분한다.
5. 옵션 제품명은 그룹 아래에 속한 행임을 알 수 있도록 들여쓴다.
6. 오늘 판매수가 전일 판매수보다 많으면 **오늘 `판매수` 셀만 파란색**으로 표시한다.
7. 오늘 판매수가 전일 판매수보다 적고, 하락 수량이 10보다 클 때만 **오늘 `판매수` 셀만 노란색**으로 표시한다.
8. 하락 수량이 정확히 10이면 노란색으로 표시하지 않는다.
9. 판매수 증감 강조색은 상품그룹 배경색보다 우선한다. 강조 대상이 아닌 다른 셀들은 원래 그룹 배경색을 유지한다.
10. 판매수와 금액은 계속 숫자 셀이어야 한다. `▲`, `▼`, 문자열 접두사 등을 값에 붙여 숫자를 텍스트로 바꾸면 안 된다.
11. 색상은 XLSX 생성 시 계산해 넣는 정적 스타일이다. 사용자가 Excel에서 값을 수정했을 때 자동으로 색이 바뀌는 Excel 조건부 서식은 이번 범위가 아니다.
12. 기존 CSV 17개 열, 화면 테이블, 인쇄 화면, 계산식, 필터 동작은 변경하지 않는다.

### 0.1 하락 조건의 확정 해석

사용자의 “그 값이 10보다 클 때만 표기대상”은 **하락한 수량의 절댓값**을 뜻하는 것으로 확정한다.

```ts
const delta = currentSalesQuantity - previousSalesQuantity;
const decreaseAmount = previousSalesQuantity - currentSalesQuantity;

if (delta > 0) {
  // 오늘 판매수가 증가함: 파란색
}

if (decreaseAmount > 10) {
  // 오늘 판매수가 11개 이상 감소함: 노란색
}
```

즉, 기준은 `현재 판매수` 자체가 10보다 큰지가 아니라 `전일 판매수 - 현재 판매수`가 10보다 큰지다.

### 0.2 색상 판정표

| 오늘 판매수 | 전일 판매수 | 변화 | 오늘 판매수 셀 |
|---:|---:|---:|---|
| 18 | 15 | +3 | 파란색 |
| 1 | 0 | +1 | 파란색 |
| 13 | 13 | 0 | 그룹 기본색 |
| 12 | 13 | -1 | 그룹 기본색 |
| 10 | 20 | -10 | 그룹 기본색 |
| 9 | 20 | -11 | 노란색 |
| 0 | 11 | -11 | 노란색 |
| 0 | 10 | -10 | 그룹 기본색 |

증가 쪽에는 별도의 10개 임계값을 적용하지 않는다. **1개라도 증가하면 파란색**이다.

### 0.3 기존 계획서와의 우선순위

저장소 루트의 기존 `쿠팡_데일리_리포트_카테고리_필터_확장_작업계획서.md`에는 CSV와 XLSX가 같은 17개 열을 사용한다고 기록되어 있다.

이번 문서는 그중 **XLSX 출력 양식에 한해 더 최신의 사용자 결정**을 담고 있으므로 다음 규칙이 우선한다.

- CSV: 기존 17개 열 유지
- XLSX: 이번 문서의 10개 열과 시각 규칙 사용

다른 카테고리·필터·검색·집계 규칙은 기존 구현을 그대로 유지한다.

---

## 1. 사용자의 의도를 구현 언어로 번역한 완료 상태

### 1.1 사용자가 보게 될 XLSX 구조

XLSX 첫 행은 10개 열의 헤더다.

```text
제품명 | 매출 | 전일자 매출 | 판매수 | 전일자 판매수 | 가구매 수 |
광고수익률 | 오가닉매출 | 최종순이익 | 전일자 최종 순이익
```

그 아래 행 순서는 다음과 같다.

```text
전체합계 또는 선택합계

상품그룹 A 합계
  상품그룹 A 옵션 1
  상품그룹 A 옵션 2
  상품그룹 A 기타사항(존재할 때)

상품그룹 B 합계
  상품그룹 B 옵션 1
  상품그룹 B 옵션 2
  상품그룹 B 기타사항(존재할 때)

단일제품 C
  단일제품 C 기타사항(존재할 때)
```

그룹 접힘 여부와 관계없이 XLSX에는 현재 필터 결과에 속한 모든 옵션을 포함한다. 이는 현재 `flattenDailyReportExportRows()`의 동작을 유지하는 것이다.

### 1.2 상품 블록의 정의

색상 배정 단위는 “화면의 최상위 제품 행 하나”다.

- `GROUP` 행:
  - 그룹합계
  - 모든 자식 옵션
  - 그룹 기타사항
  - 위 행들이 하나의 블록이며 같은 배경색을 쓴다.
- `PRODUCT` 단일제품 행:
  - 단일제품 본행
  - 단일제품 기타사항
  - 위 행들이 하나의 블록이며 같은 배경색을 쓴다.
- 전체합계/선택합계:
  - 상품 블록이 아니다.
  - 중립적인 합계 배경색을 쓴다.

### 1.3 행별 표현

#### 전체합계/선택합계

- 배경: 중립 회색
- 글씨: 굵게
- 제품명:
  - 계산이 완전하면 현재의 `전체 합계`
  - 필터 모드도 기존 의미를 유지하되 `rowKind`는 `선택합계`
  - 계산이 불완전하면 현재의 `계산 가능한 상품 부분 합계 (일부 상품 제외)`
- 판매수 증감 파란색/노란색 강조는 적용하지 않는다.

#### 그룹합계

- 같은 그룹 옵션과 **동일한 배경색**
- 글씨 굵게
- 행 상단에 조금 더 강한 경계선
- XLSX의 제품명 셀은 합계임을 알 수 있게 다음처럼 표현한다.

```text
Σ 웨이브 밸런스바 (그룹 합계 · 옵션 3개)
```

이 표현은 XLSX 전용 변환에서 만든다. 공용 `CoupangDailyExportRow.productName`을 바꾸지 않으므로 CSV의 기존 제품명은 그대로 유지한다.

`Σ`와 괄호 설명은 제품명 문자열이어도 된다. 판매수·금액 값은 숫자 셀을 유지한다.

#### 옵션

- 부모 그룹합계와 동일한 배경색
- 일반 굵기
- 제품명은 셀 정렬의 `indent=1`을 우선 사용한다.
- 들여쓰기 지원이 어렵다고 문자열 앞에 공백을 붙이는 방식으로 우회하지 않는다.
  - 현재 inline string XML은 선행 공백 보존을 위한 `xml:space="preserve"`를 쓰지 않기 때문이다.
- 대체 표현이 필요하면 공백이 아닌 `↳ 옵션명`을 사용하되, 가능한 한 Excel 정렬 들여쓰기를 구현한다.

#### 단일제품

- 단일제품 자체가 한 개의 상품 블록이다.
- 인접한 상품 블록과 다른 팔레트 색을 적용한다.
- 최상위 제품이므로 굵게 표현한다.

#### 기타사항

- 기존처럼 제품명 열에 `기타사항: ...`을 기록하고 나머지 지표 셀은 빈 값으로 유지한다.
- 해당 부모 그룹 또는 단일제품과 동일한 배경색을 사용한다.
- 필요하면 기울임꼴을 사용할 수 있으나 필수는 아니다.
- 메모 데이터 자체를 삭제하거나 합계에 포함시키지 않는다.

### 1.4 상품 블록 사이의 간격

목업과 같은 제품 경계를 위해 전체합계 뒤와 각 최상위 상품 블록 사이에 작은 빈 구분 행을 넣는다.

- 높이: 6pt 전후
- 배경: 흰색
- 테두리: 없음
- 첫 헤더 위 또는 마지막 블록 뒤에는 추가하지 않는다.
- CSV에는 구분 행을 넣지 않는다.
- 필터 데이터 범위 안에 포함되더라도 값이 모두 빈 행이어야 한다.

구분 행을 넣는 것이 Excel 자동 필터 사용성에 심각한 문제를 만들면 다음 대안 순서로 처리한다.

1. 명시적인 자동 필터 범위를 유지한 채 빈 행을 포함해 정상 작동하는지 실제 Excel에서 확인한다.
2. 문제가 확인된 경우 빈 행을 제거하고, 각 상품 블록 시작 행의 두꺼운 상단 경계선으로 동일한 시각 간격을 구현한다.

검증 없이 처음부터 구분 행을 생략하지 않는다.

---

## 2. 열과 데이터 원천의 확정 계약

### 2.1 XLSX 열 정의

| 순서 | 헤더 | `CoupangDailyExportRow` 값 | Excel 형식 | 의미 |
|---:|---|---|---|---|
| 1 | 제품명 | `productName` | Text | 합계, 그룹, 옵션, 단일제품, 기타사항 이름 |
| 2 | 매출 | `reportedSalesKrw` | `#,##0"원"` | 오늘 쿠팡 원본매출 |
| 3 | 전일자 매출 | `previousReportedSalesKrw` 신규 | `#,##0"원"` | 전일 쿠팡 원본매출 |
| 4 | 판매수 | `reportedSalesQuantity` | `#,##0` | 오늘 원본 판매수량 |
| 5 | 전일자 판매수 | `previousReportedSalesQuantity` | `#,##0` | 전일 원본 판매수량 |
| 6 | 가구매 수 | `manualPurchaseQuantity` | `#,##0` | 오늘 가구매 수량 |
| 7 | 광고수익률 | `roas` | `0.0%` | ROAS 원시값 5.406을 540.6%로 표시 |
| 8 | 오가닉매출 | `organicSalesKrw` | `#,##0"원"` | 오늘 오가닉 매출 |
| 9 | 최종순이익 | `marginKrw` | `#,##0"원"` | 오늘 최종 순이익 |
| 10 | 전일자 최종 순이익 | `previousMarginKrw` | `#,##0"원"` | 전일 최종 순이익 |

### 2.2 제거되는 XLSX 열

아래 값은 데이터 모델에서 삭제하는 것이 아니라 **새 XLSX 표시 열에서만 제외**한다.

- 조회일
- 선택 필터
- 검색어
- 소속 리포트 카테고리
- 행구분
- 광고비
- 전일 광고비
- 전일 광고수익률

이 값들은 기존 CSV에는 계속 포함한다.

파일명에 현재 날짜와 필터 조각이 들어가므로 파일 식별 방식도 유지한다.

```text
2026-07-29_쿠팡_데일리리포트_전체.xlsx
2026-07-29_쿠팡_데일리리포트_홈트레이닝.xlsx
2026-07-29_쿠팡_데일리리포트_카테고리3개.xlsx
```

### 2.3 전일자 매출 데이터

현재 합계 응답에는 전일 매출이 있지만 제품/그룹의 `previous`에는 `reportedSalesKrw`가 없다.

따라서 API와 Web 타입을 다음처럼 확장한다.

```ts
export type CoupangDailyPreviousMetrics = {
  reportedSalesKrw: number;
  reportedSalesQuantity: number;
  adSpendKrw: number;
  roas: number | null;
  marginKrw: number | null;
};
```

서버 변환은 다음 규칙을 사용한다.

```ts
export function toDailyPreviousMetrics(
  row: ProductProfitRow | undefined
): CoupangDailyPreviousMetrics {
  return {
    reportedSalesKrw: row?.reportedSalesKrw ?? 0,
    reportedSalesQuantity: row?.reportedSalesQuantity ?? 0,
    adSpendKrw: row?.adSpendKrw ?? 0,
    roas: row?.roas ?? null,
    marginKrw: row?.marginKrw ?? null
  };
}
```

그룹의 전일 매출은 이미 `aggregateCoupangProductProfitGroup()`가 전일 자식 행의 `reportedSalesKrw`를 합산하므로, 위 필드만 반환하면 정확한 그룹 전일 매출을 얻는다.

### 2.4 결측치 규칙

- 존재하지 않는 오늘/전일 판매 행의 매출과 판매수는 현재 API 규칙처럼 `0`을 사용한다.
- 계산할 수 없는 순이익은 `null`을 유지하며 Excel에는 `-`로 보일 수 있다.
- 기타사항 지표는 기존처럼 빈 문자열을 사용한다.
- `null`, 빈 문자열, 비유한 숫자는 판매수 증감 비교 대상이 아니다.
- 숫자 `0`은 결측치가 아니며 정상적으로 비교한다.

---

## 3. 색상과 스타일 규격

### 3.1 상품 블록 팔레트

아래 네 가지 배경색을 화면에 보이는 최상위 상품 블록 순서대로 순환한다.

| 팔레트 | ARGB | 용도 |
|---|---|---|
| Mint | `FFE7F3EF` | 첫 번째 상품 블록 |
| Blue | `FFEAF2FB` | 두 번째 상품 블록 |
| Sand | `FFFBF1DF` | 세 번째 상품 블록 |
| Lilac | `FFF1ECF8` | 네 번째 상품 블록 |

다섯 번째 상품 블록부터 다시 Mint를 사용한다.

```ts
const band = PRODUCT_BANDS[blockIndex % PRODUCT_BANDS.length];
```

팔레트는 서버 ID 해시로 결정하지 않는다. 해시 방식은 인접한 두 상품이 같은 색이 될 수 있기 때문이다.

최종 필터 결과의 표시 순서에 따라 `blockIndex`를 부여하면 인접 상품 블록이 항상 다른 색이 된다.

### 3.2 팔레트 배정 안정성

- 합계 행은 `blockIndex = null`
- 첫 번째 최상위 상품 행은 `blockIndex = 0`
- 그 그룹의 옵션과 기타사항은 모두 동일한 `blockIndex`
- 다음 최상위 상품 행에서만 `blockIndex + 1`
- 단일제품도 하나의 최상위 블록으로 계산
- 필터 결과가 바뀌면 화면에 보이는 상품 순서 기준으로 다시 배정
- 그룹 ID가 같으면 한 파일 안에서 반드시 같은 색

### 3.3 헤더와 합계 색상

| 대상 | 배경 | 글자 | 굵기 |
|---|---|---|---|
| 헤더 | `FF385951` | `FFFFFFFF` | Bold |
| 전체/선택 합계 | `FFEDF1F0` | `FF17221F` | Bold |
| 일반 상품 셀 | 해당 그룹 팔레트 | `FF17221F` | Normal |
| 그룹합계/단일제품 | 해당 그룹 팔레트 | `FF17221F` | Bold |

### 3.4 판매수 증가 강조

증가 강조는 오늘 `판매수` 열에만 적용한다.

- 배경: `FFDCECFF`
- 경계선: `FF6C9ED8`
- 글자: `FF1557A0`
- 굵기: Bold

판정:

```ts
current > previous
```

전체합계, 선택합계, 기타사항에는 적용하지 않는다.

### 3.5 판매수 큰 폭 하락 강조

하락 강조는 오늘 `판매수` 열에만 적용한다.

- 배경: `FFFFF2CC`
- 경계선: `FFE2B93B`
- 글자: `FF7A5A00`
- 굵기: Bold

판정:

```ts
previous - current > 10
```

`>= 10`이 아니다. 정확히 10개 감소는 강조하지 않는다.

전체합계, 선택합계, 기타사항에는 적용하지 않는다.

### 3.6 순이익 글자색

목업의 가독성을 반영해 최종순이익과 전일자 최종 순이익은 값의 부호에 따라 글자색만 바꾼다.

- 양수: `FF13744A`
- 음수: `FFB5473B`
- 0: 기본 글자색
- `null` 또는 빈 값: 기본 글자색

배경색은 해당 상품 블록 색을 유지한다.

### 3.7 테두리

- 기본 그리드: `FFD8E0DE`의 얇은 선
- 그룹합계/단일제품 시작 행:
  - 상단을 기본보다 조금 강한 선으로 표시
  - 그룹합계와 옵션의 배경색은 동일하게 유지
- 판매수 증가/하락 셀:
  - 파란색 또는 노란색 강조 경계선이 기본 그리드보다 우선
- 첫 번째 제품명 셀 왼쪽에 팔레트 계열 강조선을 넣을 수 있다.
  - 필수는 아니지만 목업과 일치시키려면 구현한다.

### 3.8 스타일 우선순위

한 셀에 여러 규칙이 적용될 수 있으므로 아래 우선순위를 고정한다.

1. 헤더 스타일
2. 전체/선택 합계 스타일
3. 오늘 판매수 증가 파란색 또는 큰 폭 하락 노란색
4. 상품 블록 배경색
5. 그룹합계/단일제품의 Bold·상단 경계선
6. 옵션 들여쓰기
7. 순이익 양수/음수 글자색
8. 기본 숫자 형식과 그리드

판매수 증감 스타일은 배경·경계선·글자색을 덮어쓰지만, 셀의 숫자 형식 `#,##0`은 유지한다.

---

## 4. 현재 코드 기준선

### 4.1 현재 XLSX 생성기

파일:

```text
apps/web/src/lib/xlsx.ts
```

현재 특징:

- 브라우저에서 OOXML 문자열을 직접 조립한다.
- `xl/styles.xml`, `xl/worksheets/sheet1.xml`을 직접 만든다.
- ZIP도 별도 라이브러리 없이 STORE 방식으로 직접 만든다.
- `XlsxCellStyle`은 14개 고정 문자열이다.
- 헤더/일반 숫자/원화/USD/퍼센트/배수/합계 스타일만 있다.
- 셀마다 하나의 고정 style ID만 선택한다.
- 행 높이, 들여쓰기, 임의 배경색, 글자색, 그룹 시작 경계선의 조합을 표현할 수 없다.
- 헤더 고정과 자동 필터는 지원한다.

이 모듈은 쿠팡 데일리 리포트 외에 아래 화면에서도 사용한다.

```text
apps/web/src/app/sales/page.tsx
```

따라서 스타일 시스템을 확장할 때 기존 판매성과 XLSX의 시각과 숫자 형식을 깨뜨리면 안 된다.

`apps/web/src/app/ads/page.tsx`에도 이름이 같은 로컬 `buildXlsxWorkbook()`가 있지만 `@/lib/xlsx`의 함수와는 별개다.

### 4.2 현재 쿠팡 XLSX 생성 위치

파일:

```text
apps/web/src/app/coupang/daily-report/page.tsx
```

현재 상태:

- `exportColumns` 하나를 CSV와 XLSX가 공유한다.
- 17개 열을 내보낸다.
- `exportXlsx()` 안에서 셀 배열과 합계 스타일을 직접 만든다.
- 전체/선택 합계만 `Total*` 스타일을 쓴다.
- 그룹합계와 옵션은 일반 행과 같은 스타일이다.
- `freezeRow: 1`, `autoFilter: { fromRow: 1 }`이다.

이번 작업에서는 CSV와 XLSX 열이 달라지므로 이 공유 구조를 분리해야 한다.

### 4.3 현재 내보내기 행

파일:

```text
apps/web/src/lib/coupang-daily-report.ts
```

현재 `CoupangDailyExportRow`에는 다음이 이미 있다.

- `rowKind`
- `productName`
- 오늘 매출
- 오늘/전일 판매수
- 오늘 가구매 수
- 오늘/전일 광고비
- 오늘/전일 ROAS
- 오늘 오가닉매출
- 오늘/전일 최종순이익

현재 없는 것:

- 제품/그룹별 전일 매출
- 상품 블록 식별자
- 상품 블록 순번
- 옵션 들여쓰기 수준

### 4.4 현재 API 응답

파일:

```text
apps/api/src/coupang/coupang.service.ts
apps/web/src/types/coupang.ts
```

현재 `CoupangDailyPreviousMetrics`:

```ts
{
  reportedSalesQuantity: number;
  adSpendKrw: number;
  roas: number | null;
  marginKrw: number | null;
}
```

`reportedSalesKrw`가 빠져 있다.

서버는 이미 오늘과 전일 각각 `buildProductProfitRows()`를 호출하고, 전일 그룹도 `aggregateCoupangProductProfitGroup()`로 합산한다. DB 조회나 계산식을 새로 만들 필요는 없다. 반환 계약에 전일 매출 필드만 추가하면 된다.

### 4.5 현재 테스트

관련 테스트:

```text
apps/api/src/coupang/coupang.service.spec.ts
apps/web/src/lib/coupang-daily-report.spec.ts
```

현재 `apps/web/src/lib/xlsx.ts` 전용 테스트는 없다. 이번 작업에서는 반드시 새 테스트를 추가해야 한다.

### 4.6 현재 작업 트리 보호

계획서 작성 시 `git status --short`에는 다음 미추적 파일이 있었다.

```text
?? design-previews/coupang-daily-report-xlsx-group-color-mockup.html
?? design-previews/coupang-performance-presentation-mockup.html
```

`design-previews/_preview-server.cjs`는 앱이 참조하지 않는 로컬 임시 미리보기 서버로 확인되어 2026-07-30 정리 작업에서 삭제했다. 다음 세션은 이 파일이 없음을 정상 상태로 취급한다.

남아 있는 HTML 목업 파일들은 삭제하거나 덮어쓰지 않는다.

목업 HTML:

```text
design-previews/coupang-daily-report-xlsx-group-color-mockup.html
```

다음 세션은 목업 파일을 읽지 않아도 이 계획서의 색상·열·규칙만으로 구현할 수 있어야 한다. 목업은 시각 비교용 보조 자료일 뿐이다.

---

## 5. 확정 데이터·타입 설계

### 5.1 API `CoupangDailyPreviousMetrics`

변경 전:

```ts
export type CoupangDailyPreviousMetrics = {
  reportedSalesQuantity: number;
  adSpendKrw: number;
  roas: number | null;
  marginKrw: number | null;
};
```

변경 후:

```ts
export type CoupangDailyPreviousMetrics = {
  reportedSalesKrw: number;
  reportedSalesQuantity: number;
  adSpendKrw: number;
  roas: number | null;
  marginKrw: number | null;
};
```

같은 변경을 Web 타입에도 적용한다.

### 5.2 `CoupangDailyExportRow`

아래 필드를 추가한다.

```ts
export type CoupangDailyExportRow = {
  // 기존 필드 유지
  previousReportedSalesKrw: DailyExportNumber;

  // XLSX 시각 블록 메타데이터
  visualBlockKey: string | null;
  visualBlockIndex: number | null;
  indentLevel: 0 | 1;
  visualChildProductCount: number | null;
};
```

각 필드 의미:

- `previousReportedSalesKrw`
  - 전일 원본매출
- `visualBlockKey`
  - 그룹: `group:${groupId}`
  - 단일제품: `product:${productId}`
  - 합계: `null`
- `visualBlockIndex`
  - 최상위 표시 순서 기준 0부터 증가
  - 팔레트 선택에 사용
- `indentLevel`
  - 옵션만 `1`
  - 나머지는 `0`
- `visualChildProductCount`
  - 그룹합계만 `row.childProductCount`
  - 그 외 행은 `null`
  - XLSX 전용 그룹합계 제품명에 `옵션 N개`를 표시할 때 사용

CSV 직렬화는 추가 필드를 무시하므로 기존 CSV 열에는 영향이 없다.

### 5.3 `flattenDailyReportExportRows()`의 블록 부여

의사코드:

```ts
const exportRows = [summaryRow];
let visualBlockIndex = 0;

for (const row of response.rows) {
  if (row.rowType === "GROUP") {
    const visualBlockKey = `group:${row.groupId}`;

    exportRows.push(groupTotalRow({
      visualBlockKey,
      visualBlockIndex,
      indentLevel: 0,
      visualChildProductCount: row.childProductCount
    }));

    for (const child of row.children) {
      exportRows.push(optionRow({
        visualBlockKey,
        visualBlockIndex,
        indentLevel: 1,
        visualChildProductCount: null
      }));
    }

    if (groupHasNotes) {
      exportRows.push(noteRow({
        visualBlockKey,
        visualBlockIndex,
        indentLevel: 0,
        visualChildProductCount: null
      }));
    }
  } else {
    const visualBlockKey = `product:${row.productId}`;

    exportRows.push(singleProductRow({
      visualBlockKey,
      visualBlockIndex,
      indentLevel: 0,
      visualChildProductCount: null
    }));

    if (productHasNote) {
      exportRows.push(noteRow({
        visualBlockKey,
        visualBlockIndex,
        indentLevel: 0,
        visualChildProductCount: null
      }));
    }
  }

  visualBlockIndex += 1;
}
```

합계 행:

```ts
visualBlockKey: null
visualBlockIndex: null
indentLevel: 0
visualChildProductCount: null
previousReportedSalesKrw: summary.previous.reportedSalesKrw
```

### 5.4 판매수 증감 분류 헬퍼

XLSX 모듈에 다음과 같은 순수 헬퍼를 둔다.

```ts
export const DAILY_SALES_DECREASE_HIGHLIGHT_THRESHOLD = 10;

export type DailySalesQuantityHighlight =
  | "INCREASE"
  | "SIGNIFICANT_DECREASE"
  | "NONE";

export function classifyDailySalesQuantityHighlight(
  row: CoupangDailyExportRow
): DailySalesQuantityHighlight {
  if (!["그룹합계", "옵션", "단일제품"].includes(row.rowKind)) {
    return "NONE";
  }

  const current = finiteExportNumber(row.reportedSalesQuantity);
  const previous = finiteExportNumber(row.previousReportedSalesQuantity);
  if (current === null || previous === null) return "NONE";

  if (current > previous) return "INCREASE";
  if (previous - current > DAILY_SALES_DECREASE_HIGHLIGHT_THRESHOLD) {
    return "SIGNIFICANT_DECREASE";
  }
  return "NONE";
}
```

헤더 문자열이나 열 인덱스로 조건을 찾지 않는다. XLSX 열 정의에 안정적인 `key`를 두고 `key === "reportedSalesQuantity"`일 때만 이 헬퍼를 적용한다.

---

## 6. 일반 XLSX 생성기 확장 설계

### 6.1 목표

`apps/web/src/lib/xlsx.ts`는 기존 판매성과 XLSX를 깨뜨리지 않으면서 아래를 표현할 수 있어야 한다.

- 셀별 배경색
- 셀별 글자색
- 셀별 Bold
- 셀 들여쓰기
- 그룹 시작 경계선
- 행별 높이
- 기존 숫자 형식 유지
- 같은 스타일 조합 중복 제거

### 6.2 공개 타입의 하위 호환

기존 호출은 계속 동작해야 한다.

```ts
rows: XlsxCell[][]
```

행 높이가 필요한 새 호출도 가능하게 한다.

```ts
export type XlsxRow = {
  cells: XlsxCell[];
  height?: number;
};

export type XlsxWorkbookInput = {
  sheetName: string;
  columns?: Array<{ width: number }>;
  rows: Array<XlsxCell[] | XlsxRow>;
  freezeRow?: number;
  autoFilter?: { fromRow: number; toRow?: number };
};
```

내부에서 모든 행을 다음처럼 정규화한다.

```ts
function normalizeXlsxRow(row: XlsxCell[] | XlsxRow): XlsxRow {
  return Array.isArray(row) ? { cells: row } : row;
}
```

`apps/web/src/app/sales/page.tsx`의 기존 `XlsxCell[][]`는 수정 없이 작동해야 한다.

### 6.3 셀 표현 확장

고정 style 이름을 수십 개 추가하지 말고, 기존 기본 형식과 시각 속성을 조합하도록 확장한다.

권장 형태:

```ts
export type XlsxCellFill =
  | "GROUP_MINT"
  | "GROUP_BLUE"
  | "GROUP_SAND"
  | "GROUP_LILAC"
  | "REPORT_HEADER"
  | "REPORT_TOTAL"
  | "SALES_INCREASE"
  | "SALES_DECREASE";

export type XlsxFontTone =
  | "DEFAULT"
  | "INVERSE"
  | "INCREASE"
  | "DECREASE"
  | "POSITIVE"
  | "NEGATIVE";

export type XlsxBorderTone =
  | "GRID"
  | "BLOCK_START"
  | "INCREASE"
  | "DECREASE"
  | "NONE";

export type XlsxCell = {
  value: string | number | null | undefined;
  style?: XlsxCellStyle;
  fill?: XlsxCellFill;
  fontTone?: XlsxFontTone;
  bold?: boolean;
  indent?: number;
  borderTone?: XlsxBorderTone;
};
```

이름은 구현 과정에서 조금 달라질 수 있지만, 고정된 `BandMintKrw`, `BandMintNumber`, `BandMintBoldKrw` 같은 조합형 enum을 대량으로 만들지 않는 방향을 지킨다.

### 6.4 동적 스타일 카탈로그

현재 `xlsxStyleId()`의 14개 고정 switch만으로는 조합 스타일을 처리하기 어렵다.

권장 흐름:

1. 모든 셀을 순회한다.
2. 아래 속성의 조합을 안정적인 key로 만든다.
   - 기본 숫자 형식
   - fill
   - font tone
   - bold
   - indent
   - border tone
3. 같은 key는 하나의 `cellXf`만 사용한다.
4. `fonts`, `fills`, `borders`, `cellXfs`의 실제 개수로 XML `count`를 작성한다.
5. 셀 XML 생성 시 카탈로그에서 style ID를 찾는다.

예:

```ts
type XlsxResolvedStyle = {
  numFmtId: number;
  fontId: number;
  fillId: number;
  borderId: number;
  alignment?: { indent?: number; horizontal?: "left" | "right" | "center" };
};
```

### 6.5 기존 스타일 보존

다음 기존 스타일의 결과는 그대로 유지해야 한다.

- Text
- Header
- Number
- Krw
- Usd
- Percent
- Ratio
- TotalText
- TotalNumber
- TotalKrw
- TotalUsd
- TotalPercent
- TotalRatio

판매성과 XLSX가 쓰는 기존 헤더·합계·원화·배수 표시가 회귀하지 않게 한다.

쿠팡 전용 새 헤더는 기존 `Header`의 전역 색을 바꾸지 말고 `REPORT_HEADER` fill과 `INVERSE` font 조합으로 만든다.

### 6.6 숫자 형식

필요 형식:

- Text: built-in text 형식 또는 기존 numFmtId 49
- Number: `#,##0`
- Krw: `#,##0"원"`
- Percent: `0.0%`
- 기존 Ratio: `0.00"배"` 유지

쿠팡 XLSX의 광고수익률은 `Ratio`가 아니라 퍼센트 형식을 사용한다.

현재 데이터가 `5.406`이면 Excel 표시가 `540.6%`가 되어야 한다.

기존 `Percent`가 두 자리 소수 형식을 사용하는 다른 화면에 영향을 줄 수 있으면 `Percent1` 또는 명확한 새 기본 형식을 추가한다. 전역 `Percent`의 의미를 무심코 바꾸지 않는다.

### 6.7 행 XML

행 높이가 있으면 다음처럼 출력한다.

```xml
<row r="5" ht="6" customHeight="1">...</row>
```

일반 행은 현재 기본 높이를 유지한다.

옵션 들여쓰기 스타일에는 다음과 같이 alignment를 추가한다.

```xml
<alignment horizontal="left" indent="1"/>
```

해당 `xf`에는 `applyAlignment="1"`을 포함한다.

### 6.8 문자열 보존

사용자 텍스트는 기존 `escapeXml()`을 계속 통과시킨다.

제품명 앞에 선행 공백을 넣는 구현을 선택할 경우 `<t xml:space="preserve">`가 필요하지만, 이번 설계는 Excel alignment indent를 사용하므로 선행 공백 방식은 사용하지 않는다.

---

## 7. 쿠팡 XLSX 전용 모듈 설계

### 7.1 새 파일

권장 신규 파일:

```text
apps/web/src/lib/coupang-daily-xlsx.ts
apps/web/src/lib/coupang-daily-xlsx.spec.ts
apps/web/src/lib/xlsx.spec.ts
```

`page.tsx`에 10개 열, 색상 분류, 스타일 우선순위, 구분 행 생성 코드를 모두 넣지 않는다.

### 7.2 모듈 책임

`coupang-daily-xlsx.ts`는 다음을 담당한다.

- XLSX 전용 10개 열 정의
- CSV 원본 제품명에 영향을 주지 않는 XLSX 전용 제품명 표현
- 열 key와 기본 숫자 형식
- 상품 블록 팔레트 선택
- 판매수 증감 분류
- 순이익 글자색 분류
- 행별 Bold·들여쓰기·경계선 결정
- 구분 행 생성
- `buildXlsxWorkbook()`에 전달할 `XlsxWorkbookInput` 생성

권장 공개 함수:

```ts
export function buildCoupangDailyXlsxWorkbook(
  exportRows: CoupangDailyExportRow[]
): Uint8Array;
```

또는 테스트 편의를 위해 input을 별도 생성한다.

```ts
export function buildCoupangDailyXlsxInput(
  exportRows: CoupangDailyExportRow[]
): XlsxWorkbookInput;

export function buildCoupangDailyXlsxWorkbook(
  exportRows: CoupangDailyExportRow[]
): Uint8Array {
  return buildXlsxWorkbook(buildCoupangDailyXlsxInput(exportRows));
}
```

두 번째 구조가 열과 셀 스타일을 ZIP 해제 없이도 단위 테스트하기 쉬우므로 권장한다.

### 7.3 XLSX 열 key

헤더 문자열로 조건을 판단하지 않도록 key를 둔다.

```ts
type CoupangDailyXlsxColumn = {
  key:
    | "productName"
    | "reportedSalesKrw"
    | "previousReportedSalesKrw"
    | "reportedSalesQuantity"
    | "previousReportedSalesQuantity"
    | "manualPurchaseQuantity"
    | "roas"
    | "organicSalesKrw"
    | "marginKrw"
    | "previousMarginKrw";
  header: string;
  style: XlsxCellStyle;
  width: number;
  value: (row: CoupangDailyExportRow) =>
    string | number | null | undefined;
};
```

제품명 열의 XLSX 전용 값 변환:

```ts
function xlsxProductName(row: CoupangDailyExportRow) {
  if (row.rowKind === "그룹합계") {
    return `Σ ${row.productName} (그룹 합계 · 옵션 ${row.visualChildProductCount ?? 0}개)`;
  }
  return row.productName;
}
```

CSV는 계속 `row.productName`을 직접 사용한다.

### 7.4 권장 열 너비

실제 Excel에서 한국어 헤더와 금액이 잘리지 않게 다음을 기준으로 시작한다.

| 열 | 너비 |
|---|---:|
| 제품명 | 34 |
| 매출 | 16 |
| 전일자 매출 | 17 |
| 판매수 | 11 |
| 전일자 판매수 | 15 |
| 가구매 수 | 12 |
| 광고수익률 | 14 |
| 오가닉매출 | 16 |
| 최종순이익 | 18 |
| 전일자 최종 순이익 | 20 |

실제 Excel 시각 검증 후 글자가 잘리면 필요한 열만 소폭 조정한다. 전체 자동 맞춤으로 과도하게 넓히지 않는다.

### 7.5 셀 생성 의사코드

```ts
function toXlsxCell(
  row: CoupangDailyExportRow,
  column: CoupangDailyXlsxColumn
): XlsxCell {
  const blockFill = fillForBlockIndex(row.visualBlockIndex);
  const isTotal = row.rowKind === "전체합계" || row.rowKind === "선택합계";
  const isBlockHeader = row.rowKind === "그룹합계" || row.rowKind === "단일제품";

  const cell: XlsxCell = {
    value: column.value(row),
    style: column.style,
    fill: isTotal ? "REPORT_TOTAL" : blockFill,
    bold: isTotal || isBlockHeader,
    indent: column.key === "productName" ? row.indentLevel : 0,
    borderTone: isBlockHeader ? "BLOCK_START" : "GRID"
  };

  if (
    column.key === "reportedSalesQuantity" &&
    !isTotal
  ) {
    const highlight = classifyDailySalesQuantityHighlight(row);
    if (highlight === "INCREASE") {
      cell.fill = "SALES_INCREASE";
      cell.fontTone = "INCREASE";
      cell.bold = true;
      cell.borderTone = "INCREASE";
    }
    if (highlight === "SIGNIFICANT_DECREASE") {
      cell.fill = "SALES_DECREASE";
      cell.fontTone = "DECREASE";
      cell.bold = true;
      cell.borderTone = "DECREASE";
    }
  }

  if (column.key === "marginKrw" || column.key === "previousMarginKrw") {
    cell.fontTone = profitFontTone(column.value(row));
  }

  return cell;
}
```

판매수 강조와 순이익 글자색이 한 셀에서 충돌하지 않으므로 서로 덮어쓸 문제는 없다.

### 7.6 구분 행 생성

`visualBlockKey`가 이전 데이터 행과 달라질 때 구분 행을 넣는다.

합계 다음 첫 상품 블록 앞에도 넣는다.

```ts
function separatorRow(columnCount: number): XlsxRow {
  return {
    height: 6,
    cells: Array.from({ length: columnCount }, () => ({
      value: "",
      style: "Text",
      borderTone: "NONE"
    }))
  };
}
```

그룹 내부의 그룹합계·옵션·기타사항 사이에는 구분 행을 넣지 않는다.

---

## 8. CSV와 XLSX 분리

### 8.1 CSV

현재 `exportColumns`의 17개 열과 순서를 유지한다.

가능한 변경:

```ts
const csvExportColumns = [
  // 기존 exportColumns 그대로
];
```

CSV 다운로드:

```ts
downloadCsv(fileName, csvExportColumns, exportRows);
```

CSV에는 색상, 들여쓰기, 구분 행이 없다.

### 8.2 XLSX

XLSX는 `csvExportColumns`를 사용하지 않는다.

```ts
const workbook = buildCoupangDailyXlsxWorkbook(exportRows);
downloadXlsx(fileName, workbook);
```

`page.tsx`의 기존 `totalCellStyle()`가 더 이상 XLSX에서 쓰이지 않으면 제거한다.

### 8.3 화면과 인쇄

다음은 변경하지 않는다.

- 데일리 리포트 화면의 현재 8열 구조
- 화면에서 오늘/전일 값을 한 셀에 같이 보여주는 방식
- 그룹 접기/펼치기
- 인쇄 CSS
- 카테고리 필터
- 검색
- 합계 계산

---

## 9. 파일별 변경 계획

### `apps/api/src/coupang/coupang.service.ts`

1. `CoupangDailyPreviousMetrics`에 `reportedSalesKrw` 추가
2. `toDailyPreviousMetrics()`가 `row?.reportedSalesKrw ?? 0`을 반환
3. 계산식이나 DB 조회는 변경하지 않음
4. 그룹 전일 매출은 기존 그룹 집계 결과를 사용

### `apps/api/src/coupang/coupang.service.spec.ts`

1. 기존 daily report 기대 객체에 `previous.reportedSalesKrw` 추가
2. 그룹 전일 매출이 자식 전일 매출 합계인지 검증
3. 옵션 전일 매출 검증
4. 전일 행이 없는 제품은 `0`인지 검증
5. 전일만 활동한 그룹 옵션이 현재 `0`, 전일 매출 값으로 보존되는지 검증

### `apps/web/src/types/coupang.ts`

`CoupangDailyPreviousMetrics`에 `reportedSalesKrw: number` 추가

### `apps/web/src/lib/coupang-daily-report.ts`

1. `CoupangDailyExportRow`에 다음 추가
   - `previousReportedSalesKrw`
   - `visualBlockKey`
   - `visualBlockIndex`
   - `indentLevel`
   - `visualChildProductCount`
2. 합계 행에 전일 매출 연결
3. 그룹합계·옵션·단일제품에 전일 매출 연결
4. 그룹 블록 메타데이터 부여
5. 기타사항이 부모 블록 메타데이터를 상속
6. 기존 행 순서와 CSV용 필드는 유지

### `apps/web/src/lib/coupang-daily-report.spec.ts`

1. 테스트 fixture의 `previous()`에 `reportedSalesKrw` 추가
2. 합계/그룹/옵션의 전일 매출 검증
3. 블록 key/index와 indent 검증
4. 그룹합계에 자식 옵션 수가 보존되는지 검증
5. 기타사항이 부모 블록 index를 상속하는지 검증
6. 그룹이 바뀔 때만 index가 증가하는지 검증
7. 기존 행 순서와 CSV용 원본 제품명·메모 테스트 유지

### `apps/web/src/lib/xlsx.ts`

1. 행 높이를 지원하는 `XlsxRow` 추가
2. 기존 `XlsxCell[][]` 입력과 하위 호환 유지
3. 셀별 fill/font/bold/indent/border 속성 추가
4. 동적 스타일 카탈로그 생성
5. 스타일 XML의 `count`를 실제 개수와 일치
6. 정렬/들여쓰기 XML 지원
7. 기존 판매성과 XLSX 스타일 회귀 방지
8. 기존 ZIP 생성, XML escape, 다운로드 방식 유지

### `apps/web/src/lib/xlsx.spec.ts` 신규

1. 기존 기본 스타일 workbook 생성 검증
2. 동적 fill/font/border/alignment 조합 검증
3. 행 높이 XML 검증
4. 숫자 셀이 inline string으로 바뀌지 않는지 검증
5. 스타일 XML count 검증
6. 기존 판매성과 형식용 기본 style이 존재하는지 검증

ZIP은 현재 STORE 방식이므로 테스트 안에서 작은 ZIP entry reader를 작성할 수 있다.

검증 대상 entry:

```text
xl/styles.xml
xl/worksheets/sheet1.xml
```

### `apps/web/src/lib/coupang-daily-xlsx.ts` 신규

1. 10개 XLSX 열 정의
2. XLSX 전용 그룹합계 제품명 표현
3. 팔레트와 색상 규칙
4. 판매수 증감 분류
5. 순이익 글자색
6. 구분 행
7. workbook input/build 함수

### `apps/web/src/lib/coupang-daily-xlsx.spec.ts` 신규

1. 10개 헤더 순서
2. 열 너비
3. 그룹합계 제품명이 `Σ ... (그룹 합계 · 옵션 N개)`인지 검증
4. 그룹 동일 배경색
5. 인접 블록 다른 색
6. 옵션 들여쓰기
7. 그룹합계 Bold/상단 경계선
8. 증가 파란색
9. 11개 이상 감소 노란색
10. 정확히 10개 감소 미강조
11. 합계/기타사항 미강조
12. 숫자 타입 유지
13. ROAS 1자리 퍼센트 형식
14. 빈 결과도 합계 1행 출력

### `apps/web/src/app/coupang/daily-report/page.tsx`

1. 기존 17개 `exportColumns`를 `csvExportColumns`로 이름 변경
2. CSV 동작은 그대로 유지
3. XLSX 인라인 셀 생성 제거
4. `buildCoupangDailyXlsxWorkbook(exportRows)` 호출
5. 파일명 규칙 유지
6. 사용하지 않는 `XlsxCell`, `XlsxCellStyle`, `totalCellStyle()` 정리

### `design-previews/coupang-daily-report-xlsx-group-color-mockup.html`

실제 구현의 필수 변경 파일은 아니다.

필요하면 노란색 하락 예시를 추가해 수동 비교에 사용하되, 실제 XLSX 구현보다 먼저 목업만 고치는 데 시간을 쓰지 않는다.

---

## 10. 자동 테스트 계획

### 10.1 API 계약

#### 그룹 전일 매출

전일 옵션 매출이 700원과 200원이면 그룹:

```ts
previous.reportedSalesKrw === 900
```

#### 옵션 전일 매출

옵션 A의 전일 매출 700원이 그대로:

```ts
child.previous.reportedSalesKrw === 700
```

#### 전일 데이터 없음

```ts
previous.reportedSalesKrw === 0
```

### 10.2 블록 메타데이터

행 순서:

```text
전체합계
그룹합계 A
옵션 A-1
옵션 A-2
기타사항 A
단일제품 B
기타사항 B
그룹합계 C
옵션 C-1
```

기대:

```text
전체합계: index null
A 관련: index 0, key group:A
B 관련: index 1, key product:B
C 관련: index 2, key group:C
```

옵션만 `indentLevel = 1`이다.

### 10.3 증가/하락 경계값

테이블 기반 테스트:

```ts
it.each([
  [18, 15, "INCREASE"],
  [1, 0, "INCREASE"],
  [13, 13, "NONE"],
  [12, 13, "NONE"],
  [10, 20, "NONE"],
  [9, 20, "SIGNIFICANT_DECREASE"],
  [0, 11, "SIGNIFICANT_DECREASE"],
  [0, 10, "NONE"]
])
```

추가:

- `전체합계` 94 대 81은 `NONE`
- `기타사항`은 `NONE`
- 빈 문자열은 `NONE`
- `null`은 `NONE`

### 10.4 스타일 우선순위

다음 셀을 직접 검사한다.

1. 그룹 기본 판매수 셀
   - 그룹 fill
2. 증가 판매수 셀
   - 파란 fill
   - 파란 font
   - Bold
   - Number format 유지
3. 11개 하락 판매수 셀
   - 노란 fill
   - 노란 font
   - Bold
   - Number format 유지
4. 10개 하락 판매수 셀
   - 그룹 fill
5. 옵션 제품명
   - 그룹 fill
   - indent 1
6. 그룹합계 제품명
   - 옵션과 동일 fill
   - Bold
   - block start border

### 10.5 실제 OOXML

`buildXlsxWorkbook()` 결과를 ZIP entry로 읽어 다음을 확인한다.

- `styles.xml`에 여덟 가지 이상의 필요한 fill이 들어 있음
- header, total, 네 그룹색, increase, decrease 색이 정확한 ARGB
- `<alignment indent="1"...>` 존재
- 감소 셀과 증가 셀이 각각 다른 style ID를 사용
- 금액과 판매수는 `<c><v>숫자</v></c>` 형태
- 문자열 셀만 `t="inlineStr"`
- `0.0%` numFmt 존재
- 구분 행에 `ht="6"`과 `customHeight="1"` 존재
- `fonts`, `fills`, `borders`, `cellXfs` count가 실제 자식 개수와 일치

### 10.6 회귀

- 기존 카페24/Meta 판매성과 XLSX 생성이 빌드되고 기존 스타일을 유지
- 쿠팡 CSV 헤더 17개와 순서 유지
- 쿠팡 화면 렌더 테스트 유지
- 빈 필터 결과 XLSX도 합계 행 포함
- 불완전 순이익의 `null` 처리 유지

---

## 11. 수동 검증 시나리오

### 11.1 준비 데이터

최소 다음 조건을 만족하는 날짜를 선택한다.

- 옵션 2개 이상인 그룹 최소 2개
- 단일제품 최소 1개
- 오늘 판매수가 증가한 옵션
- 오늘 판매수가 1~10개 감소한 옵션
- 오늘 판매수가 정확히 10개 감소한 옵션
- 오늘 판매수가 11개 이상 감소한 옵션
- 기타사항이 있는 그룹 또는 단일제품
- 양수와 음수 또는 `null` 순이익

실데이터에 경계값이 없으면 단위 테스트 fixture로 만든 샘플 workbook을 임시 출력해 검증한다.

### 11.2 Excel에서 확인

1. 쿠팡 데일리 리포트에서 날짜 선택
2. 필요하면 카테고리와 검색어 선택
3. XLSX 다운로드
4. Microsoft Excel에서 열기
5. 보안 복구 경고 없이 열리는지 확인
6. 헤더가 정확히 10개인지 확인
7. 그룹합계와 옵션의 배경색이 정확히 같은지 확인
8. 인접 상품 블록 색이 다른지 확인
9. 옵션 제품명이 들여쓰기되는지 확인
10. 그룹합계가 Bold/경계선으로 구분되는지 확인
11. 증가 판매수 오늘 셀만 파란색인지 확인
12. 11개 이상 감소 판매수 오늘 셀만 노란색인지 확인
13. 10개 감소 셀이 노란색이 아닌지 확인
14. 전일 판매수 셀에는 강조가 들어가지 않는지 확인
15. 금액에 원 단위 형식이 있는지 확인
16. ROAS가 `540.6%`와 같은 퍼센트인지 확인
17. 판매수 셀을 클릭했을 때 숫자이고 합계/필터가 가능한지 확인
18. 기타사항이 부모 블록 색을 공유하는지 확인
19. 구분 행이 너무 크거나 자동 필터를 깨뜨리지 않는지 확인
20. 파일명이 기존 규칙을 유지하는지 확인

### 11.3 CSV 확인

같은 조건에서 CSV를 다운로드해 다음을 확인한다.

- 기존 17개 열 유지
- 조회일/선택 필터/검색어/카테고리/행구분 유지
- 구분용 빈 행이 추가되지 않음
- 값과 행 순서가 기존과 같음

### 11.4 판매성과 XLSX 회귀 확인

`/sales` 화면에서도 XLSX를 한 번 다운로드해 다음을 확인한다.

- 파일이 정상적으로 열림
- 기존 헤더/합계 스타일이 유지됨
- 원화, 숫자, 비율 형식이 깨지지 않음

---

## 12. 구현 순서

### 1단계: 기준선 보호

1. `git status --short` 확인
2. 기존 미추적 목업 파일 보존
3. 관련 테스트 기준선 실행
4. 현재 브랜치와 커밋 기록

### 2단계: API 전일 매출 계약

1. API `CoupangDailyPreviousMetrics`에 `reportedSalesKrw`
2. `toDailyPreviousMetrics()` 연결
3. API 테스트 fixture와 기대값 수정
4. API 관련 테스트 통과

### 3단계: Web 타입과 export row

1. Web `CoupangDailyPreviousMetrics` 추가
2. `CoupangDailyExportRow.previousReportedSalesKrw`
3. block key/index/indent 추가
4. flatten 테스트 추가

### 4단계: 일반 XLSX 스타일 확장

1. 하위 호환 가능한 `XlsxRow`
2. 셀 시각 속성
3. 스타일 카탈로그
4. 행 높이·들여쓰기 XML
5. `xlsx.spec.ts`
6. 기존 판매성과 workbook 회귀 테스트

### 5단계: 쿠팡 XLSX 전용 빌더

1. 10개 열
2. 팔레트
3. 판매수 증감 헬퍼
4. 순이익 글자색
5. 구분 행
6. 전용 단위 테스트

### 6단계: 페이지 연결

1. CSV 열 이름 분리
2. XLSX 전용 빌더 호출
3. 불필요한 인라인 스타일 함수 제거
4. 파일명 유지

### 7단계: 전체 자동 검증

1. API 테스트
2. Web 테스트
3. TypeScript lint
4. API/Web build

### 8단계: 실제 XLSX 시각 검증

1. 실제 파일 다운로드
2. Excel 열기
3. 색상/숫자/경계값/구분 행 확인
4. 판매성과 XLSX 회귀 확인

---

## 13. 검증 명령

저장소 루트에서 실행한다.

### API 관련 테스트

```powershell
npm --workspace @meta-ads-performance/api run test -- src/coupang/coupang.service.spec.ts
```

### Web 관련 테스트

```powershell
npm --workspace @meta-ads-performance/web run test -- src/lib/coupang-daily-report.spec.ts src/lib/coupang-daily-xlsx.spec.ts src/lib/xlsx.spec.ts src/lib/csv.spec.ts
```

### 전체 테스트

```powershell
npm run test
```

### 타입 검사

```powershell
npm run lint
```

### 빌드

```powershell
npm run build
```

환경에 따라 Vitest의 경로 전달 형식이 다르면 각 workspace의 기존 `test` 스크립트 규칙에 맞춰 조정한다. 최종적으로는 `npm run test`, `npm run lint`, `npm run build` 전체를 통과시킨다.

---

## 14. 배포와 호환성

### 14.1 배포 순서

API 응답에 `previous.reportedSalesKrw`를 먼저 추가한 뒤 Web을 배포한다.

1. API 배포
2. daily report 응답에 전일 매출 필드 확인
3. Web 배포
4. 실제 XLSX 다운로드 검증

API 변경은 필드 추가이므로 구버전 Web은 새 필드를 무시할 수 있다.

반대로 새 Web이 구버전 API를 잠시 만나면 전일 매출이 `undefined`가 될 수 있으므로 API 우선 배포가 안전하다.

### 14.2 DB 변경

- Prisma 스키마 변경 없음
- 마이그레이션 없음
- 데이터 백필 없음

### 14.3 롤백

문제가 생기면 Web의 XLSX 전용 빌더 연결을 이전 17개 열 생성 방식으로 되돌릴 수 있다.

API의 `reportedSalesKrw` 추가 필드는 호환 가능한 additive change이므로 굳이 제거하지 않아도 된다.

일반 XLSX 스타일 엔진을 롤백할 때 판매성과 XLSX가 함께 영향을 받을 수 있으므로, `xlsx.ts` 변경과 쿠팡 전용 변경을 논리적으로 분리해 커밋하거나 최소한 diff를 구분 가능하게 유지한다.

---

## 15. 완료 조건

다음 항목이 모두 만족되어야 완료다.

- [ ] XLSX 헤더가 정확히 10개이며 순서가 요구사항과 일치한다.
- [ ] 제품/그룹별 전일자 매출이 정확하다.
- [ ] 그룹합계와 모든 옵션이 정확히 같은 배경색이다.
- [ ] 그룹 기타사항도 같은 배경색이다.
- [ ] 단일제품과 그 기타사항도 하나의 색상 블록이다.
- [ ] 인접 상품 블록이 다른 팔레트 색을 쓴다.
- [ ] 그룹합계는 같은 배경색을 유지하면서 Bold/경계선으로 구분된다.
- [ ] 옵션 제품명에 들여쓰기가 있다.
- [ ] 오늘 판매수가 증가한 그룹합계/옵션/단일제품의 오늘 판매수 셀만 파란색이다.
- [ ] 오늘 판매수가 11개 이상 감소한 그룹합계/옵션/단일제품의 오늘 판매수 셀만 노란색이다.
- [ ] 정확히 10개 감소한 셀은 노란색이 아니다.
- [ ] 전체합계/선택합계 판매수는 증감 강조 대상이 아니다.
- [ ] 기타사항은 증감 강조 대상이 아니다.
- [ ] 판매수·매출·순이익·ROAS가 숫자 셀이다.
- [ ] 광고수익률이 1자리 퍼센트 형식이다.
- [ ] 양수/음수 순이익 글자색이 적용된다.
- [ ] 구분 행 또는 동등한 상단 경계선으로 상품 경계가 분명하다.
- [ ] 헤더 고정과 자동 필터가 정상이다.
- [ ] 빈 결과도 합계 행이 있는 정상 XLSX로 열린다.
- [ ] CSV 17개 열은 변경되지 않았다.
- [ ] 데일리 리포트 화면과 인쇄는 변경되지 않았다.
- [ ] 판매성과 XLSX가 회귀하지 않았다.
- [ ] API/Web 테스트가 통과한다.
- [ ] lint와 build가 통과한다.
- [ ] Microsoft Excel에서 복구 경고 없이 파일이 열린다.

---

## 16. 구현 중 금지 사항

- CSV까지 10개 열로 바꾸지 않는다.
- 화면 테이블 열을 XLSX 10개 열과 맞춘다는 이유로 변경하지 않는다.
- 전일 매출을 프런트에서 추정하거나 오늘 매출로 대체하지 않는다.
- 그룹합계와 옵션의 배경색을 서로 다른 농도로 만들지 않는다.
- XLSX 그룹합계 라벨을 만들기 위해 공용 `productName`을 바꿔 CSV 제품명까지 변경하지 않는다.
- 판매수 증가/하락 표시를 전일 판매수 셀에 넣지 않는다.
- 하락 조건을 `>= 10`으로 구현하지 않는다.
- 하락 조건을 `current > 10` 또는 `previous > 10`으로 오해하지 않는다.
- 합계와 기타사항에 판매수 강조를 적용하지 않는다.
- 숫자 값에 화살표나 단위를 붙여 텍스트 셀로 만들지 않는다.
- 실제 Excel 조건부 서식을 추가하지 않는다.
- 그룹 색상을 제품 ID 해시로 정해 인접 블록이 같은 색이 되게 하지 않는다.
- `xlsx.ts`의 기존 Header/Total 스타일을 전역 변경해 판매성과 XLSX를 깨뜨리지 않는다.
- 조합마다 수십 개의 수동 style enum과 hard-coded style ID를 복사해 만들지 않는다.
- 남아 있는 미추적 HTML 목업 파일을 삭제하거나 정리하지 않는다.
- 사용자가 요청하지 않은 DB 스키마 변경을 하지 않는다.

---

## 17. 다음 세션 시작 체크리스트

1. 이 `0730수정계획.md`를 처음부터 끝까지 읽는다.
2. `git status --short`로 기존 변경과 미추적 파일을 확인한다.
3. 현재 브랜치와 최신 커밋이 문서 작성 시점과 달라졌는지 확인한다.
4. `apps/web/src/lib/xlsx.ts`가 여전히 고정 14개 스타일 구조인지 확인한다.
5. `apps/web/src/app/sales/page.tsx`가 공용 XLSX 생성기를 사용한다는 점을 기억한다.
6. `apps/web/src/app/coupang/daily-report/page.tsx`에서 CSV와 XLSX가 여전히 `exportColumns`를 공유하는지 확인한다.
7. `CoupangDailyPreviousMetrics`에 전일 매출이 아직 없는지 확인한다.
8. 관련 기준 테스트를 먼저 실행한다.
9. API 전일 매출 계약부터 추가한다.
10. Web export row에 블록 메타데이터를 추가한다.
11. 공용 XLSX 생성기를 하위 호환 방식으로 확장한다.
12. 쿠팡 XLSX 전용 빌더와 테스트를 만든다.
13. CSV와 XLSX 열 정의를 분리한다.
14. 하락 경계값 `> 10`을 테이블 테스트로 고정한다.
15. 실제 Excel에서 시각 검증하고 판매성과 XLSX도 함께 확인한다.
16. 전체 test/lint/build를 통과시킨 뒤 완료 조건을 다시 확인한다.

이 작업의 핵심은 단순히 “행에 색을 칠하는 것”이 아니다. **서버의 정확한 오늘·전일 지표를 유지한 채, 한 제품 블록의 합계·옵션·메모를 동일한 시각 단위로 묶고, 판매수 증감 중 사용자가 주목할 값만 현재 판매수 셀에서 즉시 식별할 수 있는 XLSX를 만드는 것**이다.
