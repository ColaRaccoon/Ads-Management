# Meta 동영상 재생 지표 저장·계산·표시 수정 계획

작성일: 2026-08-10  
대상 저장소: `C:\Users\seong\Desktop\workspace\Meta-Ads-Performance`  
참조 CSV: `C:\Users\seong\Desktop\항목추가\Patima-group-파티마그룹-광고-2026.-8.-10.-~-2026.-8.-10..csv`

## 1. 이 문서의 목적

이 문서는 다음 작업 세션이 다른 대화 내용이나 추가 설명 없이 이 파일 하나만 읽고도 구현, 마이그레이션, 테스트, 화면 검증까지 완료할 수 있도록 작성한 실행 계획서다. 이번 세션에서는 실제 코드나 DB 스키마를 수정하지 않는다.

핵심 요구사항은 다음과 같다.

1. Meta 광고 단위 CSV에서 동영상 재생 절대값 5개를 읽는다.
2. DB에는 CSV 원본의 절대 재생 수만 저장한다. 재생률은 DB에 저장하지 않는다.
3. Meta Daily Report와 Ads 탭에서 각 재생 수를 `도달`로 나눈 비율을 퍼센트로 보여준다.
4. 기존 CSV, 일부 동영상 컬럼만 있는 과도기 CSV, 새 컬럼 5개가 모두 있는 CSV를 모두 호환한다.
5. 기존 업로드 엔드포인트와 기존 소재 집계 엔드포인트를 재사용하고, 같은 계산을 화면마다 중복 구현하지 않는다.

## 2. 구현 완료 후 사용자가 보게 될 결과

Meta Daily Report와 Ads 탭의 소재별 성과 표에 아래 항목을 추가한다.

- 도달
- 3초 재생률
- 25% 재생률
- 50% 재생률
- 75% 재생률
- 100% 재생률

표시 계산식은 모두 동일하다.

```text
3초 재생률(%)   = 동영상 3초 이상 재생 절대값 / 도달 절대값 * 100
25% 재생률(%)   = 동영상 25% 재생 절대값 / 도달 절대값 * 100
50% 재생률(%)   = 동영상 50% 재생 절대값 / 도달 절대값 * 100
75% 재생률(%)   = 동영상 75% 재생 절대값 / 도달 절대값 * 100
100% 재생률(%)  = 동영상 100% 재생 절대값 / 도달 절대값 * 100
```

예를 들어 도달 84, 3초 재생 41, 25% 재생 8, 50% 재생 5, 75% 재생 2, 100% 재생 2이면 화면에는 각각 `48.81%`, `9.52%`, `5.95%`, `2.38%`, `2.38%`를 표시한다. DB에는 `41, 8, 5, 2, 2`만 저장한다.

## 3. 실제 새 CSV 확인 결과

계획은 추정 헤더가 아니라 사용자가 지정한 실제 CSV를 확인한 결과를 기준으로 한다.

### 3.1 파일 구조

- 행 수: 53개
- 전체 헤더 수: 41개
- 인코딩: UTF-8로 정상 해석됨
- 기존 식별·성과 컬럼을 그대로 포함하고 마지막에 동영상 재생 컬럼 5개가 추가됨
- 광고 ID도 포함됨
- 컬럼 순서가 달라지거나 이후 다른 컬럼이 더 추가되더라도 헤더 이름으로 파싱해야 하며, 전체 컬럼 수나 위치로 판별하면 안 됨

### 3.2 반드시 사용할 정확한 CSV 헤더

사용자의 설명에는 “25% 이상”이라고 표현되어 있지만 실제 Meta CSV 헤더에는 `이상`이 없다. 구현은 아래 문자열과 정확히 일치해야 한다.

| 의미 | 실제 CSV 헤더 |
|---|---|
| 3초 이상 재생 수 | `동영상 3초 이상 재생` |
| 25% 지점 재생 수 | `동영상 25% 재생` |
| 50% 지점 재생 수 | `동영상 50% 재생` |
| 75% 지점 재생 수 | `동영상 75% 재생` |
| 100% 지점 재생 수 | `동영상 100% 재생` |

### 3.3 실제 값 특성

- 값이 있는 셀은 음수가 아닌 정수 문자열이다.
- 이미지 소재로 보이는 다수 행은 동영상 관련 5개 셀이 모두 비어 있다.
- 일부 동영상 행은 3초, 25%, 50% 값은 있지만 75%, 100% 셀이 비어 있다.
- 어떤 행은 3초 값만 있고 이후 단계가 모두 비어 있다.
- 실제 데이터에 75% 재생 수가 50% 재생 수보다 큰 행이 확인됐다. 따라서 `3초 >= 25% >= 50% >= 75% >= 100%` 같은 단조 감소 검증을 추가하면 정상 Meta 데이터를 거부하게 된다.
- 확인한 새 파일에서는 재생 수가 도달보다 큰 행은 없었지만, 동영상 재생은 고유 사용자 수가 아니라 이벤트 수일 가능성이 있으므로 계산 결과를 100%로 강제 제한해서는 안 된다.

### 3.4 저장소에 이미 존재하는 파일과의 차이

- 기존 저장 파일 다수에는 동영상 재생 컬럼이 전혀 없다.
- 2026-08-09 저장 파일 중 하나에는 `동영상 3초 이상 재생`만 있고 25/50/75/100% 컬럼은 없다.
- 새 참조 CSV에는 5개 컬럼이 모두 있다.
- 따라서 “없으면 모두 0”으로 처리하면 구형 CSV의 미수집 데이터를 실제 0회로 오해하게 된다. DB 컬럼은 nullable이어야 하고, 파일에 헤더 자체가 없는 경우 `null`로 저장해야 한다.

## 4. 현재 코드의 데이터 흐름과 수정 지점

현재 Meta 광고 단위 데이터는 아래 흐름을 사용한다.

```text
POST /api/uploads/meta-ad-daily-csv
  -> UploadsService.importMetaAdDailyCsv()
  -> MetaAdDailyCsvParser.parseBuffer()/parseRow()
  -> upload_batches / upload_rows 기록
  -> UploadsService.importAdDailyMetric()
  -> meta_ad_daily_metrics 저장
  -> UploadsService.refreshAdsetAggregatesFromAdMetrics()
  -> meta_adset_daily_metrics 파생 집계 갱신

GET /api/metrics/ads/creatives
  -> MetricsService.creativeMetrics()
  -> aggregateCreativeDailyRows()
  -> apps/web/src/app/ads/page.tsx
  -> apps/web/src/app/daily-report/page.tsx
```

중요한 결론:

- Ads 탭과 Meta Daily Report는 이미 같은 `GET /api/metrics/ads/creatives` 응답을 사용한다.
- 새 업로드 엔드포인트, 새 리포트 엔드포인트, 화면별 전용 계산 엔드포인트를 만들 필요가 없다.
- `reports.service.ts`와 `reports.controller.ts`는 이 두 화면의 데이터 원천이 아니므로 이번 변경을 위해 건드리지 않는다.
- `apps/api/src/domain/meta-csv.ts`는 구형 광고세트 단위 CSV 파서다. 이번 추가 컬럼은 광고 단위 파서인 `meta-ad-daily-csv.ts`에만 반영한다.
- 동영상 절대값의 원본 저장소는 `meta_ad_daily_metrics`로 한정한다. 같은 값을 `meta_adset_daily_metrics`에도 중복 저장하면 두 테이블 간 불일치 위험이 생기므로 이번 범위에서는 추가하지 않는다.

## 5. 확정할 데이터 계약

아래 이름을 백엔드, DB, API에서 일관되게 사용한다.

| CSV 헤더 | 파서/Prisma 필드 | DB 컬럼 | API 절대값 | API 표시용 비율 |
|---|---|---|---|---|
| `동영상 3초 이상 재생` | `videoPlay3sCount` | `video_play_3s_count` | `videoPlay3sCount` | `videoPlay3sRatePct` |
| `동영상 25% 재생` | `videoPlay25Count` | `video_play_25_count` | `videoPlay25Count` | `videoPlay25RatePct` |
| `동영상 50% 재생` | `videoPlay50Count` | `video_play_50_count` | `videoPlay50Count` | `videoPlay50RatePct` |
| `동영상 75% 재생` | `videoPlay75Count` | `video_play_75_count` | `videoPlay75Count` | `videoPlay75RatePct` |
| `동영상 100% 재생` | `videoPlay100Count` | `video_play_100_count` | `videoPlay100Count` | `videoPlay100RatePct` |

기존 `reach` 필드는 분모이자 화면의 `도달` 값으로 그대로 사용한다.

### 5.1 단위 규칙

- `videoPlay*Count`: 횟수 단위 정수 또는 `null`
- `videoPlay*RatePct`: API에서 퍼센트 숫자 단위. 예를 들어 48.81%는 `48.81`로 응답
- 웹 화면: `48.81%` 문자열로 포맷
- XLSX 내부 percent 셀: 48.81을 그대로 넣지 말고 `0.4881`로 변환한 뒤 `0.00%` 서식 적용
- DB에는 `videoPlay*RatePct`에 해당하는 컬럼을 절대 만들지 않음

## 6. null, 빈 셀, 0의 처리 규칙

이 구분은 구형/신형 CSV 호환성과 실제 이미지 소재를 정확히 표현하기 위해 필수다.

### 6.1 파일에 헤더가 없는 경우

- 해당 필드는 `null`
- 구형 CSV에 컬럼이 없다는 뜻이며 0회로 간주하지 않음
- 업로드 자체는 성공해야 함
- 화면과 엑셀에서는 `-`로 표시

### 6.2 파일에 헤더는 있지만 한 행의 동영상 셀이 전부 빈 경우

- 그 행의 동영상 5개 필드는 모두 `null`
- 이미지 소재 또는 동영상 지표 미적용 행으로 취급
- 화면에서는 0%가 아니라 `-`로 표시

### 6.3 한 행에서 동영상 셀 중 하나라도 값이 있는 경우

- 값이 있는 셀은 해당 정수로 저장
- 같은 파일에 존재하는 동영상 헤더의 빈 셀은 0회로 저장
- 파일에 헤더 자체가 없는 단계는 여전히 `null`
- 예: 3초가 35, 25%가 5, 50%가 2이고 75/100% 셀이 빈 신형 행이면 `35, 5, 2, 0, 0`
- 예: 과도기 CSV에 3초 헤더만 있고 값이 35이면 `videoPlay3sCount=35`, 나머지 네 필드는 `null`

### 6.4 잘못된 값

- 음수, 숫자로 해석할 수 없는 비어 있지 않은 문자열은 현재 파서의 다른 숫자 컬럼과 동일하게 행 검증 오류로 기록하고 그 행을 import하지 않음
- 쉼표가 포함된 정수 문자열은 기존 `parseNumberValue()`가 지원하는 방식으로 정상 처리
- 소수값은 기존 count 필드와 같은 정책을 사용하되, 테스트로 동작을 고정해야 함. 기존 구현은 `Math.trunc()`를 사용하므로 이번 필드만 별도 반올림 규칙을 만들지 않음
- 단계 간 대소관계는 검증하지 않음

### 6.5 집계 구간에 null과 숫자가 섞인 경우

화면의 `도달`과 퍼센트가 같은 분모를 사용하도록 보수적으로 처리한다.

- 소재 집계에 포함된 행이 모두 해당 재생 절대값을 가지고 있을 때만 절대값을 합산하고 비율을 계산
- 하나라도 해당 필드가 `null`이면 그 단계의 집계 count와 rate를 모두 `null`로 반환
- 이 규칙으로 구형 데이터와 신형 데이터가 섞인 기간을 일부 신형 데이터만으로 계산해 전체 기간의 비율처럼 오해하는 일을 막음
- 5개 단계는 각각 독립적으로 완전성 여부를 판단. 3초는 계산 가능하지만 25% 이후는 `null`일 수 있음
- 모든 행이 숫자이고 합산 도달이 0이면 count는 합산값을 유지하되 rate는 `null`
- count 합계가 0이고 도달 합계가 양수면 rate는 정확히 `0`

## 7. DB 및 Prisma 수정 계획

### 7.1 `apps/api/prisma/schema.prisma`

`MetaAdDailyMetric`의 `reach` 근처에 다음 nullable 정수 필드를 추가한다.

```prisma
videoPlay3sCount   Int? @map("video_play_3s_count")
videoPlay25Count   Int? @map("video_play_25_count")
videoPlay50Count   Int? @map("video_play_50_count")
videoPlay75Count   Int? @map("video_play_75_count")
videoPlay100Count  Int? @map("video_play_100_count")
```

결정 근거:

- 원본이 일별 광고 단위의 음수 아닌 정수임
- 기존 `reach`도 `Int`임
- nullable이어야 구형 CSV의 “미수집”과 신형 CSV의 실제 0을 구분할 수 있음
- default 0을 지정하면 안 됨
- 비율 컬럼은 만들지 않음

### 7.2 신규 마이그레이션

권장 폴더명:

```text
apps/api/prisma/migrations/20260810170000_add_meta_video_play_counts/migration.sql
```

마이그레이션 순서:

1. `meta_ad_daily_metrics`에 nullable INTEGER 5개 추가
2. 기존 `raw_row` JSONB에서 값이 존재하는 과거 업로드를 백필
3. 각 컬럼에 `NULL OR value >= 0` 체크 제약 추가

마이그레이션은 모든 importVersion을 대상으로 한다. 현재 행만 백필하면 과거 버전 복원 시 지표가 사라질 수 있기 때문이다.

### 7.3 기존 raw JSON 백필 규칙

`meta_ad_daily_metrics.raw_row`에는 원본 CSV 행이 이미 보존돼 있다. 따라서 이미 업로드된 3초 컬럼 또는 향후 배포 전에 업로드된 새 형식 행도 재업로드 없이 복구할 수 있다.

SQL은 각 행에 대해 다음을 판별해야 한다.

```text
row_has_any_video_value = 5개 실제 헤더 값 중 trim 후 비어 있지 않은 값이 하나라도 존재
```

각 대상 컬럼의 백필 규칙:

```text
raw_row에 해당 헤더 key 자체가 없음 -> NULL
5개 값이 모두 빈 행 -> NULL
행에 다른 동영상 값이 있고 해당 셀이 빈 문자열 -> 0
해당 셀이 유효한 음수 아닌 숫자 -> 쉼표 제거 후 INTEGER
그 외 잘못된 기존 값 -> NULL로 남기고 배포 전 검증 쿼리로 건수 확인
```

PostgreSQL에서는 `raw_row ? '헤더명'`으로 key 존재 여부를 확인하고, `raw_row ->> '헤더명'`으로 문자열을 읽는다. 빈 문자열과 헤더 부재를 같은 `COALESCE(..., 0)`로 처리하면 안 된다.

백필 전 검증 쿼리를 함께 준비해 다음을 확인한다.

- 헤더 key가 하나라도 있는 행 수
- 각 헤더별 비어 있지 않은 값 수
- 쉼표 제거 후 정수로 변환되지 않는 값 수
- 음수 값 수
- 백필 후 `raw_row` 값과 새 컬럼이 불일치하는 행 수

### 7.4 수정하지 않을 DB 모델

`MetaAdsetDailyMetric`에는 이번 5개 필드를 추가하지 않는다.

- Ads 탭과 Meta Daily Report는 `MetaAdDailyMetric`을 직접 조회한다.
- `MetaAdsetDailyMetric`은 광고 단위 원본에서 다시 만들어지는 파생 집계다.
- 같은 절대값을 두 테이블에 저장하면 overwrite, delete/restore, snapshot 비활성화 시 동기화 지점이 늘어난다.
- 이후 광고세트/대시보드에도 재생률이 필요해지면 `MetaAdDailyMetric`에서 공통 집계 함수를 호출하는 방식으로 확장한다.

## 8. CSV 파서 수정 계획

대상 파일: `apps/api/src/domain/meta-ad-daily-csv.ts`

### 8.1 스키마 버전

```text
META_AD_DAILY_SCHEMA_VERSION: meta_ad_daily_v1 -> meta_ad_daily_v2
```

버전은 `UploadBatch.columnSchema`에 기록되는 파서 계약 버전이다. 구형 CSV도 v2 파서로 정상 import되므로 “파일 버전”과 혼동하지 않는다.

### 8.2 중복 없는 컬럼 정의

5개 헤더와 필드명을 여러 배열에 손으로 반복하지 않도록 하나의 descriptor 상수를 추가한다. 이 상수는 10절의 신규 `meta-video-metrics.ts`에서 export하고 CSV 파서와 집계기가 함께 import하는 것을 우선한다. 그러면 CSV 헤더, count 필드, rate 필드의 대응 관계가 백엔드 안에서 한 곳에만 존재한다. 파일 의존성을 단순하게 유지해야 한다면 파서 파일에 두되 집계 파일에서 같은 목록을 다시 선언하지 않는다.

예시 구조:

```ts
const META_VIDEO_PLAY_COLUMNS = [
  { csvColumn: "동영상 3초 이상 재생", countField: "videoPlay3sCount", rateField: "videoPlay3sRatePct" },
  { csvColumn: "동영상 25% 재생", countField: "videoPlay25Count", rateField: "videoPlay25RatePct" },
  { csvColumn: "동영상 50% 재생", countField: "videoPlay50Count", rateField: "videoPlay50RatePct" },
  { csvColumn: "동영상 75% 재생", countField: "videoPlay75Count", rateField: "videoPlay75RatePct" },
  { csvColumn: "동영상 100% 재생", countField: "videoPlay100Count", rateField: "videoPlay100RatePct" }
] as const;
```

이 descriptor를 기준으로 다음을 갱신한다.

- `META_AD_DAILY_CSV_COLUMNS`
- `META_AD_DAILY_CSV_COLUMN_MAPPINGS`
- `META_AD_DAILY_OPTIONAL_COLUMNS`
- 동영상 헤더 전체/부분 존재 여부 검사

5개는 기존 CSV 호환을 위해 `required`나 `recommended`가 아니라 `optional`로 둔다.

### 8.3 헤더 검증 정책

- 기존 필수 헤더 목록은 그대로 유지
- `도달`은 기존 파일 호환을 위해 새 필수 컬럼으로 승격하지 않되 `META_AD_DAILY_RECOMMENDED_COLUMNS`에는 포함해 누락 시 경고를 남김. 도달 헤더가 없거나 합계가 0이면 재생 count는 저장할 수 있어도 rate는 null
- 동영상 헤더가 0개인 구형 CSV: 오류 없이 허용하고 불필요한 warningCount도 늘리지 않음
- 5개가 모두 있는 신형 CSV: 정상
- 1~4개만 있는 과도기 CSV: import는 허용하되 “동영상 재생 컬럼이 일부만 존재하며 누락 단계는 표시할 수 없음” 경고 1개와 누락 헤더 목록을 남김
- 전체 컬럼 개수 41을 필수 조건으로 사용하지 않음
- 알 수 없는 추가 컬럼은 기존처럼 무시하되 `rawRow`와 `columnSchema.columns`에는 보존

가능하면 `MetaAdDailyPreviewSummary`에 다음을 추가해 업로드 결과만으로 스키마 상태를 확인할 수 있게 한다.

```ts
videoMetricSchema: "NONE" | "PARTIAL" | "FULL";
presentVideoColumns: string[];
missingVideoColumns: string[];
```

업로드 화면에 이 메타데이터를 새로 표시하는 것은 필수 범위가 아니지만, API 응답과 batch preview에는 남겨 운영 확인이 가능해야 한다.

### 8.4 `ParsedMetaAdDailyRow`

다음 필드를 추가한다.

```ts
videoPlay3sCount: number | null;
videoPlay25Count: number | null;
videoPlay50Count: number | null;
videoPlay75Count: number | null;
videoPlay100Count: number | null;
```

### 8.5 행 파싱 헬퍼

기존 `count()`의 숫자 검증을 복사하지 말고 재사용 가능한 내부 헬퍼로 정리한다.

필요한 기능:

- `Object.prototype.hasOwnProperty.call(rawRow, columnName)`으로 헤더 존재 여부 확인
- 5개 셀 중 하나라도 실제 값이 있는지 먼저 계산
- 헤더 부재, 행 전체 미적용, 적용 행의 빈 셀을 6절 규칙에 따라 구분
- `parseNumberValue()`를 재사용
- invalid/negative issue 형식은 기존 `count()`와 동일
- 유효 count는 `Math.trunc()`로 기존 정책 유지

`parseRow()` 결과 객체에 5개 절대값을 포함하면 `jsonSafeAdParsedRow()`는 현재 spread 방식이므로 날짜 처리 외 별도 복제 없이 자동으로 `UploadRow.parsedRow`에 포함된다.

## 9. 업로드 서비스 수정 계획

대상 파일: `apps/api/src/uploads/uploads.service.ts`

### 9.1 `importAdDailyMetric()`

`tx.metaAdDailyMetric.create({ data: ... })`에 다음을 그대로 전달한다.

```ts
videoPlay3sCount: input.parsedRow.videoPlay3sCount,
videoPlay25Count: input.parsedRow.videoPlay25Count,
videoPlay50Count: input.parsedRow.videoPlay50Count,
videoPlay75Count: input.parsedRow.videoPlay75Count,
videoPlay100Count: input.parsedRow.videoPlay100Count,
```

여기서 퍼센트를 계산하거나 Decimal로 변환하지 않는다.

### 9.2 기존 업로드 정책 유지

- 엔드포인트는 `POST /api/uploads/meta-ad-daily-csv` 그대로 사용
- `SKIP`, `OVERWRITE`, `NEW_VERSION` 정책 그대로 사용
- snapshot key와 중복 판단 key에는 동영상 값을 포함하지 않음. 동일 날짜/광고의 지표 변경은 기존 overwrite/version 정책으로 처리
- `rawRow`, `UploadRow.parsedRow`, `UploadBatch.columnSchema` 보존 방식 유지
- delete/restore 로직은 Prisma row 전체를 복원하므로 새 필드 때문에 별도 key 변경이 필요 없음

### 9.3 `refreshAdsetAggregatesFromAdMetrics()`

이번 범위에서는 `AdsetAggregateInput`, `aggregateAdRows()`, `importAdsetAggregateMetric()`에 동영상 필드를 추가하지 않는다. 7.4의 중복 저장 금지 결정을 따른다.

## 10. 백엔드 공통 집계 로직

### 10.1 새 공통 도메인 헬퍼

권장 신규 파일:

```text
apps/api/src/domain/meta-video-metrics.ts
apps/api/src/domain/meta-video-metrics.spec.ts
```

역할:

- `reach`와 5개 nullable count를 받음
- 6절의 완전성 규칙을 적용
- 절대값 합계와 퍼센트 비율을 한 번만 계산
- Ads, Daily Report, 캠페인/광고 상세 집계가 같은 수학을 사용하게 함

권장 입력 타입:

```ts
type MetaVideoMetricRow = {
  reach: number;
  videoPlay3sCount: number | null;
  videoPlay25Count: number | null;
  videoPlay50Count: number | null;
  videoPlay75Count: number | null;
  videoPlay100Count: number | null;
};
```

권장 결과 타입:

```ts
type MetaVideoMetricTotals = {
  reach: number;
  videoPlay3sCount: number | null;
  videoPlay25Count: number | null;
  videoPlay50Count: number | null;
  videoPlay75Count: number | null;
  videoPlay100Count: number | null;
  videoPlay3sRatePct: number | null;
  videoPlay25RatePct: number | null;
  videoPlay50RatePct: number | null;
  videoPlay75RatePct: number | null;
  videoPlay100RatePct: number | null;
};
```

비율 헬퍼는 다음 성질을 가져야 한다.

```text
count가 null -> rate null
reach가 0 -> rate null
그 외 -> count / reach * 100
반올림하지 않은 number 반환
0~100 범위로 clamp하지 않음
```

반올림은 화면 포맷에서만 2자리로 한다. API 계산 단계에서 반올림하면 후속 집계나 엑셀 값의 정밀도가 손실된다.

### 10.2 `apps/api/src/metrics/metrics.service.ts`

공통 헬퍼를 다음 두 곳에서 호출해 `totals`에 spread한다.

1. `aggregateAdDailyRows()`
2. `aggregateCreativeDailyRows()`

`aggregateCreativeDailyRows()`가 `GET /metrics/ads/creatives`의 직접 경로이므로 Ads 탭과 Meta Daily Report 요구사항은 이 수정으로 동시에 충족된다.

`aggregateAdDailyRows()`에도 같은 결과를 포함하면 기존 캠페인/광고/광고세트 상세 응답이 additive하게 동일 지표를 받을 수 있다. 별도 계산을 작성하지 않는다.

기존 `PeriodMetricCalculator`는 광고세트 단위 재무/CTR 계산에도 널리 사용되므로 동영상 null 완전성 로직을 그 클래스에 억지로 섞지 않는다. 동영상 전용 작은 헬퍼를 합성하는 편이 기존 대시보드 응답을 불필요하게 바꾸지 않고 재사용성도 높다.

### 10.3 API 응답 계약

기존 `GET /api/metrics/ads/creatives`의 각 행 `totals`에 아래 필드가 additive하게 추가된다.

```json
{
  "reach": 84,
  "videoPlay3sCount": 41,
  "videoPlay25Count": 8,
  "videoPlay50Count": 5,
  "videoPlay75Count": 2,
  "videoPlay100Count": 2,
  "videoPlay3sRatePct": 48.80952380952381,
  "videoPlay25RatePct": 9.523809523809524,
  "videoPlay50RatePct": 5.952380952380952,
  "videoPlay75RatePct": 2.380952380952381,
  "videoPlay100RatePct": 2.380952380952381
}
```

구형/미적용 데이터는 관련 count와 rate가 `null`이다. JSON 직렬화 문제를 피하기 위해 신규 DB 필드는 BigInt가 아닌 Int로 유지한다.

## 11. 웹 공통 타입과 표시 정의

현재 `CreativePerformanceRow` 타입이 Ads 페이지, Daily Report 페이지, `meta-daily-xlsx.ts`에 중복돼 있다. 이번에 필드 11개를 세 군데에 각각 복사하면 누락 가능성이 크므로 공통화한다.

권장 신규 파일:

```text
apps/web/src/types/meta.ts
apps/web/src/lib/meta-video-display.ts
```

### 11.1 `apps/web/src/types/meta.ts`

다음을 한 번만 선언해 Ads 페이지, Daily Report 페이지, 두 XLSX 빌더가 import한다.

- `MetaVideoMetricTotals`
- 기존 재무/클릭 필드를 포함한 `MetaCreativeMetricTotals`
- `MetaCreativePerformanceRow`

기존 각 파일의 로컬 `CreativePerformanceRow`/`MetaDailyCreativeRow` 정의는 가능한 범위에서 이 공통 타입을 재사용한다.

### 11.2 `apps/web/src/lib/meta-video-display.ts`

5개 비율 컬럼의 key와 사용자 라벨을 한 descriptor로 선언한다.

```text
videoPlay3sRatePct   -> 3초 재생률
videoPlay25RatePct  -> 25% 재생률
videoPlay50RatePct  -> 50% 재생률
videoPlay75RatePct  -> 75% 재생률
videoPlay100RatePct -> 100% 재생률
```

화면별 column 배열이 이 descriptor를 이용하거나 최소한 이 타입/라벨을 import하도록 한다. 퍼센트 포맷 함수도 공통으로 두어 `null -> '-'`, 숫자 -> 소수 둘째 자리 `%` 규칙을 일치시킨다. Ads 탭의 기존 CTR 렌더링도 이 함수로 바꿔 null일 때 `-%`가 되는 기존 표현을 함께 없애고, Daily Report의 기존 로컬 `formatPercent()`는 공통 함수로 대체한다.

## 12. Ads 탭 수정 계획

대상 파일: `apps/web/src/app/ads/page.tsx`

### 12.1 표 컬럼

기존 CPM 뒤에 다음 순서로 추가한다.

1. 도달
2. 3초 재생률
3. 25% 재생률
4. 50% 재생률
5. 75% 재생률
6. 100% 재생률

표시 규칙:

- 도달: `numberFmt()`로 천 단위 구분
- 비율: 공통 `formatPercent()`로 소수 둘째 자리까지 표시
- null: `-`
- API가 이미 계산한 rate만 표시하고 브라우저에서 count/reach를 다시 계산하지 않음

### 12.2 정렬

`SortKey`, `columns`, `sortValue()`에 도달과 5개 비율 key를 추가한다.

- 숫자 오름/내림차순 지원
- null은 기존 `compareValues()` 정책대로 마지막에 배치
- localStorage의 기존 sort 설정은 그대로 읽을 수 있어야 함

### 12.3 Ads 엑셀 출력 정리

현재 Ads 페이지 안에는 직접 ZIP/XLSX를 만드는 코드와 사용되지 않는 구형 `buildExcelXml()` 계열 코드가 함께 있다. 새 컬럼을 양쪽에 중복 추가하지 않는다.

권장 작업:

1. `apps/web/src/lib/meta-ads-xlsx.ts`와 spec 파일을 신설
2. 이미 있는 `apps/web/src/lib/xlsx.ts`의 `buildXlsxWorkbook()`을 재사용
3. Ads 페이지의 ZIP, CRC32, XML 생성 함수와 사용되지 않는 `buildExcelXml()/excelCell()` 블록 제거
4. 표와 동일하게 도달 및 5개 비율을 출력
5. 비율 셀은 API의 percent 숫자를 100으로 나눠 실제 Excel percentage 값으로 저장
6. null은 빈 숫자 대신 `-` 텍스트로 유지

이 리팩터링은 새 기능 구현 중 한 출력 경로만 갱신되는 오류를 방지하기 위한 범위 내 정리다.

## 13. Meta Daily Report 수정 계획

대상 파일:

- `apps/web/src/app/daily-report/page.tsx`
- `apps/web/src/lib/meta-daily-xlsx.ts`
- `apps/web/src/lib/meta-daily-xlsx.spec.ts`

### 13.1 페이지 타입과 선택 컬럼

`ColumnKey`와 `DAILY_REPORT_COLUMNS`에 다음을 추가한다.

```text
reach
videoPlay3sRatePct
videoPlay25RatePct
videoPlay50RatePct
videoPlay75RatePct
videoPlay100RatePct
```

사용자 라벨과 순서는 Ads 탭과 동일하게 한다.

### 13.2 현재값/전일값 표시

`renderColumnValue()`에 다음 case를 추가한다.

- 도달: 현재/전일 모두 `numberFmt()`
- 재생률 5개: 현재/전일 모두 공통 `formatPercent()`
- 이전 날짜에 구형 데이터만 있으면 전일 값은 `-`

비율은 이미 API가 집계했으므로 `renderColumnValue()`에서 계산하지 않는다.

### 13.3 제품 헤더와 전체 요약

이번 요구사항의 표시 단위는 소재별 표다. `ProductReportHeader`와 전체 재무 요약에는 동영상 지표를 추가하지 않는다.

- 제품 그룹 전체에 대해 Meta reach를 합산하면 사용자 중복 제거가 되지 않음
- 제품 헤더가 과도하게 넓어짐
- 필요 시 별도 요구사항으로 공통 서버 집계를 이용해 추가 가능

### 13.4 localStorage 설정 마이그레이션

현재 key는 `meta-ads-performance:daily-report-settings:v1`이다. 기존 사용자가 저장한 visibleColumns에는 새 key가 없으므로 단순히 배열에 컬럼을 추가해도 화면에 나타나지 않을 수 있다.

권장 방식:

- 새 key `meta-ads-performance:daily-report-settings:v2` 사용
- v2가 없고 v1이 있으면 query와 deliveryStatus를 유지
- 기존 visibleColumns 뒤에 새 6개 key를 한 번 추가해 v2로 저장
- v1도 없으면 새 컬럼을 포함한 전체 기본 컬럼 사용
- `isColumnKey()`는 새 key를 모두 허용

이렇게 해야 기능 배포 직후 기존 사용자에게도 새 컬럼이 실제로 보인다.

### 13.5 Daily Report XLSX

`MetaDailyColumnKey`, `MetaDailyCreativeRow.totals`, `META_DAILY_COLUMN_ORDER`, `creativeColumnsFor()`에 동일 6개 항목을 추가한다.

- 도달: Number 스타일
- 비율 5개: API percent 숫자 / 100 후 Percent 스타일
- null: `-`

현재 `META_DAILY_COLUMN_COUNT = 13`은 고정값이다. 새 선택 컬럼을 모두 내보내면 creative column 수가 13을 넘을 수 있으므로 다음처럼 동적으로 바꾼다.

```text
columnCount = max(기존 레이아웃 최소 폭 13, creativeColumns.length, SALES_HEADERS.length, SUMMARY_HEADERS.length)
```

열 너비 배열, merge 범위, padding도 이 동적 columnCount를 사용해야 한다. 그렇지 않으면 화면에서는 보이지만 XLSX에서 오른쪽 컬럼이 누락되거나 merge 범위 밖으로 나갈 수 있다.

## 14. 집계 의미상의 주의사항

Meta의 `도달`은 본래 고유 사용자 지표지만, 현재 DB에는 광고/일자별 집계값만 있고 사용자 ID가 없다. 여러 광고 또는 여러 날짜를 합칠 때 동일 사용자를 다시 제거할 수 없다.

따라서 이 기능의 기간/소재 집계는 사용자 요청에 따라 다음과 같이 계산한다.

```text
집계 재생률 = SUM(광고·일자별 재생 절대값) / SUM(광고·일자별 도달) * 100
```

다음을 하지 않는다.

- 행별 퍼센트의 단순 평균
- 가장 최근 행의 퍼센트 사용
- 도달을 노출이나 3초 재생 수로 대체
- 100% 초과 결과 clamp
- 단계별 수치 강제 보정

화면 라벨은 짧게 `도달`로 유지하되, QA 문서와 코드 주석에는 기간 선택 시 “행별 도달 합계 기준”임을 남긴다.

## 15. 테스트 계획

### 15.1 CSV 파서 테스트

대상: `apps/api/src/domain/meta-ad-daily-csv.spec.ts`

현재 테스트가 구매 result 판별 1건뿐이므로 아래를 추가한다.

1. 새 41컬럼 형식의 최소 fixture가 정확한 5개 절대값을 파싱
2. 정확한 한글 헤더 사용 검증, 특히 `동영상 25% 재생`에 `이상`이 없음을 고정
3. 5개 헤더가 전혀 없는 구형 CSV도 validator 통과, 5개 필드는 null
4. 3초 헤더만 있는 과도기 CSV는 validator 통과, partial schema 경고, 나머지 null
5. 5개 셀이 모두 빈 신형 행은 5개 모두 null
6. 일부 값이 있는 신형 행에서 같은 파일의 빈 단계는 0
7. 0 문자열은 null이 아닌 0
8. 쉼표 정수 파싱
9. 음수와 잘못된 문자열은 issue 생성
10. 75% 값이 50%보다 커도 issue를 만들지 않음
11. 헤더 순서가 달라도 이름 기준으로 정상 파싱
12. 알 수 없는 추가 컬럼이 있어도 정상 파싱
13. preview에 `NONE/PARTIAL/FULL` 및 present/missing 목록이 정확히 들어감

실제 업무 CSV 전체를 저장소 fixture로 복사하지 말고, 확인된 헤더와 몇 개의 익명화된 행만 테스트 문자열로 만든다.

### 15.2 공통 동영상 집계 테스트

대상: `apps/api/src/domain/meta-video-metrics.spec.ts`

1. 단일 행 공식 검증: 도달 84, count 41 -> 약 48.8095238
2. 여러 행은 `SUM(count)/SUM(reach)`이며 행별 비율 평균이 아님
3. count 0, reach 양수 -> 0%
4. reach 0 -> rate null
5. 해당 단계에 null 행이 하나라도 섞이면 그 단계 count/rate null
6. 한 단계 null이 다른 단계 계산을 막지 않음
7. 100% 초과 결과를 그대로 반환
8. 단계 간 대소관계가 비정상이어도 값을 그대로 합산
9. 입력 배열이 비어 있으면 reach 0, 모든 count/rate null

### 15.3 업로드/DB 매핑 테스트

대상: `apps/api/src/uploads/uploads.service.spec.ts` 또는 별도 import service spec

- `ParsedMetaAdDailyRow`의 5개 필드가 Prisma create data로 손실 없이 전달되는지 검증
- OVERWRITE/NEW_VERSION 시 새 current row에 새 count가 저장되고 이전 version은 보존되는지 검증
- 구형 행은 DB에 null, 신형 실제 0은 DB에 0으로 들어가는지 검증
- 업로드 삭제 후 이전 version 복원 시 백필된/저장된 count가 유지되는지 검증

서비스 전체 mock이 과도하면 create data 구성 부분을 작은 순수 함수로 추출해 테스트한다. 테스트만을 위해 같은 매핑을 다시 쓰지 않는다.

### 15.4 Metrics API 테스트

대상: `apps/api/src/metrics/metrics.service.spec.ts`

- `creativeAdMetric()` fixture에 reach와 5개 count 기본값 추가
- `creativeMetrics()`의 totals에 count와 rate가 포함되는지 검증
- 같은 creativeKey의 여러 행이 합산 공식대로 계산되는지 검증
- 구형/신형 행 혼합 시 완전성 규칙에 따라 null인지 검증
- 기존 spend, purchase, CPA, CTR, CPM, ROAS 테스트가 그대로 통과하는지 확인

### 15.5 웹/XLSX 테스트

1. `meta-daily-xlsx.spec.ts`에 도달 및 5개 헤더와 값 검증 추가
2. rate 48.81이 Excel 내부 값 약 0.4881과 Percent 스타일로 기록되는지 검증
3. null은 `-`인지 검증
4. 16개 이상 creative 열에서도 열 정의와 row cell 수, merge 범위가 유효한지 검증
5. 신규 `meta-ads-xlsx.spec.ts`에서 Ads export도 같은 컬럼과 percent 단위를 검증
6. Daily 설정 v1 -> v2 마이그레이션을 순수 함수로 분리해 테스트
7. Ads 정렬에서 rate 숫자 및 null 순서를 검증할 수 있도록 정렬 helper를 export 가능한 lib로 분리하거나 최소 단위 테스트 추가

### 15.6 회귀 및 빌드 명령

구현 완료 후 저장소 루트에서 다음 순서로 실행한다.

```powershell
npm.cmd run prisma:validate
npm.cmd run prisma:generate
npm.cmd --workspace @meta-ads-performance/api run test
npm.cmd --workspace @meta-ads-performance/api run lint
npm.cmd --workspace @meta-ads-performance/web run test
npm.cmd --workspace @meta-ads-performance/web run lint
npm.cmd run build
```

마이그레이션 테스트 환경에서는 실제 PostgreSQL에 신규 migration 적용 후 백필 검증 쿼리도 실행한다.

## 16. 수동 검증 시나리오

### 16.1 구형 CSV

1. 동영상 헤더가 없는 기존 CSV 업로드
2. 업로드 성공 확인
3. 기존 광고비/구매/CTR 데이터가 이전과 동일함을 확인
4. Ads/Daily의 동영상 재생률은 `-`

### 16.2 과도기 CSV

1. `동영상 3초 이상 재생`만 있는 기존 2026-08-09 형식 업로드
2. partial schema 경고 확인
3. 3초 rate만 계산 가능하고 나머지는 `-`

### 16.3 새 참조 CSV

1. 이 문서 상단의 2026-08-10 참조 CSV를 배포 후 업로드
2. 53개 행 import 및 41개 헤더 인식 확인
3. 숫자가 있는 동영상 소재의 도달 및 5개 rate 확인
4. 동영상 값 5개가 모두 빈 이미지 소재는 `-` 확인
5. 뒤 단계가 빈 동영상 행은 해당 단계 0% 확인
6. 실제 75% > 50% 행이 오류 없이 import되는지 확인

### 16.4 기간 혼합

1. Ads 탭에서 구형+신형 날짜가 모두 포함되는 기간 선택
2. 해당 creative의 수집 완전성이 부족한 단계는 `-` 확인
3. 신형 데이터만 포함하도록 기간을 좁히면 rate가 나타나는지 확인

### 16.5 엑셀

1. Ads 탭 엑셀 출력
2. Meta Daily Report에서 새 컬럼을 모두 켠 뒤 엑셀 출력
3. Excel에서 percentage 셀을 클릭했을 때 내부 값이 48.81이 아니라 0.4881인지 확인
4. 화면 숫자와 Excel 표시가 같은지 확인

## 17. 예상 수정 파일 목록

| 파일 | 작업 |
|---|---|
| `apps/api/prisma/schema.prisma` | MetaAdDailyMetric nullable count 5개 추가 |
| `apps/api/prisma/migrations/20260810170000_add_meta_video_play_counts/migration.sql` | 컬럼 추가, raw_row 백필, 음수 방지 제약 |
| `apps/api/src/domain/meta-ad-daily-csv.ts` | v2 헤더 descriptor, nullable count 파싱, schema 상태 |
| `apps/api/src/domain/meta-ad-daily-csv.spec.ts` | 구형/부분/신형/빈값/오류 테스트 |
| `apps/api/src/domain/meta-video-metrics.ts` | 도달 기준 공통 count/rate 집계 |
| `apps/api/src/domain/meta-video-metrics.spec.ts` | 공식, null, 0, 혼합 테스트 |
| `apps/api/src/uploads/uploads.service.ts` | parsed absolute count를 DB create data에 전달 |
| `apps/api/src/uploads/uploads.service.spec.ts` | import/version 매핑 회귀 테스트 |
| `apps/api/src/metrics/metrics.service.ts` | 두 ad-level 집계에서 공통 헬퍼 사용 |
| `apps/api/src/metrics/metrics.service.spec.ts` | creatives API totals 테스트 |
| `apps/web/src/types/meta.ts` | 중복 Meta creative/totals 타입 공통화 |
| `apps/web/src/lib/meta-video-display.ts` | 새 컬럼 key/label/format 공통화 |
| `apps/web/src/app/ads/page.tsx` | 화면 컬럼·정렬·공통 export 호출 |
| `apps/web/src/lib/meta-ads-xlsx.ts` | Ads XLSX 생성 로직을 공통 xlsx 기반으로 분리 |
| `apps/web/src/lib/meta-ads-xlsx.spec.ts` | Ads export 컬럼/퍼센트 테스트 |
| `apps/web/src/app/daily-report/page.tsx` | 선택 컬럼, 전일 비교, 설정 v2 마이그레이션 |
| `apps/web/src/lib/meta-daily-xlsx.ts` | 새 컬럼, 동적 columnCount |
| `apps/web/src/lib/meta-daily-xlsx.spec.ts` | 새 컬럼 및 넓은 레이아웃 검증 |
| `apps/web/src/app/globals.css` | 실제 브라우저 검증 후 필요한 경우에만 최소 폭/스크롤 조정 |

## 18. 명시적으로 수정하지 않을 것

- 신규 API 엔드포인트를 만들지 않음
- `uploads.controller.ts`와 `metrics.controller.ts`의 라우트를 늘리지 않음
- DB에 재생률/퍼센트 컬럼을 만들지 않음
- `MetaAdsetDailyMetric`에 같은 count를 중복 저장하지 않음
- CSV에 없는 count를 0으로 일괄 백필하지 않음
- 단계별 count를 다른 단계 count로 추정하지 않음
- Meta가 준 값을 단조 감소하도록 보정하지 않음
- 비율을 100% 이하로 clamp하지 않음
- 캠페인/광고세트/대시보드 화면에 새 컬럼을 추가하지 않음
- 실제 업무 CSV 전체를 테스트 fixture로 커밋하지 않음
- 현재 작업트리에 존재하는 쿠팡 관련 미완료 변경을 수정하거나 되돌리지 않음

## 19. 구현 순서

다음 순서를 지켜 타입 오류와 배포 중단을 최소화한다.

1. 현재 작업트리와 사용자 변경 확인, 쿠팡 변경 보존
2. Prisma schema와 migration 작성
3. raw_row 백필 SQL을 별도 샘플 DB에서 검증
4. Prisma client generate
5. CSV descriptor, 파서 타입, null/0 규칙 구현 및 parser test
6. UploadsService create data 연결 및 upload test
7. 공통 `meta-video-metrics` 집계 헬퍼와 test 구현
8. MetricsService의 두 ad 집계 함수에 공통 헬퍼 연결
9. `/metrics/ads/creatives` 서비스 테스트로 API 계약 확인
10. 웹 공통 타입/표시 descriptor 정리
11. Ads 표·정렬 수정 및 Ads XLSX 로직 분리
12. Daily Report 표·전일 비교·설정 v2 마이그레이션
13. Daily XLSX 새 컬럼 및 동적 폭 수정
14. 전체 test/lint/build
15. DB migration 적용 및 백필 사후 쿼리
16. 구형, 부분형, 새 참조 CSV 수동 업로드 검증
17. 두 화면과 두 XLSX 최종 검증

## 20. 배포 및 롤백 주의사항

### 배포

1. DB 백업 또는 복구 가능한 snapshot 확보
2. nullable 컬럼 추가 및 백필 migration 적용
3. 새 Prisma client를 포함한 API 배포
4. 웹 배포
5. 백필 결과와 API 응답 확인
6. 필요 시 참조 CSV를 정상 업로드

nullable additive migration이므로 기존 API가 잠시 새 DB와 함께 동작해도 기존 필드에는 영향이 없다. 반대로 새 API를 컬럼 없는 DB에 먼저 배포하면 쿼리가 실패할 수 있으므로 migration을 먼저 적용한다.

### 롤백

- 앱만 이전 버전으로 되돌리는 것은 가능. 이전 앱은 추가 nullable 컬럼을 무시함
- 즉시 컬럼을 DROP하지 않음. 백필된 원본 count 보존
- migration 자체를 되돌려야 한다면 먼저 새 앱이 더 이상 컬럼을 읽지 않는지 확인
- 원본 CSV와 `raw_row`가 있으므로 count 재구성은 가능하지만, 이를 롤백 안전성의 대체 수단으로 삼지 않음

## 21. 완료 조건

아래가 모두 만족돼야 작업 완료로 본다.

- 새 참조 CSV의 정확한 5개 헤더를 읽는다.
- 기존 CSV와 3초-only CSV도 import된다.
- DB에는 nullable 절대 count 5개만 추가되고 rate 컬럼은 없다.
- 기존 raw_row의 새 지표가 안전하게 백필된다.
- Ads/Daily가 같은 API와 같은 서버 집계 결과를 사용한다.
- 도달과 5개 퍼센트가 두 화면에 표시된다.
- 구형/미적용은 `-`, 실제 0은 `0.00%`로 구분된다.
- 기간 집계는 합계 count / 합계 reach이며 비율 평균이 아니다.
- 단계 단조성 검증과 100% clamp가 없다.
- 기존 사용자의 Daily Report 컬럼 설정에서도 새 항목이 노출된다.
- Ads 및 Daily XLSX에도 같은 값과 올바른 percentage 단위가 들어간다.
- 기존 Meta/쿠팡/Cafe24 테스트를 포함한 전체 빌드가 통과한다.
- 신규 endpoint나 중복 퍼센트 계산 함수가 생기지 않는다.
