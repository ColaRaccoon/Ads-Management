# 0731 수정계획 3 - 쿠팡 가구매 매출 전액 차감 및 발생 비용 보존

## 1. 문서 목적

이 문서는 다음 개발 세션이 추가 설명 없이도 쿠팡 가구매 손익 계산의 현재 문제를 이해하고, 사용자가 확정한 회계 의도대로 안전하게 수정할 수 있도록 작성한 구현 계획서다.

이번 수정의 핵심은 단순히 가구매 금액을 한 번 더 빼는 것이 아니다. 현재 코드는 가구매 매출과 판매수량을 정상 판매에서 제거한 뒤, 줄어든 수량과 매출로 상품원가·쿠팡 판매수수료·배송비·VAT 등을 다시 계산한다. 그 결과 가구매로 실제 발생한 비용까지 함께 사라지고, 최종 순이익은 가구매 금액보다 훨씬 적게 감소한다.

사용자가 확정한 의도는 다음과 같다.

> 가구매는 업체를 통해 회사 자금으로 가짜 구매를 발생시키는 행위다. 가구매 구매대금은 회사 돈으로 지출되므로 해당 거래의 매출은 실질적인 회사 매출이 아니다. 가구매 매출은 본매출에서 전액 제외해야 하며, 실제 거래로 발생한 쿠팡 판매수수료·VAT·상품원가·배송비 등은 없어지면 안 된다. 가구매 업체수수료는 별도 비용으로 추가 차감해야 한다.

다음 세션은 이 문서의 산식, 불변식, 예외 정책, 테스트 기준을 우선해야 한다. 특정 상품이나 특정 날짜의 숫자만 맞추는 하드코딩을 해서는 안 된다.

---

## 2. 가장 중요한 결론

### 2.1 가구매가 없는 경우

기존 쿠팡 손익 계산 결과가 변경되면 안 된다.

```text
원본 순이익
= 원본 순매출
- 상품원가
- 쿠팡 판매수수료
- 배송/물류비
- 반품예상비
- 기타비용
- VAT
- 광고비
```

### 2.2 원본 쿠팡 판매행에 가구매가 포함된 경우

가구매 매출은 매출 인식에서 전액 제외하지만, 원본 거래에서 실제 발생한 비용은 그대로 유지한다.

```text
가구매 조정 순매출
= 원본 순매출 - 가구매 매출 조정액

가구매 업체수수료 차감 전 순이익
= 가구매 조정 순매출
- 원본 판매수량 기준 상품원가
- 원본 순매출 기준 쿠팡 판매수수료
- 원본 판매수량 기준 배송/물류비
- 원본 판매수량 기준 반품예상비
- 원본 판매수량 기준 기타비용
- 원본 순매출 기준 VAT
- 광고비

최종 순이익
= 가구매 업체수수료 차감 전 순이익
- 가구매 업체수수료
```

동일한 원본 자료와 비용 설정을 놓고 가구매 입력 전후를 비교하면 다음 불변식이 반드시 성립해야 한다.

```text
가구매 입력 전 순이익 - 가구매 입력 후 순이익
= 가구매 매출 조정액 + 가구매 업체수수료
```

부동소수점 계산을 사용하므로 테스트에서는 필요한 경우 `toBeCloseTo()`를 사용한다.

### 2.3 절대 구현하면 안 되는 산식

현재 동작과 같은 아래 방식은 금지한다.

```text
가구매 조정 순이익
= 조정 순매출
- 조정 판매수량 기준 상품원가
- 조정 순매출 기준 쿠팡 판매수수료
- 조정 판매수량 기준 배송비
- 조정 순매출 기준 VAT
- 업체수수료
```

이 산식은 가구매를 제외하면서 가구매로 이미 발생한 비용도 없애 버린다.

---

## 3. 회계 논리

예를 들어 회사가 업체를 통해 30,000원짜리 상품을 가구매했다고 가정한다.

```text
회사 자금으로 구매대금 지급             -30,000원
쿠팡 판매자로 인식된 매출               +30,000원
쿠팡 판매수수료                          -3,564원
상품원가                                -10,000원
배송비                                   -3,000원
VAT                                      -2,727원
가구매 업체수수료                        -3,182원
```

회사 전체 관점에서 구매대금과 가짜 매출은 상쇄되어야 한다. 그러므로 가구매 매출은 실제 수익으로 남기지 않는다. 반면 판매수수료, 상품원가, 배송비, VAT, 업체수수료는 실제 비용이므로 남겨야 한다.

원본 보고서가 가구매를 정상 판매처럼 포함한 상태의 순이익은 다음과 같다.

```text
30,000
- 3,564
- 10,000
- 3,000
- 2,727
= 10,709원
```

가구매를 올바르게 반영한 최종 순이익은 다음과 같다.

```text
0
- 3,564
- 10,000
- 3,000
- 2,727
- 3,182
= -22,473원
```

따라서 가구매 입력 전후 순이익 차이는 다음과 같다.

```text
10,709 - (-22,473)
= 30,000 + 3,182
= 33,182원
```

쿠팡 판매수수료와 VAT를 가구매 입력 시 한 번 더 중복 차감하라는 뜻이 아니다. 원본 순이익에 이미 포함되어 있던 해당 비용을 가구매 입력 후에도 제거하지 말라는 뜻이다.

---

## 4. 용어와 계산 축

다음 세션은 “실제”, “정상”, “원본”이라는 모호한 단어만으로 구현하지 말고, 내부 코드에서 계산 축을 분명히 구분해야 한다.

### 4.1 원본 보고 판매 축

쿠팡 판매 XLSX에 실제로 기록된 매출과 수량이다. 가구매가 쿠팡 거래로 발생했다면 이 값에는 가구매도 포함되어 있다.

- `reportedSalesKrw`
- `reportedNetSalesKrw`
- `reportedSalesQuantity`
- `reported.segments`

### 4.2 매출 인식 축

회사 매출로 인정할 금액과 판매수량이다. 원본 보고 판매에서 가구매 금액과 수량을 제외한다.

- `recognizedSalesKrw` 또는 현재 API 호환 필드인 `actualSalesKrw`
- `recognizedNetSalesKrw` 또는 현재 API 호환 필드인 `actualNetSalesKrw`
- `recognizedSalesQuantity` 또는 현재 API 호환 필드인 `actualSalesQuantity`
- 현재 `adjustReportedSalesForManualPurchase()`가 만드는 조정 세그먼트

### 4.3 비용 발생 축

실제로 쿠팡 거래가 발생하여 상품원가·수수료·배송비·VAT 등의 비용이 발생한 기준이다.

원본 판매행이 존재하는 정상적인 경우 비용 발생 축은 반드시 원본 보고 판매 축을 사용한다.

- 상품원가: 원본 판매수량 기준
- 쿠팡 판매수수료: 원본 순매출 기준
- 배송/물류비: 원본 판매수량 및 원본 판매방식 세그먼트 기준
- 반품예상비: 이번 수정에서는 보수적으로 원본 판매수량 기준 유지
- 기타 단위비용: 원본 판매수량 기준
- VAT: 원본 순매출 기준
- 광고비: 기존과 동일하게 한 번만 반영

### 4.4 가구매 별도 비용 축

현재 확정된 별도 비용은 업체수수료다.

```text
가구매 업체수수료
= 건당 업체수수료 × 가구매 수량
```

현재 기본 건당 업체수수료는 3,182원이며 설정값으로 변경할 수 있다.

### 4.5 핵심 분리 원칙

```text
매출 인식 기준 != 비용 발생 기준
```

가구매 매출과 수량은 매출 인식에서는 빠지지만, 원본 거래로 발생한 비용의 기준에서는 빠지지 않는다.

---

## 5. 현재 구현 분석

### 5.1 가구매 입력과 저장

관련 파일:

- `apps/web/src/app/coupang/uploads/page.tsx`
- `apps/web/src/lib/coupang-manual-purchase.ts`
- `apps/api/src/coupang/coupang.service.ts`
- `apps/api/src/domain/coupang-profit-calculator.ts`
- `apps/api/prisma/schema.prisma`

현재 웹은 상품별 `가구매 수량`과 메모를 서버로 보낸다. 금액을 직접 입력하는 구조가 아니다.

서버의 `replaceManualPurchasesForDate()`는 다음 스냅샷을 저장한다.

```text
salesAmountKrw = 해당 날짜의 기본판매가 × 가구매 수량
vendorFeeTotalKrw = 건당 업체수수료 × 가구매 수량
totalCostKrw = vendorFeeTotalKrw
```

현재 `calculateCoupangManualPurchaseCost()`는 의도적으로 다음 값을 0으로 만든다.

- `productCostKrw`
- `coupangSalesFeeKrw`
- `shippingCostKrw`
- `otherCostKrw`

이번 수정에서 이 필드들에 원본 비용을 다시 복제해 넣으면 안 된다. 원본 상품원가·쿠팡수수료·배송비·VAT는 정상 손익의 비용 발생 축에서 유지하고, 수동 가구매 스냅샷에는 업체수수료만 저장하는 현재 불변식을 유지한다. 그렇지 않으면 같은 비용을 정상 비용과 수동 비용 양쪽에서 중복 차감할 수 있다.

### 5.2 가구매 매출 및 수량 조정

관련 파일:

- `apps/api/src/domain/coupang-profit-pipeline.ts`

현재 `adjustReportedSalesForManualPurchase()`는 다음 값을 만든다.

```text
actualSalesKrw = reportedSalesKrw - manualPurchaseSalesKrw
actualNetSalesKrw = reportedNetSalesKrw - manualPurchaseSalesKrw
actualSalesQuantity = reportedSalesQuantity - manualPurchaseQuantity
```

이 조정 자체는 매출 인식 축으로 계속 필요하다.

혼합 판매방식인 경우 `deductManualPurchaseFromReportedSegments()`는 판매자배송 세그먼트에서 먼저 수량과 매출을 차감하고, 부족하면 그로스 세그먼트에서 차감한다. 이 규칙은 매출 인식 세그먼트를 만들기 위한 기존 규칙으로 유지할 수 있다.

### 5.3 현재 문제의 직접 원인

현재 `calculateNormalCoupangProfit()`는 조정된 `actual.segments`를 그대로 `calculateCoupangProfitBySegments()`에 전달한다.

`calculateCoupangProfitBySegments()`는 하나의 세그먼트 집합으로 다음을 모두 계산한다.

- 순매출
- 상품원가
- 판매수수료
- 판매방식별 물류비
- 반품예상비
- 기타비용
- VAT

따라서 가구매를 조정한 순간 매출뿐 아니라 비용까지 모두 감소한다.

그 후 `calculateManualPurchaseProfitAdjustment()`와 `combineCoupangProfitParts()`가 업체수수료만 추가 차감한다.

현재 최종 산식은 사실상 다음과 같다.

```text
최종 순이익
= 조정 수량과 조정 매출로 다시 계산한 순이익
- 업체수수료
```

이 구조가 사용자 관점에서 순이익 감소액이 지나치게 작아지는 원인이다.

### 5.4 현재 화면 문구도 기존 정책을 명시하고 있음

다음 화면은 현재 정책을 사용자에게 안내하고 있다.

- `apps/web/src/app/coupang/uploads/page.tsx`
- `apps/web/src/app/coupang/profit/page.tsx`
- `apps/web/src/app/coupang/dashboard/page.tsx`

특히 업로드 화면에는 “가구매 비용은 실제 업체수수료만 최종 순이익에서 차감하며 상품원가·쿠팡수수료·배송비·기타 비용은 추가 차감하지 않는다”는 취지의 문구가 있다.

이 문구는 비용을 중복 차감하지 않는다는 의미로는 맞지만, 현재 계산이 기존 비용 자체까지 제거한다는 사실을 설명하지 못한다. 수정 후에는 “원본 거래에서 발생한 비용은 유지하고 업체수수료만 별도로 추가한다”는 의미가 분명하도록 바꿔야 한다.

---

## 6. 확정 구현 범위

### 6.1 반드시 수정할 것

- 가구매 매출 인식 축과 비용 발생 축 분리
- `calculateNormalCoupangProfit()`이 가구매 조정 세그먼트만으로 모든 비용을 줄이지 않도록 수정
- 원본 판매수량 기준 상품원가·물류비·반품예상비·기타비용 유지
- 원본 순매출 기준 쿠팡 판매수수료와 VAT 유지
- 광고비 한 번만 차감
- 업체수수료 한 번만 추가 차감
- 가구매 입력 전후 순이익 차이 불변식 테스트
- 가구매만으로 원본 판매수량이 0이 되는 경우에도 원본 비용 유지
- 원본 판매행이 없는 가구매의 과소 계산 방지
- 상품별, 기간별, 상품그룹별 합계 반영
- 쿠팡 마진계산, 대시보드, 일일보고서, 내보내기 결과 반영
- 화면 설명과 컬럼 라벨을 새 의미에 맞게 수정

### 6.2 이번 작업에서 하지 않을 것

- 가구매 금액 직접 입력 UI 추가
- 가구매 실제 결제내역 또는 주문번호 매핑 기능 추가
- 기본판매가 대신 프로모션가를 자동 적용
- 가구매 업체수수료 설정 체계 변경
- 쿠팡 광고 상품별 ROAS 집계축 수정
- 일반 쿠팡 판매수수료율 정책 변경
- 쿠팡 VAT 공식 자체 변경
- 기존 적용 완료 마이그레이션 파일 수정

현재 가구매 매출 조정액은 저장된 `salesAmountKrw` 스냅샷을 사용한다. 신규 저장은 지금처럼 해당 날짜 기본판매가 × 수량이다. 실제 업체 결제액이 기본판매가와 다를 수 있다는 문제는 별도 기능으로 다뤄야 하며 이번 회계축 수정에 섞지 않는다.

---

## 7. 권장 도메인 설계

### 7.1 한 세그먼트 배열로 매출과 비용을 함께 계산하지 말 것

가장 중요한 리팩터링은 `calculateCoupangProfitBySegments()` 또는 그 상위 계층에서 매출 인식 세그먼트와 비용 발생 세그먼트를 분리하는 것이다.

권장 개념 타입:

```ts
type CoupangProfitAccountingBases = {
  recognizedSegments: CoupangProfitSegmentInput[];
  incurredSegments: CoupangProfitSegmentInput[];
};
```

- `recognizedSegments`: 가구매 매출과 수량을 제외한 세그먼트
- `incurredSegments`: 원본 보고 세그먼트. 가구매 거래가 포함되어 비용이 실제 발생한 기준

이름은 프로젝트 스타일에 맞게 조정할 수 있지만 의미가 드러나야 한다. `actual`과 `normal`만 사용하면 다음 개발자가 다시 두 축을 합칠 위험이 있다.

### 7.2 계산 책임

새 계산 함수 또는 리팩터링된 함수는 다음과 같이 동작해야 한다.

```text
표시 순매출 = recognizedSegments의 순매출 합계

상품원가 = incurredSegments의 수량 × 단위 상품원가
쿠팡 판매수수료 = incurredSegments의 순매출 × 판매수수료율
배송/물류비 = incurredSegments의 판매방식별 수량 × 단위 물류비
반품예상비 = incurredSegments의 수량 × 반품률 × 단위 반품비
기타비용 = incurredSegments의 수량 × 단위 기타비용
VAT = incurredSegments의 순매출 / 11
광고비 = 입력 광고비

총 정상비용
= 상품원가 + 쿠팡 판매수수료 + 배송/물류비
+ 반품예상비 + 기타비용 + VAT + 광고비

업체수수료 차감 전 순이익
= 표시 순매출 - 총 정상비용
```

ROAS는 이번 수정 범위에서 기존 의미를 유지한다.

### 7.3 구현 방식 선택

다음 두 방식 중 하나를 선택할 수 있다.

#### 방식 A: 명시적인 이중 기준 계산 함수 추가

예:

```ts
calculateCoupangProfitByAccountingBases({
  recognizedSegments,
  incurredSegments,
  cost,
  ads,
  salesFeeRate
})
```

장점:

- 도메인 의미가 가장 분명하다.
- 향후 실수로 비용 기준을 조정 세그먼트로 바꾸기 어렵다.
- 단위 테스트가 명확하다.

#### 방식 B: 원본 비용 계산 후 조정 순매출로 이익 재구성

1. 원본 세그먼트로 현재 `calculateCoupangProfitBySegments()`를 실행한다.
2. 상품원가, 판매수수료, 물류비, VAT, 광고비 등 비용 필드는 그대로 유지한다.
3. 반환 결과의 `netSalesKrw`, `organicSalesKrw`, `marginKrw`, `marginRate`만 조정 순매출 기준으로 다시 계산한다.

장점:

- 기존 계산기를 덜 변경한다.

주의:

- 스프레드로 결과 객체를 덮어쓰는 임시 코드만 만들면 필드 간 불변식이 깨지기 쉽다.
- 재구성 로직은 반드시 이름 있는 도메인 함수로 격리하고 단위 테스트해야 한다.

권장안은 방식 A다. 다만 변경량과 `0731수정계획2.md`의 ROAS 작업 충돌을 줄여야 한다면 방식 B도 허용한다. 어느 방식을 택하든 산식과 테스트 불변식은 동일해야 한다.

---

## 8. 파일별 수정 계획

## 8.1 `apps/api/src/domain/coupang-profit-calculator.ts`

현재 주요 함수:

- `calculateCoupangProfit()`
- `calculateCoupangProfitBySegments()`
- `calculateCoupangManualPurchaseCost()`

수정 목표:

1. 매출 인식 기준과 비용 발생 기준을 분리할 수 있는 계산 진입점을 만든다.
2. 표시 순매출은 가구매 조정 후 금액을 사용한다.
3. 상품원가·판매수수료·물류비·반품비·기타비용·VAT는 원본 비용 발생 기준을 사용한다.
4. 광고비는 한 번만 반영한다.
5. `totalCostKrw`는 원본 발생 비용을 합한 값이어야 한다.
6. `marginKrw = recognizedNetSalesKrw - totalCostKrw`가 성립해야 한다.
7. `marginRate = marginKrw / recognizedNetSalesKrw`를 유지한다. 분모가 0이면 기존 `safeDivide()` 정책을 따른다.
8. `organicSalesKrw`는 기존처럼 조정 순매출에서 광고 전환매출을 뺀다. ROAS/오가닉 매출 축 변경은 `0731수정계획2.md` 범위이므로 이 작업에서 임의 변경하지 않는다.

`calculateCoupangManualPurchaseCost()`는 업체수수료 전용으로 유지한다.

```ts
productCostKrw: 0
coupangSalesFeeKrw: 0
shippingCostKrw: 0
otherCostKrw: 0
totalCostKrw: vendorFeeTotalKrw
```

이 0들은 “해당 비용이 실제로 발생하지 않는다”는 뜻이 아니라, 수동 가구매 별도 비용 스냅샷에 중복 저장하지 않는다는 뜻이다. 필요한 경우 타입/주석으로 이 의미를 명확히 남긴다.

## 8.2 `apps/api/src/domain/coupang-profit-pipeline.ts`

현재 주요 함수:

- `aggregateReportedSalesByProductDate()`
- `aggregateManualPurchasesByProductDate()`
- `adjustReportedSalesForManualPurchase()`
- `deductManualPurchaseFromReportedSegments()`
- `calculateNormalCoupangProfit()`
- `calculateManualPurchaseProfitAdjustment()`
- `combineCoupangProfitParts()`

수정 목표:

### `adjustReportedSalesForManualPurchase()`

- 매출 인식값을 만드는 책임은 유지한다.
- 원본 세그먼트를 파괴하지 않도록 복제 불변식을 유지한다.
- 가구매 매출액과 수량 초과 검증을 유지한다.
- 이 함수가 만든 세그먼트를 비용 기준으로 사용하지 않도록 타입과 호출부를 명확히 한다.

### `calculateNormalCoupangProfit()`

- `actual` 또는 매출 인식 세그먼트는 수익 계산에 사용한다.
- `reported` 원본 세그먼트는 비용 계산에 사용한다.
- 비용 규칙 필요 여부를 조정 수량이 아니라 원본 비용 발생 활동으로 판단한다.
- 모든 원본 수량이 가구매로 제거되어 조정 판매수량이 0이 되어도, 원본 판매행이 있으면 비용 규칙과 판매수수료율이 필요하다.
- 누락 물류비 경고도 원본 비용 발생 세그먼트를 기준으로 검사한다.

### `calculateManualPurchaseProfitAdjustment()`

- 업체수수료만 반환하는 현재 역할을 유지한다.
- `marginAdjustmentKrw = -vendorFeeTotalKrw`를 유지한다.
- 상품원가·쿠팡수수료·VAT를 여기에 다시 넣지 않는다.

### `combineCoupangProfitParts()`

최종 산식은 다음을 유지하되, `normal.calculated`가 새 비용 보존 방식으로 계산되어야 한다.

```ts
totalCostKrw =
  normal.calculated.totalCostKrw + manual.totalCostKrw;

marginKrw =
  normal.calculated.marginKrw - manual.totalCostKrw;
```

테스트로 아래 불변식을 추가한다.

```text
최종 순이익
= 원본 비용을 유지한 조정 순이익
- 업체수수료
```

## 8.3 `apps/api/src/coupang/coupang.service.ts`

주요 수정 지점:

- `manualPurchaseOptions()`
- `replaceManualPurchasesForDate()`
- `buildProductProfitRows()`
- `aggregateCoupangProductProfitGroup()`
- `aggregateCoupangProductDateRows()`
- `summarizeCoupangProductProfitRows()`
- 일일보고서 변환 함수들

### 저장 경로

`replaceManualPurchasesForDate()`의 기본판매가 × 수량 스냅샷과 업체수수료 스냅샷은 유지한다.

DB의 다음 값은 계속 0으로 저장한다.

- 수동 가구매 `productCostKrw`
- 수동 가구매 `coupangSalesFeeKrw`
- 수동 가구매 `shippingCostKrw`
- 수동 가구매 `otherCostKrw`

`totalCostKrw`는 업체수수료와 일치해야 한다.

### `buildProductProfitRows()`

현재 다음 흐름을 분리해야 한다.

```text
reported
-> adjustReportedSalesForManualPurchase()
-> actual
-> calculateNormalCoupangProfit()
```

새 흐름:

```text
reported 원본 세그먼트
-> 비용 발생 기준

reported - manual
-> 매출 인식 기준

두 기준
-> calculateNormalCoupangProfit()

정상 계산 결과
+ 업체수수료
-> 최종 손익
```

특히 현재 다음 조건은 점검해야 한다.

```ts
const requiresNormalCostRule =
  hasCoupangSalesSegmentActivity(actual.segments);
```

가구매로 조정 수량이 0이 되면 `actual.segments`에 활동이 없어져 비용 규칙이 불필요한 것으로 오판할 수 있다. 새 조건은 원본 비용 발생 세그먼트를 기준으로 해야 한다.

예:

```ts
const hasIncurredActivity =
  hasCoupangSalesSegmentActivity(reported.segments);
```

실제 이름은 코드 스타일에 맞추되 의미를 보존한다.

### 반환 필드 의미

- `reported*`: 쿠팡 원본값
- `actual*`: 가구매 매출·수량을 제외한 매출 인식값
- `productCostKrw`: 원본 판매수량 기준 비용
- `salesFeeKrw`: 원본 순매출 기준 비용
- `shippingCostKrw` 및 물류 세부값: 원본 판매수량/판매방식 기준 비용
- `vatKrw`: 원본 순매출 기준 VAT
- `manualPurchaseTotalCostKrw`: 업체수수료만
- `normalMarginKrw`: 가구매 매출 조정 후, 업체수수료 차감 전 순이익
- `marginKrw`: 최종 순이익

현재 `sellerSalesQuantity`와 `growthSalesQuantity`는 물류비 산출 근거로 사용된다. 수정 후 이 필드들은 원본 비용 발생 수량과 일치해야 물류비와 조정 가능하다. 화면에서 조정 판매방식별 수량이 필요하지 않다면 새 필드를 만들지 않는다. 필요하다면 `actualSellerSalesQuantity`처럼 의미가 분명한 별도 필드를 추가하고 기존 비용 근거 필드와 혼용하지 않는다.

### 집계 함수

상품그룹, 기간, 전체 요약은 자식 행을 합산한 값과 정확히 일치해야 한다.

반드시 검증할 합계:

- 원본 매출/수량
- 가구매 조정 매출/수량
- 조정 후 인식 매출/수량
- 원본 기준 상품원가
- 원본 기준 쿠팡 판매수수료
- 원본 기준 배송/물류비
- 원본 기준 VAT
- 업체수수료
- 총비용
- 업체수수료 차감 전 순이익
- 최종 순이익

합계 행에서 비용을 다시 비율 계산하지 말고 완성된 자식 행의 비용을 합산한다.

## 8.4 `apps/api/prisma/schema.prisma` 및 마이그레이션

이번 수정은 원칙적으로 새 DB 컬럼이 필요하지 않다.

이미 존재하는 스냅샷:

- `salesAmountKrw`
- `quantity`
- `vendorFeePerUnitKrw`
- `vendorFeeTotalKrw`
- `baseSalePriceKrw`
- `saleMethod`

과거 가구매 행도 이 값을 이용해 새 조회 산식으로 재계산할 수 있다.

중요:

- 적용 완료된 `20260724000000_normalize_manual_purchase_accounting`을 수정하지 않는다.
- 적용 완료된 `20260724010000_remove_coupang_manual_purchase_vat`을 수정하지 않는다.
- 삭제된 `vat_krw` 컬럼을 수동 가구매 테이블에 다시 추가하지 않는다.
- 수동 가구매 VAT를 별도 스냅샷으로 재도입하지 않는다.

VAT는 원본 판매 손익의 비용 발생 축에 이미 포함되어야 한다. 별도 수동 VAT를 추가하면 중복 차감 위험이 있다.

스키마 변경 없이 구현되면 새 마이그레이션을 만들지 않는다. 불가피한 스키마 변경이 생길 경우 먼저 필요성을 검증하고, 기존 데이터 보존과 역호환 계획을 문서화한 뒤 진행한다.

## 8.5 `apps/web/src/types/coupang.ts`

API 필드명을 유지하더라도 주석 또는 타입 수준에서 의미를 명확히 해야 한다.

특히 다음 필드를 혼동하지 않는다.

- `reportedNetSalesKrw`: 원본 순매출
- `actualNetSalesKrw`: 가구매 매출 조정 후 인식 순매출
- `productCostKrw`: 원본 거래 기준 상품원가
- `salesFeeKrw`: 원본 거래 기준 쿠팡 판매수수료
- `vatKrw`: 원본 거래 기준 VAT
- `manualPurchaseTotalCostKrw`: 업체수수료
- `normalMarginKrw`: 가구매 매출 조정 후, 업체수수료 차감 전 순이익
- `marginKrw`: 최종 순이익

필드명 변경은 API/웹/테스트/내보내기에 파급이 크므로 단순 미관 목적으로 대규모 이름 변경을 하지 않는다. 의미가 심각하게 오해되는 필드는 새 이름을 추가하고 점진적으로 교체한다.

## 8.6 `apps/web/src/app/coupang/uploads/page.tsx`

현재 가구매 안내 문구를 새 정책에 맞게 수정한다.

권장 문구:

> 가구매 매출은 해당 날짜 상품의 프로모션 없는 기본판매가 × 수량으로 계산하여 회사 매출에서 전액 제외합니다.

> 가구매 거래에서 발생한 상품원가·쿠팡 판매수수료·배송/물류비·VAT 등은 기존 비용에서 유지합니다. 가구매 업체수수료는 최종 순이익에서 별도로 추가 차감합니다.

예상 요약에는 최소한 다음 값이 보이면 좋다.

- 예상 가구매 매출 조정
- 예상 업체수수료
- 가능하다면 “예상 순이익 감소 = 매출 조정 + 업체수수료”

마지막 항목을 추가할 경우 웹 요약 테스트도 함께 추가한다. 단, 원본 판매행 존재 여부와 무관한 입력 단계의 단순 예상치임을 문구로 구분한다.

## 8.7 `apps/web/src/app/coupang/profit/page.tsx`

페이지 설명을 새 정책에 맞게 수정한다.

현재 “정상 판매 비용과 가구매 업체수수료를 각각 한 번만 반영한다”는 표현은 모호하다.

권장 설명:

> 가구매 매출은 회사 매출에서 전액 제외하고, 원본 거래에서 발생한 상품원가·쿠팡 판매수수료·물류비·VAT는 유지하며, 업체수수료를 별도로 차감합니다.

권장 라벨:

- `실제 정상 판매 순매출` → `가구매 조정 후 순매출`
- `정상 판매 순이익` → `업체수수료 차감 전 순이익`
- `가구매 비용(업체수수료)` → `가구매 업체수수료`
- `정상판매 VAT` → `쿠팡 매출 VAT(가구매 포함)`
- 필요 시 `판매수수료` → `쿠팡 판매수수료(가구매 포함)`

상품원가 및 물류비 역시 가구매 원본 거래를 포함한 비용이라는 점이 오해되지 않도록 도움말 또는 컬럼 제목을 검토한다.

## 8.8 `apps/web/src/app/coupang/dashboard/page.tsx`

마진계산 화면과 동일한 의미와 라벨을 사용한다.

대시보드 KPI와 행 테이블에서 다음 관계가 드러나야 한다.

```text
원본 순매출
- 가구매 매출 조정
= 가구매 조정 후 순매출

업체수수료 차감 전 순이익
- 가구매 업체수수료
= 최종 순이익
```

## 8.9 일일보고서와 내보내기

관련 파일:

- `apps/web/src/app/coupang/daily-report/page.tsx`
- `apps/web/src/app/coupang/daily-report/rows.tsx`
- `apps/web/src/lib/coupang-daily-report.ts`
- `apps/web/src/lib/coupang-daily-xlsx.ts`
- 해당 spec 파일

일일보고서의 `marginKrw`는 `buildProductProfitRows()`의 최종 순이익을 사용하므로 백엔드 계산 수정이 자동 반영되어야 한다.

검증할 것:

- 현재일과 이전일 비교 모두 새 최종 순이익 사용
- 상품행, 그룹합계, 선택합계, 전체합계 일치
- CSV/XLSX의 순이익이 화면과 일치
- 가구매 수량 컬럼은 유지
- 원본 매출 필터 때문에 가구매만 있는 행이 내보내기에서 조용히 사라지는지 확인
- 계산 불완전 행은 기존 정책대로 부분 합계에서 제외되는지 확인

이번 수정에서 일일보고서에 모든 비용 상세 컬럼을 추가할 필요는 없다. 다만 표시되는 순이익은 반드시 새 산식이어야 한다.

---

## 9. 원본 판매행이 없는 가구매 정책

현재 `adjustReportedSalesForManualPurchase()`는 원본 판매행이 없으면 매출과 수량을 0으로 두고 `isManualOnly: true`를 반환한다. 이후 계산은 광고비와 업체수수료만 차감할 수 있어, 상품원가·쿠팡수수료·배송비·VAT가 누락된 유리한 순이익을 만들 수 있다.

새 정책:

1. 원본 판매행이 없는 가구매를 업체수수료만 반영한 완전한 손익으로 표시하지 않는다.
2. `MANUAL_PURCHASE_WITHOUT_REPORTED_SALES`를 계산 차단 사유로 취급한다.
3. 해당 상품·날짜의 최종 `marginKrw`와 `totalCostKrw`는 `null`로 두고 `calculationStatus: "INCOMPLETE"`로 처리한다.
4. 알고 있는 가구매 매출 스냅샷·수량·업체수수료는 상세 필드에 계속 표시한다.
5. 합계에서는 기존 불완전 행 정책에 따라 제외하고, 제외된 금액/수량과 경고를 표시한다.

이 정책의 이유:

- 원본 거래가 없으면 실제 쿠팡 순매출, 취소 반영, 판매방식, 실제 수수료 발생 여부를 완전히 검증할 수 없다.
- 기본판매가와 규칙만으로 임시 비용을 합성하면 추후 원본 판매행이 업로드될 때 결과가 달라질 수 있다.
- 업체수수료만 차감하여 이익을 과대평가하는 것보다 계산 불완전으로 명확히 표시하는 것이 안전하다.

향후 사용자가 원본 판매 업로드 전에도 잠정 손익을 원한다면 “수동 가구매 예상 손익”을 별도 기능으로 설계한다. 이번 작업에서는 임의의 합성 손익을 만들지 않는다.

---

## 10. 오류 및 경고 정책

기존 차단 검증을 유지한다.

- 가구매 수량이 음수 또는 정수가 아님
- 가구매 수량이 원본 판매수량을 초과
- 가구매 매출 조정액이 원본 매출을 초과
- 가구매 조정 후 순매출이 음수
- 가구매 매출 스냅샷 누락
- 가구매 업체수수료 스냅샷 누락 또는 합계 불일치
- 원본 비용 규칙 누락
- 원본 판매방식에 필요한 물류비 누락
- 적용 가능한 쿠팡 판매수수료율 누락

추가 또는 변경할 정책:

- `MANUAL_PURCHASE_WITHOUT_REPORTED_SALES`는 정보성 경고가 아니라 최종 손익 차단 경고로 변경
- 원본 거래가 있지만 조정 판매수량이 0인 경우 비용 규칙 누락을 무시하면 안 됨
- 비용 검증은 조정 세그먼트가 아니라 원본 비용 발생 세그먼트를 기준으로 수행

웹 경고 라벨 파일인 `apps/web/src/lib/coupang-profit-warning.ts`에 사용자 친화적인 문구를 추가한다.

권장 문구:

```text
MANUAL_PURCHASE_WITHOUT_REPORTED_SALES
→ 가구매에 대응하는 쿠팡 원본 판매자료가 없어 손익을 확정할 수 없습니다
```

---

## 11. 테스트 계획

## 11.1 `apps/api/src/domain/coupang-profit-calculator.spec.ts`

다음 단위 테스트를 추가하거나 수정한다.

### 테스트 A: 매출 인식 기준과 비용 발생 기준 분리

입력:

```text
원본 순매출: 100,000원
원본 수량: 4개
조정 순매출: 75,000원
조정 수량: 3개
상품원가: 개당 10,000원
판매수수료율: 10%
판매자 배송비: 개당 1,000원
VAT: 원본 순매출 / 11
광고비: 5,000원
```

기대:

```text
표시 순매출: 75,000원
상품원가: 40,000원
판매수수료: 10,000원
배송비: 4,000원
VAT: 100,000 / 11
광고비: 5,000원
```

상품원가가 30,000원, 판매수수료가 7,500원, 배송비가 3,000원, VAT가 75,000/11로 줄어들면 실패다.

### 테스트 B: 가구매가 없으면 기존 결과와 동일

매출 인식 세그먼트와 비용 발생 세그먼트가 같을 때 기존 `calculateCoupangProfitBySegments()`와 동일한 결과가 나와야 한다.

### 테스트 C: 판매자배송/그로스 혼합

- 조정 수량은 판매자배송부터 차감 가능
- 비용은 원본 판매자배송 수량과 원본 그로스 수량 기준 유지
- 판매자 배송비, 하나로 배송비, 그로스 입출고비, 그로스 배송비가 모두 원본 기준으로 보존

### 테스트 D: 조정 수량 0

원본이 가구매 한 건뿐이라 조정 순매출과 수량이 0이어도 원본 상품원가·판매수수료·물류비·VAT가 남아 음수 순이익이 되어야 한다.

## 11.2 `apps/api/src/domain/coupang-profit-pipeline.spec.ts`

현재 다음 취지의 테스트는 새 정책에 맞게 수정해야 한다.

- “가구매 매출과 수량을 정상 비용 전에 분리”
- “가구매에는 업체수수료만 청구”
- 조정 수량 기준 상품원가 기대값
- 조정 순매출 기준 VAT 기대값

이 테스트들은 현재 잘못된 정책을 회귀 계약으로 고정하고 있으므로 이름과 기대값을 모두 바꾼다.

### 핵심 회귀 테스트

입력:

```text
reported net sales = 100,000
reported quantity = 4
manual sales = 25,000
manual quantity = 1
vendor fee = 3,182
product cost per unit = 10,000
seller shipping per unit = 1,000
sales fee rate = 10%
ad spend = 5,000
```

가구매 미입력 기준:

```text
product cost = 40,000
sales fee = 10,000
shipping = 4,000
VAT = 100,000 / 11
ad spend = 5,000
baseline margin
= 100,000 - 위 비용 합계
```

가구매 입력 후:

```text
recognized net sales = 75,000
product cost = 40,000
sales fee = 10,000
shipping = 4,000
VAT = 100,000 / 11
ad spend = 5,000
vendor fee = 3,182
final margin
= 75,000 - 원본 비용 합계 - 3,182
```

필수 단언:

```ts
expect(baselineMargin - adjustedMargin)
  .toBeCloseTo(25_000 + 3_182);
```

### 추가 테스트

- 가구매 없음: 결과 불변
- 업체수수료 0 또는 설정값 변경
- 광고비는 정확히 한 번만 차감
- 쿠팡 판매수수료는 정확히 한 번만 차감하고 원본 기준 유지
- VAT는 정확히 한 번만 차감하고 원본 기준 유지
- 상품원가와 물류비가 원본 기준으로 유지
- 가구매 전량이 판매자배송인 경우
- 가구매 전량이 그로스인 경우
- 혼합 판매에서 매출 인식 세그먼트 차감 순서와 비용 보존 동시 검증
- 가구매 수량이 원본 수량과 같아 조정 수량이 0인 경우
- 가구매 수량 초과 시 불완전
- 가구매 매출액 초과 시 불완전
- 원본 판매행 없는 가구매는 불완전
- 업체수수료 스냅샷 불완전 시 최종 손익 null
- 과거 `salesAmountKrw` 스냅샷 사용
- 광고 전환매출 경고 동작이 우발적으로 바뀌지 않음

## 11.3 `apps/api/src/coupang/coupang.service.spec.ts`

서비스 통합 수준에서 검증한다.

- `buildProductProfitRows()` 상품행 비용 보존
- 원본 판매행 전체가 가구매인 상품
- 가구매가 일부인 상품
- 가구매가 없는 상품
- 판매자배송/그로스 혼합 상품
- 기간 내 여러 날짜 중 일부 날짜만 가구매
- 상품그룹 합계
- 기간 상품 합계
- 전체 요약
- 계산 가능한 부분 합계
- 원본 판매행 없는 가구매의 불완전 처리
- `requiresNormalCostRule`가 원본 활동 기준인지 확인
- 원본 수량 기준 물류비 누락 경고 확인

합계 불변식:

```text
그룹 최종 순이익 = 완전한 자식 상품 최종 순이익 합
전체 최종 순이익 = 완전한 상품 최종 순이익 합
전체 업체수수료 = 상품별 업체수수료 합
전체 가구매 조정액 = 상품별 가구매 조정액 합
```

## 11.4 웹 테스트

관련 테스트:

- `apps/web/src/lib/coupang-manual-purchase.spec.ts`
- `apps/web/src/lib/coupang-daily-report.spec.ts`
- `apps/web/src/lib/coupang-daily-xlsx.spec.ts`
- 필요 시 페이지 계약 테스트 추가

검증:

- 업로드 화면 예상 가구매 매출 조정액
- 예상 업체수수료
- 예상 순이익 감소를 표시한다면 두 값의 합
- 새 안내 문구
- 마진계산 표 라벨
- 대시보드 KPI 라벨
- 일일보고서 현재/이전 순이익
- CSV/XLSX 순이익과 화면 순이익 일치
- 불완전 경고 한글 변환

## 11.5 기존 마이그레이션 테스트

다음 테스트는 계속 통과해야 한다.

- `coupang-manual-purchase-accounting-migration.spec.ts`
- `coupang-manual-purchase-vat-removal-migration.spec.ts`
- `coupang-sales-fee-migration.spec.ts`
- `coupang-product-cost-migration.spec.ts`

이번 수정은 기존 적용 마이그레이션을 되돌리는 작업이 아니다.

---

## 12. 수치 검증표

다음 표를 수동 검증과 테스트 픽스처에 사용할 수 있다.

| 항목 | 가구매 미입력 | 현재 잘못된 방식 | 수정 목표 |
|---|---:|---:|---:|
| 원본 순매출 | 100,000 | 100,000 | 100,000 |
| 가구매 매출 조정 | 0 | 25,000 | 25,000 |
| 인식 순매출 | 100,000 | 75,000 | 75,000 |
| 원본 수량 | 4 | 4 | 4 |
| 인식 수량 | 4 | 3 | 3 |
| 상품원가, 개당 10,000 | 40,000 | 30,000 | 40,000 |
| 판매수수료 10% | 10,000 | 7,500 | 10,000 |
| 배송비, 개당 1,000 | 4,000 | 3,000 | 4,000 |
| VAT | 100,000/11 | 75,000/11 | 100,000/11 |
| 광고비 | 5,000 | 5,000 | 5,000 |
| 업체수수료 | 0 | 3,182 | 3,182 |

목표 결과:

```text
가구매 미입력 순이익
= 100,000 - 40,000 - 10,000 - 4,000 - 100,000/11 - 5,000
= 약 31,909.09원

수정 후 순이익
= 75,000 - 40,000 - 10,000 - 4,000 - 100,000/11 - 5,000 - 3,182
= 약 3,727.09원

순이익 감소
= 31,909.09 - 3,727.09
= 28,182원
= 가구매 매출 25,000 + 업체수수료 3,182
```

---

## 13. 구현 순서

1. 이 문서와 현재 `coupang-profit-calculator.ts`, `coupang-profit-pipeline.ts`, `buildProductProfitRows()`를 다시 읽는다.
2. 기존 관련 테스트를 실행해 기준 상태를 기록한다.
3. 잘못된 현재 동작을 재현하는 새 실패 테스트를 먼저 작성한다.
4. 매출 인식 축과 비용 발생 축을 도메인 계산기에서 분리한다.
5. `calculateNormalCoupangProfit()`이 두 축을 올바르게 전달하도록 수정한다.
6. 원본 활동 기준 비용 규칙 및 물류비 검증으로 변경한다.
7. `buildProductProfitRows()`의 반환 필드 의미와 최종 결합을 검증한다.
8. 원본 판매행 없는 가구매를 불완전 처리한다.
9. 상품/그룹/기간/전체 합계 테스트를 수정한다.
10. API 및 웹 타입을 확인한다.
11. 업로드, 마진계산, 대시보드 문구와 라벨을 수정한다.
12. 일일보고서 및 CSV/XLSX 회귀 테스트를 실행한다.
13. API 전체 테스트, 웹 전체 테스트, 타입 검사, 빌드를 실행한다.
14. 실제 샘플 데이터가 있으면 가구매 입력 전후 순이익 차이가 `매출 조정액 + 업체수수료`인지 확인한다.

---

## 14. 검증 명령

PowerShell 실행 정책 때문에 `npm` 대신 `npm.cmd`가 필요할 수 있다.

```powershell
npm.cmd --workspace @meta-ads-performance/api test -- --run src/domain/coupang-profit-calculator.spec.ts src/domain/coupang-profit-pipeline.spec.ts
```

```powershell
npm.cmd --workspace @meta-ads-performance/api test -- --run src/coupang/coupang.service.spec.ts
```

```powershell
npm.cmd --workspace @meta-ads-performance/web test
```

```powershell
npm.cmd test
```

```powershell
npm.cmd run lint
```

```powershell
npm.cmd run build
```

프로젝트의 실제 스크립트가 변경되어 있으면 `package.json`을 확인한 후 동등한 명령을 사용한다.

---

## 15. 주의할 회귀 위험

### 15.1 비용 중복 차감

원본 기준 비용을 정상 손익에 유지하면서 수동 가구매 비용 필드에도 같은 비용을 넣으면 이중 차감된다.

해결:

- 원본 상품원가·쿠팡수수료·배송비·VAT는 정상 비용에만 유지
- 수동 가구매 별도 비용은 업체수수료만 유지

### 15.2 비용 누락

조정 세그먼트만 비용 계산에 사용하면 현재 버그가 재발한다.

해결:

- 비용 기준은 항상 원본 비용 발생 세그먼트
- 핵심 불변식 테스트 유지

### 15.3 가구매 전량 상품

조정 수량이 0이므로 정상 활동이 없다고 판단하면 비용이 모두 사라질 수 있다.

해결:

- 비용 규칙 필요 여부와 물류비 계산은 원본 활동 기준

### 15.4 수수료와 VAT 중복

과거 스키마에는 수동 가구매 수수료/VAT 스냅샷이 존재했던 이력이 있다. 현재 스키마에서 해당 값을 다시 살리면 원본 비용과 중복될 수 있다.

해결:

- 적용 완료 마이그레이션을 수정하지 않음
- 수동 가구매 VAT 컬럼을 재도입하지 않음

### 15.5 원본 판매행 없는 가구매

업체수수료만 차감한 값을 완전한 손익으로 표시하면 과대평가된다.

해결:

- 계산 불완전 처리
- 사용자 친화적 경고 표시

### 15.6 가격 스냅샷

현재 가구매 매출 조정액은 실제 결제액이 아니라 기본판매가 × 수량일 수 있다.

해결:

- 이번 작업에서는 저장된 스냅샷을 신뢰
- 실제 결제액 입력 기능과 섞지 않음
- UI에 기본판매가 기준임을 계속 안내

### 15.7 `0731수정계획2.md`와의 충돌

`0731수정계획2.md`는 쿠팡 상품별 ROAS 집계축 분리를 다루며 다음 파일과 겹칠 수 있다.

- `apps/api/src/domain/coupang-profit-calculator.ts`
- `apps/api/src/domain/coupang-profit-pipeline.ts`
- `apps/api/src/coupang/coupang.service.ts`
- `apps/web/src/types/coupang.ts`
- 쿠팡 마진/대시보드/일일보고서

두 계획을 같은 세션에서 함께 구현하도록 지시받지 않았다면 범위를 섞지 않는다. 이미 다른 계획의 변경이 적용되어 있다면 현재 코드를 기준으로 매출/비용 축 분리를 재배치하되 ROAS 의미를 되돌리지 않는다.

---

## 16. 완료 조건

다음 조건을 모두 만족해야 작업 완료로 판단한다.

- [ ] 가구매가 없을 때 기존 쿠팡 손익 결과가 변경되지 않는다.
- [ ] 가구매 매출 조정액이 원본 매출에서 전액 제외된다.
- [ ] 조정 순매출은 원본 순매출 - 가구매 매출 조정액이다.
- [ ] 조정 판매수량은 원본 판매수량 - 가구매 수량이다.
- [ ] 상품원가는 원본 비용 발생 수량 기준으로 유지된다.
- [ ] 쿠팡 판매수수료는 원본 순매출 기준으로 유지된다.
- [ ] 배송/물류비는 원본 판매방식별 수량 기준으로 유지된다.
- [ ] 반품예상비와 기타 단위비용은 원본 수량 기준으로 유지된다.
- [ ] VAT는 원본 순매출 기준으로 유지된다.
- [ ] 광고비는 정확히 한 번만 차감된다.
- [ ] 가구매 업체수수료는 정확히 한 번만 추가 차감된다.
- [ ] 동일 원본 기준 가구매 입력 전후 순이익 차이가 `가구매 매출 조정액 + 업체수수료`다.
- [ ] 원본 판매가 전부 가구매인 경우에도 원본 비용이 사라지지 않는다.
- [ ] 원본 판매행 없는 가구매를 업체수수료만 반영한 완전 손익으로 표시하지 않는다.
- [ ] 상품, 상품그룹, 기간, 전체 합계가 자식 행 합계와 일치한다.
- [ ] 마진계산, 대시보드, 일일보고서, CSV/XLSX가 같은 최종 순이익을 표시한다.
- [ ] 화면 설명과 라벨이 새 회계 의미와 일치한다.
- [ ] 과거 가구매 스냅샷이 재입력 없이 새 조회 산식에 반영된다.
- [ ] 기존 적용 마이그레이션을 수정하지 않는다.
- [ ] 관련 API 테스트가 모두 통과한다.
- [ ] 관련 웹 테스트가 모두 통과한다.
- [ ] 전체 테스트, 타입 검사, 빌드가 통과한다.

---

## 17. 다음 세션용 요약

현재 버그:

```text
가구매를 매출에서 제거할 때
가구매로 발생한 상품원가·쿠팡수수료·배송비·VAT도 함께 제거된다.
그래서 순이익 감소폭이 지나치게 작다.
```

목표:

```text
가구매 매출과 수량은 회사의 인식 매출에서 제외한다.
그러나 원본 쿠팡 거래에서 발생한 비용은 원본 기준으로 유지한다.
업체수수료만 별도 추가 비용으로 한 번 차감한다.
```

가장 중요한 회귀 계약:

```text
가구매 입력 전 순이익 - 가구매 입력 후 순이익
= 가구매 매출 조정액 + 가구매 업체수수료
```

이 식이 깨지면 구현이 완료된 것이 아니다.
