# 0731 수정계획 2 - 쿠팡 상품별 ROAS 집계 축 분리 및 손익 지표 의미 정리

## 1. 문서 목적

이 문서는 다음 개발 세션이 추가적인 배경 설명 없이도 쿠팡 마진계산의 상품별 ROAS 오류를 재현하고, 원인을 정확히 이해한 뒤, 기존 손익 계산을 훼손하지 않으면서 수정할 수 있도록 작성한 구현 계획서다.

이번 수정의 핵심은 자동매핑 규칙을 전면 재작성하는 것이 아니다. 현재 데이터에는 한 광고 원천 행 안에 다음 두 상품 축이 함께 존재한다.

- `spendProductId`: 광고비가 발생한 광고 집행 상품
- `conversionProductId`: 광고 클릭 후 실제 전환매출이 귀속된 판매 상품

현재 프로그램은 상품별 ROAS를 계산할 때 광고비는 `spendProductId`로 모으고 전환매출은 `conversionProductId`로 모은 뒤 같은 상품 행에서 나눈다. 상품 간 교차 전환이 있으면 분자와 분모가 서로 다른 광고 활동을 나타내므로 상품별 ROAS가 크게 왜곡된다.

다음 세션은 반드시 이 문서의 지표 정의와 완료 조건을 기준으로 작업해야 한다. 특정 상품의 숫자를 맞추기 위한 예외 처리나 하드코딩을 해서는 안 된다.

---

## 2. 사용자 의도와 이번 작업의 확정 범위

### 2.1 사용자가 원하는 결과

1. 상품/옵션별 ROAS가 해당 상품을 광고한 비용과 그 광고가 발생시킨 전체 전환매출을 같은 기준으로 사용해야 한다.
2. 다른 상품 광고로부터 유입된 전환매출이 해당 판매 상품의 ROAS 분자에 무상으로 더해지는 현상을 제거해야 한다.
3. 전체 순이익 산식은 현재처럼 실제 매출과 비용을 기준으로 유지해야 한다.
4. 오가닉 매출 계산에 사용되는 판매상품 기준 전환매출과 ROAS 계산에 사용되는 광고상품 기준 전환매출을 혼용하지 않아야 한다.
5. 현재 사용자가 의도적으로 방치한 소액 미매칭 광고비를 이번 작업에서 임의 배부하거나 자동매칭하지 않아야 한다.
6. 과거 업로드 자료도 재업로드 없이 수정된 계산 결과가 조회되어야 한다.

### 2.2 이번 작업에서 반드시 수정할 것

- `buildProductProfitRows()`의 광고 집계 구조
- ROAS 분자와 오가닉 매출 차감값의 명시적 분리
- 상품, 상품그룹, 기간합계, 일일보고서 합계에서의 재집계
- API 및 웹 타입의 모호한 필드명 정리
- 쿠팡 대시보드, 마진계산, 일일보고서, 다운로드 결과의 ROAS 의미 통일
- 교차 전환 회귀 테스트와 합계 불변성 테스트

### 2.3 이번 작업에서 하지 않을 것

- 자동매핑 규칙 전면 재작성
- 두줄보호대만을 위한 상품명 예외 처리
- ROAS를 수동 확인값 `338%`에 억지로 맞추는 보정
- 미매칭 광고비 7,306원을 임의 상품에 배부
- 상품별 광고비를 판매상품으로 자동 재배부하는 새로운 회계 정책 도입
- Cafe24 쿠폰 작업 또는 `0731수정계획.md`의 범위 수정

상품별 광고비를 판매상품에 재배부하려면 “전환매출 비율”, “전환수량 비율”, “광고상품에 유지”, “공통비 처리” 중 어떤 정책을 사용할지 별도의 사용자 결정이 필요하다. 이번 수정은 임의의 배부 정책을 추가하지 않고, 현재의 광고비 비용 귀속은 유지하되 ROAS와 전환 지표의 의미를 정확히 분리한다.

---

## 3. 확인된 실제 장애 사례

### 3.1 대상 업로드

- 자료일: `2026-07-29`
- 업로드 배치 ID: `e050a25a-ee1c-4b67-81e4-08a0c3e03ea8`
- 원본 파일명: `260729_쿠팡광고.xlsx`
- 업로드일: `2026-07-30`
- 원본 행: 1,111개
- 집계 후 행: 631개
- 동일 키로 합쳐진 중복 행: 480개
- 해당 날짜의 ADS 배치는 1개이므로 서로 다른 버전의 자료가 섞인 문제는 아니다.

### 3.2 두줄보호대 1개입의 현재 계산

현재 DB와 코드가 사용하는 값은 다음과 같다.

```text
두줄보호대 1개입 광고비(spendProductId 기준)             39,555원
두줄보호대 1개입 전환매출(conversionProductId 기준)     312,000원

현재 상품 ROAS
= 312,000 / 39,555
= 7.88775...
= 788.8%
```

사용자가 화면에서 본 “700% 정도”는 이 값이다.

`312,000원`의 구성은 다음과 같다.

```text
두줄보호대 1개입 광고 → 두줄보호대 1개입 구매       20,800원
두줄보호대 2개입 광고 → 두줄보호대 1개입 구매      270,400원
한줄보호대 2개입 광고 → 두줄보호대 1개입 구매       20,800원
------------------------------------------------------------
두줄보호대 1개입 판매상품 기준 전환매출 합계        312,000원
```

즉 현재 ROAS 분자 중 다른 상품 광고가 발생시킨 매출은 `291,200원`, 전체 분자의 약 `93.3%`다. 반면 그 다른 상품들의 광고비는 두줄보호대 1개입 ROAS 분모에 포함되지 않는다.

동일한 DB 자료에서 두줄보호대 1개입 광고가 모든 상품에 발생시킨 전환매출을 광고 집행 상품 기준으로 합하면 다음과 같다.

```text
두줄보호대 1개입 광고가 발생시킨 전체 전환매출      82,400원
두줄보호대 1개입 광고비                              39,555원

광고 집행 상품 기준 ROAS
= 82,400 / 39,555
= 약 208.3%
```

따라서 현재 DB 자료와 이 문서가 정의하는 올바른 광고성과 ROAS 기준에서는 수정 후 예상값이 약 `208.3%`다. 사용자가 수기로 본 `338%`는 현재 DB의 어떠한 명확한 소계로도 재현되지 않았다.

### 3.3 수기 338%를 완료 기준으로 사용하면 안 되는 이유

현재 업로드 파서인 `apps/api/src/domain/coupang-ads-xlsx.ts`는 다음 값을 가져온다.

- 광고비
- 주문수
- 총/직접/간접 전환매출
- 총/직접/간접 판매수량

하지만 원본 파일의 다음 ROAS 열은 가져오지 않는다.

- 총 광고 수익률 (1일)
- 직접 광고 수익률 (1일)
- 간접 광고 수익률 (1일)
- 14일 기준 ROAS 열

또한 `apps/api/src/coupang/coupang.service.ts`의 업로드 배치 생성 시 `storedFilePath: null`을 저장하므로 서버에 원본 XLSX가 남아 있지 않다. 따라서 현재 DB만으로 원본 엑셀의 어느 셀 또는 어느 필터가 `338%`였는지 역추적할 수 없다.

이 제약은 수정 자체를 막지 않는다. 수정의 정답은 특정 표시값이 아니라 “동일한 광고 집행 축에서 분자와 분모를 함께 합산한다”는 집계 불변식으로 검증해야 한다.

---

## 4. 전체 영향 범위 조사 결과

2026-07-29 자료에서 광고비 또는 광고 전환이 있는 상품은 86개다.

- 교차 전환의 영향을 받는 상품: 44개
- 교차 전환이 없어 현재 식과 수정 식이 동일한 상품: 42개
- 교차 상품 전환매출 합계: 2,448,650원
- 동일 상품그룹 내부의 교차 전환: 2,324,850원
- 서로 다른 상품그룹 간 교차 전환: 123,800원

따라서 모든 ROAS 값이 무조건 틀린 것은 아니다. 그러나 화면에는 교차 전환 여부가 표시되지 않으므로 현재 상품별 ROAS 열 전체를 사용자가 사전 판별 없이 신뢰할 수 없는 상태다.

실제 극단값도 확인되었다.

- 포론깔창: 약 3,136.9%
- 베놈공용: 약 7,377.1%
- 카프슬리브: 약 8,245.0%
- 자이로볼 메탈: 약 695.9%
- TPE 요가매트: 약 772.4%

상품그룹 합계에서는 같은 그룹 내부의 교차 전환이 어느 정도 상쇄되지만, 그룹 간 교차 전환 123,800원이 있으므로 그룹 ROAS도 현재 구조를 그대로 신뢰해서는 안 된다.

전체 원천 광고자료 합계는 다음과 같다.

```text
전체 광고비                  6,704,236원
전체 전환매출               17,285,670원
원천 전체 ROAS                    257.832%

매칭된 광고비                6,696,930원
미매칭 광고비                    7,306원
매칭된 전환매출             17,143,270원
미매칭 전환매출                142,400원
```

교차 전환은 전체 합계에서는 상품 간 이동이 상쇄된다. 다만 매칭된 상품 행만 합산하는 화면에서는 미매칭 항목을 제외하기 때문에 원천 전체 합계와 정확히 같지 않을 수 있다.

---

## 5. 현재 코드의 정확한 문제

### 5.1 오류 발생 위치

주요 파일:

- `apps/api/src/coupang/coupang.service.ts`
- `apps/api/src/domain/coupang-profit-calculator.ts`
- `apps/api/src/domain/coupang-profit-pipeline.ts`

현재 `buildProductProfitRows()`는 대략 다음 구조다.

```ts
// 광고비: 광고 집행 상품 기준
if (metric.spendProductId) {
  spendByProductDate[metric.spendProductId] += metric.adSpendKrw;
}

// 전환매출: 판매된 전환 상품 기준
if (metric.conversionProductId) {
  conversionByProductDate[metric.conversionProductId] +=
    metric.totalConversionSales1dKrw;
}

// 서로 다른 기준을 같은 상품 행에서 나눔
roas = conversion.salesKrw / adSpendKrw;
```

2026-07-31 현재 코드 위치는 다음과 같다. 구현 중 코드가 이동할 수 있으므로 함수명과 필드명으로 다시 검색한다.

- `buildProductProfitRows()`: `apps/api/src/coupang/coupang.service.ts` 약 2708행
- `spendByProductDate`: 약 2810행
- `conversionByProductDate`: 약 2811행
- 현재 ROAS 계산: 약 2975행
- 상품그룹 ROAS 재계산: 약 3988행
- 전체 요약 ROAS 재계산: 약 4133행

### 5.2 하나의 모호한 값이 두 의미로 사용되는 문제

`apps/api/src/domain/coupang-profit-calculator.ts`의 `CoupangAdInput`에는 현재 다음 값만 있다.

```ts
type CoupangAdInput = {
  adSpendKrw: number;
  adConversionSalesKrw: number;
  adConversionQuantity?: number;
};
```

이 하나의 `adConversionSalesKrw`를 다음 두 계산이 함께 사용한다.

```text
오가닉 매출 = 실제 순매출 - adConversionSalesKrw
ROAS       = adConversionSalesKrw / 광고비
```

그러나 두 계산은 서로 다른 상품 축이 필요하다.

- 오가닉 매출: 이 판매 상품에 귀속된 전환매출, 즉 `conversionProductId` 기준
- 광고성과 ROAS: 이 상품의 광고가 발생시킨 전환매출, 즉 `spendProductId` 기준

필드 하나로 두 의미를 표현하려 한 것이 구조적 원인이다.

### 5.3 자동매핑은 이번 장애의 근본 원인이 아니다

두줄보호대 1개입과 2개입은 모두 RULE/VALID 상태로 정상 매핑되었다.

- 두줄보호대 1개입 상품 ID: `45be7c65-ecd2-4b59-8a54-fd60e29010c8`
- 두줄보호대 2개입 상품 ID: `a0a92bf4-a738-44e1-82bd-44b33c2e068b`

교차 전환 행에서 `spendProductId`와 `conversionProductId`가 서로 다른 것은 매핑 실패가 아니라 실제 광고 보고서가 표현하는 정상적인 관계다. 따라서 두 ID를 강제로 같게 만들거나 전환상품 매핑을 광고상품으로 덮어쓰면 실제 교차 구매 정보를 잃게 된다.

### 5.4 이미 올바른 참고 구현이 존재한다

`apps/api/src/coupang/coupang.service.ts`의 `adsAnalysis()`는 광고 집행 상품/그룹과 캠페인/광고그룹을 기준으로 원천 행의 광고비와 전환매출을 함께 합산한 뒤 ROAS를 계산한다.

```text
ROAS = 같은 광고 집행 그룹에서 합한 전환매출
     / 같은 광고 집행 그룹에서 합한 광고비
```

`adsAnalysis()`의 방식은 광고 실행 성과 관점에서 일관된다. 이번 수정은 이 원칙을 상품 마진표, 대시보드, 일일보고서에도 동일하게 적용해야 한다.

---

## 6. 수정 후 지표 정의

구현 전에 아래 용어를 코드 주석, 타입, 화면 라벨에서 동일하게 사용한다.

### 6.1 광고비

권장 필드명: `adSpendKrw`

```text
해당 상품을 광고 집행 상품으로 하는 원천 행의 광고비 합계
집계 키: metricDate + spendProductId
```

현재 의미를 그대로 유지한다.

### 6.2 광고 발생 전환매출

권장 필드명: `adGeneratedSalesKrw`

```text
해당 상품의 광고가 모든 판매상품에 발생시킨 총 전환매출
집계 키: metricDate + spendProductId
값: totalConversionSales1dKrw 합계
```

이 값만 ROAS 분자로 사용한다.

### 6.3 광고 발생 전환수량

권장 필드명: `adGeneratedQuantity`

```text
해당 상품의 광고가 모든 판매상품에 발생시킨 총 전환수량
집계 키: metricDate + spendProductId
값: totalSalesQuantity1d 합계
```

### 6.4 판매상품 귀속 전환매출

권장 필드명: `attributedConversionSalesKrw`

```text
어떤 상품을 광고했는지와 관계없이, 해당 판매상품의 구매로 귀속된 전환매출
집계 키: metricDate + conversionProductId
값: totalConversionSales1dKrw 합계
```

이 값은 오가닉 매출 계산에만 사용한다.

### 6.5 판매상품 귀속 전환수량

권장 필드명: `attributedConversionQuantity`

```text
어떤 상품을 광고했는지와 관계없이, 해당 판매상품의 구매로 귀속된 전환수량
집계 키: metricDate + conversionProductId
값: totalSalesQuantity1d 합계
```

### 6.6 수정 후 ROAS

```text
상품 광고성과 ROAS
= adGeneratedSalesKrw / adSpendKrw
```

분모가 0이면 기존 `safeDivide()` 정책에 따라 `null`을 반환한다. 개별 ROAS를 단순 평균하지 않고 항상 합산 분자 ÷ 합산 분모로 기간/그룹/전체 값을 재계산한다.

### 6.7 수정 후 오가닉 매출

```text
실제 오가닉 매출
= actualNetSalesKrw - attributedConversionSalesKrw

원본 오가닉 매출
= reportedNetSalesKrw - attributedConversionSalesKrw
```

`adGeneratedSalesKrw`를 오가닉 매출에서 차감하면 안 된다. 광고상품과 판매상품이 다를 수 있기 때문이다.

### 6.8 순이익

현재 필수 범위에서 순이익 산식은 변경하지 않는다.

```text
총비용
= 상품원가
+ 판매수수료
+ 배송/입고/반품/기타비용
+ VAT
+ adSpendKrw

순이익 = 실제 순매출 - 총비용
```

ROAS 값 자체는 순이익 산식에 들어가지 않는다. `adGeneratedSalesKrw`와 `attributedConversionSalesKrw`도 순이익을 직접 증감시키지 않는다.

상품 행의 광고비는 계속 광고 집행 상품에 귀속된다. 따라서 상품별 순이익은 “이 옵션의 실제 판매손익에서 이 옵션 광고 집행비를 차감한 운영 기여값”으로 해석해야 하며, “이 상품 광고가 인과적으로 만든 순이익” 또는 “판매상품에 광고비를 완전 배부한 회계이익”이라고 표현하면 안 된다.

---

## 7. 목표 데이터 흐름

```text
CoupangAdMetric 원천 행
        |
        |-- spendProductId 기준 집계
        |      ├─ adSpendKrw
        |      ├─ adGeneratedSalesKrw
        |      └─ adGeneratedQuantity
        |
        └-- conversionProductId 기준 집계
               ├─ attributedConversionSalesKrw
               └─ attributedConversionQuantity

상품 손익 행
  ├─ 실제 매출/원가/수수료: 판매자료의 상품 기준
  ├─ 광고비: spendProductId 기준
  ├─ ROAS: adGeneratedSalesKrw / adSpendKrw
  ├─ 오가닉 매출: 실제 순매출 - attributedConversionSalesKrw
  └─ 순이익: 기존 비용 산식 유지
```

두 집계 맵은 이름과 타입을 명확히 분리하여 이후 개발자가 다시 혼용하지 못하게 한다.

---

## 8. 파일별 상세 구현 계획

### 8.1 `apps/api/src/coupang/coupang.service.ts`

#### A. `ProductProfitRow` 타입 분리

현재 모호한 필드:

- `adConversionSalesKrw`
- `adConversionQuantity`

명시적으로 다음 필드를 추가하거나 교체한다.

- `adGeneratedSalesKrw`
- `adGeneratedQuantity`
- `attributedConversionSalesKrw`
- `attributedConversionQuantity`

권장 방식은 내부 코드와 API 응답을 모두 명확한 새 이름으로 전환하는 것이다. 외부 소비자가 있어 하위 호환이 필요하다고 확인되는 경우에만 `adConversionSalesKrw`를 한 릴리스 동안 `attributedConversionSalesKrw`의 deprecated 별칭으로 유지한다. 웹 코드는 deprecated 필드를 사용하면 안 된다.

#### B. `buildProductProfitRows()` 광고 집계 변경

현재의 `spendByProductDate`와 `conversionByProductDate`를 의미가 드러나는 두 맵으로 변경한다.

예시 구조:

```ts
type AdPerformanceEntry = {
  productId: string;
  date: string;
  spendKrw: number;
  generatedSalesKrw: number;
  generatedQuantity: number;
};

type SalesAttributionEntry = {
  productId: string;
  date: string;
  attributedSalesKrw: number;
  attributedQuantity: number;
};
```

원천 행을 한 번 순회하면서 다음처럼 집계한다.

```ts
if (metric.spendProductId) {
  const key = productDateKey(metric.spendProductId, date);
  performance.spendKrw += numberFrom(metric.adSpendKrw);
  performance.generatedSalesKrw +=
    numberFrom(metric.totalConversionSales1dKrw);
  performance.generatedQuantity +=
    numberFrom(metric.totalSalesQuantity1d);
}

if (metric.conversionProductId) {
  const key = productDateKey(metric.conversionProductId, date);
  attribution.attributedSalesKrw +=
    numberFrom(metric.totalConversionSales1dKrw);
  attribution.attributedQuantity +=
    numberFrom(metric.totalSalesQuantity1d);
}
```

중요:

- `spendProductId`가 없는 행의 전환매출을 매칭된 상품의 광고성과 분자에 넣지 않는다. 분모를 어느 광고상품에 둘지 알 수 없기 때문이다.
- `conversionProductId`가 없는 행의 전환매출을 임의 판매상품의 오가닉 계산에 넣지 않는다.
- 두 ID가 서로 달라도 정상 행으로 처리한다.
- 두 ID를 강제로 동일하게 만들지 않는다.

#### C. 상품/날짜 행 생성

`allProductDateKeys`에는 다음 키를 모두 포함한다.

- 실제 판매자료 키
- 가구매 키
- 광고성과 키
- 판매상품 전환귀속 키

각 행에서:

```text
adSpendKrw                       = performance.spendKrw
adGeneratedSalesKrw              = performance.generatedSalesKrw
adGeneratedQuantity              = performance.generatedQuantity
attributedConversionSalesKrw     = attribution.attributedSalesKrw
attributedConversionQuantity     = attribution.attributedQuantity
roas                             = adGeneratedSalesKrw / adSpendKrw
actualOrganicSalesKrw            = actualNetSalesKrw - attributedConversionSalesKrw
reportedOrganicSalesKrw          = reportedNetSalesKrw - attributedConversionSalesKrw
```

경고 `AD_CONVERSION_EXCEEDS_NET_SALES`는 판매상품 귀속값인 `attributedConversionSalesKrw`를 기준으로 유지한다.

#### D. 기간 및 상품그룹 집계

다음 함수에서 새 필드를 각각 합산한다.

- `aggregateCoupangProductProfitGroup()`
- `aggregateCoupangProductDateRows()`
- `summarizeCoupangProductProfitRows()`

ROAS는 반드시 다음처럼 다시 계산한다.

```text
safeDivide(
  sum(adGeneratedSalesKrw),
  sum(adSpendKrw)
)
```

금지:

```text
평균(child.roas)
sum(attributedConversionSalesKrw) / sum(adSpendKrw)
```

오가닉 매출은 계속 판매상품 귀속값으로 계산된 자식 행의 오가닉 값을 합산한다.

#### E. 일일보고서 변환

다음 함수가 수정된 `row.roas`와 오가닉 값을 그대로 사용하도록 확인한다.

- `toDailyPreviousMetrics()`
- `toDailyVisibleMetrics()`
- `toCoupangDailySummary()`
- 일일보고서 상품/그룹 생성 함수

현재 이 함수들은 대부분 `row.roas`를 전달하므로 타입 변경에 맞춰 누락만 점검하면 된다.

#### F. `adsAnalysis()`와의 일관성

`adsAnalysis()`는 이미 `spendProductId` 기준으로 광고비와 전환매출을 함께 합산한다. 이 함수의 계산 결과가 같은 기간/같은 상품 집계에서 새 `adGeneratedSalesKrw`, `adSpendKrw`, `roas`와 일치하는지 테스트한다.

기존 동작을 불필요하게 재작성하지 않는다.

### 8.2 `apps/api/src/domain/coupang-profit-calculator.ts`

현재 `CoupangAdInput.adConversionSalesKrw`가 ROAS와 오가닉 매출에 동시에 쓰이는 구조를 제거한다.

권장 입력 타입:

```ts
export type CoupangAdInput = {
  adSpendKrw: number;
  adGeneratedSalesKrw: number;
  attributedConversionSalesKrw: number;
  adGeneratedQuantity?: number;
  attributedConversionQuantity?: number;
};
```

계산:

```text
totalCostKrw     → adSpendKrw만 사용
marginKrw        → 기존 산식 유지
roas             → adGeneratedSalesKrw / adSpendKrw
organicSalesKrw  → netSalesKrw - attributedConversionSalesKrw
```

이 분리를 통해 향후 어느 한쪽 집계 기준을 변경해도 다른 지표가 조용히 오염되지 않게 한다.

내부 세그먼트 계산에서 광고 입력을 0으로 전달하는 부분도 새 필드를 모두 0으로 채운다.

### 8.3 `apps/api/src/domain/coupang-profit-pipeline.ts`

`calculateNormalCoupangProfit()`과 `calculateCoupangProfitBySegments()`에 전달하는 `CoupangAdInput`을 새 타입에 맞게 변경한다.

광고비가 순이익에서 두 번 차감되지 않는지 확인한다. 세그먼트별 계산에는 지금처럼 광고비 0을 넣고 최종 합계 단계에서 한 번만 전체 광고비를 반영하는 기존 구조를 유지한다.

### 8.4 `apps/web/src/types/coupang.ts`

API 타입에 다음 값을 명확히 반영한다.

- 상품 행
- 요약
- 일일보고서 응답

웹 코드가 의미가 모호한 `adConversionSalesKrw`를 계속 참조하지 않도록 전체 검색한다.

### 8.5 `apps/web/src/app/coupang/profit/page.tsx`

상세 열의 라벨을 명확히 한다.

권장 표시:

- `광고비(집행상품 기준)`
- `광고 발생 전환매출`
- `판매상품 귀속 전환매출`
- `ROAS(집행상품 기준)`
- `오가닉 매출(판매상품 기준)`

열이 지나치게 넓어질 경우 기본 열에는 광고 발생 전환매출과 ROAS를 표시하고, 판매상품 귀속 전환매출은 상세 모드에서 표시한다. 그러나 API 값 자체를 하나로 다시 합치면 안 된다.

상품별 순이익에는 다음 의미를 툴팁 또는 설명 문구로 명시한다.

```text
광고비는 광고 집행 상품에 귀속됩니다.
교차 구매가 있는 경우 상품별 순이익은 판매상품 광고비 배부 회계값이 아니라
광고 집행비를 포함한 운영 기여값입니다.
```

### 8.6 `apps/web/src/app/coupang/dashboard/page.tsx`

- ROAS KPI는 수정된 광고 집행 기준 ROAS 사용
- 라벨을 가능하면 `광고성과 ROAS` 또는 `ROAS(집행 기준)`으로 변경
- 유기적 매출은 판매상품 귀속 전환매출을 차감한 기존 의미 유지
- 순이익 KPI 계산식은 변경하지 않음

### 8.7 쿠팡 일일보고서 화면 및 내보내기

대상:

- `apps/web/src/app/coupang/daily-report/page.tsx`
- `apps/web/src/app/coupang/daily-report/rows.tsx`
- `apps/web/src/lib/coupang-daily-report.ts`
- `apps/web/src/lib/coupang-daily-xlsx.ts`

화면과 HTML/XLSX 다운로드에서 `광고수익률`의 라벨 또는 주석을 `광고수익률(집행상품 기준)`으로 명확히 한다.

다운로드 숫자는 퍼센트 표시 전의 비율값을 유지한다. 예를 들어 `2.083`을 엑셀 퍼센트 스타일로 `208.3%` 표시해야 하며 `208.3`을 저장해 `20,830%`가 되면 안 된다.

### 8.8 `apps/web/src/app/coupang/ads/page.tsx`

이 화면의 ROAS는 이미 광고 집행 상품 기준이므로 계산을 바꾸지 않는다. 다른 화면과 동일한 한국어 라벨/도움말을 적용하는 정도만 검토한다.

---

## 9. 순이익에 대한 보존 정책

### 9.1 ROAS 오류는 순이익에 직접 전파되지 않는다

`apps/api/src/domain/coupang-profit-calculator.ts`에서:

```text
총비용 = 각종 비용 + 광고비
순이익 = 순매출 - 총비용
```

ROAS와 전환매출은 순이익 산식의 입력이 아니다. 전환매출은 오가닉 매출과 ROAS 계산에만 사용된다.

따라서 이번 수정 전후로 판매자료, 비용규칙, 광고비가 동일하다면 다음 값은 변하지 않아야 한다.

- 상품원가
- 판매수수료
- 물류비
- 반품비
- 기타비용
- VAT
- 광고비
- 총비용
- 순이익
- 마진율

### 9.2 상품별 순이익 해석의 한계

교차 전환 예:

```text
2개입 광고비 발생
→ 소비자가 1개입 구매
```

현재 재무 행에서는:

- 1개입: 실제 매출과 상품원가를 보유
- 2개입: 광고 집행비를 부담

전체 상품 합계에서는 광고비가 정확히 한 번 차감되므로 상쇄된다. 그러나 옵션별 숫자는 광고비 배부 정책에 따라 달라질 수 있다.

이번 작업에서는 이 값을 임의로 재배부하지 않는다. 향후 “판매상품 기준 완전배부 순이익”이 필요하면 별도 요구사항으로 다음 정책을 먼저 확정해야 한다.

1. 광고 집행 상품에 유지
2. 전환매출 비율로 판매상품에 배부
3. 전환수량 비율로 판매상품에 배부
4. 전환이 없는 광고비는 집행상품 또는 공통비로 처리
5. 미매칭 광고비 처리

이 결정 없이 광고비를 자동 이동시키면 수치가 달라져도 어느 값이 정답인지 검증할 수 없다.

### 9.3 미매칭 광고비 정책

2026-07-29의 미매칭 광고비는 7,306원이다. 사용자가 금액이 미미하다고 판단하여 의도적으로 방치한 항목이다.

이번 작업은:

- 미매칭 광고비를 자동매칭하지 않는다.
- 임의의 상품 또는 그룹에 분배하지 않는다.
- 기존 미매칭 화면/경고에서 계속 확인 가능하게 둔다.
- 전체 원천 광고비와 매칭 상품 합계가 7,306원 차이 나는 것을 알려진 정책 차이로 인정한다.

따라서 7월 29일의 매칭 상품 기준 전체 순이익은 원천 전체 광고비를 모두 차감한 값보다 7,306원 높을 수 있다. 이는 이번 ROAS 집계 오류와는 별개의 의도된 미매칭 정책이다.

---

## 10. 데이터베이스 및 과거 데이터 처리

### 10.1 핵심 수정에는 DB 마이그레이션이 필요하지 않음

필요한 원천 필드는 이미 `CoupangAdMetric`에 존재한다.

- `spendProductId`
- `conversionProductId`
- `adSpendKrw`
- `totalConversionSales1dKrw`
- `totalSalesQuantity1d`

상품 손익 행은 조회 시점에 계산되므로 서비스 집계 코드를 수정하면 과거 데이터도 재업로드나 백필 없이 새 계산을 사용한다.

### 10.2 원본 ROAS 열 저장은 선택적 감사 강화 작업

원본 XLSX의 ROAS 열을 파싱하거나 업로드 파일을 보존하는 기능은 핵심 오류 수정에 필요하지 않다. 추가한다면 다음 목적에만 사용한다.

- 원본 쿠팡 표시값과 내부 재계산값 비교
- 업로드 당시 자료 감사
- 수기 338%와 같은 차이 역추적

원본 ROAS 값을 내부 계산의 정답으로 그대로 복사하면 안 된다. 여러 행을 합산할 때 ROAS 퍼센트의 단순 합/평균은 올바르지 않으므로 항상 전환매출 합계 ÷ 광고비 합계로 계산해야 한다.

이 감사 강화 기능은 별도 마이그레이션과 파일 보관 정책이 필요하므로 이번 필수 범위와 분리한다.

---

## 11. 필수 테스트 계획

### 11.1 계산기 단위 테스트

대상:

- `apps/api/src/domain/coupang-profit-calculator.spec.ts`

반드시 서로 다른 두 전환매출 값을 사용한다.

예:

```text
순매출                         200,000원
광고비                          10,000원
광고 발생 전환매출              30,000원
판매상품 귀속 전환매출          80,000원
```

기대:

```text
ROAS         = 30,000 / 10,000 = 3.0
오가닉 매출  = 200,000 - 80,000 = 120,000원
```

검증 항목:

- `adGeneratedSalesKrw`만 바꾸면 ROAS만 변하고 순이익/오가닉 매출은 변하지 않는다.
- `attributedConversionSalesKrw`만 바꾸면 오가닉 매출만 변하고 ROAS/순이익은 변하지 않는다.
- `adSpendKrw`를 바꾸면 총비용, 순이익, ROAS가 변한다.
- 광고비 0일 때 ROAS는 `null`이다.
- 판매상품 귀속 전환매출이 실제 순매출보다 크면 기존 경고가 유지된다.

### 11.2 서비스 교차 전환 회귀 테스트

대상:

- `apps/api/src/coupang/coupang.service.spec.ts`

현재 주요 테스트는 `spendProductId === conversionProductId`인 행만 사용하여 오류를 잡지 못했다. 반드시 서로 다른 ID를 가진 행을 추가한다.

예시:

```text
행 1: 광고상품 A → 판매상품 A
광고비 10,000원, 전환매출 20,000원

행 2: 광고상품 B → 판매상품 A
광고비 30,000원, 전환매출 90,000원
```

기대 결과:

```text
상품 A
- adSpendKrw = 10,000
- adGeneratedSalesKrw = 20,000
- attributedConversionSalesKrw = 110,000
- roas = 2.0

상품 B
- adSpendKrw = 30,000
- adGeneratedSalesKrw = 90,000
- attributedConversionSalesKrw = 0
- roas = 3.0
```

현재 잘못된 식이라면 A의 ROAS가 `110,000 / 10,000 = 11.0`이 되므로 이 테스트가 정확히 회귀를 차단한다.

### 11.3 상품그룹 테스트

다음 두 경우를 모두 만든다.

1. 같은 그룹 내부 A → B 교차 전환
2. 서로 다른 그룹 간 A → C 교차 전환

검증:

- 그룹 ROAS 분자와 분모 모두 광고 집행 그룹 기준이다.
- 같은 그룹 내부 교차 전환도 자식 ROAS를 평균하지 않는다.
- 그룹 간 교차 전환매출은 광고를 집행한 그룹의 성과에 포함된다.
- 판매상품 귀속 전환매출은 판매된 상품/그룹의 오가닉 계산에 사용된다.

### 11.4 기간 합계 테스트

2일 이상의 행을 사용한다.

```text
기간 ROAS
= 모든 날짜의 adGeneratedSalesKrw 합계
/ 모든 날짜의 adSpendKrw 합계
```

일별 ROAS 평균과 다르게 나오는 데이터를 사용하여 단순 평균 회귀를 차단한다.

### 11.5 합계 보존 테스트

수정 전후 동일 fixture에서 다음 값이 완전히 동일해야 한다.

- `adSpendKrw`
- `totalCostKrw`
- `marginKrw`
- `marginRate`
- 전체 `knownMarginKrw`
- 완료/미완료 상품 수

달라져야 하는 값:

- 교차 전환 상품의 `roas`
- 광고성과용 전환매출

판매상품 귀속 전환매출과 오가닉 매출은 집계 정의를 그대로 보존하므로 값이 달라지면 안 된다.

### 11.6 Ads Analysis 교차 검증

동일 fixture 또는 2026-07-29 DB 자료에서:

```text
새 상품 손익 행의 adSpendKrw
== Ads Analysis의 같은 광고상품 광고비

새 상품 손익 행의 adGeneratedSalesKrw
== Ads Analysis의 같은 광고상품 총 전환매출

새 상품 손익 행의 roas
== Ads Analysis의 같은 광고상품 ROAS
```

캠페인/광고그룹 하위 행을 모두 합친 뒤 비교해야 한다.

### 11.7 웹 및 다운로드 테스트

대상:

- `apps/web/src/lib/coupang-daily-report.spec.ts`
- `apps/web/src/lib/coupang-daily-xlsx.spec.ts`
- 관련 컴포넌트 타입 검사

검증:

- 새 필드가 HTML/XLSX에 올바르게 전달된다.
- `2.083`이 `208.3%`로 표시된다.
- 0과 `null`의 표시가 기존 정책과 동일하다.
- 이전 기간 비교도 같은 집계 기준을 사용한다.
- 화면 라벨이 광고 집행 기준임을 오해 없이 표시한다.

---

## 12. 실제 자료 검증 절차

구현 후 가능하면 개발 DB에서 2026-07-29를 다시 조회한다.

### 12.1 두줄보호대 1개입

확인할 값:

```text
adSpendKrw                       39,555원
adGeneratedSalesKrw              82,400원
attributedConversionSalesKrw    312,000원
roas                             약 2.083, 화면 약 208.3%
```

오가닉 매출은 기존과 같이 판매상품 귀속 전환매출 312,000원을 사용해야 한다.

주의: `338%`가 나오지 않는다는 이유만으로 구현 실패로 판단하지 않는다. 현재 저장 자료에서 검증 가능한 일관된 결과는 약 208.3%다.

### 12.2 전체 자료

다음 불변식을 쿼리 또는 진단 스크립트로 검증한다.

```text
상품행 adSpendKrw 합계
= spendProductId가 매칭된 원천 행의 광고비 합계

상품행 adGeneratedSalesKrw 합계
= spendProductId가 매칭된 원천 행의 전환매출 합계

상품행 attributedConversionSalesKrw 합계
= conversionProductId가 매칭된 원천 행의 전환매출 합계
```

미매칭 정책 때문에 세 합계가 원천 전체 합계와 다를 수 있다. 서로 다른 매칭 조건의 합계를 억지로 동일하게 만들면 안 된다.

### 12.3 순이익 비교

수정 전 결과를 캡처한 뒤 수정 후와 비교한다.

- 매칭 상품 전체 광고비 합계 동일
- 전체 총비용 동일
- 전체 순이익 동일
- 두줄보호대 1개입/2개입의 상품별 광고비와 순이익 동일
- ROAS와 광고성과 전환매출만 새 정의로 변경

7,306원 미매칭 광고비는 계속 상품 합계에서 제외되므로 이 금액을 수정 전후 차이로 오판하지 않는다.

---

## 13. 실행 순서

다음 세션은 아래 순서로 작업한다.

1. `git status --short`로 기존 사용자 변경사항을 확인하고 보존한다.
2. 이 문서와 현재 `ProductProfitRow`, `CoupangAdInput`, `buildProductProfitRows()`를 다시 읽는다.
3. 교차 전환 회귀 테스트를 먼저 추가하여 현재 코드에서 실패하는 것을 확인한다.
4. 계산기 입력을 광고성과 축과 판매귀속 축으로 분리한다.
5. 서비스의 두 집계 맵을 구현한다.
6. 상품/기간/그룹/전체 요약의 합산 필드를 수정한다.
7. API와 웹 타입을 갱신한다.
8. 마진계산, 대시보드, 일일보고서, HTML/XLSX의 라벨과 필드를 갱신한다.
9. API 테스트, 웹 테스트, 타입 검사, 빌드를 실행한다.
10. 2026-07-29 실제 자료로 두줄보호대와 전체 합계 불변식을 확인한다.
11. 수정 파일만 검토하고 Cafe24 및 기존 사용자 작업을 건드리지 않았는지 확인한다.

권장 검증 명령:

```powershell
npm --workspace @meta-ads-performance/api run test
npm --workspace @meta-ads-performance/api run lint
npm --workspace @meta-ads-performance/web run test
npm run build
```

테스트 시간이 길면 관련 spec을 먼저 실행하되 최종 완료 전 전체 검증을 수행한다.

---

## 14. 구현 시 금지사항

1. `spendProductId`와 `conversionProductId`가 다르다는 이유로 행을 오류 처리하지 않는다.
2. 전환상품 ID를 광고상품 ID로 덮어쓰지 않는다.
3. 자동매핑 규칙에 “두줄보호대” 전용 예외를 추가하지 않는다.
4. 특정 날짜나 특정 금액을 하드코딩하지 않는다.
5. 상품별 ROAS를 평균하여 그룹 또는 기간 ROAS를 만들지 않는다.
6. 오가닉 매출에서 `adGeneratedSalesKrw`를 차감하지 않는다.
7. ROAS 계산에 `attributedConversionSalesKrw`를 사용하지 않는다.
8. ROAS 값으로 순이익을 다시 계산하지 않는다.
9. 미매칭 7,306원을 임의 상품에 더하지 않는다.
10. 원본 XLSX의 ROAS 퍼센트를 내부 계산값으로 그대로 복사하지 않는다.
11. unrelated dirty worktree 변경을 정리하거나 되돌리지 않는다.
12. `0731수정계획.md`의 Cafe24 작업과 이번 수정 범위를 섞지 않는다.

---

## 15. 완료 조건

아래 조건을 모두 충족해야 작업 완료다.

- [ ] 교차 전환이 있는 상품의 ROAS가 광고 집행 상품 기준으로 계산된다.
- [ ] ROAS 분자와 광고비 분모가 모두 `spendProductId` 기준이다.
- [ ] 오가닉 매출은 계속 `conversionProductId` 기준 전환매출을 차감한다.
- [ ] 광고성과 전환매출과 판매상품 귀속 전환매출이 별도 필드다.
- [ ] 상품그룹, 기간, 전체 ROAS가 합산 분자 ÷ 합산 분모로 계산된다.
- [ ] Ads Analysis와 상품 손익 화면의 동일 상품 ROAS가 같은 기준을 사용한다.
- [ ] 2026-07-29 두줄보호대 1개입이 현재 DB 기준 약 208.3%로 재계산된다.
- [ ] 수정 전후 전체 광고비, 총비용, 순이익이 동일하다.
- [ ] 미매칭 광고비 7,306원 정책이 변경되지 않는다.
- [ ] 자동매핑 규칙과 정상 교차 전환 관계가 보존된다.
- [ ] 쿠팡 대시보드, 마진계산, 일일보고서, HTML/XLSX가 새 의미를 일관되게 표시한다.
- [ ] 교차 상품, 그룹 간 교차, 기간 합계, 0 광고비 테스트가 존재한다.
- [ ] API/웹 테스트, 타입 검사 및 빌드가 통과한다.
- [ ] 기존 Cafe24 및 사용자 작업 파일에 불필요한 변경이 없다.

---

## 16. 최종 인수 요약

이번 장애는 “자동매핑이 틀려서 두줄보호대 1개입에 엉뚱한 상품이 붙은 문제”가 아니다. 자동매핑이 정상적으로 보존한 광고 집행 상품과 전환 판매 상품이라는 두 관계를 이후 집계 코드가 하나의 상품 기준처럼 혼합한 문제다.

해결 원칙은 간단하지만 반드시 끝까지 일관되어야 한다.

```text
광고성과 ROAS
= 광고 집행 상품 기준 전환매출
/ 광고 집행 상품 기준 광고비

오가닉 매출
= 판매상품 실제 순매출
- 판매상품 기준 귀속 전환매출

순이익
= 실제 순매출
- 상품원가·수수료·물류비·VAT 등 상품 관련 비용
- 광고 집행 상품 기준 광고비
```

이 세 계산을 서로 다른 목적의 지표로 분리하면 자동매핑 구조를 뜯어고치지 않고도 ROAS 오류를 제거할 수 있다. 전체 손익은 보존되고, 상품별 광고성과는 교차 구매를 포함한 실제 광고 기여를 올바르게 보여주게 된다.
